---
file_type: "task"
task_id: "5.1"
task_title: "`read_files_with_line_numbers` doc set"
unit: "5"
parent_orchestration: ".plan/5-inspection-read-docs/orchestration.md"
classification: "ISOLATED"
 status: "done"
estimated_complexity: "HIGH"
files_modified:
  - "src/domain/inspection/read-files-with-line-numbers/CONVENTIONS.md"
  - "src/domain/inspection/read-files-with-line-numbers/DESCRIPTION.md"
  - "src/domain/inspection/read-files-with-line-numbers/README.md"
files_read:
  - "src/domain/inspection/read-files-with-line-numbers/handler.ts"
  - "src/domain/inspection/read-files-with-line-numbers/schema.ts"
  - "src/domain/inspection/read-file-content/handler.ts"
  - "src/domain/inspection/read-file-content/schema.ts"
  - "src/application/server/register-inspection-tool-catalog.ts"
  - "README.md"
  - "DESCRIPTION.md"
  - "__bak__/plan-ugrep/.plan/3-read-content/3.1-add-read-file-content-endpoint-with-streaming-ranges.md"
dependencies:
  - task_id: "1.4"
    type: "WAITING"
    status: "RESOLVED"
blocked_by: "none"
information_completeness_mode: "FULLY_SPECIFIED"
implementation_scenarios:
  - "OBJECT_CREATION"
  - "OTHER"
execution_surface_band: "GREEN"
primary_split_axis: "none"
single_agent_viability_note: "The task is limited to one endpoint-local documentation triplet and the coexistence rationale of the bounded multi-file read endpoint."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
decision_surface_register:
  - surface: "naming"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "The endpoint-local triplet must be named `CONVENTIONS.md`, `DESCRIPTION.md`, and `README.md`."
  - surface: "path_and_placement"
    status: "BOUND_LITERAL"
    binding_source: "existing project state"
    notes: "The files live directly in `src/domain/inspection/read-files-with-line-numbers/`."
  - surface: "literal_values_and_budgets"
    status: "BOUND_REFERENCE"
    binding_source: "current code state"
    notes: "Batch-read caps and line-numbered output budgeting must be derived from current code and the internal SSOT refactor task."
  - surface: "defaults_and_fallbacks"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "The docs must explain that this endpoint remains the small multi-file line-numbered reader while `read_file_content` owns the advanced single-file modes."
  - surface: "types_and_shapes"
    status: "BOUND_REFERENCE"
    binding_source: "handler and schema surfaces"
    notes: "The docs must explain the bounded `paths` batch contract and line-numbered text-output semantics accurately."
  - surface: "function_signatures_and_returns"
    status: "BOUND_REFERENCE"
    binding_source: "authoritative code surfaces"
    notes: "The current bounded multi-file handler remains authoritative, but its internal SSOT convergence depends on task `1.4`."
  - surface: "object_schema_and_mapper_contracts"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "The three local markdown files have non-overlapping SSOT roles."
  - surface: "dependencies_and_import_surfaces"
    status: "BOUND_REFERENCE"
    binding_source: "current read-surface refactor and sibling endpoint context"
    notes: "The docs must reflect the finalized internal read-core SSOT refactor and explicitly contrast this endpoint against `read_file_content`."
  - surface: "validation_and_boundary_rules"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "The docs must explain that this endpoint is a bounded inline batch-read surface and must not be reframed as a general large-file or range/cursor endpoint."
  - surface: "consumer_and_callsite_bindings"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "Root docs will later re-reference this local triplet rather than duplicate it."
authoritative_literal_bindings:
  - binding_domain: "endpoint-local SSOT split"
    exact_binding: "`CONVENTIONS.md` owns endpoint-local conventions, `DESCRIPTION.md` owns endpoint-local architecture description, and `README.md` owns endpoint-local DX summary."
    source: "explicit user requirement"
    placement_or_consumer: "endpoint-local doc triplet"
    no_autonomous_choice_rule: "The execute agent must not collapse these roles into fewer files."
  - binding_domain: "public coexistence rule"
    exact_binding: "This endpoint remains the bounded multi-file line-numbered reader while `read_file_content` remains the advanced single-file reader."
    source: "accepted architecture"
    placement_or_consumer: "local conventions and description"
    no_autonomous_choice_rule: "The execute agent must not document this endpoint as a public mega-reader or as a deprecated surface."
  - binding_domain: "historical lineage rule"
    exact_binding: "The old `read_file_content` introduction plan in `__bak__/plan-ugrep` is a historical implementation reference only and may be used only to explain why the bounded batch-read surface remained public."
    source: "root PLAN backup policy"
    placement_or_consumer: "local conventions and description"
    no_autonomous_choice_rule: "The execute agent must not promote the backup plan to current authority."
requires_authoritative_code_surfaces: true
authoritative_code_surfaces:
  - surface_name: "Public registration contract"
    kind: "shape"
    snippet_or_contract: "The tool is registered as `read_files_with_line_numbers` and described as the inline multi-file line-numbered reader for bounded workloads."
    source: "current registration surface"
    placement_or_consumer: "`src/application/server/register-inspection-tool-catalog.ts`"
  - surface_name: "Bounded batch-read contract"
    kind: "shape"
    snippet_or_contract: "The endpoint accepts one or more text-file paths and returns line-numbered text blocks rather than structured cursor or range metadata."
    source: "current handler and schema"
    placement_or_consumer: "`src/domain/inspection/read-files-with-line-numbers/handler.ts`, `src/domain/inspection/read-files-with-line-numbers/schema.ts`"
authoritative_references:
  - path: "src/domain/inspection/read-files-with-line-numbers/handler.ts"
    kind: "business-file"
    usage: "Current bounded multi-file line-numbered read behavior"
    required_before_execution: true
  - path: "src/domain/inspection/read-files-with-line-numbers/schema.ts"
    kind: "schema-file"
    usage: "Current request contract for bounded batch reads"
    required_before_execution: true
  - path: "src/domain/inspection/read-file-content/handler.ts"
    kind: "business-file"
    usage: "Current advanced single-file reader used for coexistence contrast"
    required_before_execution: true
  - path: "src/domain/inspection/read-file-content/schema.ts"
    kind: "schema-file"
    usage: "Current advanced single-file request/result contract used for coexistence contrast"
    required_before_execution: true
  - path: "src/application/server/register-inspection-tool-catalog.ts"
    kind: "business-file"
    usage: "Current public registration wording"
    required_before_execution: true
  - path: "README.md"
    kind: "markdown-doc"
    usage: "Current root DX surface that will later reference the endpoint-local README"
    required_before_execution: true
  - path: "DESCRIPTION.md"
    kind: "markdown-doc"
    usage: "Current root architecture surface that will later reference the endpoint-local description"
    required_before_execution: true
  - path: "__bak__/plan-ugrep/.plan/3-read-content/3.1-add-read-file-content-endpoint-with-streaming-ranges.md"
    kind: "plan-file"
    usage: "Historical read-endpoint lineage only"
    required_before_execution: true
upstream_plan_references:
  - task_id: "1.4"
    path: ".plan/1-runtime-architecture-refactors/1.4-read-endpoint-internal-ssot-refactor.md"
    must_read_before_execution: true
    reason: "The read-doc triplet must reflect the finalized internal shared read-core SSOT refactor instead of the current transient state."
runtime_discovery:
  required: false
  scope: []
  allowed_topics: []
checkpoint_strategy: "ARTIFACT_MILESTONES_REQUIRED"
checkpoint_eligibility_contract:
  durable_skip_authority_only: true
  ephemeral_freshness_steps:
    - "Re-read the current read-endpoint code, registration text, root docs, and upstream read-refactor task"
  durable_resume_checkpoints:
    - "cp-1-context-reanchor"
    - "cp-2-endpoint-conventions"
    - "cp-3-endpoint-description"
    - "cp-4-endpoint-readme"
resume_validation_contract:
  lowest_level_truth_surface: "artifact-evidence"
  task_lifecycle_truth_surface: ".plan/5-inspection-read-docs/orchestration.md"
  global_frontier_truth_surface: "PLAN.md"
  artifact_evidence_sources:
    - "src/domain/inspection/read-files-with-line-numbers/CONVENTIONS.md"
    - "src/domain/inspection/read-files-with-line-numbers/DESCRIPTION.md"
    - "src/domain/inspection/read-files-with-line-numbers/README.md"
  conflict_requires_user_confirmation: true
  partial_progress_requires_user_decision: true
  durable_skip_authority_only: true
  ephemeral_checkpoint_conflict_requires_reexecution: true
artifact_progress_inference_rules:
  - "Treat the task as partially complete only when one or more of the endpoint-local documentation files already exist."
  - "Do not treat root-doc edits or advanced-read doc edits alone as completion evidence for the endpoint-local SSOT triplet."
  - "Ephemeral re-reads never create durable skip authority."
user_resume_decision_gate:
  required_on_partial_progress: true
  required_on_state_conflict: true
  default_user_choices:
    - "ACCEPT_AND_CONTINUE"
    - "REVERIFY_EXISTING_OUTPUTS"
persona_generation_required: true
persona_generation_mode: "runtime_regenerate"
persona_source_prompt_path: "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
persona_primary_domain_kernel: "Enterprise endpoint documentation architecture"
persona_secondary_specializations:
  - "DX-oriented documentation design"
  - "read-family contract description"
persona_current_task_specialization: "Bounded multi-file read endpoint documentation"
persona_generation_lineage: "Derived from the endpoint-local SSOT documentation rule, the current bounded batch-read code contract, and the internal read-core refactor dependency."
persona_runtime_review_required: true
persona_dx_snapshot:
  title: "Enterprise Endpoint Documentation Architect"
  summary: "Creates endpoint-local SSOT documentation triplets that are precise for agents and concise for developers."
  weighting_note: "The endpoint-local SSOT rule dominates over local wording preferences."
checkpoint_definitions:
  - checkpoint_id: "cp-1-context-reanchor"
    kind: "sequential_step"
    completion_signal: "All authoritative references and upstream task `1.4` are re-read."
    produces_artifacts: []
  - checkpoint_id: "cp-2-endpoint-conventions"
    kind: "artifact_milestone"
    completion_signal: "`CONVENTIONS.md` exists and owns endpoint-local bounded batch-read policies and guardrails."
    produces_artifacts:
      - "src/domain/inspection/read-files-with-line-numbers/CONVENTIONS.md"
  - checkpoint_id: "cp-3-endpoint-description"
    kind: "artifact_milestone"
    completion_signal: "`DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents."
    produces_artifacts:
      - "src/domain/inspection/read-files-with-line-numbers/DESCRIPTION.md"
  - checkpoint_id: "cp-4-endpoint-readme"
    kind: "artifact_milestone"
    completion_signal: "`README.md` exists and provides a concise DX summary with links to the local docs."
    produces_artifacts:
      - "src/domain/inspection/read-files-with-line-numbers/README.md"
execution_progress_ledger:
  ledger_mode: "CHECKPOINT_LEDGER"
  checkpoints:
    - checkpoint_id: "cp-1-context-reanchor"
      status: "DONE"
    - checkpoint_id: "cp-2-endpoint-conventions"
      status: "DONE"
    - checkpoint_id: "cp-3-endpoint-description"
      status: "DONE"
    - checkpoint_id: "cp-4-endpoint-readme"
      status: "DONE"
resume_conflict_resolution_rules:
  - "If one or two endpoint-local doc files exist but the triplet is incomplete, stop and ask whether the partial state should be accepted or reverified."
  - "If endpoint-local docs are drafted before task `1.4` is complete, stop and escalate."
  - "If endpoint-local docs collapse the bounded batch-reader into the advanced single-file reader, stop and escalate."
checkpoint_update_protocol:
  - "Mark `cp-1-context-reanchor` DONE only after all authoritative references and upstream task `1.4` are re-read."
  - "Mark `cp-2-endpoint-conventions` DONE only after endpoint-local bounded batch-read guardrails and policy rationale are fully documented."
  - "Mark `cp-3-endpoint-description` DONE only after the endpoint-local architectural description is complete and LLM-optimized."
  - "Mark `cp-4-endpoint-readme` DONE only after the DX summary exists and links the local docs."
halt_conditions:
  plan_reality_mismatch: true
  missing_required_plan_information: true
  autonomous_decision_required: true
user_notification_required: true
---

# Task 5.1: `read_files_with_line_numbers` doc set
[INTENT: ANWEISUNG]

## Goal
[INTENT: KONTEXT]

Create the endpoint-local documentation triplet for `read_files_with_line_numbers` so endpoint-specific bounded multi-file read conventions, coexistence rationale, and DX guidance are owned locally instead of being implied only by code and root docs.

## Expected Result
[INTENT: KONTEXT]

- `src/domain/inspection/read-files-with-line-numbers/CONVENTIONS.md` exists and owns endpoint-local guardrails and conventions.
- `src/domain/inspection/read-files-with-line-numbers/DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents.
- `src/domain/inspection/read-files-with-line-numbers/README.md` exists and provides a concise DX-oriented summary plus links.

## Preconditions
[INTENT: CONSTRAINT]

- Task `1.4` is complete and must be re-anchored as the finalized read-core SSOT authority before drafting begins.

## Execution Metadata Register
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Task ID | `5.1` | Unique task identifier |
| Classification | `ISOLATED` | The read-core blocker is resolved; the task now proceeds as an isolated bounded-batch-read documentation surface after re-anchoring completed unit-1 outputs |
| Information Completeness Mode | `FULLY_SPECIFIED` | All determinable doc surfaces are bound |
| Implementation Scenarios | `OBJECT_CREATION`, `OTHER` | Documentation triplet creation |
| Execution Surface Band | `GREEN` | One endpoint-local documentation concern |
| Primary Split Axis | `none` | No further split is required |
| Parent Orchestration | `.plan/5-inspection-read-docs/orchestration.md` | Lifecycle status is tracked there |
| Blocked By | `none` | No unresolved blocker remains |
| Requires Upstream Plan Re-Anchoring | `yes` | Upstream task `1.4` must be re-read |
| Runtime Discovery Allowed | `no` | This task is fully specified |
| Checkpoint Strategy | `ARTIFACT_MILESTONES_REQUIRED` | The three documentation files are durable outputs |
| Durable Skip Authority Only | `true` | Only durable documentation artifacts create skip authority |
| Ephemeral Freshness Steps Present | `yes` | Re-reading code, root docs, and upstream task `1.4` remains non-durable |
| User Notification Required On Gap/Mismatch | `true` | Mismatches must stop execution |
| Persona Generation Required | `true` | Runtime persona regeneration remains mandatory |

## Task Contract Closure
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Decision Surfaces Closed | `true` | All determinable surfaces are bound |
| Unresolved Determinable Decisions | `0` | No deterministic gap remains |
| Expected Execute-Agent Questions | `0` | A fresh execute agent should not need follow-up questions |

## Decision Surface Register
[INTENT: REFERENZ]

| Surface | Status | Binding Source | Notes |
|---------|--------|----------------|-------|
| `naming` | `BOUND_LITERAL` | user requirement | The endpoint-local triplet must be named `CONVENTIONS.md`, `DESCRIPTION.md`, and `README.md`. |
| `path_and_placement` | `BOUND_LITERAL` | existing project state | The files live directly in `src/domain/inspection/read-files-with-line-numbers/`. |
| `literal_values_and_budgets` | `BOUND_REFERENCE` | current code state | Batch-read caps and line-numbered output budgeting must be derived from current code and the internal SSOT refactor task. |
| `defaults_and_fallbacks` | `BOUND_LITERAL` | accepted architecture | The docs must explain that this endpoint remains the small multi-file line-numbered reader while `read_file_content` owns advanced single-file modes. |
| `types_and_shapes` | `BOUND_REFERENCE` | handler and schema surfaces | The docs must explain the bounded `paths` batch contract and line-numbered text-output semantics accurately. |
| `function_signatures_and_returns` | `BOUND_REFERENCE` | authoritative code surfaces | The current bounded multi-file handler remains authoritative, but its internal SSOT convergence depends on task `1.4`. |
| `object_schema_and_mapper_contracts` | `BOUND_LITERAL` | user requirement | The three local markdown files have non-overlapping SSOT roles. |
| `dependencies_and_import_surfaces` | `BOUND_REFERENCE` | current read-surface refactor and sibling endpoint context | The docs must reflect the finalized internal read-core SSOT refactor and explicitly contrast this endpoint against `read_file_content`. |
| `validation_and_boundary_rules` | `BOUND_LITERAL` | accepted architecture | The docs must explain that this endpoint is a bounded inline batch-read surface and must not be reframed as a general large-file or range/cursor endpoint. |
| `consumer_and_callsite_bindings` | `BOUND_LITERAL` | user requirement | Root docs will later re-reference this local triplet rather than duplicate it. |

## Authoritative Literal Bindings
[INTENT: REFERENZ]

| Binding Domain | Exact Binding | Source | Placement / Consumer | No Autonomous Choice Rule |
|---------------|---------------|--------|----------------------|---------------------------|
| Endpoint-local SSOT split | `CONVENTIONS.md` owns endpoint-local conventions, `DESCRIPTION.md` owns endpoint-local architecture description, and `README.md` owns endpoint-local DX summary. | explicit user requirement | endpoint-local doc triplet | The execute agent must not collapse these roles into fewer files. |
| Public coexistence rule | This endpoint remains the bounded multi-file line-numbered reader while `read_file_content` remains the advanced single-file reader. | accepted architecture | local conventions and description | The execute agent must not document this endpoint as a public mega-reader or as a deprecated surface. |
| Historical lineage rule | The old `read_file_content` introduction plan in `__bak__/plan-ugrep` is a historical implementation reference only and may be used only to explain why the bounded batch-read surface remained public. | root PLAN backup policy | local conventions and description | The execute agent must not promote the backup plan to current authority. |

## Authoritative Code / Contract Surfaces
[INTENT: REFERENZ]

| Surface Name | Kind | Snippet / Contract | Source | Placement / Consumer |
|-------------|------|--------------------|--------|----------------------|
| Public registration contract | `shape` | The tool is registered as `read_files_with_line_numbers` and described as the inline multi-file line-numbered reader for bounded workloads. | current registration surface | `src/application/server/register-inspection-tool-catalog.ts` |
| Bounded batch-read contract | `shape` | The endpoint accepts one or more text-file paths and returns line-numbered text blocks rather than structured cursor or range metadata. | current handler and schema | `src/domain/inspection/read-files-with-line-numbers/handler.ts`, `src/domain/inspection/read-files-with-line-numbers/schema.ts` |

## Execute-Agent Zero-Question Gate
[INTENT: CONSTRAINT]

- **Gate Result:** `PASS`
- **Checked Domains:** triplet naming, placement, bounded batch-read scope, coexistence wording, and root TOC relationship.
- **Rule:** A fresh execute agent must not need to ask where the files go, what each doc owns, or whether this endpoint still coexists with `read_file_content` after the internal SSOT refactor.

## Authoritative References
[INTENT: REFERENZ]

| Path | Kind | Must Read Before Execution | Purpose | Referenced By |
|------|------|---------------------------|---------|---------------|
| `src/domain/inspection/read-files-with-line-numbers/handler.ts` | `business-file` | `true` | current bounded multi-file line-numbered read behavior | actions 1-4 |
| `src/domain/inspection/read-files-with-line-numbers/schema.ts` | `schema-file` | `true` | current request contract for bounded batch reads | actions 1-4 |
| `src/domain/inspection/read-file-content/handler.ts` | `business-file` | `true` | current advanced single-file reader used for coexistence contrast | actions 1-4 |
| `src/domain/inspection/read-file-content/schema.ts` | `schema-file` | `true` | current advanced single-file request/result contract used for coexistence contrast | actions 1-4 |
| `src/application/server/register-inspection-tool-catalog.ts` | `business-file` | `true` | current public registration wording | actions 1-4 |
| `README.md` | `markdown-doc` | `true` | current root DX surface that will later reference the endpoint-local README | context |
| `DESCRIPTION.md` | `markdown-doc` | `true` | current root architecture surface that will later reference the endpoint-local description | context |
| `__bak__/plan-ugrep/.plan/3-read-content/3.1-add-read-file-content-endpoint-with-streaming-ranges.md` | `plan-file` | `true` | historical read-endpoint lineage only | context |

## Resume Validation Contract
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Lowest Level Truth Surface | `artifact-evidence` | The three endpoint-local documentation files are the primary resume truth |
| Task Lifecycle Truth Surface | `.plan/5-inspection-read-docs/orchestration.md` | Parent orchestration owns lifecycle state |
| Global Frontier Truth Surface | `PLAN.md` | Global frontier SSOT |
| Conflict Requires User Confirmation | `true` | Conflicting partial documentation state requires escalation |
| Partial Progress Requires User Decision | `true` | Partial artifact state requires a user choice |
| Durable Skip Authority Only | `true` | Only durable documentation artifacts may authorize skip |
| Ephemeral Freshness Steps Must Re-Execute | `true` | Pure re-anchor work remains re-executable preparation |

## Artifact Progress Inference Rules
[INTENT: REFERENZ]

- Treat the task as partially complete only when one or more of the endpoint-local documentation files already exist.
- Do not treat root-doc edits or advanced-read doc edits alone as completion evidence for the endpoint-local SSOT triplet.
- `EPHEMERAL_FRESHNESS_STEP` re-reads never create durable skip authority.

## User Resume Decision Gate
[INTENT: CONSTRAINT]

- **Required on Partial Progress:** `true`
- **Required on State Conflict:** `true`
- **Allowed User Choices:** `ACCEPT_AND_CONTINUE` / `REVERIFY_EXISTING_OUTPUTS`

## Persona Derivation Manifest
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Persona Generation Required | `true` | task-specific persona derivation required |
| Persona Generation Mode | `runtime_regenerate` | execute regenerates the persona at runtime |
| Persona Source Prompt Path | `src/personas/it/__prompts__/silent/enterprise/creation/prompt.md` | authoritative persona source |
| Persona Primary Domain Kernel | `Enterprise endpoint documentation architecture` | highest-weight persona layer |
| Persona Secondary Specializations | `DX-oriented documentation design`, `read-family contract description` | secondary persona layers |
| Persona Current Task Specialization | `bounded multi-file read endpoint documentation` | current specialization |
| Persona Generation Lineage | `Derived from the endpoint-local SSOT documentation rule, the current bounded batch-read code contract, and the internal read-core refactor dependency.` | derivation lineage |
| Persona Runtime Review Required | `true` | runtime review is required |

## Persona DX Snapshot
[INTENT: KONTEXT]

- **Persona Title:** `Enterprise Endpoint Documentation Architect`
- **Persona Summary:** Creates endpoint-local SSOT documentation triplets that are precise for agents and concise for developers.
- **Persona Weighting Note:** The endpoint-local SSOT rule dominates over local wording preferences.

## Context
[INTENT: KONTEXT]

- **Domain:** endpoint-local read documentation
- **Related Files:** bounded batch-read handler and schema, advanced single-file reader surfaces, current registration, root docs, upstream read-refactor task, historical read-content plan
- **Technical Context:** this endpoint provides direct line-numbered inline reads for smaller bounded batches and must remain distinct from advanced single-file range/cursor access
- **Business Context:** agents and developers need endpoint-local guidance that explains why this endpoint still exists and what caller role it serves
- **Architectural Context:** endpoint-local SSOT comes first; root TOC integration happens later
- **Plan Context Dependencies:** task `1.4` must be complete first
- **Runtime Discovery Context:** none

## Execution Surface Assessment
[INTENT: REFERENZ]

- **Execution Surface Band:** `GREEN`
- **Primary Split Axis:** `none`
- **Hard-Gap Result:** `no RED or BLACK trigger active`
- **Single-Agent Viability Rationale:** The task is limited to one endpoint-local documentation triplet for one bounded read endpoint plus one coexistence contrast surface.

## Information Units
[INTENT: SPEZIFIKATION]

### IU-001: Endpoint-local `CONVENTIONS.md`
**Type:** `object`

**Description:**
Create the conventions file that becomes the local SSOT for bounded batch-read guardrails, line-numbered output semantics, and architectural rationale.

**Ist-Zustand:**
The endpoint has code and registration surfaces but no endpoint-local conventions file.

**Soll-Zustand:**
`CONVENTIONS.md` explains bounded multi-file reads, line-numbered output, coexistence with `read_file_content`, and the endpoint’s separation from advanced single-file range/cursor modes.

### IU-002: Endpoint-local `DESCRIPTION.md`
**Type:** `object`

**Description:**
Create the endpoint-local architectural description optimized for LLM-agent comprehension.

**Ist-Zustand:**
Only code and registration surfaces describe the endpoint today.

**Soll-Zustand:**
`DESCRIPTION.md` explains the endpoint contract, line-numbered output model, coexistence rationale, and read-family role in agent-oriented detail.

### IU-003: Endpoint-local `README.md`
**Type:** `object`

**Description:**
Create the concise DX summary for the endpoint.

**Ist-Zustand:**
There is no endpoint-local DX summary file.

**Soll-Zustand:**
`README.md` summarizes what the endpoint does, links the local conventions and description, and stays concise.

## Actions
[INTENT: ANWEISUNG]

1. **Re-anchor the upstream read-refactor task plus the current read endpoints and root surfaces**
   - **What:** re-read task `1.4` plus the bounded batch-read endpoint, the advanced single-file reader, current registration, root docs, and historical read-content plan
   - **Where:** authoritative references and upstream plan references listed above
   - **How:** read them completely before drafting any endpoint-local doc content
   - **Why:** the docs must reflect the finalized internal SSOT refactor and the accepted coexistence rationale
   - **Inputs / References:** all authoritative references in this task file
   - **Output Contract:** current endpoint context, coexistence context, upstream refactor context, and historical lineage context are active
   - **Checkpoint Impact:** completes `cp-1-context-reanchor`
   - **Verification:** all required references are re-read successfully
   - **Stop if:** finalized task `1.4` cannot be re-anchored from the current completed runtime state or any required reference is unreadable
2. **Create endpoint-local `CONVENTIONS.md`**
   - **What:** create the conventions file for this endpoint
   - **Where:** `src/domain/inspection/read-files-with-line-numbers/CONVENTIONS.md`
   - **How:** document endpoint-local bounded batch-read guardrails, line-numbered output semantics, coexistence rationale, and separation from advanced single-file modes
   - **Why:** endpoint-specific policy detail must have a local SSOT
   - **Inputs / References:** current endpoint code, registration text, upstream read-refactor task, advanced single-file reader, and historical read-content lineage
   - **Output Contract:** the endpoint-local conventions SSOT exists
   - **Checkpoint Impact:** completes `cp-2-endpoint-conventions`
   - **Verification:** the file explains endpoint-local policies and does not duplicate root TOC content as its primary structure
   - **Stop if:** conventions would need to invent runtime behavior not implemented in code
3. **Create endpoint-local `DESCRIPTION.md`**
   - **What:** create the endpoint-local description file
   - **Where:** `src/domain/inspection/read-files-with-line-numbers/DESCRIPTION.md`
   - **How:** describe the endpoint contract, line-numbered output model, and coexistence role in LLM-oriented detail
   - **Why:** agents need a dedicated endpoint description surface beyond raw code and registration text
   - **Inputs / References:** current endpoint code, schema, advanced reader surfaces, registration, conventions, and upstream task `1.4`
   - **Output Contract:** the endpoint-local architectural description exists
   - **Checkpoint Impact:** completes `cp-3-endpoint-description`
   - **Verification:** the file is detailed, LLM-oriented, and aligned to the current code surface
   - **Stop if:** the description would claim behavior not present in the handler/schema surfaces
4. **Create endpoint-local `README.md`**
   - **What:** create the concise DX summary file
   - **Where:** `src/domain/inspection/read-files-with-line-numbers/README.md`
   - **How:** summarize the endpoint purpose, key conventions, and links to the local `CONVENTIONS.md` and `DESCRIPTION.md`
   - **Why:** developers need a concise entry surface with quick orientation
   - **Inputs / References:** current endpoint code plus the newly created local docs
   - **Output Contract:** the endpoint-local DX summary exists
   - **Checkpoint Impact:** completes `cp-4-endpoint-readme`
   - **Verification:** the README stays concise and points to the local SSOT surfaces
   - **Stop if:** the README would need to duplicate the full conventions or description content

## Checkpoint Definitions
[INTENT: REFERENZ]

| Checkpoint ID | Kind | Completion Signal | Produces Artifacts | Resume Meaning |
|---------------|------|-------------------|--------------------|----------------|
| `cp-1-context-reanchor` | `sequential_step` | All authoritative references and upstream task `1.4` are re-read | none | drafting may begin |
| `cp-2-endpoint-conventions` | `artifact_milestone` | `CONVENTIONS.md` exists and owns endpoint-local bounded batch-read policies and guardrails | `src/domain/inspection/read-files-with-line-numbers/CONVENTIONS.md` | conventions SSOT is complete |
| `cp-3-endpoint-description` | `artifact_milestone` | `DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents | `src/domain/inspection/read-files-with-line-numbers/DESCRIPTION.md` | architectural description is complete |
| `cp-4-endpoint-readme` | `artifact_milestone` | `README.md` exists and provides a concise DX summary with links | `src/domain/inspection/read-files-with-line-numbers/README.md` | endpoint doc triplet is complete |

## Execution Progress Ledger
[INTENT: REFERENZ]

| Checkpoint ID | Status | Evidence | Notes |
|---------------|--------|----------|-------|
| `cp-1-context-reanchor` | `PENDING` | authoritative references and upstream task `1.4` re-read | start here |
| `cp-2-endpoint-conventions` | `PENDING` | endpoint-local `CONVENTIONS.md` | depends on `cp-1-context-reanchor` |
| `cp-3-endpoint-description` | `PENDING` | endpoint-local `DESCRIPTION.md` | depends on `cp-2-endpoint-conventions` |
| `cp-4-endpoint-readme` | `PENDING` | endpoint-local `README.md` | depends on `cp-3-endpoint-description` |

## Resume Conflict Resolution Rules
[INTENT: CONSTRAINT]

- If one or two endpoint-local doc files exist but the triplet is incomplete, stop and ask whether the partial state should be accepted or reverified.
- If finalized task `1.4` cannot be re-anchored before local drafting begins, stop and escalate.
- If endpoint-local docs collapse the bounded batch-reader into the advanced single-file reader, stop and escalate.

## Checkpoint Update Protocol
[INTENT: ANWEISUNG]

1. Mark `cp-1-context-reanchor` DONE only after all authoritative references and upstream task `1.4` are re-read.
2. Mark `cp-2-endpoint-conventions` DONE only after endpoint-local bounded batch-read guardrails and policy rationale are fully documented.
3. Mark `cp-3-endpoint-description` DONE only after the endpoint-local architectural description is complete and LLM-optimized.
4. Mark `cp-4-endpoint-readme` DONE only after the DX summary exists and links the local docs.

## Acceptance Criteria
[INTENT: REFERENZ]

- [x] `src/domain/inspection/read-files-with-line-numbers/CONVENTIONS.md` exists and is the endpoint-local SSOT for conventions and guardrails.
- [x] `src/domain/inspection/read-files-with-line-numbers/DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents in detail.
- [x] `src/domain/inspection/read-files-with-line-numbers/README.md` exists and provides a concise DX summary with links to the local docs.
- [x] The documentation reflects the finalized coexistence rationale accurately and preserves the bounded multi-file line-numbered role of this endpoint.
- [x] The endpoint-local docs are ready to be referenced later from root-level TOC surfaces.

## Stop Conditions & User Escalation
[INTENT: CONSTRAINT]

- **Plan-vs-Reality Mismatch:** Stop if the current endpoint behavior differs materially from the assumptions documented here.
- **Missing Required Plan Information:** Stop if coexistence rationale, output semantics, or doc-role split surfaces cannot be bound exactly.
- **Autonomous Decision Required:** Stop if execution would require inventing endpoint behavior, read-family semantics, or root-TOC behavior not already agreed.
- **Decision Surface Not Closed:** If any determinable documentation-ownership or runtime-wording surface remains open, stop and inform the user.
- **Allowed Runtime Discovery:** None.
- **User Message Contract:** `Plan says X / current reality is Y / missing or conflicting information is Z / continuing would require an autonomous decision.`

## Post-Completion Instructions
[INTENT: ANWEISUNG]

1. Mark this task `[x]` in [`.plan/5-inspection-read-docs/orchestration.md`](./orchestration.md).
2. Keep the resulting doc triplet ready for later root-level TOC linking.
3. Re-anchor [`PLAN.md`](../../PLAN.md).

## File Path Index
[INTENT: REFERENZ]

| # | Path | Kind | Why Relevant | Must Read Before Execution |
|---|------|------|--------------|----------------------------|
| 1 | `src/domain/inspection/read-files-with-line-numbers/handler.ts` | `business-file` | current bounded multi-file line-numbered read behavior | `true` |
| 2 | `src/domain/inspection/read-files-with-line-numbers/schema.ts` | `schema-file` | current request contract for bounded batch reads | `true` |
| 3 | `src/domain/inspection/read-file-content/handler.ts` | `business-file` | advanced single-file reader used for coexistence contrast | `true` |
| 4 | `src/domain/inspection/read-file-content/schema.ts` | `schema-file` | advanced single-file request/result contract used for coexistence contrast | `true` |
| 5 | `src/application/server/register-inspection-tool-catalog.ts` | `business-file` | current public registration wording | `true` |
| 6 | `README.md` | `markdown-doc` | current root DX surface that will later reference the endpoint-local README | `true` |
| 7 | `DESCRIPTION.md` | `markdown-doc` | current root architecture surface that will later reference the endpoint-local description | `true` |
| 8 | `__bak__/plan-ugrep/.plan/3-read-content/3.1-add-read-file-content-endpoint-with-streaming-ranges.md` | `plan-file` | historical read-endpoint lineage only | `true` |

## Notes
[INTENT: KONTEXT]

- This task creates endpoint-local documentation only; root TOC integration happens later.
- The local docs must stay aligned to the finalized internal read-core SSOT refactor and must treat the backup plan as historical lineage only.
