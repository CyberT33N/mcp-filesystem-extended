---
file_type: "task"
task_id: "2.1"
task_title: "`list_directory_entries` doc set"
unit: "2"
parent_orchestration: ".plan/2-inspection-discovery-docs/orchestration.md"
classification: "WAITING"
status: "pending"
estimated_complexity: "MEDIUM"
files_modified:
  - "src/domain/inspection/list-directory-entries/CONVENTIONS.md"
  - "src/domain/inspection/list-directory-entries/DESCRIPTION.md"
  - "src/domain/inspection/list-directory-entries/README.md"
files_read:
  - "src/domain/inspection/list-directory-entries/handler.ts"
  - "src/domain/inspection/list-directory-entries/schema.ts"
  - "src/application/server/register-inspection-tool-catalog.ts"
  - "src/domain/shared/guardrails/traversal-scope-policy.ts"
  - "src/domain/shared/guardrails/filesystem-preflight.ts"
  - "src/domain/shared/guardrails/traversal-runtime-budget.ts"
  - "README.md"
  - "DESCRIPTION.md"
dependencies:
  - task_id: "1.6"
    type: "WAITING"
    status: "UNRESOLVED"
blocked_by: "1.6"
information_completeness_mode: "FULLY_SPECIFIED"
implementation_scenarios:
  - "OBJECT_CREATION"
  - "OTHER"
execution_surface_band: "GREEN"
primary_split_axis: "none"
single_agent_viability_note: "The task is bounded to one endpoint-local documentation triplet and one already existing endpoint behavior surface."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
decision_surface_register:
  - surface: "naming"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "The endpoint-local documentation triplet must be named `CONVENTIONS.md`, `DESCRIPTION.md`, and `README.md`."
  - surface: "path_and_placement"
    status: "BOUND_LITERAL"
    binding_source: "existing project state"
    notes: "The documentation files live directly inside `src/domain/inspection/list-directory-entries/`."
  - surface: "literal_values_and_budgets"
    status: "BOUND_REFERENCE"
    binding_source: "current code state"
    notes: "Numeric or runtime details must be derived from the current handler, schema, and shared guardrail references rather than invented."
  - surface: "defaults_and_fallbacks"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "Endpoint-local docs own endpoint-specific conventions only; root TOC integration happens later and must not be duplicated here."
  - surface: "types_and_shapes"
    status: "BOUND_REFERENCE"
    binding_source: "handler and schema surfaces"
    notes: "The docs must explain the structured root/entry result model without rewriting code ownership."
  - surface: "function_signatures_and_returns"
    status: "BOUND_REFERENCE"
    binding_source: "authoritative code surfaces"
    notes: "The documentation must describe the public tool contract and the handler-owned structured result semantics."
  - surface: "object_schema_and_mapper_contracts"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "`CONVENTIONS.md` is the endpoint-local SSOT for endpoint-specific policies, `DESCRIPTION.md` is the endpoint-local architectural description, and `README.md` is the DX summary."
  - surface: "dependencies_and_import_surfaces"
    status: "BOUND_REFERENCE"
    binding_source: "runtime architecture task"
    notes: "The docs must wait for task `1.5` because traversal-governance wording must reflect the finalized runtime model."
  - surface: "validation_and_boundary_rules"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "The docs must explain default exclusions, explicit descendant reopening, and traversal-budget behavior without implying caller override paths that do not exist."
  - surface: "consumer_and_callsite_bindings"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "These endpoint-local docs will later be referenced from root-level `CONVENTIONS.md`, `DESCRIPTION.md`, and `README.md` as TOC entries."
authoritative_literal_bindings:
  - binding_domain: "endpoint-local SSOT split"
    exact_binding: "`CONVENTIONS.md` owns endpoint-specific conventions and guardrails, `DESCRIPTION.md` owns the endpoint-local architecture description, and `README.md` owns the concise DX summary."
    source: "explicit user requirement"
    placement_or_consumer: "endpoint-local doc triplet"
    no_autonomous_choice_rule: "The execute agent must not merge the three doc roles into one file."
  - binding_domain: "root TOC relationship"
    exact_binding: "Root documentation will later re-reference this endpoint-local triplet instead of duplicating it."
    source: "explicit user requirement"
    placement_or_consumer: "later root documentation tasks"
    no_autonomous_choice_rule: "This task must not prematurely rewrite root docs instead of creating the endpoint-local SSOT."
  - binding_domain: "runtime-governance dependency"
    exact_binding: "The content of this doc set must reflect the finalized traversal-governance model from task `1.5`."
    source: "plan dependency"
    placement_or_consumer: "endpoint-local conventions and description"
    no_autonomous_choice_rule: "The execute agent must not finalize traversal wording before task `1.5` is complete."
requires_authoritative_code_surfaces: true
authoritative_code_surfaces:
  - surface_name: "Public registration contract"
    kind: "shape"
    snippet_or_contract: "The tool is registered as `list_directory_entries` with structured output and description text that frames it as a structured directory-listing surface."
    source: "current registration surface"
    placement_or_consumer: "`src/application/server/register-inspection-tool-catalog.ts`"
  - surface_name: "Endpoint result contract"
    kind: "shape"
    snippet_or_contract: "The endpoint returns structured roots and nested listed entries, with optional grouped metadata and recursive child entries."
    source: "current handler and schema"
    placement_or_consumer: "`src/domain/inspection/list-directory-entries/handler.ts`, `src/domain/inspection/list-directory-entries/schema.ts`"
authoritative_references:
  - path: "src/domain/inspection/list-directory-entries/handler.ts"
    kind: "business-file"
    usage: "Current endpoint behavior and structured result ownership"
    required_before_execution: true
  - path: "src/domain/inspection/list-directory-entries/schema.ts"
    kind: "schema-file"
    usage: "Current request and result contract surfaces"
    required_before_execution: true
  - path: "src/application/server/register-inspection-tool-catalog.ts"
    kind: "business-file"
    usage: "Current public registration wording"
    required_before_execution: true
  - path: "src/domain/shared/guardrails/traversal-scope-policy.ts"
    kind: "schema-file"
    usage: "Default traversal exclusion and re-include policy that endpoint conventions must explain"
    required_before_execution: true
  - path: "src/domain/shared/guardrails/filesystem-preflight.ts"
    kind: "schema-file"
    usage: "Metadata-first validation model relevant to endpoint conventions"
    required_before_execution: true
  - path: "src/domain/shared/guardrails/traversal-runtime-budget.ts"
    kind: "schema-file"
    usage: "Runtime traversal-safeguard semantics relevant after task `1.5` resolves"
    required_before_execution: true
  - path: "README.md"
    kind: "markdown-doc"
    usage: "Current root DX surface that will later re-reference the endpoint-local README"
    required_before_execution: true
  - path: "DESCRIPTION.md"
    kind: "markdown-doc"
    usage: "Current root architecture description that will later re-reference the endpoint-local description"
    required_before_execution: true
upstream_plan_references:
  - task_id: "1.6"
    path: ".plan/1-runtime-architecture-refactors/1.6-traversal-workload-admission-and-lane-routing-completion.md"
    must_read_before_execution: true
    reason: "The endpoint-local conventions and description must document the finalized traversal-admission model, not the phase-one transient state from task `1.5`."
runtime_discovery:
  required: false
  scope: []
  allowed_topics: []
checkpoint_strategy: "ARTIFACT_MILESTONES_REQUIRED"
checkpoint_eligibility_contract:
  durable_skip_authority_only: true
  ephemeral_freshness_steps:
    - "Re-read current endpoint code and root documentation"
  durable_resume_checkpoints:
    - "cp-1-context-reanchor"
    - "cp-2-endpoint-conventions"
    - "cp-3-endpoint-description"
    - "cp-4-endpoint-readme"
resume_validation_contract:
  lowest_level_truth_surface: "artifact-evidence"
  task_lifecycle_truth_surface: ".plan/2-inspection-discovery-docs/orchestration.md"
  global_frontier_truth_surface: "PLAN.md"
  artifact_evidence_sources:
    - "src/domain/inspection/list-directory-entries/CONVENTIONS.md"
    - "src/domain/inspection/list-directory-entries/DESCRIPTION.md"
    - "src/domain/inspection/list-directory-entries/README.md"
  conflict_requires_user_confirmation: true
  partial_progress_requires_user_decision: true
  durable_skip_authority_only: true
  ephemeral_checkpoint_conflict_requires_reexecution: true
artifact_progress_inference_rules:
  - "Treat the task as partially complete only when one or more of the endpoint-local documentation files exist."
  - "Do not treat notes in root docs as completion evidence for the endpoint-local SSOT triplet."
  - "Ephemeral re-reads of current code and root docs never create durable skip authority."
user_resume_decision_gate:
  required_on_partial_progress: true
  required_on_state_conflict: true
  default_user_choices:
    - "ACCEPT_AND_CONTINUE"
    - "REVERIFY_EXISTING_OUTPUTS"
persona_generation_required: true
persona_generation_mode: "runtime_regenerate"
persona_source_prompt_path: "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
persona_primary_domain_kernel: "Enterprise endpoint documentation architecture"
persona_secondary_specializations:
  - "DX-oriented documentation design"
  - "inspection-domain contract description"
persona_current_task_specialization: "Structured directory-listing endpoint documentation"
persona_generation_lineage: "Derived from the endpoint-local SSOT documentation requirement and the finalized traversal-governance dependency."
persona_runtime_review_required: true
persona_dx_snapshot:
  title: "Enterprise Endpoint Documentation Architect"
  summary: "Creates endpoint-local SSOT documentation triplets that are precise for agents and concise for developers."
  weighting_note: "The endpoint-local SSOT rule dominates over local wording preferences."
checkpoint_definitions:
  - checkpoint_id: "cp-1-context-reanchor"
    kind: "sequential_step"
    completion_signal: "All authoritative references and the upstream runtime-governance task are re-read."
    produces_artifacts: []
  - checkpoint_id: "cp-2-endpoint-conventions"
    kind: "artifact_milestone"
    completion_signal: "`CONVENTIONS.md` exists and documents endpoint-local guardrails, policies, and separation rationale."
    produces_artifacts:
      - "src/domain/inspection/list-directory-entries/CONVENTIONS.md"
  - checkpoint_id: "cp-3-endpoint-description"
    kind: "artifact_milestone"
    completion_signal: "`DESCRIPTION.md` exists and describes the endpoint for LLM-agent consumption."
    produces_artifacts:
      - "src/domain/inspection/list-directory-entries/DESCRIPTION.md"
  - checkpoint_id: "cp-4-endpoint-readme"
    kind: "artifact_milestone"
    completion_signal: "`README.md` exists and summarizes the endpoint behavior and doc links from a DX perspective."
    produces_artifacts:
      - "src/domain/inspection/list-directory-entries/README.md"
execution_progress_ledger:
  ledger_mode: "CHECKPOINT_LEDGER"
  checkpoints:
    - checkpoint_id: "cp-1-context-reanchor"
      status: "PENDING"
    - checkpoint_id: "cp-2-endpoint-conventions"
      status: "PENDING"
    - checkpoint_id: "cp-3-endpoint-description"
      status: "PENDING"
    - checkpoint_id: "cp-4-endpoint-readme"
      status: "PENDING"
resume_conflict_resolution_rules:
  - "If one of the endpoint-local doc files exists but its content duplicates root-level TOC content instead of owning endpoint-local SSOT detail, stop and escalate."
  - "If task `1.5` is still unresolved when endpoint-local traversal wording is about to be written, stop and escalate."
checkpoint_update_protocol:
  - "Mark `cp-1-context-reanchor` DONE only after the endpoint code, schema, registration, root docs, and upstream task `1.5` are re-read."
  - "Mark `cp-2-endpoint-conventions` DONE only after endpoint-local guardrails and policy rationale are fully documented."
  - "Mark `cp-3-endpoint-description` DONE only after the endpoint-local architectural description is complete and LLM-oriented."
  - "Mark `cp-4-endpoint-readme` DONE only after the DX summary exists and links the other two endpoint-local docs."
halt_conditions:
  plan_reality_mismatch: true
  missing_required_plan_information: true
  autonomous_decision_required: true
user_notification_required: true
---

# Task 2.1: `list_directory_entries` doc set
[INTENT: ANWEISUNG]

## Goal
[INTENT: KONTEXT]

Create the endpoint-local documentation triplet for [`list_directory_entries`](src/application/server/register-inspection-tool-catalog.ts:141) so endpoint-specific conventions, architecture, and DX summary are owned locally instead of being duplicated only at root level.

The docs must explain the endpoint’s structured listing model, traversal policy, grouped metadata semantics, and the reason broad-root traversal remains guarded.

## Expected Result
[INTENT: KONTEXT]

- `src/domain/inspection/list-directory-entries/CONVENTIONS.md` exists and owns endpoint-local guardrails and conventions.
- `src/domain/inspection/list-directory-entries/DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents.
- `src/domain/inspection/list-directory-entries/README.md` exists and provides a concise DX-oriented summary plus links.

## Preconditions
[INTENT: CONSTRAINT]

- Task `1.6` must be completed because traversal-admission wording depends on the finalized runtime model.

## Plan Correction Addendum
[INTENT: KONTEXT]

Task `1.5` remains the phase-one traversal refactor, but downstream documentation may no longer treat it as the finalized traversal authority. Follow-up task [`1.6-traversal-workload-admission-and-lane-routing-completion.md`](../1-runtime-architecture-refactors/1.6-traversal-workload-admission-and-lane-routing-completion.md) now closes the residual admission-to-execution control-plane gap for broad valid workloads, so this doc set must anchor to `1.6`.

## Execution Metadata Register
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Task ID | `2.1` | Unique task identifier |
| Classification | `WAITING` | The task is blocked by finalized traversal-governance work in another unit |
| Information Completeness Mode | `FULLY_SPECIFIED` | All determinable doc-architecture surfaces are bound |
| Implementation Scenarios | `OBJECT_CREATION`, `OTHER` | Documentation triplet creation |
| Execution Surface Band | `GREEN` | One endpoint-local documentation concern |
| Primary Split Axis | `none` | No further split is required |
| Single-Agent Viability Note | The task is limited to one endpoint-local documentation triplet and one already stable endpoint contract. |
| Parent Orchestration | `.plan/2-inspection-discovery-docs/orchestration.md` | Lifecycle status is tracked there |
| Blocked By | `1.6` | Cross-unit traversal-admission blocker |
| Requires Upstream Plan Re-Anchoring | `yes` | Task `1.6` and all authoritative references must be re-read |
| Runtime Discovery Allowed | `no` | This task is fully specified |
| Checkpoint Strategy | `ARTIFACT_MILESTONES_REQUIRED` | The three documentation files are durable outputs |
| Durable Skip Authority Only | `true` | Only durable documentation artifacts create skip authority |
| Ephemeral Freshness Steps Present | `yes` | Re-reading code and root docs remains non-durable |
| User Notification Required On Gap/Mismatch | `true` | Mismatches must stop execution |
| Persona Generation Required | `true` | Runtime persona regeneration remains mandatory |
| Persona Generation Mode | `runtime_regenerate` | Persona is not fully persisted |
| Persona Primary Domain Kernel | `Enterprise endpoint documentation architecture` | Highest-weight persona kernel |
| Persona Current Task Specialization | `Structured directory-listing endpoint docs` | Current task specialization |
| Persona Runtime Review Required | `true` | Runtime review is required |

## Task Contract Closure
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Decision Surfaces Closed | `true` | All determinable surfaces are bound |
| Unresolved Determinable Decisions | `0` | No deterministic gap remains |
| Expected Execute-Agent Questions | `0` | A fresh execute agent should not need follow-up questions |

## Decision Surface Register
[INTENT: REFERENZ]

| Surface | Status | Binding Source | Notes |
|---------|--------|----------------|-------|
| `naming` | `BOUND_LITERAL` | user requirement | The endpoint-local documentation triplet must be named `CONVENTIONS.md`, `DESCRIPTION.md`, and `README.md`. |
| `path_and_placement` | `BOUND_LITERAL` | existing project state | The three files live directly in `src/domain/inspection/list-directory-entries/`. |
| `literal_values_and_budgets` | `BOUND_REFERENCE` | current code state | Endpoint specifics must be derived from the handler, schema, registration, and shared guardrails rather than invented. |
| `defaults_and_fallbacks` | `BOUND_LITERAL` | accepted architecture | Endpoint-local docs own endpoint-local detail only; root TOC integration happens later. |
| `types_and_shapes` | `BOUND_REFERENCE` | handler and schema surfaces | The docs must explain the structured root/entry output model accurately. |
| `function_signatures_and_returns` | `BOUND_REFERENCE` | authoritative code surfaces | The public tool contract and handler-owned structured output remain authoritative. |
| `object_schema_and_mapper_contracts` | `BOUND_LITERAL` | user requirement | The three doc roles are explicitly separated and must not be merged. |
| `dependencies_and_import_surfaces` | `BOUND_REFERENCE` | runtime architecture task | Final traversal wording depends on task `1.6`. |
| `validation_and_boundary_rules` | `BOUND_LITERAL` | accepted architecture | The docs must explain broad-root default exclusions, additive re-includes, and runtime guardrails without implying unsupported override paths. |
| `consumer_and_callsite_bindings` | `BOUND_LITERAL` | user requirement | Root-level docs will later reference this endpoint-local doc triplet as TOC entries. |

## Authoritative Literal Bindings
[INTENT: REFERENZ]

| Binding Domain | Exact Binding | Source | Placement / Consumer | No Autonomous Choice Rule |
|---------------|---------------|--------|----------------------|---------------------------|
| Endpoint-local SSOT split | `CONVENTIONS.md` owns endpoint-local conventions, `DESCRIPTION.md` owns endpoint-local architecture description, `README.md` owns endpoint-local DX summary | explicit user requirement | endpoint-local doc triplet | The execute agent must not collapse the three roles into one markdown file. |
| Root TOC relationship | root docs re-reference this endpoint-local triplet later rather than duplicating it | explicit user requirement | later root docs | This task must not skip local SSOT creation in favor of premature root rewrites. |
| Runtime-governance dependency | endpoint-local traversal wording must reflect finalized task `1.6` behavior | plan dependency | endpoint-local conventions and description | The execute agent must not finalize traversal wording before task `1.6` is complete. |

## Authoritative Code / Contract Surfaces
[INTENT: REFERENZ]

| Surface Name | Kind | Snippet / Contract | Source | Placement / Consumer |
|-------------|------|--------------------|--------|----------------------|
| Public registration contract | `shape` | The tool is registered as `list_directory_entries` with structured output and description text that frames it as a structured directory-listing surface. | current registration surface | `src/application/server/register-inspection-tool-catalog.ts` |
| Endpoint result contract | `shape` | The endpoint returns structured roots and nested listed entries with grouped metadata and optional recursion. | current handler and schema | `src/domain/inspection/list-directory-entries/handler.ts`, `src/domain/inspection/list-directory-entries/schema.ts` |

## Execute-Agent Zero-Question Gate
[INTENT: CONSTRAINT]

- **Gate Result:** `PASS`
- **Checked Domains:** doc triplet naming, placement, runtime-governance dependency, structured output description, root TOC relationship
- **Rule:** A fresh execute agent must not need to ask where the files go, what each doc owns, or whether traversal wording may be finalized before task `1.6` resolves.

## Authoritative References
[INTENT: REFERENZ]

| Path | Kind | Must Read Before Execution | Purpose | Referenced By |
|------|------|---------------------------|---------|---------------|
| `src/domain/inspection/list-directory-entries/handler.ts` | `business-file` | `true` | current endpoint behavior and structured result ownership | actions 1-4 |
| `src/domain/inspection/list-directory-entries/schema.ts` | `schema-file` | `true` | current request and result contract surfaces | actions 1-4 |
| `src/application/server/register-inspection-tool-catalog.ts` | `business-file` | `true` | current public registration wording | actions 1-4 |
| `src/domain/shared/guardrails/traversal-scope-policy.ts` | `schema-file` | `true` | default traversal exclusion and re-include model | actions 1-4 |
| `src/domain/shared/guardrails/filesystem-preflight.ts` | `schema-file` | `true` | metadata-first validation model relevant to conventions | actions 1-4 |
| `src/domain/shared/guardrails/traversal-runtime-budget.ts` | `schema-file` | `true` | deeper runtime safeguard semantics after task `1.6` | actions 1-4 |
| `README.md` | `markdown-doc` | `true` | root DX surface that will later reference the endpoint-local README | context |
| `DESCRIPTION.md` | `markdown-doc` | `true` | root architecture description that will later reference the endpoint-local description | context |

## Resume Validation Contract
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Lowest Level Truth Surface | `artifact-evidence` | The three endpoint-local documentation files are the primary resume truth |
| Task Lifecycle Truth Surface | `.plan/2-inspection-discovery-docs/orchestration.md` | Parent orchestration owns lifecycle state |
| Global Frontier Truth Surface | `PLAN.md` | Global frontier SSOT |
| Conflict Requires User Confirmation | `true` | Conflicting partial documentation state requires escalation |
| Partial Progress Requires User Decision | `true` | Partial artifact state requires a user choice |
| Durable Skip Authority Only | `true` | Only durable documentation artifacts may authorize skip |
| Ephemeral Freshness Steps Must Re-Execute | `true` | Pure re-anchor work remains re-executable preparation |

## Artifact Progress Inference Rules
[INTENT: REFERENZ]

- Treat the task as partially complete only when one or more of the endpoint-local doc files exist.
- Do not treat root-doc edits as completion evidence for this endpoint-local SSOT triplet.
- `EPHEMERAL_FRESHNESS_STEP` re-reads never create durable skip authority.

## User Resume Decision Gate
[INTENT: CONSTRAINT]

- **Required on Partial Progress:** `true`
- **Required on State Conflict:** `true`
- **Allowed User Choices:** `ACCEPT_AND_CONTINUE` / `REVERIFY_EXISTING_OUTPUTS`

## Persona Derivation Manifest
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Persona Generation Required | `true` | task-specific persona derivation required |
| Persona Generation Mode | `runtime_regenerate` | execute regenerates the persona at runtime |
| Persona Source Prompt Path | `src/personas/it/__prompts__/silent/enterprise/creation/prompt.md` | authoritative persona source |
| Persona Primary Domain Kernel | `Enterprise endpoint documentation architecture` | highest-weight persona layer |
| Persona Secondary Specializations | `DX-oriented documentation design`, `inspection-domain contract description` | secondary persona layers |
| Persona Current Task Specialization | `structured directory-listing endpoint documentation` | current specialization |
| Persona Generation Lineage | `Derived from the endpoint-local SSOT documentation rule and finalized traversal-admission dependency.` | derivation lineage |
| Persona Runtime Review Required | `true` | runtime review is required |

## Persona DX Snapshot
[INTENT: KONTEXT]

- **Persona Title:** `Enterprise Endpoint Documentation Architect`
- **Persona Summary:** Creates endpoint-local SSOT documentation triplets that are precise for agents and concise for developers.
- **Persona Weighting Note:** The endpoint-local SSOT rule dominates over local wording preferences.

## Context
[INTENT: KONTEXT]

- **Domain:** endpoint-local discovery documentation
- **Related Files:** endpoint handler, endpoint schema, current registration, traversal guardrails, root docs
- **Technical Context:** this endpoint exposes structured recursive directory listing with grouped metadata and traversal hardening
- **Business Context:** agents and developers need endpoint-local guidance that explains why broad-root traversal stays guarded
- **Architectural Context:** endpoint-local SSOT comes first; root TOC integration happens later
- **Plan Context Dependencies:** task `1.6` must be complete first
- **Runtime Discovery Context:** none

## Execution Surface Assessment
[INTENT: REFERENZ]

- **Execution Surface Band:** `GREEN`
- **Primary Split Axis:** `none`
- **Hard-Gap Result:** `no RED or BLACK trigger active`
- **Single-Agent Viability Rationale:** The task is limited to one endpoint-local documentation triplet for one already bounded endpoint family.

## Information Units
[INTENT: SPEZIFIKATION]

### IU-001: Endpoint-local `CONVENTIONS.md`
**Type:** `object`

**Description:**
Create the conventions file that becomes the local SSOT for endpoint-specific guardrails, policies, and architectural rationale.

**Ist-Zustand:**
The endpoint has code and registration surfaces but no endpoint-local conventions file.

**Soll-Zustand:**
`CONVENTIONS.md` explains traversal hardening, metadata behavior, grouped metadata selection, and why the endpoint is separate from other discovery tools.

**Architectural Rationale:**
Endpoint-specific conventions must not live only in root docs.

### IU-002: Endpoint-local `DESCRIPTION.md`
**Type:** `object`

**Description:**
Create the endpoint-local architectural description optimized for LLM-agent comprehension.

**Ist-Zustand:**
Only code and registration surfaces describe the endpoint today.

**Soll-Zustand:**
`DESCRIPTION.md` explains the endpoint contract, shape, runtime model, and relationships to traversal guardrails and metadata selection.

**Architectural Rationale:**
The endpoint needs a dedicated, LLM-optimized description surface.

### IU-003: Endpoint-local `README.md`
**Type:** `object`

**Description:**
Create the concise DX summary for the endpoint.

**Ist-Zustand:**
There is no endpoint-local DX summary file.

**Soll-Zustand:**
`README.md` summarizes what the endpoint does, links the local conventions and description, and stays concise.

**Architectural Rationale:**
Developers need a short, high-signal entry surface instead of being forced directly into long conventions text.

## Actions
[INTENT: ANWEISUNG]

1. **Re-anchor the finalized traversal-admission task and current endpoint surfaces**
   - **What:** re-read task `1.6` plus the endpoint code, schema, registration, and current root docs
   - **Where:** authoritative references listed above
   - **How:** read them completely before drafting any endpoint-local doc content
   - **Why:** the endpoint-local docs must reflect the final runtime behavior and current code truth
   - **Inputs / References:** all authoritative references in this task file
   - **Output Contract:** current endpoint and dependency context are active
   - **Checkpoint Impact:** completes `cp-1-context-reanchor`
   - **Verification:** all required references are re-read successfully
   - **Stop if:** task `1.5` remains unresolved or any required reference is unreadable
2. **Create endpoint-local `CONVENTIONS.md`**
   - **What:** create the conventions file for this endpoint
   - **Where:** `src/domain/inspection/list-directory-entries/CONVENTIONS.md`
   - **How:** document endpoint-local guardrails, traversal policy, grouped metadata rules, and why the endpoint is distinct
   - **Why:** endpoint-specific policy detail must have a local SSOT
   - **Inputs / References:** current endpoint code plus finalized traversal-admission surfaces
   - **Output Contract:** the endpoint-local conventions SSOT exists
   - **Checkpoint Impact:** completes `cp-2-endpoint-conventions`
   - **Verification:** the file explains endpoint-local policies and does not duplicate root TOC content as its primary structure
   - **Stop if:** conventions would need to invent runtime behavior not implemented in code
3. **Create endpoint-local `DESCRIPTION.md`**
   - **What:** create the endpoint-local description file
   - **Where:** `src/domain/inspection/list-directory-entries/DESCRIPTION.md`
   - **How:** describe the endpoint contract, structured result model, traversal behavior, and architectural role in LLM-oriented detail
   - **Why:** agents need a dedicated endpoint description surface beyond raw code and registration text
   - **Inputs / References:** current endpoint code, schema, registration, and conventions
   - **Output Contract:** the endpoint-local architectural description exists
   - **Checkpoint Impact:** completes `cp-3-endpoint-description`
   - **Verification:** the file is detailed, LLM-oriented, and aligned to the current code surface
   - **Stop if:** the description would claim behavior not present in the handler/schema surfaces
4. **Create endpoint-local `README.md`**
   - **What:** create the concise DX summary file
   - **Where:** `src/domain/inspection/list-directory-entries/README.md`
   - **How:** summarize the endpoint purpose, key conventions, and links to the local `CONVENTIONS.md` and `DESCRIPTION.md`
   - **Why:** developers need a concise entry surface with quick orientation
   - **Inputs / References:** current endpoint code plus the newly created local docs
   - **Output Contract:** the endpoint-local DX summary exists
   - **Checkpoint Impact:** completes `cp-4-endpoint-readme`
   - **Verification:** the README stays concise and points to the local SSOT surfaces
   - **Stop if:** the README would need to duplicate the full conventions or description content

## Checkpoint Definitions
[INTENT: REFERENZ]

| Checkpoint ID | Kind | Completion Signal | Produces Artifacts | Resume Meaning |
|---------------|------|-------------------|--------------------|----------------|
| `cp-1-context-reanchor` | `sequential_step` | Task `1.6` and all authoritative references are re-read | none | drafting may begin |
| `cp-2-endpoint-conventions` | `artifact_milestone` | `CONVENTIONS.md` exists with endpoint-local policy ownership | `src/domain/inspection/list-directory-entries/CONVENTIONS.md` | conventions SSOT is complete |
| `cp-3-endpoint-description` | `artifact_milestone` | `DESCRIPTION.md` exists with endpoint-local architecture description | `src/domain/inspection/list-directory-entries/DESCRIPTION.md` | architectural description is complete |
| `cp-4-endpoint-readme` | `artifact_milestone` | `README.md` exists with concise DX summary and links | `src/domain/inspection/list-directory-entries/README.md` | endpoint doc triplet is complete |

## Execution Progress Ledger
[INTENT: REFERENZ]

| Checkpoint ID | Status | Evidence | Notes |
|---------------|--------|----------|-------|
| `cp-1-context-reanchor` | `PENDING` | authoritative references re-read | start here |
| `cp-2-endpoint-conventions` | `PENDING` | endpoint-local `CONVENTIONS.md` | depends on `cp-1-context-reanchor` |
| `cp-3-endpoint-description` | `PENDING` | endpoint-local `DESCRIPTION.md` | depends on `cp-2-endpoint-conventions` |
| `cp-4-endpoint-readme` | `PENDING` | endpoint-local `README.md` | depends on `cp-3-endpoint-description` |

## Resume Conflict Resolution Rules
[INTENT: CONSTRAINT]

- If one or two endpoint-local doc files exist but the triplet is incomplete, stop and ask whether the partial doc state should be accepted or reverified.
- If endpoint-local docs duplicate root-level TOC structure instead of owning endpoint-local detail, stop and escalate.
- If finalized task `1.6` cannot be re-anchored before local drafting begins, stop and escalate.

## Checkpoint Update Protocol
[INTENT: ANWEISUNG]

1. Mark `cp-1-context-reanchor` DONE only after all authoritative references are re-read and completed task `1.6` has been re-anchored.
2. Mark `cp-2-endpoint-conventions` DONE only after endpoint-local guardrails, policies, and architectural rationale are fully documented.
3. Mark `cp-3-endpoint-description` DONE only after the endpoint-local architectural description is complete and LLM-optimized.
4. Mark `cp-4-endpoint-readme` DONE only after the concise DX summary exists and links the other local docs.

## Acceptance Criteria
[INTENT: REFERENZ]

- [ ] `src/domain/inspection/list-directory-entries/CONVENTIONS.md` exists and is the endpoint-local SSOT for conventions and guardrails.
- [ ] `src/domain/inspection/list-directory-entries/DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents in detail.
- [ ] `src/domain/inspection/list-directory-entries/README.md` exists and provides a concise DX summary with links to the local docs.
- [ ] The documentation reflects finalized traversal-admission behavior and does not invent runtime behavior.
- [ ] The endpoint-local docs are ready to be referenced later from root-level TOC surfaces.

## Stop Conditions & User Escalation
[INTENT: CONSTRAINT]

- **Plan-vs-Reality Mismatch:** Stop if the current endpoint behavior differs materially from the assumptions documented here.
- **Missing Required Plan Information:** Stop if the finalized traversal-admission dependency or endpoint-local doc-role split cannot be bound exactly.
- **Autonomous Decision Required:** Stop if execution would require inventing endpoint behavior, policy semantics, or root-TOC behavior not already agreed.
- **Decision Surface Not Closed:** If any determinable documentation-ownership or runtime-wording surface remains open, stop and inform the user.
- **Allowed Runtime Discovery:** None.
- **User Message Contract:** `Plan says X / current reality is Y / missing or conflicting information is Z / continuing would require an autonomous decision.`

## Post-Completion Instructions
[INTENT: ANWEISUNG]

1. Mark this task `[x]` in [`.plan/2-inspection-discovery-docs/orchestration.md`](./orchestration.md).
2. Keep the resulting doc triplet ready for later root-level TOC linking.
3. Re-anchor [`PLAN.md`](../../PLAN.md).

## File Path Index
[INTENT: REFERENZ]

| # | Path | Kind | Why Relevant | Must Read Before Execution |
|---|------|------|--------------|----------------------------|
| 1 | `src/domain/inspection/list-directory-entries/handler.ts` | `business-file` | current endpoint behavior and structured result ownership | `true` |
| 2 | `src/domain/inspection/list-directory-entries/schema.ts` | `schema-file` | current request/result contract surfaces | `true` |
| 3 | `src/application/server/register-inspection-tool-catalog.ts` | `business-file` | current public registration wording | `true` |
| 4 | `src/domain/shared/guardrails/traversal-scope-policy.ts` | `schema-file` | traversal exclusion and re-include model | `true` |
| 5 | `src/domain/shared/guardrails/filesystem-preflight.ts` | `schema-file` | metadata-first validation model | `true` |
| 6 | `src/domain/shared/guardrails/traversal-runtime-budget.ts` | `schema-file` | deeper runtime safeguard semantics after task `1.6` | `true` |
| 7 | `README.md` | `markdown-doc` | root DX surface that will later reference the endpoint-local README | `true` |
| 8 | `DESCRIPTION.md` | `markdown-doc` | root architecture surface that will later reference the endpoint-local description | `true` |

## Notes
[INTENT: KONTEXT]

- This task creates endpoint-local documentation only; root TOC integration happens in unit 10.
- The docs must anchor directly to the finalized traversal semantics from completed task `1.6`.

