---
file_type: "task"
task_id: "1.4"
task_title: "Read endpoint internal SSOT refactor"
unit: "1"
parent_orchestration: ".plan/1-runtime-architecture-refactors/orchestration.md"
classification: "SEQUENTIAL"
status: "pending"
estimated_complexity: "HIGH"
files_modified:
  - "src/domain/inspection/read-file-content/handler.ts"
  - "src/domain/inspection/read-files-with-line-numbers/handler.ts"
  - "src/infrastructure/filesystem/streaming-file-content-reader.ts"
  - "src/infrastructure/filesystem/text-read-core.ts"
  - "src/domain/shared/search/inspection-content-state.ts"
files_read:
  - "src/domain/inspection/read-file-content/schema.ts"
  - "src/domain/inspection/read-file-content/handler.ts"
  - "src/domain/inspection/read-files-with-line-numbers/schema.ts"
  - "src/domain/inspection/read-files-with-line-numbers/handler.ts"
  - "src/infrastructure/filesystem/streaming-file-content-reader.ts"
  - "src/application/server/register-inspection-tool-catalog.ts"
  - "__bak__/plan-ugrep/.plan/3-read-content/3.1-add-read-file-content-endpoint-with-streaming-ranges.md"
dependencies:
  - task_id: "1.3"
    type: "SEQUENTIAL"
    status: "UNRESOLVED"
blocked_by: "1.3"
information_completeness_mode: "FULLY_SPECIFIED"
implementation_scenarios:
  - "FUNCTION_CREATION"
  - "FUNCTION_UPDATE"
  - "OBJECT_UPDATE"
execution_surface_band: "GREEN"
primary_split_axis: "none"
single_agent_viability_note: "The task stays inside the two public read endpoints and a new shared internal read core without broadening into search/count documentation rollout."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
decision_surface_register:
  - surface: "naming"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "Preserve the two public endpoint names unchanged. Introduce `text-read-core.ts` as the internal SSOT file."
  - surface: "path_and_placement"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "Create the shared internal read core in `src/infrastructure/filesystem/text-read-core.ts` and refactor both handlers to consume it."
  - surface: "literal_values_and_budgets"
    status: "BOUND_REFERENCE"
    binding_source: "existing project state"
    notes: "Preserve current line-range, byte-range, cursor, and response-budget thresholds."
  - surface: "defaults_and_fallbacks"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "Public split remains; only internal shared business logic converges."
  - surface: "types_and_shapes"
    status: "BOUND_LITERAL"
    binding_source: "task contract"
    notes: "No public contract merge is allowed. The new shared core is internal only."
  - surface: "function_signatures_and_returns"
    status: "BOUND_REFERENCE"
    binding_source: "authoritative code surfaces"
    notes: "The two public handlers remain intact; the new internal core owns shared whole-read and shaping logic."
  - surface: "object_schema_and_mapper_contracts"
    status: "BOUND_LITERAL"
    binding_source: "task contract"
    notes: "The internal core must support small full-text reads, line numbering, and shared content-state gating without changing public schemas."
  - surface: "dependencies_and_import_surfaces"
    status: "BOUND_REFERENCE"
    binding_source: "shared SSOT"
    notes: "Read gating consumes the shared inspection-state taxonomy."
  - surface: "validation_and_boundary_rules"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "The task must not collapse the two public endpoints into one mega-endpoint."
  - surface: "consumer_and_callsite_bindings"
    status: "BOUND_LITERAL"
    binding_source: "existing project state"
    notes: "Tool registration descriptions may be updated later, but this task preserves the current public tool split."
authoritative_literal_bindings:
  - binding_domain: "public split rule"
    exact_binding: "`read_files_with_line_numbers` and `read_file_content` remain separate public endpoints."
    source: "accepted architecture"
    placement_or_consumer: "read handlers, schemas, registration reasoning"
    no_autonomous_choice_rule: "The execute agent must not merge them into one public endpoint in this task."
  - binding_domain: "internal SSOT rule"
    exact_binding: "Introduce one internal shared read core that owns common small full-read logic, line-number shaping, and content-state gating used by both read handlers."
    source: "accepted architecture"
    placement_or_consumer: "`src/infrastructure/filesystem/text-read-core.ts` and both read handlers"
    no_autonomous_choice_rule: "The execute agent must not leave the duplicated whole-read logic in both handlers as the long-term architecture."
  - binding_domain: "public contract preservation"
    exact_binding: "The existing request schemas and endpoint names remain stable; only internal shared logic converges."
    source: "accepted architecture"
    placement_or_consumer: "schemas and handlers"
    no_autonomous_choice_rule: "The execute agent must not widen `read_file_content` into a multi-target public tool."
requires_authoritative_code_surfaces: true
authoritative_code_surfaces:
  - surface_name: "Internal read core contract"
    kind: "shape"
    snippet_or_contract: "The shared internal read core must expose reusable helpers for small full-text reads, line-number shaping, and content-state validation that both public handlers consume."
    source: "task contract"
    placement_or_consumer: "`src/infrastructure/filesystem/text-read-core.ts`"
  - surface_name: "Read-handler preservation contract"
    kind: "signature"
    snippet_or_contract: "Both public handlers remain and keep their current external responsibility split while delegating common logic to the new internal core."
    source: "task contract"
    placement_or_consumer: "`src/domain/inspection/read-file-content/handler.ts`, `src/domain/inspection/read-files-with-line-numbers/handler.ts`"
authoritative_references:
  - path: "src/domain/inspection/read-file-content/schema.ts"
    kind: "schema-file"
    usage: "current advanced single-file contract that must stay stable"
    required_before_execution: true
  - path: "src/domain/inspection/read-file-content/handler.ts"
    kind: "business-file"
    usage: "current advanced single-file handler"
    required_before_execution: true
  - path: "src/domain/inspection/read-files-with-line-numbers/schema.ts"
    kind: "schema-file"
    usage: "current bounded multi-file contract that must stay stable"
    required_before_execution: true
  - path: "src/domain/inspection/read-files-with-line-numbers/handler.ts"
    kind: "business-file"
    usage: "current bounded multi-file handler"
    required_before_execution: true
  - path: "src/infrastructure/filesystem/streaming-file-content-reader.ts"
    kind: "business-file"
    usage: "existing advanced read helper surface"
    required_before_execution: true
  - path: "src/application/server/register-inspection-tool-catalog.ts"
    kind: "business-file"
    usage: "current public read-tool split reasoning"
    required_before_execution: true
  - path: "__bak__/plan-ugrep/.plan/3-read-content/3.1-add-read-file-content-endpoint-with-streaming-ranges.md"
    kind: "plan-file"
    usage: "historical read endpoint lineage only"
    required_before_execution: true
upstream_plan_references:
  - task_id: "1.3"
    path: ".plan/1-runtime-architecture-refactors/1.3-regex-and-count-state-gating-alignment.md"
    must_read_before_execution: true
    reason: "The shared state taxonomy and gating semantics must already be stable before the read-core SSOT refactor lands."
runtime_discovery:
  required: false
  scope: []
  allowed_topics: []
checkpoint_strategy: "ARTIFACT_MILESTONES_REQUIRED"
checkpoint_eligibility_contract:
  durable_skip_authority_only: true
  ephemeral_freshness_steps:
    - "Re-read both public read handlers and schemas"
  durable_resume_checkpoints:
    - "cp-1-shared-read-core"
    - "cp-2-read-handler-convergence"
resume_validation_contract:
  lowest_level_truth_surface: "artifact-evidence"
  task_lifecycle_truth_surface: ".plan/1-runtime-architecture-refactors/orchestration.md"
  global_frontier_truth_surface: "PLAN.md"
  artifact_evidence_sources:
    - "src/infrastructure/filesystem/text-read-core.ts"
    - "src/domain/inspection/read-file-content/handler.ts"
    - "src/domain/inspection/read-files-with-line-numbers/handler.ts"
  conflict_requires_user_confirmation: true
  partial_progress_requires_user_decision: true
  durable_skip_authority_only: true
  ephemeral_checkpoint_conflict_requires_reexecution: true
artifact_progress_inference_rules:
  - "Treat the task as partially complete only when the new internal core exists or at least one public handler delegates shared logic to it."
  - "Do not treat registration description changes alone as proof of internal SSOT convergence."
  - "Ephemeral reference re-reads never create durable skip authority."
user_resume_decision_gate:
  required_on_partial_progress: true
  required_on_state_conflict: true
  default_user_choices:
    - "ACCEPT_AND_CONTINUE"
    - "REVERIFY_EXISTING_OUTPUTS"
persona_generation_required: true
persona_generation_mode: "runtime_regenerate"
persona_source_prompt_path: "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
persona_primary_domain_kernel: "Enterprise read-surface architecture"
persona_secondary_specializations:
  - "TypeScript handler refactoring"
  - "shared internal SSOT design"
persona_current_task_specialization: "Separate public read endpoints with internal shared core"
persona_generation_lineage: "Derived from the accepted public-split/private-SSOT architecture and the current read endpoint lineage."
persona_runtime_review_required: true
persona_dx_snapshot:
  title: "Enterprise Read Surface Architect"
  summary: "Preserves distinct public read tools while converging their shared internal logic into one reusable core."
  weighting_note: "The public split rule dominates over lower-level helper implementation choices."
checkpoint_definitions:
  - checkpoint_id: "cp-1-shared-read-core"
    kind: "artifact_milestone"
    completion_signal: "The new internal shared read core exists and exposes the bound common logic."
    produces_artifacts:
      - "src/infrastructure/filesystem/text-read-core.ts"
  - checkpoint_id: "cp-2-read-handler-convergence"
    kind: "artifact_milestone"
    completion_signal: "Both public read handlers delegate their shared logic to the new core while preserving the public split."
    produces_artifacts:
      - "src/domain/inspection/read-file-content/handler.ts"
      - "src/domain/inspection/read-files-with-line-numbers/handler.ts"
execution_progress_ledger:
  ledger_mode: "CHECKPOINT_LEDGER"
  checkpoints:
    - checkpoint_id: "cp-1-shared-read-core"
      status: "PENDING"
    - checkpoint_id: "cp-2-read-handler-convergence"
      status: "PENDING"
resume_conflict_resolution_rules:
  - "If the new internal core exists but only one public handler delegates to it, stop and escalate the partial state."
  - "If either public handler is broadened into the other tool’s public role, stop and escalate."
checkpoint_update_protocol:
  - "Mark `cp-1-shared-read-core` DONE only after the internal core exists with the bound helper contract."
  - "Mark `cp-2-read-handler-convergence` DONE only after both handlers delegate shared logic to the core and still preserve their distinct public responsibilities."
halt_conditions:
  plan_reality_mismatch: true
  missing_required_plan_information: true
  autonomous_decision_required: true
user_notification_required: true
---

# Task 1.4: Read endpoint internal SSOT refactor
[INTENT: ANWEISUNG]

## Goal
[INTENT: KONTEXT]

Preserve the two existing public read endpoints while refactoring their duplicated internal full-read and shaping logic onto one shared internal SSOT.

This task explicitly rejects a public mega-endpoint and instead implements the architecture that was accepted in the current analysis: separate public tools, shared internal business logic.

## Expected Result
[INTENT: KONTEXT]

- The public endpoints `read_files_with_line_numbers` and `read_file_content` both remain available.
- A new internal shared read core exists.
- Both handlers delegate their shared logic to the new core while keeping their distinct public roles.

## Preconditions
[INTENT: CONSTRAINT]

- Task `1.3` must be completed.
- The execute agent must not merge the public endpoints or widen `read_file_content` into a multi-target tool.

## Execution Metadata Register
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Task ID | `1.4` | Unique task identifier |
| Classification | `SEQUENTIAL` | The task depends on earlier shared runtime-state alignment |
| Information Completeness Mode | `FULLY_SPECIFIED` | All determinable surfaces are bound |
| Implementation Scenarios | `FUNCTION_CREATION`, `FUNCTION_UPDATE`, `OBJECT_UPDATE` | New internal core plus handler convergence |
| Execution Surface Band | `GREEN` | One bounded read-surface refactor |
| Primary Split Axis | `none` | No further split is required |
| Single-Agent Viability Note | The task stays inside the two public read handlers and one new internal core, without spilling into endpoint documentation or search/count rollout. |
| Parent Orchestration | `.plan/1-runtime-architecture-refactors/orchestration.md` | Lifecycle status is tracked there |
| Blocked By | `1.3` | Shared state and gating blocker |
| Requires Upstream Plan Re-Anchoring | `yes` | Task `1.3` must be read first |
| Runtime Discovery Allowed | `no` | This task is fully specified |
| Checkpoint Strategy | `ARTIFACT_MILESTONES_REQUIRED` | Shared core creation and handler convergence are both durable outputs |
| Durable Skip Authority Only | `true` | Only durable artifact changes create skip authority |
| Ephemeral Freshness Steps Present | `yes` | Reference re-reads remain non-durable |
| User Notification Required On Gap/Mismatch | `true` | Mismatches must stop execution |
| Persona Generation Required | `true` | Runtime persona regeneration remains mandatory |
| Persona Generation Mode | `runtime_regenerate` | Persona is not fully persisted |
| Persona Primary Domain Kernel | `Enterprise read-surface architecture` | Highest-weight persona kernel |
| Persona Current Task Specialization | `Internal SSOT refactor while preserving public read split` | Current task specialization |
| Persona Runtime Review Required | `true` | Runtime review is required |

## Task Contract Closure
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Decision Surfaces Closed | `true` | All determinable surfaces are bound |
| Unresolved Determinable Decisions | `0` | No deterministic gap remains |
| Expected Execute-Agent Questions | `0` | A fresh execute agent should not need follow-up questions |

## Decision Surface Register
[INTENT: REFERENZ]

| Surface | Status | Binding Source | Notes |
|---------|--------|----------------|-------|
| `naming` | `BOUND_LITERAL` | accepted architecture | Preserve the two public endpoint names; add `text-read-core.ts` as the internal SSOT surface. |
| `path_and_placement` | `BOUND_LITERAL` | plan output | New internal core goes in `src/infrastructure/filesystem/`. |
| `literal_values_and_budgets` | `BOUND_REFERENCE` | existing project state | Preserve the current public thresholds and budgets. |
| `defaults_and_fallbacks` | `BOUND_LITERAL` | accepted architecture | Public split remains; only internal duplication is removed. |
| `types_and_shapes` | `BOUND_LITERAL` | task contract | Public schemas stay stable. |
| `function_signatures_and_returns` | `BOUND_REFERENCE` | authoritative code surfaces | The new internal core and both handler delegation points are the decisive surfaces. |
| `object_schema_and_mapper_contracts` | `BOUND_LITERAL` | task contract | Shared full-read and line-number shaping logic must centralize internally. |
| `dependencies_and_import_surfaces` | `BOUND_REFERENCE` | shared SSOT | Read gating consumes the shared inspection-state taxonomy. |
| `validation_and_boundary_rules` | `BOUND_LITERAL` | accepted architecture | The task must not create a public all-in-one read endpoint. |
| `consumer_and_callsite_bindings` | `BOUND_LITERAL` | existing project state | Public registrations remain separate and continue to describe distinct user-facing roles. |

## Authoritative Literal Bindings
[INTENT: REFERENZ]

| Binding Domain | Exact Binding | Source | Placement / Consumer | No Autonomous Choice Rule |
|---------------|---------------|--------|----------------------|---------------------------|
| Public split rule | `read_files_with_line_numbers` and `read_file_content` both remain public | accepted architecture | handlers, schemas, registration reasoning | The execute agent must not merge them into one public endpoint. |
| Internal SSOT rule | one shared internal read core owns common full-read, shaping, and gating logic | accepted architecture | `src/infrastructure/filesystem/text-read-core.ts` | The execute agent must not leave duplicated shared logic in both handlers as the target state. |
| Public contract preservation rule | existing schemas remain stable while internals converge | accepted architecture | schemas and handlers | The execute agent must not widen `read_file_content` into a multi-target endpoint. |

## Authoritative Code / Contract Surfaces
[INTENT: REFERENZ]

| Surface Name | Kind | Snippet / Contract | Source | Placement / Consumer |
|-------------|------|--------------------|--------|----------------------|
| Internal read core contract | `shape` | expose reusable helpers for small full-text reads, line-number shaping, and content-state validation | task contract | `src/infrastructure/filesystem/text-read-core.ts` |
| Read-handler preservation contract | `signature` | both public handlers remain and delegate shared logic to the internal core | task contract | `src/domain/inspection/read-file-content/handler.ts`, `src/domain/inspection/read-files-with-line-numbers/handler.ts` |

## Execute-Agent Zero-Question Gate
[INTENT: CONSTRAINT]

- **Gate Result:** `PASS`
- **Checked Domains:** public endpoint split, internal-core placement, shared logic ownership, and public contract preservation
- **Rule:** A fresh execute agent must not need to ask whether the public endpoints are merged, where the internal core belongs, or whether the public schemas may change.

## Authoritative References
[INTENT: REFERENZ]

| Path | Kind | Must Read Before Execution | Purpose | Referenced By |
|------|------|---------------------------|---------|---------------|
| `src/domain/inspection/read-file-content/schema.ts` | `schema-file` | `true` | current advanced single-file contract | actions 1-3 |
| `src/domain/inspection/read-file-content/handler.ts` | `business-file` | `true` | current advanced single-file handler | actions 1-3 |
| `src/domain/inspection/read-files-with-line-numbers/schema.ts` | `schema-file` | `true` | current bounded multi-file contract | actions 1-3 |
| `src/domain/inspection/read-files-with-line-numbers/handler.ts` | `business-file` | `true` | current bounded multi-file handler | actions 1-3 |
| `src/infrastructure/filesystem/streaming-file-content-reader.ts` | `business-file` | `true` | existing advanced read helper surface | actions 1-3 |
| `src/application/server/register-inspection-tool-catalog.ts` | `business-file` | `true` | current public read-tool split | context |
| `__bak__/plan-ugrep/.plan/3-read-content/3.1-add-read-file-content-endpoint-with-streaming-ranges.md` | `plan-file` | `true` | historical read endpoint lineage only | context |

## Resume Validation Contract
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Lowest Level Truth Surface | `artifact-evidence` | The new internal core and both updated handlers are the primary resume truth |
| Task Lifecycle Truth Surface | `.plan/1-runtime-architecture-refactors/orchestration.md` | Parent orchestration owns lifecycle state |
| Global Frontier Truth Surface | `PLAN.md` | Global frontier SSOT |
| Conflict Requires User Confirmation | `true` | Conflicting partial state requires escalation |
| Partial Progress Requires User Decision | `true` | Partial artifact state requires a user choice |
| Durable Skip Authority Only | `true` | Only durable artifact changes may authorize skip |
| Ephemeral Freshness Steps Must Re-Execute | `true` | Pure reference re-reads remain re-executable preparation |

## Artifact Progress Inference Rules
[INTENT: REFERENZ]

- Treat the task as partially complete only when the new internal core exists or at least one public handler delegates shared logic to it.
- Do not treat registration description changes alone as proof of internal SSOT convergence.
- `EPHEMERAL_FRESHNESS_STEP` re-reads never create durable skip authority.

## User Resume Decision Gate
[INTENT: CONSTRAINT]

- **Required on Partial Progress:** `true`
- **Required on State Conflict:** `true`
- **Allowed User Choices:** `ACCEPT_AND_CONTINUE` / `REVERIFY_EXISTING_OUTPUTS`

## Persona Derivation Manifest
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Persona Generation Required | `true` | task-specific persona derivation required |
| Persona Generation Mode | `runtime_regenerate` | execute regenerates the persona at runtime |
| Persona Source Prompt Path | `src/personas/it/__prompts__/silent/enterprise/creation/prompt.md` | authoritative persona source |
| Persona Primary Domain Kernel | `Enterprise read-surface architecture` | highest-weight persona layer |
| Persona Secondary Specializations | `TypeScript handler refactoring`, `shared internal SSOT design` | secondary persona layers |
| Persona Current Task Specialization | `preserve public split, unify internal read logic` | current specialization |
| Persona Generation Lineage | `Derived from the accepted public-read split and internal SSOT architecture.` | derivation lineage |
| Persona Runtime Review Required | `true` | runtime review is required |

## Persona DX Snapshot
[INTENT: KONTEXT]

- **Persona Title:** `Enterprise Read Surface Architect`
- **Persona Summary:** Preserves distinct public read tools while converging their shared internal logic into one reusable core.
- **Persona Weighting Note:** The public split rule dominates over lower-level helper implementation choices.

## Context
[INTENT: KONTEXT]

- **Domain:** public read-surface preservation with internal SSOT convergence
- **Related Files:** both read schemas, both read handlers, existing streaming reader, registration surface, historical read endpoint plan
- **Technical Context:** the current implementation duplicates some small full-read logic across the two public handlers
- **Business Context:** agents benefit from two distinct public read tools, but the server should not maintain redundant internal business logic
- **Architectural Context:** keep the public split, centralize the internal core
- **Plan Context Dependencies:** read task `1.3` first
- **Runtime Discovery Context:** none

## Execution Surface Assessment
[INTENT: REFERENZ]

- **Execution Surface Band:** `GREEN`
- **Primary Split Axis:** `none`
- **Hard-Gap Result:** `no RED or BLACK trigger active`
- **Single-Agent Viability Rationale:** The task stays inside one coherent read-surface refactor concern.

## Information Units
[INTENT: SPEZIFIKATION]

### IU-001: Preserve separate public read endpoints
**Type:** `constraint`

**Description:**
Keep the two current public read endpoints separate.

**Ist-Zustand:**
The server already exposes two distinct public read tools.

**Soll-Zustand:**
The two public tools remain separate because they serve different caller-facing roles.

**Architectural Rationale:**
Prompt efficiency and MCP clarity are higher with two narrow tools than with one mega-endpoint.

### IU-002: Introduce the shared internal read core
**Type:** `function`

**Description:**
Create the new internal shared read core that centralizes duplicated common logic.

**Ist-Zustand:**
Both handlers own overlapping whole-read and shaping behavior.

**Soll-Zustand:**
One internal core owns the shared logic while both public handlers remain intact.

**Architectural Rationale:**
The system needs a true internal SSOT without weakening the public endpoint contract model.

### IU-003: Converge both handlers onto the shared core
**Type:** `function`

**Description:**
Refactor both public handlers to delegate common logic to the new internal core.

**Ist-Zustand:**
The handlers duplicate overlapping logic.

**Soll-Zustand:**
Shared logic lives in one place while each public handler keeps its distinct public responsibility.

**Architectural Rationale:**
The accepted architecture is public split + private SSOT.

## Actions
[INTENT: ANWEISUNG]

1. **Create the internal shared read core**
   - **What:** add the new internal read-core file
   - **Where:** `src/infrastructure/filesystem/text-read-core.ts`
   - **How:** centralize small full-read, line-number shaping, and content-state validation helpers that both public handlers can consume
   - **Why:** duplicated shared logic must move into one SSOT
   - **Inputs / References:** both current handlers, schemas, streaming reader, and shared state taxonomy
   - **Output Contract:** the internal core exists with full TSDoc and a stable helper contract
   - **Checkpoint Impact:** completes `cp-1-shared-read-core`
   - **Verification:** the new core exposes the bound common-logic surfaces
   - **Stop if:** the new core would require merging the public endpoints or changing public request schemas
2. **Refactor `read_files_with_line_numbers` onto the internal core**
   - **What:** update the bounded multi-file handler to use the internal core for shared full-read and shaping behavior
   - **Where:** `src/domain/inspection/read-files-with-line-numbers/handler.ts`
   - **How:** preserve the public contract, delegate common logic only
   - **Why:** remove duplicated shared logic without changing the tool’s public role
   - **Inputs / References:** current handler, current schema, new internal core
   - **Output Contract:** handler remains public and distinct but uses the shared core for common behavior
   - **Checkpoint Impact:** contributes to `cp-2-read-handler-convergence`
   - **Verification:** public batch semantics remain intact and shared logic delegation is visible
   - **Stop if:** implementation would broaden the tool into range/cursor semantics
3. **Refactor `read_file_content` onto the internal core**
   - **What:** update the advanced single-file handler to use the internal core for the overlapping shared logic while keeping its explicit range/cursor modes
   - **Where:** `src/domain/inspection/read-file-content/handler.ts`
   - **How:** preserve mode-specific advanced behavior and delegate only shared logic
   - **Why:** the public split remains, but shared business logic converges
   - **Inputs / References:** current handler, current schema, streaming reader, new internal core
   - **Output Contract:** handler remains public and distinct while delegating the overlapping shared logic
   - **Checkpoint Impact:** completes `cp-2-read-handler-convergence`
   - **Verification:** public single-file advanced behavior stays intact and duplicated shared logic is removed
   - **Stop if:** implementation would widen the endpoint into a multi-target public tool

## Checkpoint Definitions
[INTENT: REFERENZ]

| Checkpoint ID | Kind | Completion Signal | Produces Artifacts | Resume Meaning |
|---------------|------|-------------------|--------------------|----------------|
| `cp-1-shared-read-core` | `artifact_milestone` | The internal shared read core exists with the bound helper contract | `src/infrastructure/filesystem/text-read-core.ts` | Shared internal SSOT exists |
| `cp-2-read-handler-convergence` | `artifact_milestone` | Both public handlers delegate shared logic to the new core while preserving the public split | `src/domain/inspection/read-file-content/handler.ts`, `src/domain/inspection/read-files-with-line-numbers/handler.ts` | The read-surface refactor is complete |

## Execution Progress Ledger
[INTENT: REFERENZ]

| Checkpoint ID | Status | Evidence | Notes |
|---------------|--------|----------|-------|
| `cp-1-shared-read-core` | `PENDING` | new internal read core | start here |
| `cp-2-read-handler-convergence` | `PENDING` | updated public handlers | depends on `cp-1-shared-read-core` |

## Resume Conflict Resolution Rules
[INTENT: CONSTRAINT]

- If the new internal core exists but only one handler delegates to it, stop and escalate.
- If either public handler is broadened into the other tool’s public role, stop and escalate.
- If duplicated shared logic still exists in both handlers after the claimed refactor, stop and escalate.

## Checkpoint Update Protocol
[INTENT: ANWEISUNG]

1. Mark `cp-1-shared-read-core` DONE only after the internal core exists with the bound helper contract.
2. Mark `cp-2-read-handler-convergence` DONE only after both handlers delegate shared logic to the core and still preserve their distinct public responsibilities.

## Acceptance Criteria
[INTENT: REFERENZ]

- [ ] Both public read endpoints still exist and remain separate.
- [ ] A new internal shared read core exists.
- [ ] Both public read handlers delegate common shared logic to the internal core.
- [ ] No public contract merge or public mega-endpoint is introduced.

## Stop Conditions & User Escalation
[INTENT: CONSTRAINT]

- **Plan-vs-Reality Mismatch:** Stop if the current read handlers already use a conflicting internal SSOT model.
- **Missing Required Plan Information:** Stop if the internal-core placement or public split rule cannot be bound exactly.
- **Autonomous Decision Required:** Stop if execution would require merging the public endpoints or widening their public schemas.
- **Decision Surface Not Closed:** If any determinable public-split or internal-SSOT rule is still open, stop and inform the user.
- **Allowed Runtime Discovery:** None.
- **User Message Contract:** `Plan says X / current reality is Y / missing or conflicting information is Z / continuing would require an autonomous decision.`

## Post-Completion Instructions
[INTENT: ANWEISUNG]

1. Mark this task `[x]` in [`.plan/1-runtime-architecture-refactors/orchestration.md`](./orchestration.md).
2. Unblock [`1.5-traversal-preflight-and-runtime-budget-refactor.md`](./1.5-traversal-preflight-and-runtime-budget-refactor.md).
3. Re-anchor [`PLAN.md`](../../PLAN.md).

## File Path Index
[INTENT: REFERENZ]

| # | Path | Kind | Why Relevant | Must Read Before Execution |
|---|------|------|--------------|----------------------------|
| 1 | `src/domain/inspection/read-file-content/schema.ts` | `schema-file` | advanced single-file contract | `true` |
| 2 | `src/domain/inspection/read-file-content/handler.ts` | `business-file` | advanced single-file handler | `true` |
| 3 | `src/domain/inspection/read-files-with-line-numbers/schema.ts` | `schema-file` | bounded multi-file contract | `true` |
| 4 | `src/domain/inspection/read-files-with-line-numbers/handler.ts` | `business-file` | bounded multi-file handler | `true` |
| 5 | `src/infrastructure/filesystem/streaming-file-content-reader.ts` | `business-file` | existing advanced read helper surface | `true` |
| 6 | `src/application/server/register-inspection-tool-catalog.ts` | `business-file` | public read-tool split reasoning | `true` |
| 7 | `__bak__/plan-ugrep/.plan/3-read-content/3.1-add-read-file-content-endpoint-with-streaming-ranges.md` | `plan-file` | historical read endpoint lineage only | `true` |

## Notes
[INTENT: KONTEXT]

- The accepted architecture is explicit: separate public read endpoints, unified internal SSOT.
- This task must not turn `read_file_content` into a multi-target public tool.

