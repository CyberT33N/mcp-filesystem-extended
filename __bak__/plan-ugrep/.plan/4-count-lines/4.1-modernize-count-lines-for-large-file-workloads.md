---
file_type: "task"
task_id: "4.1"
task_title: "Modernize `count_lines` for large-file-safe total and pattern-aware counting"
unit: "4"
parent_orchestration: ".plan/4-count-lines/orchestration.md"
classification: "WAITING"
status: "pending"
estimated_complexity: "HIGH"
files_modified:
  - "src/domain/inspection/count-lines/schema.ts"
  - "src/domain/inspection/count-lines/handler.ts"
  - "src/infrastructure/filesystem/streaming-line-counter.ts"
  - "src/domain/shared/search/count-query-policy.ts"
dependencies:
  - task_id: "1.1"
    type: "WAITING"
    status: "UNRESOLVED"
  - task_id: "2.1"
    type: "WAITING"
    status: "UNRESOLVED"
blocked_by: "1.1"
files_read:
  - ".plan/1-runtime-governance/1.1-io-capability-profile-and-execution-policy-registry.md"
  - ".plan/2-search-platform/2.1-ugrep-adapter-and-classification-surfaces.md"
  - "src/domain/inspection/count-lines/handler.ts"
  - "src/domain/inspection/count-lines/schema.ts"
  - "src/domain/shared/guardrails/tool-guardrail-limits.ts"
  - "src/domain/shared/guardrails/text-response-budget.ts"
information_completeness_mode: "FULLY_SPECIFIED"
implementation_scenarios:
  - "FUNCTION_UPDATE"
  - "FUNCTION_CREATION"
  - "OBJECT_CREATION"
execution_surface_band: "YELLOW"
primary_split_axis: "none"
single_agent_viability_note: "The task changes one endpoint family and introduces two tightly related supporting surfaces for large-file-safe counting."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
decision_surface_register:
  - surface: "naming"
    status: "BOUND_LITERAL"
    binding_source: "existing-project-state"
    notes: "Preserve the public endpoint name `count_lines` and the current schema field names unless an additive contract-safe extension is explicitly required."
  - surface: "path_and_placement"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "Modify only the existing count-lines files plus the two new support files bound in `files_modified`."
  - surface: "literal_values_and_budgets"
    status: "BOUND_REFERENCE"
    binding_source: "runtime-policy-and-guardrail-ssot"
    notes: "Consume the runtime capability registry and the shared limit registry instead of inventing endpoint-local large-file heuristics."
  - surface: "defaults_and_fallbacks"
    status: "BOUND_LITERAL"
    binding_source: "architecture-decision"
    notes: "Total counts use the streaming line counter; pattern-aware counts use the shared native-search backend via an explicit count query policy."
  - surface: "types_and_shapes"
    status: "BOUND_LITERAL"
    binding_source: "task-contract"
    notes: "The public request surface remains centered on `paths`, recursion, regex, include/exclude globs, `includeExcludedGlobs`, `respectGitIgnore`, and `ignoreEmptyLines`."
  - surface: "function_signatures_and_returns"
    status: "BOUND_REFERENCE"
    binding_source: "authoritative-code-surface"
    notes: "Create explicit streaming counter and count-query-policy surfaces without renaming the public endpoint entrypoints."
  - surface: "object_schema_and_mapper_contracts"
    status: "BOUND_LITERAL"
    binding_source: "task-contract"
    notes: "The count query policy must distinguish total-only counting from pattern-aware counting."
  - surface: "dependencies_and_import_surfaces"
    status: "BOUND_REFERENCE"
    binding_source: "upstream-task-contracts"
    notes: "Consume the runtime policy registry from task `1.1` and the shared native-search adapter assumptions from task `2.1`."
  - surface: "validation_and_boundary_rules"
    status: "BOUND_LITERAL"
    binding_source: "architecture-decision"
    notes: "Preserve traversal policy, path validation, and response-budget enforcement."
  - surface: "consumer_and_callsite_bindings"
    status: "BOUND_LITERAL"
    binding_source: "existing-project-state"
    notes: "The public `count_lines` registration stays in the inspection tool catalog."
authoritative_literal_bindings:
  - binding_domain: "public endpoint name"
    exact_binding: "`count_lines` remains the public endpoint name."
    source: "existing project state"
    placement_or_consumer: "Count-lines schema, handler, and registration"
    no_autonomous_choice_rule: "Do not rename the public counting endpoint."
  - binding_domain: "execution split"
    exact_binding: "Pure total-line counts use the streaming line counter; regex or pattern-aware counts use the shared native-search backend via the new count query policy."
    source: "agreed architecture"
    placement_or_consumer: "Count-lines handler"
    no_autonomous_choice_rule: "Do not keep giant whole-file in-process reads for large-file counting."
  - binding_domain: "large-file applicability"
    exact_binding: "`count_lines` is part of the large-file architecture and must inherit the runtime I/O capability profile for escalation and policy decisions."
    source: "explicit user agreement"
    placement_or_consumer: "Count-lines policy and handler"
    no_autonomous_choice_rule: "Do not treat count-lines as a permanently small-file-only endpoint."
requires_authoritative_code_surfaces: true
authoritative_code_surfaces:
  - surface_name: "Streaming line counter contract"
    kind: "signature"
    snippet_or_contract: "Provide dedicated total-line and matching-line counting helpers that can operate without loading giant files as one string."
    source: "task contract"
    placement_or_consumer: "`src/infrastructure/filesystem/streaming-line-counter.ts`"
  - surface_name: "Count query policy contract"
    kind: "shape"
    snippet_or_contract: "Expose explicit policy fields for total-only counting, pattern-aware counting, preview/response expectations, and escalation thresholds."
    source: "task contract"
    placement_or_consumer: "`src/domain/shared/search/count-query-policy.ts`"
  - surface_name: "Public schema contract"
    kind: "shape"
    snippet_or_contract: "Preserve the current public schema field names and semantics unless a contract-safe additive extension is strictly required."
    source: "existing project state"
    placement_or_consumer: "`src/domain/inspection/count-lines/schema.ts`"
authoritative_references:
  - path: ".plan/1-runtime-governance/1.1-io-capability-profile-and-execution-policy-registry.md"
    kind: "plan-file"
    usage: "Runtime capability and escalation vocabulary"
    required_before_execution: true
  - path: ".plan/2-search-platform/2.1-ugrep-adapter-and-classification-surfaces.md"
    kind: "plan-file"
    usage: "Shared native-search adapter and classification assumptions"
    required_before_execution: true
  - path: "src/domain/inspection/count-lines/handler.ts"
    kind: "business-file"
    usage: "Current endpoint behavior to refactor"
    required_before_execution: true
  - path: "src/domain/inspection/count-lines/schema.ts"
    kind: "schema-file"
    usage: "Current public contract to preserve"
    required_before_execution: true
  - path: "src/domain/shared/guardrails/tool-guardrail-limits.ts"
    kind: "schema-file"
    usage: "Shared caps and guardrail registry"
    required_before_execution: true
  - path: "src/domain/shared/guardrails/text-response-budget.ts"
    kind: "schema-file"
    usage: "Response-budget utilities"
    required_before_execution: true
upstream_plan_references:
  - task_id: "1.1"
    path: ".plan/1-runtime-governance/1.1-io-capability-profile-and-execution-policy-registry.md"
    must_read_before_execution: true
    reason: "The counting logic must use the shared runtime capability registry."
  - task_id: "2.1"
    path: ".plan/2-search-platform/2.1-ugrep-adapter-and-classification-surfaces.md"
    must_read_before_execution: true
    reason: "Pattern-aware counting must align with the shared native-search adapter assumptions."
runtime_discovery:
  required: false
  scope: []
  allowed_topics: []
checkpoint_strategy: "ARTIFACT_MILESTONES_REQUIRED"
resume_validation_contract:
  lowest_level_truth_surface: "artifact-evidence"
  task_lifecycle_truth_surface: ".plan/4-count-lines/orchestration.md"
  global_frontier_truth_surface: "PLAN.md"
  artifact_evidence_sources:
    - "src/domain/inspection/count-lines/schema.ts"
    - "src/domain/inspection/count-lines/handler.ts"
    - "src/infrastructure/filesystem/streaming-line-counter.ts"
    - "src/domain/shared/search/count-query-policy.ts"
  conflict_requires_user_confirmation: true
  partial_progress_requires_user_decision: true
artifact_progress_inference_rules:
  - "Treat the task as partial progress if any subset of the four declared output files exists."
  - "Treat the task as complete only when the handler, schema, streaming line counter, and count query policy all exist and align."
user_resume_decision_gate:
  required_on_partial_progress: true
  required_on_state_conflict: true
  default_user_choices:
    - "ACCEPT_AND_CONTINUE"
    - "REVERIFY_EXISTING_OUTPUTS"
halt_conditions:
  plan_reality_mismatch: true
  missing_required_plan_information: true
  autonomous_decision_required: true
user_notification_required: true
---

# Task 4.1: Modernize `count_lines` for large-file-safe total and pattern-aware counting
[INTENT: ANWEISUNG]

## Goal
[INTENT: KONTEXT]

Refactor the existing `count_lines` endpoint so it can participate in the large-file architecture without relying on whole-file in-process reads for large workloads.

The final endpoint must differentiate between total-line counts and pattern-aware counts while keeping the public endpoint contract stable.

## Expected Result
[INTENT: KONTEXT]

- The public `count_lines` endpoint remains available.
- Large-file total counting uses a streaming line counter.
- Pattern-aware counting uses the shared native-search lane through the new count query policy.

## Preconditions
[INTENT: CONSTRAINT]

- Tasks `1.1` and `2.1` must be completed.

## Execution Metadata Register
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Task ID | `4.1` | Unique task identifier |
| Classification | `WAITING` | Cross-unit blockers exist |
| Information Completeness Mode | `FULLY_SPECIFIED` | All determinable surfaces are bound |
| Implementation Scenarios | `FUNCTION_UPDATE`, `FUNCTION_CREATION`, `OBJECT_CREATION` | Existing endpoint refactor plus two new supporting surfaces |
| Execution Surface Band | `YELLOW` | Four output files within one counting concern |
| Primary Split Axis | `none` | No further split is required |
| Parent Orchestration | `.plan/4-count-lines/orchestration.md` | Lifecycle authority |
| Blocked By | `1.1` | Runtime-governance blocker |
| Requires Upstream Plan Re-Anchoring | `yes` | Tasks `1.1` and `2.1` must be re-read |
| Runtime Discovery Allowed | `no` | Fully specified |
| Checkpoint Strategy | `ARTIFACT_MILESTONES_REQUIRED` | Multiple output files |
| User Notification Required On Gap/Mismatch | `true` | Stop on any mismatch |

## Task Contract Closure
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Decision Surfaces Closed | `true` | All determinable surfaces are bound |
| Unresolved Determinable Decisions | `0` | No deterministic gap remains |
| Expected Execute-Agent Questions | `0` | The task is execution-ready without clarification |

## Decision Surface Register
[INTENT: REFERENZ]

| Surface | Status | Binding Source | Notes |
|---------|--------|----------------|-------|
| `naming` | `BOUND_LITERAL` | Existing project state | Preserve the public endpoint name `count_lines`. |
| `path_and_placement` | `BOUND_LITERAL` | Plan output | Modify only the bound files in this task. |
| `literal_values_and_budgets` | `BOUND_REFERENCE` | Shared guardrail + runtime policy SSOT | Reuse the shared runtime policy and guardrail caps. |
| `defaults_and_fallbacks` | `BOUND_LITERAL` | Architecture decision | Split total-only counting from pattern-aware counting. |
| `types_and_shapes` | `BOUND_LITERAL` | Task contract | Preserve the current public schema semantics where intended. |
| `function_signatures_and_returns` | `BOUND_REFERENCE` | Authoritative code surfaces | The handler entrypoints remain stable; new helper surfaces are explicit. |
| `object_schema_and_mapper_contracts` | `BOUND_LITERAL` | Task contract | Count query policy defines the split behavior for total vs pattern-aware counts. |
| `dependencies_and_import_surfaces` | `BOUND_REFERENCE` | Upstream tasks | Runtime policy and shared search adapter assumptions are required. |
| `validation_and_boundary_rules` | `BOUND_LITERAL` | Architecture decision | Preserve path validation, traversal hardening, and response budgets. |
| `consumer_and_callsite_bindings` | `BOUND_LITERAL` | Existing project state | Registration surface remains the current inspection tool catalog. |

## Authoritative Literal Bindings
[INTENT: REFERENZ]

| Binding Domain | Exact Binding | Source | Placement / Consumer | No Autonomous Choice Rule |
|---------------|---------------|--------|----------------------|---------------------------|
| Public endpoint name | `count_lines` | Existing project state | Count-lines schema, handler, registration | Do not rename the endpoint. |
| Total-only execution path | Use the dedicated streaming line counter | Agreed architecture | Handler and new streaming helper | Do not route pure total counting through `ugrep`. |
| Pattern-aware execution path | Use the shared native-search lane through the count query policy | Agreed architecture | Handler and `count-query-policy.ts` | Do not leave regex-based large-file counts on full in-process reads. |
| Large-file applicability | `count_lines` participates in the same runtime I/O capability governance as the search endpoints | Explicit user agreement | Handler and policy surface | Do not treat `count_lines` as permanently exempt from large-file governance. |

## Authoritative Code / Contract Surfaces
[INTENT: REFERENZ]

| Surface Name | Kind | Snippet / Contract | Source | Placement / Consumer |
|-------------|------|--------------------|--------|----------------------|
| Streaming line counter | `signature` | Provide explicit helpers for total-line counting and, where appropriate, matching-line counting without whole-file reads. | Task contract | `src/infrastructure/filesystem/streaming-line-counter.ts` |
| Count query policy | `shape` | Expose the split between total-only counting and pattern-aware counting, along with their runtime policy hooks. | Task contract | `src/domain/shared/search/count-query-policy.ts` |
| Public schema contract | `shape` | Preserve the current field names and semantics unless an additive extension is strictly necessary. | Existing project state | `src/domain/inspection/count-lines/schema.ts` |

## Execute-Agent Zero-Question Gate
[INTENT: CONSTRAINT]

- **Gate Result:** `PASS`
- **Checked Domains:** public endpoint naming, split execution path, support file placement, and policy dependencies
- **Rule:** A fresh execute agent must not need to ask whether `count_lines` stays public, whether pure total counting uses a streaming counter, or whether pattern-aware counts reuse the shared native search lane.

## Authoritative References
[INTENT: REFERENZ]

| Path | Kind | Must Read Before Execution | Purpose | Referenced By |
|------|------|---------------------------|---------|---------------|
| `.plan/1-runtime-governance/1.1-io-capability-profile-and-execution-policy-registry.md` | `plan-file` | `true` | Runtime capability and escalation vocabulary | Context, actions 1-4 |
| `.plan/2-search-platform/2.1-ugrep-adapter-and-classification-surfaces.md` | `plan-file` | `true` | Shared native-search adapter assumptions | Context, actions 1-4 |
| `src/domain/inspection/count-lines/handler.ts` | `business-file` | `true` | Current count-lines behavior | IU-001, actions 2-4 |
| `src/domain/inspection/count-lines/schema.ts` | `schema-file` | `true` | Current count-lines contract | IU-001 |
| `src/domain/shared/guardrails/tool-guardrail-limits.ts` | `schema-file` | `true` | Shared caps and limits | IU-002 |
| `src/domain/shared/guardrails/text-response-budget.ts` | `schema-file` | `true` | Response-budget utilities | IU-002 |

## Resume Validation Contract
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Lowest Level Truth Surface | `artifact-evidence` | The four declared output files are the task-local truth |
| Task Lifecycle Truth Surface | `.plan/4-count-lines/orchestration.md` | Parent orchestration controls lifecycle |
| Global Frontier Truth Surface | `PLAN.md` | Root frontier authority |
| Conflict Requires User Confirmation | `true` | Conflicting implementation state requires escalation |
| Partial Progress Requires User Decision | `true` | Partial output state requires confirmation before resume |

## Artifact Progress Inference Rules
[INTENT: REFERENZ]

- Treat the task as partial progress if any subset of the four declared output files exists.
- Treat the task as complete only when all four bound surfaces exist and align with the split execution model.

## User Resume Decision Gate
[INTENT: CONSTRAINT]

- **Required on Partial Progress:** `true`
- **Required on State Conflict:** `true`
- **Allowed User Choices:** `ACCEPT_AND_CONTINUE` / `REVERIFY_EXISTING_OUTPUTS`

## Context
[INTENT: KONTEXT]

- **Domain:** Line-count endpoint modernization
- **Related Files:** Current count-lines files, shared runtime policy, shared native-search adapter assumptions, and guardrail utilities
- **Technical Context:** The current handler still reads whole file contents in-process when counting lines
- **Business Context:** The endpoint must support large-file workloads later and cannot remain a small-file-only in-process counter
- **Architectural Context:** Counting is split into streaming total counts and pattern-aware counts while preserving the public endpoint surface
- **Plan Context Dependencies:** Read tasks `1.1` and `2.1` before execution
- **Runtime Discovery Context:** None

## Execution Surface Assessment
[INTENT: REFERENZ]

- **Execution Surface Band:** `YELLOW`
- **Primary Split Axis:** `none`
- **Hard-Gap Result:** `no RED or BLACK trigger active`
- **Single-Agent Viability Rationale:** The task remains bounded to one endpoint family and two new supporting files with a closed decision surface.

## Information Units
[INTENT: SPEZIFIKATION]

### IU-001: Preserve the public `count_lines` contract
**Type:** `constraint`

**Description:**
Keep the public endpoint name and preserve the current request fields wherever possible.

**Ist-Zustand:**
The existing endpoint already exposes `count_lines` with path, recursion, regex, glob, and ignore controls.

**Soll-Zustand:**
The public name and overall contract style stay stable.

**Architectural Rationale:**
The user explicitly wants this endpoint inside the same large-file governance, not replaced by a different public API.

### IU-002: Split total counts from pattern-aware counts
**Type:** `function`

**Description:**
Implement separate internal paths for total-only counts and pattern-aware counts.

**Ist-Zustand:**
The current handler performs in-process file reads and regex matching directly.

**Soll-Zustand:**
Pure counts use the streaming line counter; pattern-aware counts use the shared native search backend via an explicit count query policy.

**Architectural Rationale:**
This preserves performance and keeps counting aligned with the platform-wide search/runtime architecture.

### IU-003: Keep traversal and response guardrails intact
**Type:** `constraint`

**Description:**
Preserve the current traversal-hardening and response-budget behavior while modernizing the counting engine.

**Ist-Zustand:**
The current endpoint already respects traversal policy and discovery-family response caps.

**Soll-Zustand:**
Those guardrails stay active after the engine modernization.

**Architectural Rationale:**
Large-file support must not weaken the surrounding guardrail stack.

## Actions
[INTENT: ANWEISUNG]

1. **Re-anchor the upstream runtime and search-platform tasks plus the current count-lines surfaces**
   - **What:** Re-read upstream tasks and the current count-lines files.
   - **Where:** Declared upstream plan references and authoritative references.
   - **How:** Use only the bound references.
   - **Why:** The new counting model must align with the shared runtime and native-search assumptions.
   - **Inputs / References:** All authoritative references in this task file.
   - **Output Contract:** Contract context is active.
   - **Checkpoint Impact:** Starts checkpoint `cp-1-context-and-current-count-lines-read`.
   - **Verification:** All required references are readable.
   - **Stop if:** Any required reference is missing.
2. **Create the count query policy**
   - **What:** Implement `src/domain/shared/search/count-query-policy.ts`.
   - **Where:** New shared policy file.
   - **How:** Bind the split between total-only counting and pattern-aware counting, plus their runtime policy hooks.
   - **Why:** The handler must not encode this split ad hoc.
   - **Inputs / References:** Runtime policy task and current count-lines files.
   - **Output Contract:** The policy file exists with complete TSDoc.
   - **Checkpoint Impact:** Completes checkpoint `cp-2-count-query-policy`.
   - **Verification:** The file exposes the bound split behavior clearly.
   - **Stop if:** The split cannot be modeled without renaming the public endpoint.
3. **Create the streaming line counter**
   - **What:** Implement `src/infrastructure/filesystem/streaming-line-counter.ts`.
   - **Where:** New infrastructure file.
   - **How:** Provide the helpers needed for large-file-safe total counting and any shared matching support that does not require whole-file reads.
   - **Why:** Total counting must not depend on giant in-process reads.
   - **Inputs / References:** Runtime policy task, shared guardrail files, and the current handler.
   - **Output Contract:** The streaming line counter exists with complete TSDoc.
   - **Checkpoint Impact:** Completes checkpoint `cp-3-streaming-line-counter`.
   - **Verification:** The helper supports large-file-safe counting without whole-file string loading.
   - **Stop if:** The helper would require implicit unbounded in-process loading.
4. **Refactor the public count-lines handler and schema**
   - **What:** Update the existing handler and schema to consume the new split execution model while keeping the public contract stable.
   - **Where:** `src/domain/inspection/count-lines/handler.ts` and `schema.ts`
   - **How:** Preserve the public endpoint name and schema field names, keep traversal and response guardrails, and route total vs pattern-aware counting to the correct internal helpers.
   - **Why:** The existing endpoint must evolve into the large-file architecture without losing its public identity.
   - **Inputs / References:** The new policy and streaming helper plus the existing endpoint files.
   - **Output Contract:** The public endpoint remains stable while the implementation becomes large-file-safe.
   - **Checkpoint Impact:** Completes checkpoint `cp-4-public-endpoint-refactor`.
   - **Verification:** The endpoint no longer performs large-file whole-file counting in the old path.
   - **Stop if:** The change would require breaking the current public contract or bypassing traversal/response guardrails.

## Checkpoint Definitions
[INTENT: REFERENZ]

| Checkpoint ID | Kind | Completion Signal | Produces Artifacts | Resume Meaning |
|---------------|------|-------------------|--------------------|----------------|
| `cp-1-context-and-current-count-lines-read` | `sequential_step` | Upstream tasks and current endpoint surfaces were re-read | none | Implementation may begin |
| `cp-2-count-query-policy` | `artifact_milestone` | Shared count query policy file exists | `src/domain/shared/search/count-query-policy.ts` | Split-execution policy is defined |
| `cp-3-streaming-line-counter` | `artifact_milestone` | Streaming counter file exists | `src/infrastructure/filesystem/streaming-line-counter.ts` | Large-file total-count helper is complete |
| `cp-4-public-endpoint-refactor` | `artifact_milestone` | Existing handler and schema reflect the new split execution model | `src/domain/inspection/count-lines/handler.ts`, `schema.ts` | Public endpoint modernization is complete |

## Execution Progress Ledger
[INTENT: REFERENZ]

| Checkpoint ID | Status | Evidence | Notes |
|---------------|--------|----------|-------|
| `cp-1-context-and-current-count-lines-read` | `DONE` | authoritative references read | Required references and current count-lines surfaces re-anchored. |
| `cp-2-count-query-policy` | `DONE` | `src/domain/shared/search/count-query-policy.ts` | Bound split-execution policy created and re-anchored. |
| `cp-3-streaming-line-counter` | `DONE` | `src/infrastructure/filesystem/streaming-line-counter.ts` | Streaming helper created and re-anchored. |
| `cp-4-public-endpoint-refactor` | `DONE` | updated `handler.ts` and `schema.ts` | Split execution model applied to the public handler and schema surfaces. |

## Resume Conflict Resolution Rules
[INTENT: CONSTRAINT]

- If the new policy exists but the handler still uses only whole-file in-process logic, stop and escalate.
- If the endpoint schema changes in a way that breaks the current public contract, stop and escalate.
- If the streaming line counter is missing while the handler claims large-file support, stop and escalate.

## Checkpoint Update Protocol
[INTENT: ANWEISUNG]

1. Mark `cp-2-count-query-policy` as `DONE` only after the count query split is explicit and typed.
2. Mark `cp-3-streaming-line-counter` as `DONE` only after the helper exists with complete TSDoc.
3. Mark `cp-4-public-endpoint-refactor` as `DONE` only after the updated handler and schema preserve the public contract while using the new split execution model.

## Acceptance Criteria
[INTENT: REFERENZ]

- [ ] The public endpoint name remains `count_lines`.
- [ ] Large-file total counting uses the new streaming line counter.
- [ ] Pattern-aware counting uses the shared native-search lane via the new count query policy.
- [ ] Traversal hardening and response-budget enforcement remain active.
- [ ] The touched TypeScript files contain detailed enterprise-grade TSDoc explaining the architecture and rationale for developers and LLM agents.

## Stop Conditions & User Escalation
[INTENT: CONSTRAINT]

- **Plan-vs-Reality Mismatch:** Stop if the current `count_lines` contract or behavior differs materially from the bound assumptions here.
- **Missing Required Plan Information:** Stop if any public naming, split-execution rule, or path binding is missing.
- **Autonomous Decision Required:** Stop if implementation would require inventing a different public endpoint, a different split model, or bypassing the shared runtime/search policy surfaces.
- **Allowed Runtime Discovery:** None.
- **User Message Contract:** `Plan says X / current reality is Y / missing or conflicting information is Z / continuing would require an autonomous decision.`

## Post-Completion Instructions
[INTENT: ANWEISUNG]

1. Mark this task `[x]` in [`.plan/4-count-lines/orchestration.md`](.plan/4-count-lines/orchestration.md).
2. Re-anchor [`PLAN.md`](PLAN.md) after completion.

## File Path Index
[INTENT: REFERENZ]

| # | Path | Kind | Why Relevant | Must Read Before Execution |
|---|------|------|--------------|----------------------------|
| 1 | `.plan/1-runtime-governance/1.1-io-capability-profile-and-execution-policy-registry.md` | `plan-file` | Runtime capability and escalation vocabulary | `true` |
| 2 | `.plan/2-search-platform/2.1-ugrep-adapter-and-classification-surfaces.md` | `plan-file` | Shared native-search adapter assumptions | `true` |
| 3 | `src/domain/inspection/count-lines/handler.ts` | `business-file` | Existing counting implementation | `true` |
| 4 | `src/domain/inspection/count-lines/schema.ts` | `schema-file` | Existing public contract | `true` |
| 5 | `src/domain/shared/guardrails/tool-guardrail-limits.ts` | `schema-file` | Shared caps and limits | `true` |
| 6 | `src/domain/shared/guardrails/text-response-budget.ts` | `schema-file` | Response-budget utilities | `true` |

## Notes
[INTENT: KONTEXT]

- This task must keep `count_lines` inside the large-text architecture exactly as agreed in the discussion.
- TSDocs in this task must explain why total-only counting and pattern-aware counting take different internal paths.

