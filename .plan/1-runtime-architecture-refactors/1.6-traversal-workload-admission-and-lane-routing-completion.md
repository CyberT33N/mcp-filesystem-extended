---
file_type: "task"
task_id: "1.6"
task_title: "Traversal workload admission and lane-routing completion"
unit: "1"
parent_orchestration: ".plan/1-runtime-architecture-refactors/orchestration.md"
classification: "SEQUENTIAL"
status: "pending"
estimated_complexity: "HIGH"
files_modified:
  - "src/domain/shared/guardrails/filesystem-preflight.ts"
  - "src/domain/shared/guardrails/traversal-runtime-budget.ts"
  - "src/domain/shared/guardrails/traversal-workload-admission.ts"
  - "src/domain/shared/search/search-execution-policy.ts"
  - "src/domain/inspection/search-file-contents-by-regex/search-regex-path-result.ts"
  - "src/domain/inspection/search-file-contents-by-fixed-string/search-fixed-string-path-result.ts"
  - "src/domain/inspection/list-directory-entries/handler.ts"
  - "src/domain/inspection/find-files-by-glob/handler.ts"
  - "src/domain/inspection/find-paths-by-name/helpers.ts"
  - "src/domain/inspection/count-lines/handler.ts"
files_read:
  - "src/domain/shared/guardrails/filesystem-preflight.ts"
  - "src/domain/shared/guardrails/traversal-runtime-budget.ts"
  - "src/domain/shared/search/search-execution-policy.ts"
  - "src/infrastructure/runtime/io-capability-detector.ts"
  - "src/domain/inspection/search-file-contents-by-regex/search-regex-path-result.ts"
  - "src/domain/inspection/search-file-contents-by-fixed-string/search-fixed-string-path-result.ts"
  - "src/domain/inspection/list-directory-entries/handler.ts"
  - "src/domain/inspection/find-files-by-glob/handler.ts"
  - "src/domain/inspection/find-paths-by-name/helpers.ts"
  - "src/domain/inspection/count-lines/handler.ts"
  - ".plan/1-runtime-architecture-refactors/1.5-traversal-preflight-and-runtime-budget-refactor.md"
dependencies:
  - task_id: "1.5"
    type: "SEQUENTIAL"
    status: "RESOLVED"
blocked_by: "none"
information_completeness_mode: "FULLY_SPECIFIED"
implementation_scenarios:
  - "FUNCTION_CREATION"
  - "FUNCTION_UPDATE"
  - "OBJECT_UPDATE"
execution_surface_band: "GREEN"
primary_split_axis: "none"
single_agent_viability_note: "The task is broad in file count but singular in architecture: one shared admission-to-execution control-plane closure across recursive discovery/search/count consumers."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
decision_surface_register:
  - surface: "naming"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "Introduce one shared planner surface named `traversal-workload-admission.ts`; public endpoint names remain unchanged."
  - surface: "path_and_placement"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "The new shared planner belongs in `src/domain/shared/guardrails/` and all bound recursive consumers must consume it before traversal begins."
  - surface: "literal_values_and_budgets"
    status: "BOUND_REFERENCE"
    binding_source: "current code state plus accepted architecture"
    notes: "The corrective task must not be satisfied by merely increasing `TRAVERSAL_RUNTIME_SOFT_TIME_BUDGET_MS` or deleting the runtime safeguard."
  - surface: "defaults_and_fallbacks"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "Broad valid workloads must now receive an explicit admission outcome before recursive traversal: inline, preview-first, task-backed-required, or narrowing-required."
  - surface: "types_and_shapes"
    status: "BOUND_LITERAL"
    binding_source: "task contract"
    notes: "The shared planner must return one deterministic admission outcome plus caller-guidance text without widening public endpoint schemas."
  - surface: "function_signatures_and_returns"
    status: "BOUND_REFERENCE"
    binding_source: "authoritative code surfaces"
    notes: "`filesystem-preflight.ts`, `search-execution-policy.ts`, and the bound recursive consumers are the decisive change surfaces."
  - surface: "object_schema_and_mapper_contracts"
    status: "BOUND_LITERAL"
    binding_source: "task contract"
    notes: "The planner must model real downstream traversal cost rather than only preflight probe counters."
  - surface: "dependencies_and_import_surfaces"
    status: "BOUND_REFERENCE"
    binding_source: "shared SSOT"
    notes: "Consume runtime capability and execution-policy vocabulary already present in the server instead of inventing endpoint-local heuristics."
  - surface: "validation_and_boundary_rules"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "No bound recursive consumer may enter its broad traversal loop without first consuming the shared admission outcome."
  - surface: "consumer_and_callsite_bindings"
    status: "BOUND_LITERAL"
    binding_source: "existing project state"
    notes: "Search, discovery, and count consumers must converge on one admission-to-execution model before downstream docs or public-contract wording are finalized."
authoritative_literal_bindings:
  - binding_domain: "shared admission planner rule"
    exact_binding: "One shared traversal workload-admission planner must decide broad-root execution before recursive traversal begins."
    source: "accepted architecture"
    placement_or_consumer: "shared guardrail layer and all bound recursive consumers"
    no_autonomous_choice_rule: "The execute agent must not leave admission planning distributed across endpoint-local loops."
  - binding_domain: "runtime safeguard role"
    exact_binding: "The 5-second runtime safeguard remains the deeper emergency fuse and must not be the normal first caller-facing stop for broad valid workloads."
    source: "accepted architecture"
    placement_or_consumer: "traversal runtime budget module and all consumers"
    no_autonomous_choice_rule: "The execute agent must neither delete the safeguard nor solve the problem by simply raising its threshold."
  - binding_domain: "admission outcome rule"
    exact_binding: "Broad valid workloads must surface an explicit pre-traversal admission outcome: inline, preview-first, task-backed-required, or narrowing-required."
    source: "accepted architecture"
    placement_or_consumer: "shared planner and recursive consumers"
    no_autonomous_choice_rule: "The execute agent must not allow broad workloads to drift into runtime-budget exhaustion without a prior admission decision."
  - binding_domain: "task-backed boundary rule"
    exact_binding: "If a family lacks a real task-backed execution surface, the planner must reject or narrow before traversal rather than pretending task-backed execution exists."
    source: "accepted architecture"
    placement_or_consumer: "shared planner and endpoint-specific consumer handling"
    no_autonomous_choice_rule: "The execute agent must not invent a new public async contract inside this corrective task."
requires_authoritative_code_surfaces: true
authoritative_code_surfaces:
  - surface_name: "Shared workload-admission contract"
    kind: "shape"
    snippet_or_contract: "Expose one deterministic admission outcome plus guidance text before recursive traversal begins."
    source: "task contract"
    placement_or_consumer: "`src/domain/shared/guardrails/traversal-workload-admission.ts`"
  - surface_name: "Preflight-to-planner bridge contract"
    kind: "shape"
    snippet_or_contract: "Preflight and execution-policy surfaces must provide the planner enough evidence to model downstream traversal cost instead of only admission-probe cost."
    source: "task contract"
    placement_or_consumer: "`src/domain/shared/guardrails/filesystem-preflight.ts`, `src/domain/shared/search/search-execution-policy.ts`"
  - surface_name: "Recursive consumer gate contract"
    kind: "shape"
    snippet_or_contract: "Bound recursive consumers must consume the shared admission outcome before entering broad traversal loops."
    source: "task contract"
    placement_or_consumer: "search, discovery, and count runtime handlers/helpers"
authoritative_references:
  - path: "src/domain/shared/guardrails/filesystem-preflight.ts"
    kind: "business-file"
    usage: "current metadata-first admission model"
    required_before_execution: true
  - path: "src/domain/shared/guardrails/traversal-runtime-budget.ts"
    kind: "business-file"
    usage: "current deeper runtime safeguard and error semantics"
    required_before_execution: true
  - path: "src/domain/shared/search/search-execution-policy.ts"
    kind: "business-file"
    usage: "current sync/preview/task policy vocabulary"
    required_before_execution: true
  - path: "src/infrastructure/runtime/io-capability-detector.ts"
    kind: "business-file"
    usage: "runtime capability input surface"
    required_before_execution: true
  - path: "src/domain/inspection/search-file-contents-by-regex/search-regex-path-result.ts"
    kind: "business-file"
    usage: "regex recursive consumer"
    required_before_execution: true
  - path: "src/domain/inspection/search-file-contents-by-fixed-string/search-fixed-string-path-result.ts"
    kind: "business-file"
    usage: "fixed-string recursive consumer"
    required_before_execution: true
  - path: "src/domain/inspection/list-directory-entries/handler.ts"
    kind: "business-file"
    usage: "structured discovery recursive consumer"
    required_before_execution: true
  - path: "src/domain/inspection/find-files-by-glob/handler.ts"
    kind: "business-file"
    usage: "glob discovery recursive consumer"
    required_before_execution: true
  - path: "src/domain/inspection/find-paths-by-name/helpers.ts"
    kind: "business-file"
    usage: "name-discovery recursive consumer"
    required_before_execution: true
  - path: "src/domain/inspection/count-lines/handler.ts"
    kind: "business-file"
    usage: "count recursive consumer"
    required_before_execution: true
  - path: ".plan/1-runtime-architecture-refactors/1.5-traversal-preflight-and-runtime-budget-refactor.md"
    kind: "plan-file"
    usage: "phase-one lineage and residual-gap boundary"
    required_before_execution: true
upstream_plan_references:
  - task_id: "1.5"
    path: ".plan/1-runtime-architecture-refactors/1.5-traversal-preflight-and-runtime-budget-refactor.md"
    must_read_before_execution: true
    reason: "Task `1.6` closes the residual admission-to-execution gap left after the phase-one traversal refactor in task `1.5`."
runtime_discovery:
  required: false
  scope: []
  allowed_topics: []
checkpoint_strategy: "ARTIFACT_MILESTONES_REQUIRED"
checkpoint_eligibility_contract:
  durable_skip_authority_only: true
  ephemeral_freshness_steps:
    - "Re-read shared preflight, runtime-budget, execution-policy, and bound recursive-consumer surfaces"
  durable_resume_checkpoints:
    - "cp-1-shared-admission-planner"
    - "cp-2-recursive-consumer-admission-alignment"
    - "cp-3-runtime-contract-readiness"
resume_validation_contract:
  lowest_level_truth_surface: "artifact-evidence"
  task_lifecycle_truth_surface: ".plan/1-runtime-architecture-refactors/orchestration.md"
  global_frontier_truth_surface: "PLAN.md"
  artifact_evidence_sources:
    - "src/domain/shared/guardrails/traversal-workload-admission.ts"
    - "src/domain/inspection/search-file-contents-by-regex/search-regex-path-result.ts"
    - "src/domain/inspection/search-file-contents-by-fixed-string/search-fixed-string-path-result.ts"
    - "src/domain/inspection/list-directory-entries/handler.ts"
    - "src/domain/inspection/find-files-by-glob/handler.ts"
    - "src/domain/inspection/find-paths-by-name/helpers.ts"
    - "src/domain/inspection/count-lines/handler.ts"
  conflict_requires_user_confirmation: true
  partial_progress_requires_user_decision: true
  durable_skip_authority_only: true
  ephemeral_checkpoint_conflict_requires_reexecution: true
artifact_progress_inference_rules:
  - "Treat the task as partially complete only when the shared planner exists or one or more recursive consumers visibly gate broad traversal through it."
  - "Do not treat runtime-budget wording changes alone as proof that broad-root admission moved out of the traversal loop."
  - "Ephemeral reference re-reads never create durable skip authority."
user_resume_decision_gate:
  required_on_partial_progress: true
  required_on_state_conflict: true
  default_user_choices:
    - "ACCEPT_AND_CONTINUE"
    - "REVERIFY_EXISTING_OUTPUTS"
persona_generation_required: true
persona_generation_mode: "runtime_regenerate"
persona_source_prompt_path: "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
persona_primary_domain_kernel: "Enterprise traversal admission and execution governance"
persona_secondary_specializations:
  - "shared control-plane design"
  - "recursive inspection runtime coordination"
persona_current_task_specialization: "Broad-root traversal admission closure"
persona_generation_lineage: "Derived from the residual admission-to-execution gap identified after task 1.5 and the accepted server-owned scope-governance architecture."
persona_runtime_review_required: true
persona_dx_snapshot:
  title: "Enterprise Traversal Admission Architect"
  summary: "Closes the residual gap between root admission and recursive runtime execution so broad valid workloads receive explicit server-owned lane decisions before traversal begins."
  weighting_note: "Shared admission control dominates over endpoint-local traversal loop details."
checkpoint_definitions:
  - checkpoint_id: "cp-1-shared-admission-planner"
    kind: "artifact_milestone"
    completion_signal: "A shared traversal workload-admission planner exists and owns pre-traversal lane decisions."
    produces_artifacts:
      - "src/domain/shared/guardrails/traversal-workload-admission.ts"
      - "src/domain/shared/guardrails/filesystem-preflight.ts"
      - "src/domain/shared/search/search-execution-policy.ts"
  - checkpoint_id: "cp-2-recursive-consumer-admission-alignment"
    kind: "artifact_milestone"
    completion_signal: "All bound recursive consumers consult the shared admission outcome before entering broad traversal loops."
    produces_artifacts:
      - "src/domain/inspection/search-file-contents-by-regex/search-regex-path-result.ts"
      - "src/domain/inspection/search-file-contents-by-fixed-string/search-fixed-string-path-result.ts"
      - "src/domain/inspection/list-directory-entries/handler.ts"
      - "src/domain/inspection/find-files-by-glob/handler.ts"
      - "src/domain/inspection/find-paths-by-name/helpers.ts"
      - "src/domain/inspection/count-lines/handler.ts"
  - checkpoint_id: "cp-3-runtime-contract-readiness"
    kind: "artifact_milestone"
    completion_signal: "Downstream docs and public contract alignment can now anchor to task `1.6` without inventing missing traversal semantics."
    produces_artifacts:
      - "PLAN.md"
      - ".plan/2-inspection-discovery-docs/**"
      - ".plan/4-inspection-search-and-count-docs/**"
      - ".plan/11-cross-cutting-validation-and-backup-policy/11.1-public-code-contract-alignment.md"
execution_progress_ledger:
  ledger_mode: "CHECKPOINT_LEDGER"
  checkpoints:
    - checkpoint_id: "cp-1-shared-admission-planner"
      status: "PENDING"
    - checkpoint_id: "cp-2-recursive-consumer-admission-alignment"
      status: "PENDING"
    - checkpoint_id: "cp-3-runtime-contract-readiness"
      status: "PENDING"
resume_conflict_resolution_rules:
  - "If the shared planner exists but one or more bound consumers still enter traversal loops without consuming it, stop and escalate."
  - "If the fix only raises the runtime budget or suppresses the runtime error text without changing admission ordering, stop and escalate."
  - "If broad valid workloads still surface first as runtime-budget exhaustion after the claimed refactor, stop and escalate."
checkpoint_update_protocol:
  - "Mark `cp-1-shared-admission-planner` DONE only after pre-traversal lane decisions are owned by one shared planner surface."
  - "Mark `cp-2-recursive-consumer-admission-alignment` DONE only after all bound recursive consumers consult the planner before broad traversal begins."
  - "Mark `cp-3-runtime-contract-readiness` DONE only after downstream plan surfaces can truthfully anchor to task `1.6` instead of task `1.5` alone."
halt_conditions:
  plan_reality_mismatch: true
  missing_required_plan_information: true
  autonomous_decision_required: true
  user_notification_required: true
---

# Task 1.6: Traversal workload admission and lane-routing completion
[INTENT: ANWEISUNG]

## Goal
[INTENT: KONTEXT]

Close the residual gap between root-level preflight admission and the real recursive execution path so broad valid workloads receive an explicit server-owned lane decision before the bound recursive consumers enter their traversal loops.

This task treats task `1.5` as the phase-one refactor and completes the missing control-plane closure that is still allowing broad valid roots to surface first as `runtime_budget_exceeded` failures.

## Expected Result
[INTENT: KONTEXT]

- A shared traversal workload-admission planner exists and combines preflight evidence, execution-policy vocabulary, and runtime capability signals before broad traversal begins.
- Bound recursive discovery/search/count consumers consult that planner before entering their traversal loops.
- Broad valid workloads that cannot run safely inline are surfaced through explicit admission outcomes or narrowing guidance before the 5-second runtime safeguard becomes the dominant first caller-facing stop.
- The 5-second runtime safeguard remains present as the deeper emergency fuse.

## Preconditions
[INTENT: CONSTRAINT]

- Task `1.5` must be completed.
- The execute agent must not satisfy this task by merely increasing `TRAVERSAL_RUNTIME_SOFT_TIME_BUDGET_MS` or deleting the runtime safeguard.
- The execute agent must not invent a new public async contract unless an already existing endpoint family can genuinely consume it.

## Notes
[INTENT: KONTEXT]

- Task `1.5` remains complete as the phase-one traversal refactor.
- Task `1.6` is now the final unit-1 runtime blocker for downstream discovery/search docs and public code-contract alignment.
