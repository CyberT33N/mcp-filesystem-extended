---
file_type: "task"
task_id: "1.5"
task_title: "Traversal preflight and runtime-budget refactor"
unit: "1"
parent_orchestration: ".plan/1-runtime-architecture-refactors/orchestration.md"
classification: "SEQUENTIAL"
status: "pending"
estimated_complexity: "HIGH"
files_modified:
  - "src/domain/shared/guardrails/tool-guardrail-limits.ts"
  - "src/domain/shared/guardrails/traversal-runtime-budget.ts"
  - "src/domain/shared/guardrails/filesystem-preflight.ts"
  - "src/domain/inspection/search-file-contents-by-regex/search-regex-path-result.ts"
  - "src/domain/inspection/search-file-contents-by-fixed-string/search-fixed-string-path-result.ts"
  - "src/domain/inspection/list-directory-entries/handler.ts"
  - "src/domain/inspection/find-files-by-glob/handler.ts"
  - "src/domain/inspection/find-paths-by-name/helpers.ts"
  - "src/domain/inspection/count-lines/handler.ts"
files_read:
  - "src/domain/shared/guardrails/tool-guardrail-limits.ts"
  - "src/domain/shared/guardrails/traversal-runtime-budget.ts"
  - "src/domain/shared/guardrails/filesystem-preflight.ts"
  - "src/domain/shared/search/search-execution-policy.ts"
  - "src/infrastructure/runtime/io-capability-detector.ts"
  - "src/domain/inspection/search-file-contents-by-regex/search-regex-path-result.ts"
  - "src/domain/inspection/search-file-contents-by-fixed-string/search-fixed-string-path-result.ts"
  - "src/domain/inspection/list-directory-entries/handler.ts"
  - "src/domain/inspection/find-files-by-glob/handler.ts"
  - "src/domain/inspection/find-paths-by-name/helpers.ts"
  - "src/domain/inspection/count-lines/handler.ts"
  - "src/application/server/register-inspection-tool-catalog.ts"
  - "src/application/server/server-instructions.ts"
  - "src/application/server/filesystem-server.ts"
  - "__bak__/plan-ugrep/PLAN.md"
dependencies:
  - task_id: "1.4"
    type: "SEQUENTIAL"
    status: "UNRESOLVED"
blocked_by: "1.4"
information_completeness_mode: "FULLY_SPECIFIED"
implementation_scenarios:
  - "FUNCTION_UPDATE"
  - "CONSTANT_UPDATE"
  - "OBJECT_UPDATE"
execution_surface_band: "GREEN"
primary_split_axis: "none"
single_agent_viability_note: "The task is broad in file count but narrow in architecture: all touched surfaces belong to one preflight/traversal governance change."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
decision_surface_register:
  - surface: "naming"
    status: "BOUND_REFERENCE"
    binding_source: "existing project state"
    notes: "Public tool names and shared guardrail filenames remain stable."
  - surface: "path_and_placement"
    status: "BOUND_LITERAL"
    binding_source: "existing project state"
    notes: "Modify only the shared traversal/preflight guardrails and recursive consumer surfaces."
  - surface: "literal_values_and_budgets"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "The 5-second traversal budget is demoted to a deeper safeguard."
  - surface: "defaults_and_fallbacks"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "Server-side preflight and narrowing guidance become primary."
  - surface: "types_and_shapes"
    status: "BOUND_LITERAL"
    binding_source: "task contract"
    notes: "Refusal surfaces must explicitly explain scope pressure and narrowing guidance."
  - surface: "function_signatures_and_returns"
    status: "BOUND_REFERENCE"
    binding_source: "authoritative code surfaces"
    notes: "Shared preflight and recursive consumer surfaces are the decisive change boundaries."
  - surface: "object_schema_and_mapper_contracts"
    status: "BOUND_LITERAL"
    binding_source: "task contract"
    notes: "Preflight estimation and deeper safeguard semantics must be explicit."
  - surface: "dependencies_and_import_surfaces"
    status: "BOUND_REFERENCE"
    binding_source: "shared SSOT"
    notes: "Consume runtime capability and execution-policy vocabulary already present in the server."
  - surface: "validation_and_boundary_rules"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "The traversal safeguard stays, but it no longer acts as the primary front-door governor for broad valid workloads."
  - surface: "consumer_and_callsite_bindings"
    status: "BOUND_LITERAL"
    binding_source: "existing project state"
    notes: "All recursive discovery/search/count consumers must align to the same model."
authoritative_literal_bindings:
  - binding_domain: "preflight-first rule"
    exact_binding: "Server-side scope preflight and lane routing become primary."
    source: "accepted architecture"
    placement_or_consumer: "shared preflight/recursive consumers"
    no_autonomous_choice_rule: "The task must not preserve timeout-first caller control as the main model."
  - binding_domain: "traversal safeguard role"
    exact_binding: "The current soft traversal runtime budget remains as a deeper safeguard."
    source: "accepted architecture"
    placement_or_consumer: "traversal runtime budget module and its callers"
    no_autonomous_choice_rule: "The task must neither delete the safeguard nor leave it as the dominant first-line control."
  - binding_domain: "server-side responsibility"
    exact_binding: "Primary scope estimation belongs to the MCP server, not to caller-side prompting."
    source: "accepted architecture"
    placement_or_consumer: "shared preflight helpers and recursive consumers"
    no_autonomous_choice_rule: "The task must not offload primary scope estimation to prompt engineering."
requires_authoritative_code_surfaces: true
authoritative_code_surfaces:
  - surface_name: "Traversal preflight contract"
    kind: "shape"
    snippet_or_contract: "Expose primary scope-estimation and narrowing guidance before broad recursive traversal proceeds."
    source: "task contract"
    placement_or_consumer: "`src/domain/shared/guardrails/filesystem-preflight.ts` and related helpers"
  - surface_name: "Traversal safeguard contract"
    kind: "shape"
    snippet_or_contract: "Keep runtime-budget enforcement as a deeper emergency safeguard with explicit guidance."
    source: "task contract"
    placement_or_consumer: "`src/domain/shared/guardrails/traversal-runtime-budget.ts`"
authoritative_references:
  - path: "src/domain/shared/guardrails/tool-guardrail-limits.ts"
    kind: "schema-file"
    usage: "current shared registry and the existing 5-second runtime budget"
    required_before_execution: true
  - path: "src/domain/shared/guardrails/traversal-runtime-budget.ts"
    kind: "schema-file"
    usage: "current traversal-budget enforcement"
    required_before_execution: true
  - path: "src/domain/shared/guardrails/filesystem-preflight.ts"
    kind: "schema-file"
    usage: "metadata-first preflight SSOT"
    required_before_execution: true
  - path: "src/domain/shared/search/search-execution-policy.ts"
    kind: "business-file"
    usage: "runtime sync/task vocabulary"
    required_before_execution: true
  - path: "src/infrastructure/runtime/io-capability-detector.ts"
    kind: "business-file"
    usage: "runtime capability input surface"
    required_before_execution: true
  - path: "src/domain/inspection/search-file-contents-by-regex/search-regex-path-result.ts"
    kind: "business-file"
    usage: "recursive regex consumer"
    required_before_execution: true
  - path: "src/domain/inspection/search-file-contents-by-fixed-string/search-fixed-string-path-result.ts"
    kind: "business-file"
    usage: "recursive fixed-string consumer"
    required_before_execution: true
  - path: "src/domain/inspection/list-directory-entries/handler.ts"
    kind: "business-file"
    usage: "recursive listing consumer"
    required_before_execution: true
  - path: "src/domain/inspection/find-files-by-glob/handler.ts"
    kind: "business-file"
    usage: "recursive glob consumer"
    required_before_execution: true
  - path: "src/domain/inspection/find-paths-by-name/helpers.ts"
    kind: "business-file"
    usage: "recursive name-search consumer"
    required_before_execution: true
  - path: "src/domain/inspection/count-lines/handler.ts"
    kind: "business-file"
    usage: "recursive count consumer"
    required_before_execution: true
  - path: "src/application/server/register-inspection-tool-catalog.ts"
    kind: "business-file"
    usage: "later caller-facing alignment dependency"
    required_before_execution: true
  - path: "src/application/server/server-instructions.ts"
    kind: "business-file"
    usage: "later caller-facing alignment dependency"
    required_before_execution: true
  - path: "src/application/server/filesystem-server.ts"
    kind: "business-file"
    usage: "global fuse shell context"
    required_before_execution: true
  - path: "__bak__/plan-ugrep/PLAN.md"
    kind: "plan-file"
    usage: "historical large-file plan lineage only"
    required_before_execution: true
upstream_plan_references:
  - task_id: "1.4"
    path: ".plan/1-runtime-architecture-refactors/1.4-read-endpoint-internal-ssot-refactor.md"
    must_read_before_execution: true
    reason: "Traversal and preflight refactor should land after read-surface internal SSOT convergence so the runtime contract remains coherent."
runtime_discovery:
  required: false
  scope: []
  allowed_topics: []
checkpoint_strategy: "ARTIFACT_MILESTONES_REQUIRED"
checkpoint_eligibility_contract:
  durable_skip_authority_only: true
  ephemeral_freshness_steps:
    - "Re-read current traversal and recursive consumer surfaces"
  durable_resume_checkpoints:
    - "cp-1-preflight-governance-refactor"
    - "cp-2-recursive-consumer-alignment"
resume_validation_contract:
  lowest_level_truth_surface: "artifact-evidence"
  task_lifecycle_truth_surface: ".plan/1-runtime-architecture-refactors/orchestration.md"
  global_frontier_truth_surface: "PLAN.md"
  artifact_evidence_sources:
    - "src/domain/shared/guardrails/filesystem-preflight.ts"
    - "src/domain/shared/guardrails/traversal-runtime-budget.ts"
    - "src/domain/inspection/search-file-contents-by-regex/search-regex-path-result.ts"
    - "src/domain/inspection/search-file-contents-by-fixed-string/search-fixed-string-path-result.ts"
    - "src/domain/inspection/list-directory-entries/handler.ts"
    - "src/domain/inspection/find-files-by-glob/handler.ts"
    - "src/domain/inspection/find-paths-by-name/helpers.ts"
    - "src/domain/inspection/count-lines/handler.ts"
  conflict_requires_user_confirmation: true
  partial_progress_requires_user_decision: true
  durable_skip_authority_only: true
  ephemeral_checkpoint_conflict_requires_reexecution: true
artifact_progress_inference_rules:
  - "Treat the task as partially complete only when shared guardrails visibly move toward preflight-first governance and recursive consumers begin consuming that model."
  - "Do not treat description-only text changes as proof that recursive runtime governance is aligned."
  - "Ephemeral reference re-reads never create durable skip authority."
user_resume_decision_gate:
  required_on_partial_progress: true
  required_on_state_conflict: true
  default_user_choices:
    - "ACCEPT_AND_CONTINUE"
    - "REVERIFY_EXISTING_OUTPUTS"
persona_generation_required: true
persona_generation_mode: "runtime_regenerate"
persona_source_prompt_path: "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
persona_primary_domain_kernel: "Enterprise traversal and preflight governance"
persona_secondary_specializations:
  - "guardrail refactoring"
  - "recursive inspection runtime control"
persona_current_task_specialization: "Preflight-first recursive inspection governance"
persona_generation_lineage: "Derived from the accepted timeout analysis and server-side scope-governance architecture."
persona_runtime_review_required: true
persona_dx_snapshot:
  title: "Enterprise Traversal Governance Architect"
  summary: "Refactors recursive inspection control so preflight and lane routing lead, while runtime budgets stay as deeper safety rails."
  weighting_note: "Server-side traversal governance dominates over local recursive handler differences."
checkpoint_definitions:
  - checkpoint_id: "cp-1-preflight-governance-refactor"
    kind: "artifact_milestone"
    completion_signal: "Shared preflight/traversal guardrails expose the new ordering and deeper-safeguard role."
    produces_artifacts:
      - "src/domain/shared/guardrails/filesystem-preflight.ts"
      - "src/domain/shared/guardrails/traversal-runtime-budget.ts"
      - "src/domain/shared/guardrails/tool-guardrail-limits.ts"
  - checkpoint_id: "cp-2-recursive-consumer-alignment"
    kind: "artifact_milestone"
    completion_signal: "All bound recursive consumers align to the shared preflight-first traversal model."
    produces_artifacts:
      - "src/domain/inspection/search-file-contents-by-regex/search-regex-path-result.ts"
      - "src/domain/inspection/search-file-contents-by-fixed-string/search-fixed-string-path-result.ts"
      - "src/domain/inspection/list-directory-entries/handler.ts"
      - "src/domain/inspection/find-files-by-glob/handler.ts"
      - "src/domain/inspection/find-paths-by-name/helpers.ts"
      - "src/domain/inspection/count-lines/handler.ts"
execution_progress_ledger:
  ledger_mode: "CHECKPOINT_LEDGER"
  checkpoints:
    - checkpoint_id: "cp-1-preflight-governance-refactor"
      status: "PENDING"
    - checkpoint_id: "cp-2-recursive-consumer-alignment"
      status: "PENDING"
resume_conflict_resolution_rules:
  - "If shared guardrails move to preflight-first ordering but one or more recursive consumers remain timeout-first, stop and escalate."
  - "If the runtime safeguard disappears entirely rather than becoming a deeper layer, stop and escalate."
  - "If later caller-facing wording would still need to invent behavior after this task, stop and escalate."
checkpoint_update_protocol:
  - "Mark `cp-1-preflight-governance-refactor` DONE only after the new shared ordering is visible in the bound guardrail surfaces."
  - "Mark `cp-2-recursive-consumer-alignment` DONE only after all bound recursive consumers align to the new ordering."
halt_conditions:
  plan_reality_mismatch: true
  missing_required_plan_information: true
  autonomous_decision_required: true
user_notification_required: true
---

# Task 1.5: Traversal preflight and runtime-budget refactor
[INTENT: ANWEISUNG]

## Goal
[INTENT: KONTEXT]

Refactor recursive inspection governance so server-side scope preflight and lane routing become the primary control surface for broad valid workloads, while the current traversal runtime budget remains a deeper emergency safeguard instead of the main caller-facing governor.

This task aligns search, discovery, and count traversal behavior with the enterprise architecture accepted in the current analysis.

## Expected Result
[INTENT: KONTEXT]

- Shared traversal/preflight guardrails become preflight-first rather than timeout-first.
- Recursive search/discovery/count consumers align to the same shared traversal-governance model.
- The current soft traversal runtime budget remains present, but it is no longer the dominant user-facing control plane for valid broad workloads.

## Preconditions
[INTENT: CONSTRAINT]

- Task `1.4` must be completed.
- The execute agent must not simply remove the traversal safeguard without replacing its primary-governor role with explicit preflight-first governance.

## Execution Metadata Register
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Task ID | `1.5` | Unique task identifier |
| Classification | `SEQUENTIAL` | The task depends on prior runtime-state and read-surface stabilization |
| Information Completeness Mode | `FULLY_SPECIFIED` | All determinable surfaces are bound |
| Implementation Scenarios | `FUNCTION_UPDATE`, `CONSTANT_UPDATE`, `OBJECT_UPDATE` | Shared guardrail refactor plus recursive consumer alignment |
| Execution Surface Band | `GREEN` | One cross-cutting traversal governance concern |
| Primary Split Axis | `none` | No further split is required |
| Single-Agent Viability Note | The task is broad in file count but narrow in architecture: all touched surfaces belong to one preflight/traversal governance change. |
| Parent Orchestration | `.plan/1-runtime-architecture-refactors/orchestration.md` | Lifecycle status is tracked there |
| Blocked By | `1.4` | Shared read/runtime blocker |
| Requires Upstream Plan Re-Anchoring | `yes` | Task `1.4` must be read first |
| Runtime Discovery Allowed | `no` | This task is fully specified |
| Checkpoint Strategy | `ARTIFACT_MILESTONES_REQUIRED` | Shared guardrail and recursive consumer alignment are separate durable outputs |
| Durable Skip Authority Only | `true` | Only durable artifact changes create skip authority |
| Ephemeral Freshness Steps Present | `yes` | Reference re-reads remain non-durable |
| User Notification Required On Gap/Mismatch | `true` | Mismatches must stop execution |
| Persona Generation Required | `true` | Runtime persona regeneration remains mandatory |
| Persona Generation Mode | `runtime_regenerate` | Persona is not fully persisted |
| Persona Primary Domain Kernel | `Enterprise traversal and preflight governance` | Highest-weight persona kernel |
| Persona Current Task Specialization | `Preflight-first recursive inspection governance` | Current task specialization |
| Persona Runtime Review Required | `true` | Runtime review is required |

## Task Contract Closure
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Decision Surfaces Closed | `true` | All determinable surfaces are bound |
| Unresolved Determinable Decisions | `0` | No deterministic gap remains |
| Expected Execute-Agent Questions | `0` | A fresh execute agent should not need follow-up questions |

## Decision Surface Register
[INTENT: REFERENZ]

| Surface | Status | Binding Source | Notes |
|---------|--------|----------------|-------|
| `naming` | `BOUND_REFERENCE` | existing project state | Public tool names and shared guardrail filenames remain stable. |
| `path_and_placement` | `BOUND_LITERAL` | existing project state | Modify only the shared traversal/preflight guardrails and recursive consumer surfaces. |
| `literal_values_and_budgets` | `BOUND_LITERAL` | accepted architecture | The 5-second traversal budget is demoted to a deeper safeguard. |
| `defaults_and_fallbacks` | `BOUND_LITERAL` | accepted architecture | Server-side preflight and narrowing guidance become primary. |
| `types_and_shapes` | `BOUND_LITERAL` | task contract | Refusal surfaces must explicitly explain scope pressure and narrowing guidance. |
| `function_signatures_and_returns` | `BOUND_REFERENCE` | authoritative code surfaces | Shared preflight and recursive consumer surfaces are the decisive change boundaries. |
| `object_schema_and_mapper_contracts` | `BOUND_LITERAL` | task contract | Preflight estimation and deeper safeguard semantics must be explicit. |
| `dependencies_and_import_surfaces` | `BOUND_REFERENCE` | shared SSOT | Consume runtime capability and execution-policy vocabulary already present in the server. |
| `validation_and_boundary_rules` | `BOUND_LITERAL` | accepted architecture | The traversal safeguard stays, but it no longer acts as the primary front-door governor for broad valid workloads. |
| `consumer_and_callsite_bindings` | `BOUND_LITERAL` | existing project state | All recursive discovery/search/count consumers must align to the same model. |

## Authoritative Literal Bindings
[INTENT: REFERENZ]

| Binding Domain | Exact Binding | Source | Placement / Consumer | No Autonomous Choice Rule |
|---------------|---------------|--------|----------------------|---------------------------|
| Preflight-first rule | Server-side scope preflight and lane routing become primary. | accepted architecture | shared preflight/recursive consumers | The task must not preserve timeout-first caller control as the main model. |
| Traversal safeguard role | The current soft traversal runtime budget remains as a deeper safeguard. | accepted architecture | traversal runtime budget module and its callers | The task must neither delete the safeguard nor leave it as the dominant first-line control. |
| Server-side responsibility | Primary scope estimation belongs to the MCP server, not to caller-side prompting. | accepted architecture | shared preflight helpers and recursive consumers | The task must not offload primary scope estimation to prompt engineering. |

## Authoritative Code / Contract Surfaces
[INTENT: REFERENZ]

| Surface Name | Kind | Snippet / Contract | Source | Placement / Consumer |
|-------------|------|--------------------|--------|----------------------|
| Traversal preflight contract | `shape` | Expose primary scope-estimation and narrowing guidance before broad recursive traversal proceeds. | task contract | `src/domain/shared/guardrails/filesystem-preflight.ts` and related helpers |
| Traversal safeguard contract | `shape` | Keep runtime-budget enforcement as a deeper emergency safeguard with explicit guidance. | task contract | `src/domain/shared/guardrails/traversal-runtime-budget.ts` |

## Execute-Agent Zero-Question Gate
[INTENT: CONSTRAINT]

- **Gate Result:** `PASS`
- **Checked Domains:** traversal governor ordering, safeguard role, server-side scope responsibility, recursive consumer alignment
- **Rule:** A fresh execute agent must not need to ask whether the current 5-second budget is removed, whether preflight becomes primary, or where recursive-consumer alignment belongs.

## Authoritative References
[INTENT: REFERENZ]

| Path | Kind | Must Read Before Execution | Purpose | Referenced By |
|------|------|---------------------------|---------|---------------|
| `src/domain/shared/guardrails/tool-guardrail-limits.ts` | `schema-file` | `true` | current shared registry and the existing 5-second runtime budget | actions 1-3 |
| `src/domain/shared/guardrails/traversal-runtime-budget.ts` | `schema-file` | `true` | current traversal-budget enforcement | actions 1-3 |
| `src/domain/shared/guardrails/filesystem-preflight.ts` | `schema-file` | `true` | metadata-first preflight SSOT | actions 1-3 |
| `src/domain/shared/search/search-execution-policy.ts` | `business-file` | `true` | runtime sync/task vocabulary | actions 1-3 |
| `src/infrastructure/runtime/io-capability-detector.ts` | `business-file` | `true` | runtime capability input surface | context |
| `src/domain/inspection/search-file-contents-by-regex/search-regex-path-result.ts` | `business-file` | `true` | recursive regex consumer | actions 1-3 |
| `src/domain/inspection/search-file-contents-by-fixed-string/search-fixed-string-path-result.ts` | `business-file` | `true` | recursive fixed-string consumer | actions 1-3 |
| `src/domain/inspection/list-directory-entries/handler.ts` | `business-file` | `true` | recursive listing consumer | actions 1-3 |
| `src/domain/inspection/find-files-by-glob/handler.ts` | `business-file` | `true` | recursive glob consumer | actions 1-3 |
| `src/domain/inspection/find-paths-by-name/helpers.ts` | `business-file` | `true` | recursive name-search consumer | actions 1-3 |
| `src/domain/inspection/count-lines/handler.ts` | `business-file` | `true` | recursive count consumer | actions 1-3 |
| `src/application/server/register-inspection-tool-catalog.ts` | `business-file` | `true` | later caller-facing alignment dependency | context |
| `src/application/server/server-instructions.ts` | `business-file` | `true` | later caller-facing alignment dependency | context |
| `src/application/server/filesystem-server.ts` | `business-file` | `true` | global fuse shell context | context |
| `__bak__/plan-ugrep/PLAN.md` | `plan-file` | `true` | historical large-file plan lineage only | context |

## Resume Validation Contract
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Lowest Level Truth Surface | `artifact-evidence` | Shared guardrails and aligned recursive consumers are the primary resume truth |
| Task Lifecycle Truth Surface | `.plan/1-runtime-architecture-refactors/orchestration.md` | Parent orchestration owns lifecycle state |
| Global Frontier Truth Surface | `PLAN.md` | Global frontier SSOT |
| Conflict Requires User Confirmation | `true` | Conflicting partial state requires escalation |
| Partial Progress Requires User Decision | `true` | Partial artifact state requires a user choice |
| Durable Skip Authority Only | `true` | Only durable artifact changes may authorize skip |
| Ephemeral Freshness Steps Must Re-Execute | `true` | Pure reference re-reads remain re-executable preparation |

## Artifact Progress Inference Rules
[INTENT: REFERENZ]

- Treat the task as partially complete only when shared guardrails visibly move toward preflight-first governance and recursive consumers begin consuming that model.
- Do not treat description-only text changes as proof that recursive runtime governance is aligned.
- `EPHEMERAL_FRESHNESS_STEP` re-reads never create durable skip authority.

## User Resume Decision Gate
[INTENT: CONSTRAINT]

- **Required on Partial Progress:** `true`
- **Required on State Conflict:** `true`
- **Allowed User Choices:** `ACCEPT_AND_CONTINUE` / `REVERIFY_EXISTING_OUTPUTS`

## Persona Derivation Manifest
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Persona Generation Required | `true` | task-specific persona derivation required |
| Persona Generation Mode | `runtime_regenerate` | execute regenerates the persona at runtime |
| Persona Source Prompt Path | `src/personas/it/__prompts__/silent/enterprise/creation/prompt.md` | authoritative persona source |
| Persona Primary Domain Kernel | `Enterprise traversal and preflight governance` | highest-weight persona layer |
| Persona Secondary Specializations | `guardrail refactoring`, `recursive inspection runtime control` | secondary persona layers |
| Persona Current Task Specialization | `preflight-first traversal governance` | current specialization |
| Persona Generation Lineage | `Derived from the accepted timeout analysis and server-side scope-governance architecture.` | derivation lineage |
| Persona Runtime Review Required | `true` | runtime review is required |

## Persona DX Snapshot
[INTENT: KONTEXT]

- **Persona Title:** `Enterprise Traversal Governance Architect`
- **Persona Summary:** Refactors recursive inspection control so preflight and lane routing lead, while runtime budgets stay as deeper safety rails.
- **Persona Weighting Note:** Server-side traversal governance dominates over local recursive handler differences.

## Context
[INTENT: KONTEXT]

- **Domain:** recursive inspection traversal governance
- **Related Files:** shared guardrail registry, traversal runtime budget, filesystem preflight, recursive inspection handlers, server contract wording, historical large-file plan
- **Technical Context:** the currently visible 5-second runtime-budget refusal is generated before the server can expose a cleaner preflight-first contract for broad valid searches
- **Business Context:** agents must be able to discover and inspect broad valid scopes without hitting timeout-first ambiguity
- **Architectural Context:** runtime budget stays, but only as a deeper safeguard below preflight-first server governance
- **Plan Context Dependencies:** read task `1.4` first
- **Runtime Discovery Context:** none

## Execution Surface Assessment
[INTENT: REFERENZ]

- **Execution Surface Band:** `GREEN`
- **Primary Split Axis:** `none`
- **Hard-Gap Result:** `no RED or BLACK trigger active`
- **Single-Agent Viability Rationale:** The task is broad in touched files but singular in architecture: one traversal-governance refactor objective.

## Information Units
[INTENT: SPEZIFIKATION]

### IU-001: Shared traversal-governance ordering
**Type:** `constraint`

**Description:**
Demote timeout-first traversal refusal and make server-side preflight plus lane routing the primary controller for broad valid workloads.

**Ist-Zustand:**
The recursive search endpoints visibly trip the 5-second traversal budget as an early caller-facing refusal.

**Soll-Zustand:**
Preflight-first server governance controls scope and narrowing guidance before the runtime safeguard becomes visible.

**Architectural Rationale:**
The MCP server, not the system prompt, must own primary scope estimation and narrowing control.

### IU-002: Recursive consumer alignment
**Type:** `function`

**Description:**
Align recursive search, discovery, and count consumers to the same shared traversal-governance model.

**Ist-Zustand:**
Only some consumers enforce the traversal runtime budget, and they do so before a clean preflight-first contract exists.

**Soll-Zustand:**
Recursive consumers use one coherent preflight-first model and one deeper safeguard model.

**Architectural Rationale:**
Cross-endpoint traversal behavior must be coherent for both developers and LLM agents.

### IU-003: Shared guardrail and caller-guidance coherence
**Type:** `object`

**Description:**
Make the shared guardrail surfaces produce deterministic narrowing guidance that later caller-facing descriptions can accurately reflect.

**Ist-Zustand:**
The current runtime refusal is accurate but too dominant and too early for the desired broad-valid-workload architecture.

**Soll-Zustand:**
The guardrail stack exposes a cleaner hierarchy: preflight, lane routing, then deeper safeguard.

**Architectural Rationale:**
The server must explain its behavior in a way that the eventual endpoint docs and caller contracts can truthfully document.

## Actions
[INTENT: ANWEISUNG]

1. **Refactor the shared preflight and traversal-governance ordering**
   - **What:** update shared preflight and traversal-budget surfaces so preflight-first governance becomes the primary model
   - **Where:** `src/domain/shared/guardrails/filesystem-preflight.ts`, `src/domain/shared/guardrails/traversal-runtime-budget.ts`, `src/domain/shared/guardrails/tool-guardrail-limits.ts`
   - **How:** preserve the runtime safeguard while reordering its role beneath server-side scope estimation and narrowing guidance
   - **Why:** timeout-first caller experience is architecturally wrong for broad valid workloads
   - **Inputs / References:** current shared guardrails, runtime policy, runtime capability detector, historical large-file plan
   - **Output Contract:** shared guardrails expose the new preflight-first ordering without deleting the deeper safeguard
   - **Checkpoint Impact:** completes `cp-1-preflight-governance-refactor`
   - **Verification:** preflight and guidance surfaces are primary; the runtime safeguard remains but is explicitly secondary
   - **Stop if:** implementation would simply delete the safeguard or leave it as the dominant first-line controller
2. **Align recursive search/discovery/count consumers to the shared model**
   - **What:** update recursive consumers so they follow the same preflight-first traversal governance
   - **Where:** recursive search, discovery, and count handlers/helpers listed in `files_modified`
   - **How:** bind them to the updated shared guardrail ordering and structured narrowing behavior
   - **Why:** cross-endpoint recursive behavior must become coherent
   - **Inputs / References:** shared guardrails plus all bound recursive consumers
   - **Output Contract:** recursive consumers reflect the same primary-governor ordering and deeper safeguard role
   - **Checkpoint Impact:** completes `cp-2-recursive-consumer-alignment`
   - **Verification:** no bound consumer remains timeout-first as its primary caller-facing governor
   - **Stop if:** any recursive consumer would require a public contract break to consume the shared model
3. **Verify public-contract readiness for later description alignment**
   - **What:** ensure the updated runtime behavior is precise enough that later description tasks can document it truthfully
   - **Where:** shared guardrails and recursive consumers only
   - **How:** validate that the new runtime behavior now clearly distinguishes preflight-first guidance from deeper runtime safeguards
   - **Why:** later documentation units depend on this runtime coherence
   - **Inputs / References:** current caller-facing files plus updated guardrail behavior
   - **Output Contract:** later documentation tasks can truthfully describe the implemented traversal model
   - **Checkpoint Impact:** contributes to `cp-2-recursive-consumer-alignment`
   - **Verification:** caller-facing wording can later be aligned without inventing behavior not implemented here
   - **Stop if:** any remaining behavior is too ambiguous to document without invention

## Checkpoint Definitions
[INTENT: REFERENZ]

| Checkpoint ID | Kind | Completion Signal | Produces Artifacts | Resume Meaning |
|---------------|------|-------------------|--------------------|----------------|
| `cp-1-preflight-governance-refactor` | `artifact_milestone` | Shared preflight/traversal guardrails expose the new ordering and deeper-safeguard role | `src/domain/shared/guardrails/filesystem-preflight.ts`, `src/domain/shared/guardrails/traversal-runtime-budget.ts`, `src/domain/shared/guardrails/tool-guardrail-limits.ts` | Shared traversal governance is refactored |
| `cp-2-recursive-consumer-alignment` | `artifact_milestone` | All bound recursive consumers align to the shared preflight-first traversal model | recursive consumer files in `files_modified` | Recursive runtime behavior is coherent |

## Execution Progress Ledger
[INTENT: REFERENZ]

| Checkpoint ID | Status | Evidence | Notes |
|---------------|--------|----------|-------|
| `cp-1-preflight-governance-refactor` | `DONE` | updated shared traversal/preflight guardrails | shared preflight admission now leads and the runtime safeguard is explicitly deeper |
| `cp-2-recursive-consumer-alignment` | `DONE` | updated recursive consumer surfaces | bound recursive consumers now consume the shared preflight-first traversal model |

## Resume Conflict Resolution Rules
[INTENT: CONSTRAINT]

- If shared guardrails move to preflight-first ordering but one or more recursive consumers remain timeout-first, stop and escalate.
- If the runtime safeguard disappears entirely rather than becoming a deeper layer, stop and escalate.
- If later caller-facing wording would still need to invent behavior after this task, stop and escalate.

## Checkpoint Update Protocol
[INTENT: ANWEISUNG]

1. Mark `cp-1-preflight-governance-refactor` DONE only after the new shared ordering is visible in the bound guardrail surfaces.
2. Mark `cp-2-recursive-consumer-alignment` DONE only after all bound recursive consumers align to the new ordering.

## Acceptance Criteria
[INTENT: REFERENZ]

- [ ] Shared traversal/preflight guardrails expose preflight-first governance with the runtime budget as a deeper safeguard.
- [ ] Recursive search/discovery/count consumers align to the same traversal-governance model.
- [ ] The current 5-second runtime safeguard remains present but is no longer the dominant first-line caller-facing control for valid broad workloads.
- [ ] Later public descriptions can truthfully document the new behavior without inventing semantics.

## Stop Conditions & User Escalation
[INTENT: CONSTRAINT]

- **Plan-vs-Reality Mismatch:** Stop if the current recursive consumer surfaces already expose a conflicting traversal-governance refactor.
- **Missing Required Plan Information:** Stop if the ordering between preflight, lane routing, and deeper safeguard cannot be bound exactly.
- **Autonomous Decision Required:** Stop if execution would require inventing a new public recursive contract or delegating primary scope estimation to the system prompt.
- **Decision Surface Not Closed:** If any determinable traversal-governance rule is still open, stop and inform the user.
- **Allowed Runtime Discovery:** None.
- **User Message Contract:** `Plan says X / current reality is Y / missing or conflicting information is Z / continuing would require an autonomous decision.`

## Post-Completion Instructions
[INTENT: ANWEISUNG]

1. Mark this task `[x]` in [`.plan/1-runtime-architecture-refactors/orchestration.md`](./orchestration.md).
2. Unblock the waiting documentation units that depend on finalized runtime architecture.
3. Re-anchor [`PLAN.md`](../../PLAN.md).

## File Path Index
[INTENT: REFERENZ]

| # | Path | Kind | Why Relevant | Must Read Before Execution |
|---|------|------|--------------|----------------------------|
| 1 | `src/domain/shared/guardrails/tool-guardrail-limits.ts` | `schema-file` | current shared registry and 5-second runtime budget | `true` |
| 2 | `src/domain/shared/guardrails/traversal-runtime-budget.ts` | `schema-file` | current traversal-budget enforcement | `true` |
| 3 | `src/domain/shared/guardrails/filesystem-preflight.ts` | `schema-file` | metadata-first preflight SSOT | `true` |
| 4 | `src/domain/shared/search/search-execution-policy.ts` | `business-file` | runtime sync/task vocabulary | `true` |
| 5 | `src/infrastructure/runtime/io-capability-detector.ts` | `business-file` | runtime capability input surface | `true` |
| 6 | `src/domain/inspection/search-file-contents-by-regex/search-regex-path-result.ts` | `business-file` | recursive regex consumer | `true` |
| 7 | `src/domain/inspection/search-file-contents-by-fixed-string/search-fixed-string-path-result.ts` | `business-file` | recursive fixed-string consumer | `true` |
| 8 | `src/domain/inspection/list-directory-entries/handler.ts` | `business-file` | recursive listing consumer | `true` |
| 9 | `src/domain/inspection/find-files-by-glob/handler.ts` | `business-file` | recursive glob consumer | `true` |
| 10 | `src/domain/inspection/find-paths-by-name/helpers.ts` | `business-file` | recursive name-search consumer | `true` |
| 11 | `src/domain/inspection/count-lines/handler.ts` | `business-file` | recursive count consumer | `true` |
| 12 | `src/application/server/register-inspection-tool-catalog.ts` | `business-file` | later caller-facing alignment dependency | `true` |
| 13 | `src/application/server/server-instructions.ts` | `business-file` | later caller-facing alignment dependency | `true` |
| 14 | `src/application/server/filesystem-server.ts` | `business-file` | global fuse shell context | `true` |
| 15 | `__bak__/plan-ugrep/PLAN.md` | `plan-file` | historical large-file plan lineage only | `true` |

## Notes
[INTENT: KONTEXT]

- This task is the final runtime-architecture blocker for most documentation units.
- The later documentation tasks should not start until this runtime ordering is real in code.

