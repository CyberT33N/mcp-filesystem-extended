---
file_type: "task"
task_id: "1.15"
task_title: "Infrastructure constants and diff-fence helper — wrapDiffInSafeFencedBlock, SSOT tool-name constants"
unit: "1"
parent_orchestration: ".plan/1-runtime-architecture-refactors/orchestration.md"
classification: "ISOLATED"
status: "done"
estimated_complexity: "LOW"
files_modified:
  - "src/infrastructure/formatting/unified-diff.ts"
  - "src/domain/comparison/diff-files/handler.ts"
  - "src/domain/comparison/diff-text-content/handler.ts"
  - "src/domain/mutation/replace-file-line-ranges/helpers.ts"
  - "src/domain/inspection/search-file-contents-by-fixed-string/schema.ts"
  - "src/domain/inspection/search-file-contents-by-fixed-string/fixed-string-search-support.ts"
  - "src/domain/inspection/search-file-contents-by-fixed-string/fixed-string-search-file-entry.ts"
  - "src/domain/inspection/search-file-contents-by-regex/schema.ts"
files_read:
  - "src/infrastructure/formatting/unified-diff.ts"
  - "src/domain/comparison/diff-files/handler.ts"
  - "src/domain/comparison/diff-text-content/handler.ts"
  - "src/domain/mutation/replace-file-line-ranges/helpers.ts"
  - "src/domain/inspection/search-file-contents-by-fixed-string/schema.ts"
  - "src/domain/inspection/search-file-contents-by-fixed-string/fixed-string-search-support.ts"
  - "src/domain/inspection/search-file-contents-by-fixed-string/fixed-string-search-file-entry.ts"
  - "src/domain/inspection/search-file-contents-by-regex/schema.ts"
dependencies: []
blocked_by: "none"
information_completeness_mode: "FULLY_SPECIFIED"
implementation_scenarios:
  - "FUNCTION_CREATION"
  - "CONSTANT_ADDITION"
  - "FUNCTION_UPDATE"
execution_surface_band: "GREEN"
primary_split_axis: "none"
single_agent_viability_note: "8 files, all mechanical extractions: one new infrastructure helper function, one tool-name constant export in two schema files, and corresponding import replacements in three callers. No business logic changes."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
decision_surface_register:
  - surface: "naming"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "wrapDiffInSafeFencedBlock, SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME, SEARCH_FILE_CONTENTS_BY_REGEX_TOOL_NAME — all bound below"
  - surface: "path_and_placement"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "New function in unified-diff.ts; constants in schema.ts files"
  - surface: "literal_values_and_budgets"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "Constant values are exact MCP tool name strings; function body is fully specified"
  - surface: "defaults_and_fallbacks"
    status: "NOT_APPLICABLE"
    binding_source: "—"
    notes: "No defaults"
  - surface: "types_and_shapes"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "Function signature specified below"
  - surface: "function_signatures_and_returns"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "wrapDiffInSafeFencedBlock: (diff: string) => string"
  - surface: "object_schema_and_mapper_contracts"
    status: "NOT_APPLICABLE"
    binding_source: "—"
    notes: "No schema changes"
  - surface: "dependencies_and_import_surfaces"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "All import paths specified below"
  - surface: "validation_and_boundary_rules"
    status: "NOT_APPLICABLE"
    binding_source: "—"
    notes: "No validation changes"
  - surface: "consumer_and_callsite_bindings"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "All 3 diff callers + 2 fixed-string tool-name consumers listed"
authoritative_literal_bindings:
  - binding_domain: "naming"
    exact_binding: "wrapDiffInSafeFencedBlock"
    source: "plan-output"
    placement_or_consumer: "src/infrastructure/formatting/unified-diff.ts — exported function"
    no_autonomous_choice_rule: "Name must be a verb phrase describing what it does; not to be confused with createUnifiedDiff"
  - binding_domain: "literal_values_and_budgets"
    exact_binding: "SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME = \"search_file_contents_by_fixed_string\""
    source: "plan-output — matches exact MCP tool registration name"
    placement_or_consumer: "src/domain/inspection/search-file-contents-by-fixed-string/schema.ts — exported const"
    no_autonomous_choice_rule: "Must match the exact string used in MCP tool registration; mirrors READ_FILE_CONTENT_TOOL_NAME precedent in read-file-content/schema.ts"
  - binding_domain: "literal_values_and_budgets"
    exact_binding: "SEARCH_FILE_CONTENTS_BY_REGEX_TOOL_NAME = \"search_file_contents_by_regex\""
    source: "plan-output"
    placement_or_consumer: "src/domain/inspection/search-file-contents-by-regex/schema.ts — exported const"
    no_autonomous_choice_rule: "Must match exact MCP registration name"
  - binding_domain: "dependencies_and_import_surfaces"
    exact_binding: "import { createUnifiedDiff, wrapDiffInSafeFencedBlock } from \"@infrastructure/formatting/unified-diff\";"
    source: "plan-output"
    placement_or_consumer: "diff-files/handler.ts, diff-text-content/handler.ts, replace-file-line-ranges/helpers.ts — extend existing import"
    no_autonomous_choice_rule: "Must be added to the existing import statement, not as a separate duplicate"
  - binding_domain: "dependencies_and_import_surfaces"
    exact_binding: "import { SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME } from \"./schema\";"
    source: "plan-output"
    placement_or_consumer: "fixed-string-search-support.ts and fixed-string-search-file-entry.ts"
    no_autonomous_choice_rule: "Use relative path ./schema since these files are in the same directory"
requires_authoritative_code_surfaces: true
authoritative_code_surfaces:
  - surface_name: "wrapDiffInSafeFencedBlock"
    kind: "signature"
    snippet_or_contract: |
      /**
       * Wraps a unified diff string in a fenced code block, dynamically selecting
       * the minimum number of backticks needed to avoid premature fence closure.
       *
       * @param diff - Raw unified diff string from `createUnifiedDiff`.
       * @returns Fenced diff block safe for embedding in Markdown output.
       */
      export function wrapDiffInSafeFencedBlock(diff: string): string {
        let numBackticks = 3;
        while (diff.includes("`".repeat(numBackticks))) {
          numBackticks++;
        }
        return `${"`".repeat(numBackticks)}diff\n${diff}${"`".repeat(numBackticks)}`;
      }
    source: "Extracted verbatim from the three identical inline implementations in diff-files/handler.ts, diff-text-content/handler.ts, and replace-file-line-ranges/helpers.ts"
    placement_or_consumer: "src/infrastructure/formatting/unified-diff.ts — placed after createUnifiedDiff export"
  - surface_name: "caller replacement pattern"
    kind: "other"
    snippet_or_contract: |
      // Before (each caller had this inline):
      let numBackticks = 3;
      while (diff.includes("`".repeat(numBackticks))) {
        numBackticks++;
      }
      return `${"`".repeat(numBackticks)}diff\n${diff}${"`".repeat(numBackticks)}`;

      // After:
      return wrapDiffInSafeFencedBlock(diff);
    source: "plan-output"
    placement_or_consumer: "diff-files/handler.ts getFormattedFileDiff(), diff-text-content/handler.ts getFormattedContentDiff(), replace-file-line-ranges/helpers.ts applyFileLineRangeReplacements()"
  - surface_name: "fixed-string callers tool-name replacement"
    kind: "other"
    snippet_or_contract: |
      // Before (in fixed-string-search-support.ts and fixed-string-search-file-entry.ts):
      const SEARCH_FIXED_STRING_TOOL_NAME = "search_file_contents_by_fixed_string";

      // After:
      // Remove local const; use imported SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME
      import { SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME } from "./schema";
    source: "plan-output"
    placement_or_consumer: "fixed-string-search-support.ts and fixed-string-search-file-entry.ts"
authoritative_references:
  - path: "src/infrastructure/formatting/unified-diff.ts"
    kind: "business-file"
    usage: "Target for new wrapDiffInSafeFencedBlock export; read to understand existing exports"
    required_before_execution: true
  - path: "src/domain/comparison/diff-files/handler.ts"
    kind: "business-file"
    usage: "Caller of inline backtick fence pattern to replace"
    required_before_execution: true
  - path: "src/domain/comparison/diff-text-content/handler.ts"
    kind: "business-file"
    usage: "Same"
    required_before_execution: true
  - path: "src/domain/mutation/replace-file-line-ranges/helpers.ts"
    kind: "business-file"
    usage: "Same"
    required_before_execution: true
  - path: "src/domain/inspection/search-file-contents-by-fixed-string/schema.ts"
    kind: "business-file"
    usage: "Receives SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME export; note this was modified in the 1.14 task (BatchOperationErrorBaseSchema import) — re-anchor before editing"
    required_before_execution: true
  - path: "src/domain/inspection/search-file-contents-by-fixed-string/fixed-string-search-support.ts"
    kind: "business-file"
    usage: "Has local SEARCH_FIXED_STRING_TOOL_NAME const to remove"
    required_before_execution: true
  - path: "src/domain/inspection/search-file-contents-by-fixed-string/fixed-string-search-file-entry.ts"
    kind: "business-file"
    usage: "Same"
    required_before_execution: true
  - path: "src/domain/inspection/search-file-contents-by-regex/schema.ts"
    kind: "business-file"
    usage: "Receives SEARCH_FILE_CONTENTS_BY_REGEX_TOOL_NAME export; note this was modified in the 1.13+1.14 sessions — re-anchor before editing"
    required_before_execution: true
  - path: "src/domain/inspection/read-file-content/schema.ts"
    kind: "business-file"
    usage: "Precedent reference: READ_FILE_CONTENT_TOOL_NAME export pattern to follow"
    required_before_execution: false
upstream_plan_references:
  - task_id: "1.14"
    path: ".plan/1-runtime-architecture-refactors/1.14-schema-contract-layer-centralization.md"
    must_read_before_execution: true
    reason: "1.14 modified search-file-contents-by-fixed-string/schema.ts and search-file-contents-by-regex/schema.ts; re-anchor those files after 1.14 lands before this task edits them"
runtime_discovery:
  required: false
  scope: []
  allowed_topics: []
checkpoint_strategy: "SEQUENTIAL_CHECKPOINTS_REQUIRED"
checkpoint_eligibility_contract:
  durable_skip_authority_only: true
  ephemeral_freshness_steps:
    - "Initial full-file re-anchor reads of all 8 target files"
  durable_resume_checkpoints:
    - "CHK-1.15-A"
    - "CHK-1.15-B"
    - "CHK-1.15-C"
resume_validation_contract:
  lowest_level_truth_surface: "checkpoint-ledger"
  task_lifecycle_truth_surface: ".plan/1-runtime-architecture-refactors/orchestration.md"
  global_frontier_truth_surface: "PLAN.md"
  artifact_evidence_sources:
    - "src/infrastructure/formatting/unified-diff.ts — presence of wrapDiffInSafeFencedBlock export"
    - "diff-files/handler.ts — absence of inline numBackticks while loop"
    - "fixed-string-search-support.ts — absence of local SEARCH_FIXED_STRING_TOOL_NAME const"
    - "search-file-contents-by-regex/schema.ts — presence of SEARCH_FILE_CONTENTS_BY_REGEX_TOOL_NAME"
  conflict_requires_user_confirmation: true
  partial_progress_requires_user_decision: true
  durable_skip_authority_only: true
  ephemeral_checkpoint_conflict_requires_reexecution: false
artifact_progress_inference_rules:
  - "If wrapDiffInSafeFencedBlock is already exported from unified-diff.ts, CHK-1.15-A is DONE"
  - "If diff-files/handler.ts has no inline numBackticks while loop, its portion of CHK-1.15-B is DONE"
  - "If fixed-string-search-support.ts has no local SEARCH_FIXED_STRING_TOOL_NAME const, CHK-1.15-C is partially DONE for that file"
  - "Freshness re-anchor reads never create durable skip authority"
user_resume_decision_gate:
  required_on_partial_progress: true
  required_on_state_conflict: true
  default_user_choices:
    - "ACCEPT_AND_CONTINUE"
    - "REVERIFY_EXISTING_OUTPUTS"
task_completion_order_contract:
  ordered_phase_sequence:
    - "task_file_local_completion_closure"
    - "parent_orchestration_status_update"
    - "dependency_resolution_and_unblock"
    - "unit_and_plan_completion_cascade"
  parent_status_write_blocked_until_task_file_done: true
  plan_update_blocked_until_parent_sync: true
current_target_binding_contract:
  binding_surface: "active_task_file"
  proof_scope: "task_file_local"
  invalidate_on_target_or_frontier_change: true
task_local_completion_ledger:
  required_units:
    - "actions_executed"
    - "acceptance_criteria_pass"
    - "required_artifacts_materialized_or_verified"
    - "checkpoint_or_artifact_progress_synced"
    - "persona_write_permission_green_go"
task_local_pre_write_gate_report_contract:
  gate_inputs:
    - "current_target_binding"
    - "ordered_phase_contract"
    - "task_local_completion_ledger"
    - "no_heuristic_exit_governance"
  write_allowed_only_if_all_pass: true
no_heuristic_exit_governance:
  task_file_done_requires_visible_local_proof: true
  broader_parent_or_plan_pass_not_sufficient: true
persona_generation_required: true
persona_generation_mode: "runtime_regenerate"
persona_source_prompt_path: "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
persona_primary_domain_kernel: "TypeScript Node.js MCP Server Infrastructure and Domain Architecture — SSOT Extraction"
persona_secondary_specializations:
  - "Infrastructure formatting module design"
  - "Structural extraction refactoring without breaking changes"
  - "TypeScript module constant and helper function placement"
persona_current_task_specialization: "Backtick-fence helper extraction and tool-name SSOT constants — mechanical infrastructure refactoring"
persona_generation_lineage: "Derived from task contract: infrastructure helper extraction + SSOT constant declaration, TypeScript, no runtime behavior change"
persona_runtime_review_required: true
persona_source_load_policy:
  source_loading_mode: "DEFERRED_UNTIL_PRE_EXECUTION_STATE_SNAPSHOT_CLOSED"
  early_source_load_forbidden: true
  late_load_trigger: "after pre_execution_state_snapshot_closed and immediately before execute_time_persona_subprocess"
  transitive_source_chain_must_be_closed_after_load: true
persona_state_disclosure_contract:
  pre_creator_state_report_required: true
  post_creator_ledger_state_report_required: true
  required_state_fields:
    - "task_id"
    - "pre_execution_state_snapshot_closed"
    - "persona_source_documents_loaded"
    - "persona_source_chain_closed"
    - "creator_phase_execution_ledger_status"
    - "runtime_persona_outcome"
    - "persona_write_permission"
  final_dx_log_after_proof_only: true
persona_embedded_creator_execution_contract:
  execution_mode: "FULL_CONTRACT_EQUIVALENT_EXECUTION"
  creator_phase_source: "required_creator_execution_phases"
  creator_phase_execution_ledger_required: true
  creator_phase_order_non_swappable: true
  local_creator_logic_redefinition_forbidden: true
persona_write_gate_contract:
  persona_write_permission_default: "BLOCKED"
  persona_write_permission_release_requirements:
    - "pre_execution_state_snapshot_closed = true"
    - "persona_source_documents_loaded = true"
    - "persona_source_chain_closed = true"
    - "creator_phase_execution_ledger_complete = true"
    - "creator_completion_proof_pass = true"
    - "runtime_persona_outcome = GREEN_GO"
  final_green_go_required_for_write: true
persona_dx_log_contract:
  pre_proof_surface: "persona_state_report_only"
  final_dx_log_after_proof_only: true
  required_final_fields:
    - "runtime_persona_title"
    - "runtime_persona_summary"
    - "runtime_persona_alignment_percent"
    - "runtime_persona_outcome"
    - "creator_completion_proof_summary"
persona_creator_execution_proof_contract:
  transitive_persona_source_chain:
    - "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
    - "src/personas/it/__prompts__/interactive/best-options/enterprise/creation/prompt.md"
  canonical_creator_source_path: "src/personas/it/__prompts__/interactive/best-options/enterprise/creation/prompt.md"
  creator_equivalence_rule: "Runtime persona must match the canonical creator decision for the same final pre_execution_state_snapshot"
  required_creator_execution_phases:
    - "Context extraction and persona axis mapping"
    - "Multi-option generation across domain/language/operation axes"
    - "Weighted scoring and top-option selection"
    - "Quality gate and persona finalization"
  required_creator_proof_fields:
    - "creator_transitive_chain_verified"
    - "creator_required_phases_completed"
    - "creator_equivalence_confirmed"
    - "snapshot_substitution_detected = false"
    - "creator_completion_proof_pass = true"
  non_substitutable_persona_surfaces:
    - "persona_derivation_manifest"
    - "persona_dx_snapshot"
    - "persona_title"
    - "persona_summary"
  proof_gate_blocks_green_go: true
  proof_failure_outcome: "PLAN_GAP"
persona_review_binding_contract:
  contract_source_ingestion_is_not_review: true
  pre_execution_state_required_surfaces:
    - "task_contract"
    - "upstream_plan_references"
    - "authoritative_references"
    - "resume_state_mode"
    - "artifact_evidence"
    - "files_read"
    - "files_modified"
  pre_execution_state_required_reads:
    - "authoritative_references"
    - "files_read"
    - "files_modified"
  persona_review_finalization_gate: "GREEN_GO only after pre_execution_state_snapshot_closed = true"
  persona_review_invalidation_triggers:
    - "new_required_reference"
    - "new_or_changed_files_read_or_files_modified_reality"
    - "new_artifact_evidence"
    - "resume_candidate_change"
    - "shared_file_reality_change_before_first_action"
    - "new_boundary_or_consumer_evidence"
  persona_review_reentry_protocol: "invalidate_persona_outcome_and_rerun_review_before_first_action"
persona_dx_snapshot:
  title: "TypeScript Infrastructure Extraction Architect — MCP Server SSOT Refactoring"
  summary: "An enterprise TypeScript architect specializing in infrastructure module design, SSOT constant extraction, and mechanical helper function centralization. Applies strict no-breaking-change discipline to pull repeated inline implementations into canonical shared locations."
  weighting_note: "Infrastructure module architecture dominates over language specialization (TypeScript) and narrow operation type (function extraction)."
checkpoint_definitions:
  - checkpoint_id: "CHK-1.15-A"
    kind: "sequential_step"
    completion_signal: "wrapDiffInSafeFencedBlock is exported from src/infrastructure/formatting/unified-diff.ts"
    produces_artifacts:
      - "src/infrastructure/formatting/unified-diff.ts"
  - checkpoint_id: "CHK-1.15-B"
    kind: "artifact_milestone"
    completion_signal: "All 3 callers (diff-files/handler.ts, diff-text-content/handler.ts, replace-file-line-ranges/helpers.ts) use wrapDiffInSafeFencedBlock and have no inline numBackticks while loop"
    produces_artifacts:
      - "src/domain/comparison/diff-files/handler.ts"
      - "src/domain/comparison/diff-text-content/handler.ts"
      - "src/domain/mutation/replace-file-line-ranges/helpers.ts"
  - checkpoint_id: "CHK-1.15-C"
    kind: "artifact_milestone"
    completion_signal: "SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME exported from fixed-string schema; local const removed from support.ts and file-entry.ts; SEARCH_FILE_CONTENTS_BY_REGEX_TOOL_NAME exported from regex schema"
    produces_artifacts:
      - "src/domain/inspection/search-file-contents-by-fixed-string/schema.ts"
      - "src/domain/inspection/search-file-contents-by-fixed-string/fixed-string-search-support.ts"
      - "src/domain/inspection/search-file-contents-by-fixed-string/fixed-string-search-file-entry.ts"
      - "src/domain/inspection/search-file-contents-by-regex/schema.ts"
execution_progress_ledger:
  ledger_mode: "CHECKPOINT_LEDGER"
  checkpoints:
    - checkpoint_id: "CHK-1.15-A"
      status: "DONE"
    - checkpoint_id: "CHK-1.15-B"
      status: "DONE"
    - checkpoint_id: "CHK-1.15-C"
      status: "DONE"
resume_conflict_resolution_rules:
  - "If CHK-1.15-A DONE but wrapDiffInSafeFencedBlock is absent from unified-diff.ts, STOP and request user confirmation"
  - "If any caller still has the inline numBackticks while loop after CHK-1.15-B is claimed DONE, STOP and request user decision"
  - "Never begin CHK-1.15-B before CHK-1.15-A — callers cannot import before the export exists"
checkpoint_update_protocol:
  - "Mark checkpoint IN_PROGRESS when editing begins on the first file for that group"
  - "Mark checkpoint DONE immediately after all files in the group are verified"
  - "Never use the checkpoint ledger as a substitute for task lifecycle status in parent orchestration"
halt_conditions:
  plan_reality_mismatch: true
  missing_required_plan_information: true
  autonomous_decision_required: true
user_notification_required: true
---

# Task 1.15: Infrastructure Constants and Diff-Fence Helper
[INTENT: ANWEISUNG]

## Goal
[INTENT: KONTEXT]

This task eliminates three categories of small but genuine redundancy in the infrastructure and inspection domain layers. First, an identical 4-line backtick-fence wrapping block is defined inline in three separate callers that all import from the same formatting infrastructure module — extracting it into a named helper there is the correct placement. Second, the tool-name literal `"search_file_contents_by_fixed_string"` is independently declared in two files within the same endpoint directory — a single exported constant from the endpoint schema file eliminates this duplication. Third, the regex search endpoint is missing a tool-name constant entirely, relying on inline string literals.

All changes are mechanical structural extractions with zero behavior changes.

## Expected Result
[INTENT: KONTEXT]

- `wrapDiffInSafeFencedBlock(diff: string): string` is exported from `src/infrastructure/formatting/unified-diff.ts` and used by all three callers.
- `SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME` is exported from the fixed-string schema and used by both support files.
- `SEARCH_FILE_CONTENTS_BY_REGEX_TOOL_NAME` is exported from the regex schema.
- No inline duplicates remain for any of these three surfaces.

## Preconditions
[INTENT: CONSTRAINT]

- Task 1.14 must have landed before this task modifies `search-file-contents-by-fixed-string/schema.ts` and `search-file-contents-by-regex/schema.ts` (those files were modified in 1.14).
- No other dependency — this task is ISOLATED.

## Execution Metadata Register
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Task ID | `1.15` | Eindeutige Aufgabenkennung |
| Classification | `ISOLATED` | No shared file conflicts with other tasks in the current plan |
| Information Completeness Mode | `FULLY_SPECIFIED` | All names, signatures, and import paths bound |
| Implementation Scenarios | `FUNCTION_CREATION, CONSTANT_ADDITION, FUNCTION_UPDATE` | New helper export + constant exports + caller updates |
| Execution Surface Band | `GREEN` | 8 files, all mechanical, single infrastructure anchor |
| Primary Split Axis | `none` | Not split further |
| Single-Agent Viability Note | All changes are 1-5 line replacements in each file; anchored to one stable infrastructure module |
| Parent Orchestration | `.plan/1-runtime-architecture-refactors/orchestration.md` |  |
| Blocked By | `none` |  |
| Requires Upstream Plan Re-Anchoring | `yes` | Re-read 1.14 task and its modified schema files before editing |
| Runtime Discovery Allowed | `no` |  |
| Checkpoint Strategy | `SEQUENTIAL_CHECKPOINTS_REQUIRED` | Shared export must exist before callers import |
| Durable Skip Authority Only | `true` |  |
| Ephemeral Freshness Steps Present | `yes` | Initial re-anchor reads |
| User Notification Required On Gap/Mismatch | `true` |  |
| Persona Generation Required | `true` |  |
| Persona Generation Mode | `runtime_regenerate` |  |
| Persona Primary Domain Kernel | `TypeScript Node.js MCP Server Infrastructure and Domain Architecture — SSOT Extraction` |  |
| Persona Current Task Specialization | `Backtick-fence helper extraction and tool-name SSOT constants` |  |
| Persona Runtime Review Required | `true` |  |
| Persona Source Load Policy | `DEFERRED_UNTIL_PRE_EXECUTION_STATE_SNAPSHOT_CLOSED` |  |

## Task Contract Closure
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Decision Surfaces Closed | `true` | All surfaces bound |
| Unresolved Determinable Decisions | `0` | None open |
| Expected Execute-Agent Questions | `0` | None |

## Decision Surface Register
[INTENT: REFERENZ]

| Surface | Status | Binding Source | Notes |
|---------|--------|----------------|-------|
| `naming` | `BOUND_LITERAL` | `plan-output` | All three new names bound in frontmatter |
| `path_and_placement` | `BOUND_LITERAL` | `plan-output` | unified-diff.ts for helper; schema.ts files for constants |
| `literal_values_and_budgets` | `BOUND_LITERAL` | `plan-output` | Constant values are MCP tool name strings |
| `defaults_and_fallbacks` | `NOT_APPLICABLE` | — | — |
| `types_and_shapes` | `BOUND_LITERAL` | `plan-output` | Function signature in code surfaces |
| `function_signatures_and_returns` | `BOUND_LITERAL` | `plan-output` | `(diff: string) => string` |
| `object_schema_and_mapper_contracts` | `NOT_APPLICABLE` | — | — |
| `dependencies_and_import_surfaces` | `BOUND_LITERAL` | `plan-output` | Import paths in frontmatter |
| `validation_and_boundary_rules` | `NOT_APPLICABLE` | — | — |
| `consumer_and_callsite_bindings` | `BOUND_LITERAL` | `plan-output` | All callers listed |

## Authoritative Literal Bindings
[INTENT: REFERENZ]

*(See YAML frontmatter `authoritative_literal_bindings` for the complete list)*

## Authoritative Code / Contract Surfaces
[INTENT: REFERENZ]

*(See YAML frontmatter `authoritative_code_surfaces` for function body and caller replacement patterns)*

## Execute-Agent Zero-Question Gate
[INTENT: CONSTRAINT]

- **Gate Result:** `PASS`
- **Checked Domains:** naming, function_signatures_and_returns, literal_values_and_budgets, dependencies_and_import_surfaces, consumer_and_callsite_bindings
- **Rule:** A fresh Execute-Agent needs no follow-up questions for any determinable surface.

## Authoritative References
[INTENT: REFERENZ]

| Path | Kind | Must Read Before Execution | Purpose |
|------|------|---------------------------|---------|
| `src/infrastructure/formatting/unified-diff.ts` | business-file | true | Target for new helper; understand existing exports |
| `src/domain/comparison/diff-files/handler.ts` | business-file | true | Caller with inline fence pattern |
| `src/domain/comparison/diff-text-content/handler.ts` | business-file | true | Same |
| `src/domain/mutation/replace-file-line-ranges/helpers.ts` | business-file | true | Same |
| `src/domain/inspection/search-file-contents-by-fixed-string/schema.ts` | business-file | true | Receives constant export; modified by 1.14 — re-anchor |
| `src/domain/inspection/search-file-contents-by-fixed-string/fixed-string-search-support.ts` | business-file | true | Has local const to remove |
| `src/domain/inspection/search-file-contents-by-fixed-string/fixed-string-search-file-entry.ts` | business-file | true | Same |
| `src/domain/inspection/search-file-contents-by-regex/schema.ts` | business-file | true | Receives constant; modified by 1.13+1.14 — re-anchor |
| `.plan/1-runtime-architecture-refactors/1.14-schema-contract-layer-centralization.md` | plan-file | true | Documents 1.14's changes to fixed-string schema and regex schema |

## Resume Validation Contract
[INTENT: REFERENZ]

*(See YAML frontmatter `resume_validation_contract`)*

## Artifact Progress Inference Rules
[INTENT: REFERENZ]

- `wrapDiffInSafeFencedBlock` present in unified-diff.ts → CHK-1.15-A DONE
- Inline `numBackticks` while loop absent from diff-files/handler.ts → that file's portion of CHK-1.15-B DONE
- Local `SEARCH_FIXED_STRING_TOOL_NAME` const absent from fixed-string-search-support.ts → partially DONE
- Freshness re-anchor reads never create durable skip authority

## User Resume Decision Gate
[INTENT: CONSTRAINT]

- **Required on Partial Progress:** `true`
- **Required on State Conflict:** `true`
- **Allowed User Choices:** `ACCEPT_AND_CONTINUE` / `REVERIFY_EXISTING_OUTPUTS`

## Task Completion Order Contract
[INTENT: REFERENZ]

- **Ordered Phase Sequence:** `task_file_local_completion_closure` → `parent_orchestration_status_update` → `dependency_resolution_and_unblock` → `unit_and_plan_completion_cascade`
- **Parent Status Write Blocked Until Task File Done:** `true`
- **Plan Update Blocked Until Parent Sync:** `true`

## Current Target Binding Contract / Task Local Completion Ledger / Task Local Pre-Write Gate Report / No-Heuristic Exit Governance
[INTENT: REFERENZ]

*(Standard contracts — see YAML frontmatter for full specification)*

## Persona Derivation Manifest
[INTENT: REFERENZ]

*(See YAML frontmatter — persona_primary_domain_kernel, persona_current_task_specialization, persona_generation_lineage)*

## Persona Creator Execution Proof Contract / Source Load Policy / State Disclosure Contract / Embedded Creator Execution Contract / Write Gate Contract / DX Log Contract / Review Binding Contract
[INTENT: REFERENZ]

*(See YAML frontmatter for complete specifications of all persona contract surfaces)*

## Persona DX Snapshot
[INTENT: KONTEXT]

- **Persona Title:** `TypeScript Infrastructure Extraction Architect — MCP Server SSOT Refactoring`
- **Persona Summary:** An enterprise TypeScript architect specializing in infrastructure module design, SSOT constant extraction, and mechanical helper function centralization with strict no-breaking-change discipline.
- **Persona Weighting Note:** Infrastructure module architecture and SSOT design dominate over narrow operation type (extraction refactoring).

## Context
[INTENT: KONTEXT]

- **Domain:** Infrastructure formatting module + fixed-string and regex inspection endpoint directories
- **Technical Context:** TypeScript, infrastructure module at `@infrastructure/formatting/unified-diff`. The three callers all already import `createUnifiedDiff` from that module. The fixed-string endpoint has two separate files that independently define the same constant string.
- **Business Context:** Pure SSOT extraction — no public behavior changes.
- **Architectural Context:** Per DDD single-source-of-truth principle, infrastructure helpers belong in infrastructure modules, and endpoint-local constants belong in the schema that names the endpoint.
- **Plan Context Dependencies:** Task 1.14 modified fixed-string/schema.ts and regex/schema.ts; re-anchor both before editing.
- **Runtime Discovery Context:** None.

## Execution Surface Assessment
[INTENT: REFERENZ]

- **Execution Surface Band:** `GREEN`
- **Primary Split Axis:** `none`
- **Hard-Gap Result:** 8 files, no RED/BLACK trigger
- **Single-Agent Viability Rationale:** Each file change is 1-10 lines. All modifications are anchored to one infrastructure module and two schema files with no cross-boundary reasoning.

## Information Units
[INTENT: SPEZIFIKATION]

### IU-001: Extract backtick-fence wrapper to unified-diff infrastructure module

**Type:** `function`

**Description:** Extract the identical 4-line backtick-fence body from 3 callers into `wrapDiffInSafeFencedBlock` in `unified-diff.ts`, then replace each inline block with a single function call.

**Ist-Zustand:** Three files each contain an identical `let numBackticks = 3; while (diff.includes(...)) { numBackticks++; } return \`...\`` block.

**Soll-Zustand:** One function in `unified-diff.ts`; three callers each reduced to `return wrapDiffInSafeFencedBlock(diff);`.

**Exact Target:** `src/infrastructure/formatting/unified-diff.ts` — add after `normalizeLineEndings` or `createUnifiedDiff`.

**Implementation Contract:** Function body is exactly specified in `authoritative_code_surfaces`.

**Checkpoint Relevance:**
- **Checkpoint Strategy:** `SEQUENTIAL_CHECKPOINTS_REQUIRED`
- **Checkpoint or Artifact Impact:** CHK-1.15-A (add export) must precede CHK-1.15-B (update callers)

**Scenario-Specific Specification (FUNCTION_CREATION):**

| Field | Value |
|------|-------|
| Function Name | `wrapDiffInSafeFencedBlock` |
| Signature | `(diff: string): string` |
| Return Type | `string` |
| Inputs | `diff: string` — raw unified diff string |
| Side Effects | None |
| Call Sites | `diff-files/handler.ts:getFormattedFileDiff`, `diff-text-content/handler.ts:getFormattedContentDiff`, `replace-file-line-ranges/helpers.ts:applyFileLineRangeReplacements` |
| Error Behaviour | None — pure string transformation |
| Exact Placement | `src/infrastructure/formatting/unified-diff.ts` after existing exports |
| No-Breaking Constraint | Callers must produce identical string output; only the backtick logic is extracted, not altered |

---

### IU-002: SSOT tool-name constant for fixed-string search endpoint

**Type:** `constant`

**Description:** Export `SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME` from the fixed-string schema and remove both independent local const declarations in the support files.

**Ist-Zustand:** `fixed-string-search-support.ts` and `fixed-string-search-file-entry.ts` each define `const SEARCH_FIXED_STRING_TOOL_NAME = "search_file_contents_by_fixed_string"` independently.

**Soll-Zustand:** One exported constant in `schema.ts`; both files import from `./schema` and use it by the canonical name.

**Checkpoint Relevance:**
- **Checkpoint or Artifact Impact:** CHK-1.15-C

**Scenario-Specific Specification (CONSTANT_ADDITION):**

| Field | Value |
|------|-------|
| Constant Name | `SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME` |
| Exact Literal Value | `"search_file_contents_by_fixed_string"` |
| Unit / Scale / Ceiling Type | MCP tool registration name (exact string match required) |
| Default / Fallback Relation | None |
| Authoritative Source | Plan-output; mirrors READ_FILE_CONTENT_TOOL_NAME precedent |
| Determination Rule | Matches the MCP endpoint registration name verbatim |
| Exact Placement | `src/domain/inspection/search-file-contents-by-fixed-string/schema.ts` — exported const |
| Consumers | `fixed-string-search-support.ts`, `fixed-string-search-file-entry.ts` |
| Boundary / Refusal Behaviour | Not a guardrail value; used only as a tool identifier in guardrail calls |
| No-Breaking Constraint | String value must remain identical; only naming surface changes |

---

### IU-003: SSOT tool-name constant for regex search endpoint

**Type:** `constant`

**Description:** Export `SEARCH_FILE_CONTENTS_BY_REGEX_TOOL_NAME` from the regex schema. The handler currently uses inline string literals or a local const — this export provides the canonical location.

**Soll-Zustand:** `search-file-contents-by-regex/schema.ts` exports `SEARCH_FILE_CONTENTS_BY_REGEX_TOOL_NAME`. Handler imports and uses it.

**Checkpoint Relevance:**
- **Checkpoint or Artifact Impact:** CHK-1.15-C

**Note:** The regex handler file is NOT in files_modified for this task because task 1.16 will already touch the regex handler for R8 (error message extraction). The handler will import the constant during task 1.16 to avoid a shared-file conflict between 1.15 and 1.16. The constant export itself is added here.

**Scenario-Specific Specification (CONSTANT_ADDITION):**

| Field | Value |
|------|-------|
| Constant Name | `SEARCH_FILE_CONTENTS_BY_REGEX_TOOL_NAME` |
| Exact Literal Value | `"search_file_contents_by_regex"` |
| Exact Placement | `src/domain/inspection/search-file-contents-by-regex/schema.ts` — exported const |
| Consumers | `search-file-contents-by-regex/handler.ts` (import handled in task 1.16) |
| No-Breaking Constraint | String value must remain identical |

## Actions
[INTENT: ANWEISUNG]

1. **Re-anchor all target files (ephemeral freshness step)**
   - **What:** Read all 8 files from files_modified plus 1.14 task file
   - **Where:** As listed in Authoritative References
   - **How:** Full read; this does not advance any checkpoint
   - **Checkpoint Impact:** None (ephemeral)
   - **Stop if:** Any file state materially contradicts this plan; inform user

2. **Add wrapDiffInSafeFencedBlock to unified-diff.ts (CHK-1.15-A)**
   - **What:** Export the new helper function
   - **Where:** `src/infrastructure/formatting/unified-diff.ts`
   - **How:** Add the function after existing exports using the exact body from `authoritative_code_surfaces`. Add TSDoc as specified.
   - **Output Contract:** Function is exported; no existing export removed
   - **Checkpoint Impact:** COMPLETES CHK-1.15-A
   - **Verification:** Function signature matches `(diff: string): string`; docstring present
   - **Stop if:** File structure differs materially; inform user

3. **Update 3 callers to use wrapDiffInSafeFencedBlock (CHK-1.15-B)**
   - **What:** Replace inline fence blocks with function calls
   - **Where:** `diff-files/handler.ts`, `diff-text-content/handler.ts`, `replace-file-line-ranges/helpers.ts`
   - **How:** In each file, add `wrapDiffInSafeFencedBlock` to the existing `@infrastructure/formatting/unified-diff` import. Replace the `let numBackticks` + while loop + return block with `return wrapDiffInSafeFencedBlock(diff);`. For `diff-text-content/handler.ts` the function is async so the return is `return wrapDiffInSafeFencedBlock(diff);` (no await needed — pure string op).
   - **Output Contract:** No inline `numBackticks` while loop remains in any of the 3 files; output string is identical
   - **Checkpoint Impact:** COMPLETES CHK-1.15-B
   - **Verification:** Each function that previously had the inline block now calls the helper; diff output format unchanged
   - **Stop if:** Cannot locate the inline block in any of the three files; the file may have already been modified; inform user

4. **Add tool-name constants to fixed-string and regex schema files (CHK-1.15-C)**
   - **What:** Export `SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME` and `SEARCH_FILE_CONTENTS_BY_REGEX_TOOL_NAME`
   - **Where:** Fixed-string schema.ts and regex schema.ts
   - **How:** Add `export const SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME = "search_file_contents_by_fixed_string" as const;` to fixed-string schema (near top, before Zod schemas). Same pattern for regex schema. Then in `fixed-string-search-support.ts` and `fixed-string-search-file-entry.ts`: add `import { SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME } from "./schema";` and remove the local `const SEARCH_FIXED_STRING_TOOL_NAME = ...` declaration. Update all usages of `SEARCH_FIXED_STRING_TOOL_NAME` to `SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME`.
   - **Output Contract:** No local tool-name const remains in support or file-entry files; both schema files export their respective constants
   - **Checkpoint Impact:** COMPLETES CHK-1.15-C when all 4 files updated
   - **Verification:** Exactly one definition per tool-name literal; usages in support files reference the imported constant
   - **Stop if:** The local const names differ from what the plan expects (e.g. the actual local const name in the file was different); inform user

## Checkpoint Definitions / Execution Progress Ledger / Resume Conflict Resolution Rules / Checkpoint Update Protocol
[INTENT: REFERENZ]

*(See YAML frontmatter for complete specifications)*

## Acceptance Criteria
[INTENT: REFERENZ]

- [ ] `wrapDiffInSafeFencedBlock` is exported from `src/infrastructure/formatting/unified-diff.ts` with the exact signature `(diff: string): string`
- [ ] No inline `numBackticks` while loop remains in `diff-files/handler.ts`, `diff-text-content/handler.ts`, or `replace-file-line-ranges/helpers.ts`
- [ ] All 3 callers import `wrapDiffInSafeFencedBlock` from `@infrastructure/formatting/unified-diff`
- [ ] `SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME = "search_file_contents_by_fixed_string"` is exported from `search-file-contents-by-fixed-string/schema.ts`
- [ ] No local `SEARCH_FIXED_STRING_TOOL_NAME` const declaration remains in `fixed-string-search-support.ts` or `fixed-string-search-file-entry.ts`
- [ ] Both support files import the constant from `./schema`
- [ ] `SEARCH_FILE_CONTENTS_BY_REGEX_TOOL_NAME = "search_file_contents_by_regex"` is exported from `search-file-contents-by-regex/schema.ts`
- [ ] All diff outputs and search tool behavior remain functionally identical

## Stop Conditions & User Escalation
[INTENT: CONSTRAINT]

- **Plan-vs-Reality Mismatch:** If a file already has the helper imported or the local const already removed, confirm with user before skipping or re-applying.
- **Missing Required Plan Information:** If the inline pattern in any caller file differs from what the plan describes (e.g., the function is already factored out), stop and inform user.
- **Autonomous Decision Required:** If the local const name in the fixed-string files is NOT `SEARCH_FIXED_STRING_TOOL_NAME` but some other name, stop and inform user before proceeding.
- **User Message Contract:** `Plan says [X state] / current reality is [Y state] / [what specifically differs] / continuing would require an autonomous decision`.

## Post-Completion Instructions
[INTENT: ANWEISUNG]

1. Mark this task file locally `DONE` and close the task-local completion ledger.
2. Mark task 1.15 [x] in parent orchestration: `.plan/1-runtime-architecture-refactors/orchestration.md`
3. Task 1.16 (SEQUENTIAL after 1.15) is now unblocked — confirm it can proceed.
4. Do not advance PLAN.md frontier until all tasks 1.14–1.17 are DONE.

## File Path Index
[INTENT: REFERENZ]

| # | Path | Kind | Why Relevant | Must Read Before Execution |
|---|------|------|--------------|----------------------------|
| 1 | `src/infrastructure/formatting/unified-diff.ts` | business-file | New helper target | true |
| 2 | `src/domain/comparison/diff-files/handler.ts` | business-file | Caller to update | true |
| 3 | `src/domain/comparison/diff-text-content/handler.ts` | business-file | Caller to update | true |
| 4 | `src/domain/mutation/replace-file-line-ranges/helpers.ts` | business-file | Caller to update | true |
| 5 | `src/domain/inspection/search-file-contents-by-fixed-string/schema.ts` | business-file | Receives constant | true |
| 6 | `src/domain/inspection/search-file-contents-by-fixed-string/fixed-string-search-support.ts` | business-file | Local const to remove | true |
| 7 | `src/domain/inspection/search-file-contents-by-fixed-string/fixed-string-search-file-entry.ts` | business-file | Local const to remove | true |
| 8 | `src/domain/inspection/search-file-contents-by-regex/schema.ts` | business-file | Receives constant | true |
| 9 | `.plan/1-runtime-architecture-refactors/1.14-schema-contract-layer-centralization.md` | plan-file | Documents 1.14 schema edits to re-anchor | true |
| 10 | `.plan/1-runtime-architecture-refactors/orchestration.md` | plan-file | Mark task DONE post-completion | false |
| 11 | `PLAN.md` | plan-file | Global frontier — advance after all 1.14-1.17 done | false |

## Notes
[INTENT: KONTEXT]

- The regex handler (`search-file-contents-by-regex/handler.ts`) is deliberately NOT included in this task's `files_modified`. It will import `SEARCH_FILE_CONTENTS_BY_REGEX_TOOL_NAME` in task 1.16 when the error-message utility is also applied there — combining both changes in one handler file touch avoids a shared-file conflict between tasks 1.15 and 1.16 for that specific file.
- The `fixed-string-search-support.ts` file uses the local const name `SEARCH_FIXED_STRING_TOOL_NAME` (without the `SEARCH_FILE_CONTENTS_BY_` prefix). The imported constant from schema will be `SEARCH_FILE_CONTENTS_BY_FIXED_STRING_TOOL_NAME`. Update all usages in both support files accordingly.
- Task 1.15 SEQUENTIAL precondition for 1.16: the shared-file conflict exists on `diff-files/handler.ts` and `diff-text-content/handler.ts`.
