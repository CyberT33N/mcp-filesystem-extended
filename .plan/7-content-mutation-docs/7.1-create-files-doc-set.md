---
file_type: "task"
task_id: "7.1"
task_title: "`create_files` doc set"
unit: "7"
parent_orchestration: ".plan/7-content-mutation-docs/orchestration.md"
classification: "ISOLATED"
 status: "done"
estimated_complexity: "MEDIUM"
files_modified:
  - "src/domain/mutation/create-files/CONVENTIONS.md"
  - "src/domain/mutation/create-files/DESCRIPTION.md"
  - "src/domain/mutation/create-files/README.md"
files_read:
  - "src/domain/mutation/create-files/schema.ts"
  - "src/domain/mutation/create-files/handler.ts"
  - "src/domain/mutation/append-files/schema.ts"
  - "src/domain/mutation/replace-file-line-ranges/schema.ts"
  - "src/application/server/register-comparison-and-mutation-tool-catalog.ts"
  - "README.md"
  - "DESCRIPTION.md"
dependencies: []
blocked_by: "none"
information_completeness_mode: "FULLY_SPECIFIED"
implementation_scenarios:
  - "OBJECT_CREATION"
  - "OTHER"
execution_surface_band: "GREEN"
primary_split_axis: "none"
single_agent_viability_note: "The task is bounded to one endpoint-local documentation triplet for one additive content-mutation endpoint with narrow contrast surfaces to append and replacement behavior."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
decision_surface_register:
  - surface: "naming"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "The endpoint-local triplet must be named `CONVENTIONS.md`, `DESCRIPTION.md`, and `README.md`."
  - surface: "path_and_placement"
    status: "BOUND_LITERAL"
    binding_source: "existing project state"
    notes: "The files live directly in `src/domain/mutation/create-files/`."
  - surface: "literal_values_and_budgets"
    status: "BOUND_REFERENCE"
    binding_source: "current code state"
    notes: "Per-item content caps and cumulative content-bearing mutation budgets must be described from the current schema and handler surfaces."
  - surface: "defaults_and_fallbacks"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "The docs must explain that this endpoint creates only new text files and must not be documented as a general rewrite or append surface."
  - surface: "types_and_shapes"
    status: "BOUND_REFERENCE"
    binding_source: "handler and schema surfaces"
    notes: "The docs must explain the guarded `files[]` batch contract with `path` and full `content` payloads accurately."
  - surface: "function_signatures_and_returns"
    status: "BOUND_REFERENCE"
    binding_source: "authoritative code surfaces"
    notes: "The current additive handler and registration wording remain authoritative."
  - surface: "object_schema_and_mapper_contracts"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "The three local markdown files have non-overlapping SSOT roles."
  - surface: "dependencies_and_import_surfaces"
    status: "BOUND_REFERENCE"
    binding_source: "current mutation and registration surfaces"
    notes: "The docs must reflect the current mutation registration family and the endpoint-local TOC role beneath later root docs."
  - surface: "validation_and_boundary_rules"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "The docs must explain non-existing-target creation, automatic parent-directory creation, and refusal on existing files."
  - surface: "consumer_and_callsite_bindings"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "Root docs will later re-reference this local triplet rather than duplicate it."
authoritative_literal_bindings:
  - binding_domain: "endpoint-local SSOT split"
    exact_binding: "`CONVENTIONS.md` owns endpoint-local conventions, `DESCRIPTION.md` owns endpoint-local architecture description, and `README.md` owns endpoint-local DX summary."
    source: "explicit user requirement"
    placement_or_consumer: "endpoint-local doc triplet"
    no_autonomous_choice_rule: "The execute agent must not collapse these roles into fewer files."
  - binding_domain: "public endpoint role"
    exact_binding: "`create_files` creates only new text files and refuses modification of already existing files."
    source: "current code state and accepted architecture"
    placement_or_consumer: "local conventions and description"
    no_autonomous_choice_rule: "The execute agent must not document this endpoint as append, replacement, or generic overwrite behavior."
  - binding_domain: "contrast boundary"
    exact_binding: "`append_files` owns additive end-of-file writes, and `replace_file_line_ranges` owns targeted existing-file replacement; `create_files` is distinct from both."
    source: "current mutation family architecture"
    placement_or_consumer: "local conventions and description"
    no_autonomous_choice_rule: "The execute agent must not blur the additive-create surface with the append or replacement surfaces."
requires_authoritative_code_surfaces: true
authoritative_code_surfaces:
  - surface_name: "Public registration contract"
    kind: "shape"
    snippet_or_contract: "The tool is registered as `create_files` and described as the new-file creation surface whose oversized payloads are refused rather than truncated."
    source: "current registration surface"
    placement_or_consumer: "`src/application/server/register-comparison-and-mutation-tool-catalog.ts`"
  - surface_name: "Additive creation contract"
    kind: "shape"
    snippet_or_contract: "The endpoint accepts `files[]` entries with `path` and full `content`, creates missing parent directories, and rejects writes when the file already exists."
    source: "current handler and schema"
    placement_or_consumer: "`src/domain/mutation/create-files/schema.ts`, `src/domain/mutation/create-files/handler.ts`"
authoritative_references:
  - path: "src/domain/mutation/create-files/schema.ts"
    kind: "schema-file"
    usage: "Current request contract for additive new-file creation"
    required_before_execution: true
  - path: "src/domain/mutation/create-files/handler.ts"
    kind: "business-file"
    usage: "Current additive new-file creation behavior and refusal semantics"
    required_before_execution: true
  - path: "src/domain/mutation/append-files/schema.ts"
    kind: "schema-file"
    usage: "Contrast surface for additive append semantics"
    required_before_execution: true
  - path: "src/domain/mutation/replace-file-line-ranges/schema.ts"
    kind: "schema-file"
    usage: "Contrast surface for targeted replacement semantics"
    required_before_execution: true
  - path: "src/application/server/register-comparison-and-mutation-tool-catalog.ts"
    kind: "business-file"
    usage: "Current public registration wording for the content-mutation family"
    required_before_execution: true
  - path: "README.md"
    kind: "markdown-doc"
    usage: "Current root DX surface that will later reference the endpoint-local README"
    required_before_execution: true
  - path: "DESCRIPTION.md"
    kind: "markdown-doc"
    usage: "Current root architecture surface that will later reference the endpoint-local description"
    required_before_execution: true
upstream_plan_references: []
runtime_discovery:
  required: false
  scope: []
  allowed_topics: []
checkpoint_strategy: "ARTIFACT_MILESTONES_REQUIRED"
checkpoint_eligibility_contract:
  durable_skip_authority_only: true
  ephemeral_freshness_steps:
    - "Re-read the current create-files contract, content-mutation registration wording, root docs, and nearby contrast schemas"
  durable_resume_checkpoints:
    - "cp-1-context-reanchor"
    - "cp-2-endpoint-conventions"
    - "cp-3-endpoint-description"
    - "cp-4-endpoint-readme"
resume_validation_contract:
  lowest_level_truth_surface: "artifact-evidence"
  task_lifecycle_truth_surface: ".plan/7-content-mutation-docs/orchestration.md"
  global_frontier_truth_surface: "PLAN.md"
  artifact_evidence_sources:
    - "src/domain/mutation/create-files/CONVENTIONS.md"
    - "src/domain/mutation/create-files/DESCRIPTION.md"
    - "src/domain/mutation/create-files/README.md"
  conflict_requires_user_confirmation: true
  partial_progress_requires_user_decision: true
  durable_skip_authority_only: true
  ephemeral_checkpoint_conflict_requires_reexecution: true
artifact_progress_inference_rules:
  - "Treat the task as partially complete only when one or more of the endpoint-local documentation files already exist."
  - "Do not treat code edits outside the endpoint-local triplet as completion evidence for the documentation SSOT."
  - "Ephemeral re-reads never create durable skip authority."
user_resume_decision_gate:
  required_on_partial_progress: true
  required_on_state_conflict: true
  default_user_choices:
    - "ACCEPT_AND_CONTINUE"
    - "REVERIFY_EXISTING_OUTPUTS"
persona_generation_required: true
persona_generation_mode: "runtime_regenerate"
persona_source_prompt_path: "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
persona_primary_domain_kernel: "Enterprise endpoint documentation architecture"
persona_secondary_specializations:
  - "DX-oriented documentation design"
  - "content-bearing mutation contract description"
persona_current_task_specialization: "Additive new-file creation endpoint documentation"
persona_generation_lineage: "Derived from the endpoint-local SSOT documentation rule and the current content-mutation registration plus additive creation contract."
persona_runtime_review_required: true
persona_dx_snapshot:
  title: "Enterprise Endpoint Documentation Architect"
  summary: "Creates endpoint-local SSOT documentation triplets that are precise for agents and concise for developers."
  weighting_note: "The endpoint-local SSOT rule dominates over local wording preferences."
checkpoint_definitions:
  - checkpoint_id: "cp-1-context-reanchor"
    kind: "sequential_step"
    completion_signal: "All authoritative references are re-read."
    produces_artifacts: []
  - checkpoint_id: "cp-2-endpoint-conventions"
    kind: "artifact_milestone"
    completion_signal: "`CONVENTIONS.md` exists and owns endpoint-local create-files policies and guardrails."
    produces_artifacts:
      - "src/domain/mutation/create-files/CONVENTIONS.md"
  - checkpoint_id: "cp-3-endpoint-description"
    kind: "artifact_milestone"
    completion_signal: "`DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents."
    produces_artifacts:
      - "src/domain/mutation/create-files/DESCRIPTION.md"
  - checkpoint_id: "cp-4-endpoint-readme"
    kind: "artifact_milestone"
    completion_signal: "`README.md` exists and provides a concise DX summary with links to the local docs."
    produces_artifacts:
      - "src/domain/mutation/create-files/README.md"
execution_progress_ledger:
  ledger_mode: "CHECKPOINT_LEDGER"
  checkpoints:
    - checkpoint_id: "cp-1-context-reanchor"
      status: "PENDING"
    - checkpoint_id: "cp-2-endpoint-conventions"
      status: "PENDING"
    - checkpoint_id: "cp-3-endpoint-description"
      status: "PENDING"
    - checkpoint_id: "cp-4-endpoint-readme"
      status: "PENDING"
resume_conflict_resolution_rules:
  - "If one or two endpoint-local doc files exist but the triplet is incomplete, stop and ask whether the partial state should be accepted or reverified."
  - "If endpoint-local docs describe overwrite or targeted replacement semantics for `create_files`, stop and escalate."
  - "If endpoint-local docs misclassify existing-file refusal behavior, stop and escalate."
checkpoint_update_protocol:
  - "Mark `cp-1-context-reanchor` DONE only after all authoritative references are re-read."
  - "Mark `cp-2-endpoint-conventions` DONE only after endpoint-local additive new-file policies and guardrails are fully documented."
  - "Mark `cp-3-endpoint-description` DONE only after the endpoint-local architectural description is complete and LLM-optimized."
  - "Mark `cp-4-endpoint-readme` DONE only after the DX summary exists and links the local docs."
halt_conditions:
  plan_reality_mismatch: true
  missing_required_plan_information: true
  autonomous_decision_required: true
user_notification_required: true
---

# Task 7.1: `create_files` doc set
[INTENT: ANWEISUNG]

## Goal
[INTENT: KONTEXT]

Create the endpoint-local documentation triplet for `create_files` so endpoint-specific additive new-file creation conventions, refusal semantics, and DX guidance are owned locally instead of being implied only by code and root docs.

## Expected Result
[INTENT: KONTEXT]

- `src/domain/mutation/create-files/CONVENTIONS.md` exists and owns endpoint-local guardrails and conventions.
- `src/domain/mutation/create-files/DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents.
- `src/domain/mutation/create-files/README.md` exists and provides a concise DX-oriented summary plus links.

## Preconditions
[INTENT: CONSTRAINT]

- No preconditions (ISOLATED task).

## Execution Metadata Register
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Task ID | `7.1` | Unique task identifier |
| Classification | `ISOLATED` | No blocker exists |
| Information Completeness Mode | `FULLY_SPECIFIED` | All determinable doc surfaces are bound |
| Implementation Scenarios | `OBJECT_CREATION`, `OTHER` | Documentation triplet creation |
| Execution Surface Band | `GREEN` | One endpoint-local documentation concern |
| Primary Split Axis | `none` | No further split is required |
| Parent Orchestration | `.plan/7-content-mutation-docs/orchestration.md` | Lifecycle status is tracked there |
| Blocked By | `none` | No blocker |
| Requires Upstream Plan Re-Anchoring | `no` | No upstream plan task must complete first |
| Runtime Discovery Allowed | `no` | This task is fully specified |
| Checkpoint Strategy | `ARTIFACT_MILESTONES_REQUIRED` | The three documentation files are durable outputs |
| Durable Skip Authority Only | `true` | Only durable documentation artifacts create skip authority |
| Ephemeral Freshness Steps Present | `yes` | Re-reading code and contrast surfaces remains non-durable |
| User Notification Required On Gap/Mismatch | `true` | Mismatches must stop execution |
| Persona Generation Required | `true` | Runtime persona regeneration remains mandatory |

## Task Contract Closure
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Decision Surfaces Closed | `true` | All determinable surfaces are bound |
| Unresolved Determinable Decisions | `0` | No deterministic gap remains |
| Expected Execute-Agent Questions | `0` | A fresh execute agent should not need follow-up questions |

## Decision Surface Register
[INTENT: REFERENZ]

| Surface | Status | Binding Source | Notes |
|---------|--------|----------------|-------|
| `naming` | `BOUND_LITERAL` | user requirement | The endpoint-local triplet must be named `CONVENTIONS.md`, `DESCRIPTION.md`, and `README.md`. |
| `path_and_placement` | `BOUND_LITERAL` | existing project state | The files live directly in `src/domain/mutation/create-files/`. |
| `literal_values_and_budgets` | `BOUND_REFERENCE` | current code state | Per-item content caps and cumulative content-bearing mutation budgets must be derived from current code. |
| `defaults_and_fallbacks` | `BOUND_LITERAL` | accepted architecture | This endpoint creates new files only and is not an append or targeted replacement surface. |
| `types_and_shapes` | `BOUND_REFERENCE` | handler and schema surfaces | The docs must explain the guarded `files[]` batch contract with `path` and full `content`. |
| `function_signatures_and_returns` | `BOUND_REFERENCE` | authoritative code surfaces | The current additive handler and registration wording remain authoritative. |
| `object_schema_and_mapper_contracts` | `BOUND_LITERAL` | user requirement | The three local markdown files have non-overlapping SSOT roles. |
| `dependencies_and_import_surfaces` | `BOUND_REFERENCE` | current mutation and registration surfaces | The docs must reflect the current mutation registration family and later root TOC re-reference behavior. |
| `validation_and_boundary_rules` | `BOUND_LITERAL` | accepted architecture | The docs must explain non-existing-target creation, automatic parent-directory creation, and refusal on already existing files. |
| `consumer_and_callsite_bindings` | `BOUND_LITERAL` | user requirement | Root docs will later re-reference this local triplet rather than duplicate it. |

## Authoritative Literal Bindings
[INTENT: REFERENZ]

| Binding Domain | Exact Binding | Source | Placement / Consumer | No Autonomous Choice Rule |
|---------------|---------------|--------|----------------------|---------------------------|
| Endpoint-local SSOT split | `CONVENTIONS.md` owns endpoint-local conventions, `DESCRIPTION.md` owns endpoint-local architecture description, and `README.md` owns endpoint-local DX summary. | explicit user requirement | endpoint-local doc triplet | The execute agent must not collapse these roles into fewer files. |
| Public endpoint role | `create_files` creates only new text files and refuses modification of already existing files. | current code state and accepted architecture | local conventions and description | The execute agent must not document this endpoint as append, replacement, or generic overwrite behavior. |
| Contrast boundary | `append_files` owns additive end-of-file writes, and `replace_file_line_ranges` owns targeted existing-file replacement; `create_files` is distinct from both. | current mutation family architecture | local conventions and description | The execute agent must not blur the additive-create surface with append or replacement semantics. |

## Authoritative Code / Contract Surfaces
[INTENT: REFERENZ]

| Surface Name | Kind | Snippet / Contract | Source | Placement / Consumer |
|-------------|------|--------------------|--------|----------------------|
| Public registration contract | `shape` | The tool is registered as `create_files` and described as the new-file creation surface whose oversized payloads are refused rather than truncated. | current registration surface | `src/application/server/register-comparison-and-mutation-tool-catalog.ts` |
| Additive creation contract | `shape` | The endpoint accepts `files[]` entries with `path` and full `content`, creates missing parent directories, and rejects writes when the file already exists. | current handler and schema | `src/domain/mutation/create-files/schema.ts`, `src/domain/mutation/create-files/handler.ts` |

## Execute-Agent Zero-Question Gate
[INTENT: CONSTRAINT]

- **Gate Result:** `PASS`
- **Checked Domains:** triplet naming, placement, additive new-file scope, refusal-on-existing semantics, and root TOC relationship.
- **Rule:** A fresh execute agent must not need to ask where the files go, what each doc owns, or whether this endpoint creates, appends, or replaces content.

## Authoritative References
[INTENT: REFERENZ]

| Path | Kind | Must Read Before Execution | Purpose | Referenced By |
|------|------|---------------------------|---------|---------------|
| `src/domain/mutation/create-files/schema.ts` | `schema-file` | `true` | current request contract for additive new-file creation | actions 1-4 |
| `src/domain/mutation/create-files/handler.ts` | `business-file` | `true` | current additive new-file creation behavior and refusal semantics | actions 1-4 |
| `src/domain/mutation/append-files/schema.ts` | `schema-file` | `true` | contrast surface for additive append semantics | context |
| `src/domain/mutation/replace-file-line-ranges/schema.ts` | `schema-file` | `true` | contrast surface for targeted replacement semantics | context |
| `src/application/server/register-comparison-and-mutation-tool-catalog.ts` | `business-file` | `true` | current public registration wording for the content-mutation family | actions 1-4 |
| `README.md` | `markdown-doc` | `true` | current root DX surface that will later reference the endpoint-local README | context |
| `DESCRIPTION.md` | `markdown-doc` | `true` | current root architecture surface that will later reference the endpoint-local description | context |

## Resume Validation Contract
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Lowest Level Truth Surface | `artifact-evidence` | The three endpoint-local documentation files are the primary resume truth |
| Task Lifecycle Truth Surface | `.plan/7-content-mutation-docs/orchestration.md` | Parent orchestration owns lifecycle state |
| Global Frontier Truth Surface | `PLAN.md` | Global frontier SSOT |
| Conflict Requires User Confirmation | `true` | Conflicting partial documentation state requires escalation |
| Partial Progress Requires User Decision | `true` | Partial artifact state requires a user choice |
| Durable Skip Authority Only | `true` | Only durable documentation artifacts may authorize skip |
| Ephemeral Freshness Steps Must Re-Execute | `true` | Pure re-anchor work remains re-executable preparation |

## Artifact Progress Inference Rules
[INTENT: REFERENZ]

- Treat the task as partially complete only when one or more of the endpoint-local documentation files already exist.
- Do not treat code edits outside the endpoint-local triplet as completion evidence for the documentation SSOT.
- `EPHEMERAL_FRESHNESS_STEP` re-reads never create durable skip authority.

## User Resume Decision Gate
[INTENT: CONSTRAINT]

- **Required on Partial Progress:** `true`
- **Required on State Conflict:** `true`
- **Allowed User Choices:** `ACCEPT_AND_CONTINUE` / `REVERIFY_EXISTING_OUTPUTS`

## Persona Derivation Manifest
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Persona Generation Required | `true` | task-specific persona derivation required |
| Persona Generation Mode | `runtime_regenerate` | execute regenerates the persona at runtime |
| Persona Source Prompt Path | `src/personas/it/__prompts__/silent/enterprise/creation/prompt.md` | authoritative persona source |
| Persona Primary Domain Kernel | `Enterprise endpoint documentation architecture` | highest-weight persona layer |
| Persona Secondary Specializations | `DX-oriented documentation design`, `content-bearing mutation contract description` | secondary persona layers |
| Persona Current Task Specialization | `additive new-file creation endpoint documentation` | current specialization |
| Persona Generation Lineage | `Derived from the endpoint-local SSOT documentation rule and the current content-mutation registration plus additive creation contract.` | derivation lineage |
| Persona Runtime Review Required | `true` | runtime review is required |

## Persona DX Snapshot
[INTENT: KONTEXT]

- **Persona Title:** `Enterprise Endpoint Documentation Architect`
- **Persona Summary:** Creates endpoint-local SSOT documentation triplets that are precise for agents and concise for developers.
- **Persona Weighting Note:** The endpoint-local SSOT rule dominates over local wording preferences.

## Context
[INTENT: KONTEXT]

- **Domain:** endpoint-local content-mutation documentation
- **Related Files:** additive new-file schema and handler, contrast schemas for append and targeted replacement, current registration, root docs
- **Technical Context:** this endpoint creates new files, creates missing parent directories, and rejects modifications to already existing files
- **Business Context:** agents and developers need endpoint-local guidance that explains why this endpoint is distinct from append and replacement behavior
- **Architectural Context:** endpoint-local SSOT comes first; root TOC integration happens later
- **Plan Context Dependencies:** none
- **Runtime Discovery Context:** none

## Execution Surface Assessment
[INTENT: REFERENZ]

- **Execution Surface Band:** `GREEN`
- **Primary Split Axis:** `none`
- **Hard-Gap Result:** `no RED or BLACK trigger active`
- **Single-Agent Viability Rationale:** The task is limited to one endpoint-local documentation triplet for one additive creation endpoint plus narrow contrast surfaces.

## Information Units
[INTENT: SPEZIFIKATION]

### IU-001: Endpoint-local `CONVENTIONS.md`
**Type:** `object`

**Description:**
Create the conventions file that becomes the local SSOT for additive new-file creation guardrails, refusal semantics, and architectural boundaries.

**Ist-Zustand:**
The endpoint has code and registration surfaces but no endpoint-local conventions file.

**Soll-Zustand:**
`CONVENTIONS.md` explains new-file-only behavior, refusal on existing targets, automatic parent-directory creation, and the endpoint’s separation from append and replacement surfaces.

### IU-002: Endpoint-local `DESCRIPTION.md`
**Type:** `object`

**Description:**
Create the endpoint-local architectural description optimized for LLM-agent comprehension.

**Ist-Zustand:**
Only code and registration surfaces describe the endpoint today.

**Soll-Zustand:**
`DESCRIPTION.md` explains the endpoint contract, additive creation role, refusal semantics, and place inside the mutation family in agent-oriented detail.

### IU-003: Endpoint-local `README.md`
**Type:** `object`

**Description:**
Create the concise DX summary for the endpoint.

**Ist-Zustand:**
There is no endpoint-local DX summary file.

**Soll-Zustand:**
`README.md` summarizes what the endpoint does, links the local conventions and description, and stays concise.

## Actions
[INTENT: ANWEISUNG]

1. **Re-anchor the current create-files contract, registration wording, root docs, and nearby contrast surfaces**
   - **What:** re-read the additive new-file creation schema and handler, current content-mutation registration wording, root docs, and nearby contrast schemas
   - **Where:** authoritative references listed above
   - **How:** read them completely before drafting any endpoint-local doc content
   - **Why:** the docs must reflect the current code truth and explain clear separation from append and replacement behavior
   - **Inputs / References:** all authoritative references in this task file
   - **Output Contract:** current endpoint context is active
   - **Checkpoint Impact:** completes `cp-1-context-reanchor`
   - **Verification:** all required references are re-read successfully
   - **Stop if:** any required reference is unreadable
2. **Create endpoint-local `CONVENTIONS.md`**
   - **What:** create the conventions file for this endpoint
   - **Where:** `src/domain/mutation/create-files/CONVENTIONS.md`
   - **How:** document additive new-file creation guardrails, refusal on existing targets, automatic parent-directory creation, and separation from append and replacement semantics
   - **Why:** endpoint-specific policy detail must have a local SSOT
   - **Inputs / References:** current creation code, registration text, and nearby contrast surfaces
   - **Output Contract:** the endpoint-local conventions SSOT exists
   - **Checkpoint Impact:** completes `cp-2-endpoint-conventions`
   - **Verification:** the file explains endpoint-local policies and does not duplicate root TOC content as its primary structure
   - **Stop if:** conventions would need to invent runtime behavior not implemented in code
3. **Create endpoint-local `DESCRIPTION.md`**
   - **What:** create the endpoint-local description file
   - **Where:** `src/domain/mutation/create-files/DESCRIPTION.md`
   - **How:** describe the endpoint contract, additive creation model, refusal behavior, and mutation-family role in LLM-oriented detail
   - **Why:** agents need a dedicated endpoint description surface beyond raw code and registration text
   - **Inputs / References:** current creation code, registration, conventions, and contrast surfaces
   - **Output Contract:** the endpoint-local architectural description exists
   - **Checkpoint Impact:** completes `cp-3-endpoint-description`
   - **Verification:** the file is detailed, LLM-oriented, and aligned to the current code surface
   - **Stop if:** the description would claim behavior not present in the handler/schema surfaces
4. **Create endpoint-local `README.md`**
   - **What:** create the concise DX summary file
   - **Where:** `src/domain/mutation/create-files/README.md`
   - **How:** summarize the endpoint purpose, key conventions, and links to the local `CONVENTIONS.md` and `DESCRIPTION.md`
   - **Why:** developers need a concise entry surface with quick orientation
   - **Inputs / References:** current endpoint code plus the newly created local docs
   - **Output Contract:** the endpoint-local DX summary exists
   - **Checkpoint Impact:** completes `cp-4-endpoint-readme`
   - **Verification:** the README stays concise and points to the local SSOT surfaces
   - **Stop if:** the README would need to duplicate the full conventions or description content

## Checkpoint Definitions
[INTENT: REFERENZ]

| Checkpoint ID | Kind | Completion Signal | Produces Artifacts | Resume Meaning |
|---------------|------|-------------------|--------------------|----------------|
| `cp-1-context-reanchor` | `sequential_step` | All authoritative references are re-read | none | drafting may begin |
| `cp-2-endpoint-conventions` | `artifact_milestone` | `CONVENTIONS.md` exists and owns endpoint-local create-files policies and guardrails | `src/domain/mutation/create-files/CONVENTIONS.md` | conventions SSOT is complete |
| `cp-3-endpoint-description` | `artifact_milestone` | `DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents | `src/domain/mutation/create-files/DESCRIPTION.md` | architectural description is complete |
| `cp-4-endpoint-readme` | `artifact_milestone` | `README.md` exists and provides a concise DX summary with links | `src/domain/mutation/create-files/README.md` | endpoint doc triplet is complete |

## Execution Progress Ledger
[INTENT: REFERENZ]

| Checkpoint ID | Status | Evidence | Notes |
|---------------|--------|----------|-------|
| `cp-1-context-reanchor` | `PENDING` | authoritative references re-read | start here |
| `cp-2-endpoint-conventions` | `PENDING` | endpoint-local `CONVENTIONS.md` | depends on `cp-1-context-reanchor` |
| `cp-3-endpoint-description` | `PENDING` | endpoint-local `DESCRIPTION.md` | depends on `cp-2-endpoint-conventions` |
| `cp-4-endpoint-readme` | `PENDING` | endpoint-local `README.md` | depends on `cp-3-endpoint-description` |

## Resume Conflict Resolution Rules
[INTENT: CONSTRAINT]

- If one or two endpoint-local doc files exist but the triplet is incomplete, stop and ask whether the partial state should be accepted or reverified.
- If endpoint-local docs describe overwrite, append, or targeted replacement semantics for `create_files`, stop and escalate.
- If endpoint-local docs omit the existing-target refusal rule, stop and escalate.

## Checkpoint Update Protocol
[INTENT: ANWEISUNG]

1. Mark `cp-1-context-reanchor` DONE only after all authoritative references are re-read.
2. Mark `cp-2-endpoint-conventions` DONE only after endpoint-local additive new-file policies and guardrails are fully documented.
3. Mark `cp-3-endpoint-description` DONE only after the endpoint-local architectural description is complete and LLM-optimized.
4. Mark `cp-4-endpoint-readme` DONE only after the DX summary exists and links the local docs.

## Acceptance Criteria
[INTENT: REFERENZ]

- [ ] `src/domain/mutation/create-files/CONVENTIONS.md` exists and is the endpoint-local SSOT for conventions and guardrails.
- [ ] `src/domain/mutation/create-files/DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents in detail.
- [ ] `src/domain/mutation/create-files/README.md` exists and provides a concise DX summary with links to the local docs.
- [ ] The documentation reflects additive new-file semantics, refusal on existing targets, and separation from append and targeted replacement accurately.
- [ ] The endpoint-local docs are ready to be referenced later from root-level TOC surfaces.

## Stop Conditions & User Escalation
[INTENT: CONSTRAINT]

- **Plan-vs-Reality Mismatch:** Stop if the current endpoint behavior differs materially from the assumptions documented here.
- **Missing Required Plan Information:** Stop if additive new-file semantics, refusal behavior, or doc-role split surfaces cannot be bound exactly.
- **Autonomous Decision Required:** Stop if execution would require inventing endpoint behavior, mutation-family semantics, or root-TOC behavior not already agreed.
- **Decision Surface Not Closed:** If any determinable documentation-ownership or runtime-wording surface remains open, stop and inform the user.
- **Allowed Runtime Discovery:** None.
- **User Message Contract:** `Plan says X / current reality is Y / missing or conflicting information is Z / continuing would require an autonomous decision.`

## Post-Completion Instructions
[INTENT: ANWEISUNG]

1. Mark this task `[x]` in [`.plan/7-content-mutation-docs/orchestration.md`](./orchestration.md).
2. Keep the resulting doc triplet ready for later root-level TOC linking.
3. Re-anchor [`PLAN.md`](../../PLAN.md).

## File Path Index
[INTENT: REFERENZ]

| # | Path | Kind | Why Relevant | Must Read Before Execution |
|---|------|------|--------------|----------------------------|
| 1 | `src/domain/mutation/create-files/schema.ts` | `schema-file` | current request contract for additive new-file creation | `true` |
| 2 | `src/domain/mutation/create-files/handler.ts` | `business-file` | current additive new-file creation behavior and refusal semantics | `true` |
| 3 | `src/domain/mutation/append-files/schema.ts` | `schema-file` | contrast surface for additive append semantics | `true` |
| 4 | `src/domain/mutation/replace-file-line-ranges/schema.ts` | `schema-file` | contrast surface for targeted replacement semantics | `true` |
| 5 | `src/application/server/register-comparison-and-mutation-tool-catalog.ts` | `business-file` | current public registration wording for the content-mutation family | `true` |
| 6 | `README.md` | `markdown-doc` | current root DX surface that will later reference the endpoint-local README | `true` |
| 7 | `DESCRIPTION.md` | `markdown-doc` | current root architecture surface that will later reference the endpoint-local description | `true` |

## Notes
[INTENT: KONTEXT]

- This task creates endpoint-local documentation only; root TOC integration happens later.
- The local docs must preserve the distinction between additive new-file creation, append-at-end behavior, and targeted line-range replacement.
