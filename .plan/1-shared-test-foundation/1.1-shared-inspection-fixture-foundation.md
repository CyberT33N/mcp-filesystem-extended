---
file_type: "task"
task_id: "1.1"
task_title: "Shared inspection fixture foundation"
unit: "1"
parent_orchestration: ".plan/1-shared-test-foundation/orchestration.md"
classification: "ISOLATED"
status: "pending"
estimated_complexity: "MEDIUM"
files_modified:
  - "test/shared/utils/inspection/search-fixture-registry.ts"
  - "test/shared/utils/inspection/search-fixture-loader.ts"
  - "test/shared/utils/inspection/search-result-assertions.ts"
files_read:
  - "test/fixtures/search-explicit-file-scope-contract/patient-explicit-file-scope.csv"
  - "test/unit/domain/inspection/search-explicit-file-scope-contract.test.ts"
  - "test/unit/domain/inspection/search-file-contents-by-fixed-string/search-file-contents-by-fixed-string.test.ts"
  - "test/unit/domain/inspection/search-file-contents-by-regex/search-file-contents-by-regex.test.ts"
dependencies: []
blocked_by: "none"
information_completeness_mode: "PARTIALLY_DISCOVERED"
implementation_scenarios:
  - "OBJECT_CREATION"
  - "OTHER"
execution_surface_band: "GREEN"
primary_split_axis: "artifact_family"
single_agent_viability_note: "The task is bounded to shared test-support creation for one inspection search fixture family and does not require production-file mutation."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
---

# Task 1.1: Shared inspection fixture foundation

## Goal
Create one shared inspection-search fixture foundation so later search endpoint tests stop defining reusable fixture data and reusable structured-response assertions inline.

## Expected Result
- A shared fixture registry, loader, and assertion helper exist under `test/shared/utils/inspection/**`.
- Later search-family tests can consume common fixture inputs without creating duplicate inline fixture payloads.

## Preconditions
- No blocking prerequisite.

## Execution Metadata Register
| Field | Value | Meaning |
|------|-------|---------|
| Task ID | `1.1` | Shared test-support foundation task |
| Classification | `ISOLATED` | No prerequisite task must complete first |
| Information Completeness Mode | `PARTIALLY_DISCOVERED` | Exact helper signatures may be refined only inside the named test-support files |
| Execution Surface Band | `GREEN` | Narrow shared helper surface with no production-file edits |

## Decision Surface Register
- `naming`: `BOUND_LITERAL` → use the exact helper filenames declared in `files_modified`.
- `path_and_placement`: `BOUND_LITERAL` → all new helpers must live under `test/shared/utils/inspection/**`.
- `literal_values_and_budgets`: `BOUND_REFERENCE` → fixture content must be derived from existing CSV and search test surfaces, not invented globally.
- `types_and_shapes`: `BOUNDED_RUNTIME_DISCOVERY` → only inside the named helper files.
- `validation_and_boundary_rules`: `BOUND_REFERENCE` → preserve existing explicit-file-scope contract behavior.

## Authoritative Literal Bindings
| Binding Domain | Exact Binding | Source | Placement / Consumer |
|---------------|---------------|--------|----------------------|
| Shared registry path | `test/shared/utils/inspection/search-fixture-registry.ts` | This plan | Fixed-string and regex endpoint tests |
| Shared loader path | `test/shared/utils/inspection/search-fixture-loader.ts` | This plan | Search-family endpoint tests |
| Shared assertion path | `test/shared/utils/inspection/search-result-assertions.ts` | This plan | Structured search result assertions |

## Execute-Agent Zero-Question Gate
- **Gate Result:** PASS
- **Rule:** The execute agent must not choose different shared fixture-helper locations or recreate equivalent inline shared search fixtures elsewhere.

## Authoritative References
| Path | Purpose |
|------|---------|
| `test/fixtures/search-explicit-file-scope-contract/patient-explicit-file-scope.csv` | Existing shared search sample anchor |
| `test/unit/domain/inspection/search-explicit-file-scope-contract.test.ts` | Current explicit file-scope contract usage |
| `test/unit/domain/inspection/search-file-contents-by-fixed-string/search-file-contents-by-fixed-string.test.ts` | Existing fixed-string endpoint test style |
| `test/unit/domain/inspection/search-file-contents-by-regex/search-file-contents-by-regex.test.ts` | Existing regex endpoint test style |

## Runtime Discovery
- **Required:** true
- **Scope:** `test/shared/utils/inspection/**`
- **Allowed Topics:** minimal helper signatures, minimal reusable assertion utilities, minimal registry metadata structure.

## Persona Derivation and Review Contract
- **Persona Primary Domain Kernel:** Enterprise Vitest unit-test architecture for MCP inspection surfaces.
- **Current Task Specialization:** Shared fixture and assertion SSOT design for search-family tests.
- **Creator Proof Rule:** The later execute agent must regenerate the task persona at runtime and may not treat this summary as a full-persona substitute.
- **Source Load Policy:** Persona source loading remains deferred until the final pre-execution state snapshot is closed.

## Actions
1. Read the existing explicit file-scope contract test and the two existing search endpoint tests to extract common fixture-access and result-assertion patterns.
2. Create `search-fixture-registry.ts` with one canonical registry for reusable inspection search fixtures.
3. Create `search-fixture-loader.ts` that resolves fixture paths from the registry instead of duplicating inline path construction.
4. Create `search-result-assertions.ts` for common structured result expectations shared by the fixed-string and regex search families.
5. Keep the explicit CSV fixture as the first canonical registry-backed sample instead of moving it out of `test/fixtures/**`.

## Acceptance Criteria
- [ ] Shared inspection search fixture helpers exist at the exact planned paths.
- [ ] No later search-family task needs to invent an alternative shared helper location.
- [ ] The explicit file-scope CSV fixture remains reusable through the shared registry/loader model.

## Stop Conditions & User Escalation
- Stop if the existing search tests already depend on a different shared helper contract that would be broken by the planned helper names.
- Stop if implementing the registry would require changing production code.

## Post-Completion Instructions
1. Mark task `1.1` done in `.plan/1-shared-test-foundation/orchestration.md`.
2. Unblock tasks `4.2.1` and `4.2.2` in `PLAN.md`.

## File Path Index
| # | Path | Kind | Why Relevant |
|---|------|------|--------------|
| 1 | `test/shared/utils/inspection/search-fixture-registry.ts` | New shared test utility | Canonical fixture registry |
| 2 | `test/shared/utils/inspection/search-fixture-loader.ts` | New shared test utility | Canonical fixture loader |
| 3 | `test/shared/utils/inspection/search-result-assertions.ts` | New shared test utility | Canonical shared assertions |
| 4 | `test/fixtures/search-explicit-file-scope-contract/patient-explicit-file-scope.csv` | Existing fixture | Seed shared search fixture |

