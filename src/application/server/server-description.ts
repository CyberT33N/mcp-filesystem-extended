/**
 * Stable server-level description exposed during MCP initialization.
 */
export const SERVER_DESCRIPTION =
  "Extended local filesystem MCP server with explicit safety caps for multi-path operations, search endpoints, and line-based reading, plus structured directory listing and checksum utilities.";
