---
file_type: "task"
task_id: "2.3"
task_title: "Add the fixed-string search endpoint and register it"
unit: "2"
parent_orchestration: ".plan/2-search-platform/orchestration.md"
classification: "SEQUENTIAL"
status: "pending"
estimated_complexity: "HIGH"
files_modified:
  - "src/domain/inspection/search-file-contents-by-fixed-string/schema.ts"
  - "src/domain/inspection/search-file-contents-by-fixed-string/handler.ts"
  - "src/application/server/register-inspection-tool-catalog.ts"
dependencies:
  - task_id: "2.2"
    type: "SEQUENTIAL"
    status: "UNRESOLVED"
blocked_by: "2.2"
files_read:
  - ".plan/2-search-platform/2.1-ugrep-adapter-and-classification-surfaces.md"
  - ".plan/2-search-platform/2.2-refactor-regex-endpoint-to-native-search.md"
  - "src/application/server/register-inspection-tool-catalog.ts"
  - "src/domain/inspection/search-file-contents-by-regex/schema.ts"
  - "src/domain/shared/guardrails/tool-guardrail-limits.ts"
  - "src/domain/shared/guardrails/text-response-budget.ts"
information_completeness_mode: "FULLY_SPECIFIED"
implementation_scenarios:
  - "FUNCTION_CREATION"
  - "SCHEMA_CHANGE"
execution_surface_band: "GREEN"
primary_split_axis: "none"
single_agent_viability_note: "The task is bounded to one new endpoint family with three concrete file surfaces and reuses the shared native search backend from prior tasks."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
decision_surface_register:
  - surface: "naming"
    status: "BOUND_LITERAL"
    binding_source: "architecture-decision"
    notes: "Use the exact public endpoint name `search_file_contents_by_fixed_string`."
  - surface: "path_and_placement"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "Add the new endpoint family under `src/domain/inspection/search-file-contents-by-fixed-string` and register it in the inspection tool catalog only."
  - surface: "literal_values_and_budgets"
    status: "BOUND_REFERENCE"
    binding_source: "shared-guardrail-ssot"
    notes: "Use the shared guardrail registry and the agreed fixed-string thresholds instead of inventing separate local caps."
  - surface: "defaults_and_fallbacks"
    status: "BOUND_LITERAL"
    binding_source: "architecture-decision"
    notes: "Fixed-string search is the preferred large-file path for literal search and may use more generous scan thresholds than regex while still preserving preview-first and task escalation."
  - surface: "types_and_shapes"
    status: "BOUND_LITERAL"
    binding_source: "task-contract"
    notes: "The request surface must mirror the regex endpoint structure where reasonable, but the search input must be an exact fixed-string field rather than a free regex."
  - surface: "function_signatures_and_returns"
    status: "BOUND_REFERENCE"
    binding_source: "authoritative-code-surface"
    notes: "Create and export a human-readable handler entrypoint and a structured-result entrypoint analogous to the regex family."
  - surface: "object_schema_and_mapper_contracts"
    status: "BOUND_LITERAL"
    binding_source: "task-contract"
    notes: "The result contract must expose preview/truncation metadata consistent with the agreed search-platform model."
  - surface: "dependencies_and_import_surfaces"
    status: "BOUND_REFERENCE"
    binding_source: "upstream-task-contracts"
    notes: "Consume the shared `ugrep` adapter and the runtime execution policy registry."
  - surface: "validation_and_boundary_rules"
    status: "BOUND_LITERAL"
    binding_source: "architecture-decision"
    notes: "Preserve path validation, traversal policy, text/binary classification, response budgeting, and the global response fuse."
  - surface: "consumer_and_callsite_bindings"
    status: "BOUND_LITERAL"
    binding_source: "existing-project-state"
    notes: "Register the new endpoint in the inspection tool catalog with an explicit description that it is the literal/fixed-string variant."
authoritative_literal_bindings:
  - binding_domain: "public endpoint name"
    exact_binding: "`search_file_contents_by_fixed_string`"
    source: "agreed architecture"
    placement_or_consumer: "Schema, handler, and inspection tool registration"
    no_autonomous_choice_rule: "Do not use `grep`, `search_grep`, or any alternate public name."
  - binding_domain: "search backend"
    exact_binding: "Use the shared `ugrep` adapter from task `2.1` as the only primary backend."
    source: "agreed architecture"
    placement_or_consumer: "Handler runtime path"
    no_autonomous_choice_rule: "Do not introduce a second primary fixed-string engine."
  - binding_domain: "input semantics"
    exact_binding: "The search input is an exact fixed-string field; do not accept free regex syntax on this endpoint."
    source: "agreed architecture"
    placement_or_consumer: "Schema and handler"
    no_autonomous_choice_rule: "This endpoint is semantically distinct from the regex endpoint."
  - binding_domain: "response behavior"
    exact_binding: "Use preview-first and task-escalation semantics for large but valid workloads, with more generous scan presets than the regex endpoint."
    source: "agreed architecture"
    placement_or_consumer: "Handler and later registration text"
    no_autonomous_choice_rule: "Do not downgrade large valid workloads to blanket hard refusal unless a service hard gap or unsupported surface is hit."
requires_authoritative_code_surfaces: true
authoritative_code_surfaces:
  - surface_name: "Fixed-string request contract"
    kind: "shape"
    snippet_or_contract: "Request fields should stay parallel to the regex family where possible, but use an explicit fixed-string input field and keep path/glob/traversal fields consistent with the search family."
    source: "task contract"
    placement_or_consumer: "`src/domain/inspection/search-file-contents-by-fixed-string/schema.ts`"
  - surface_name: "Fixed-string result contract"
    kind: "shape"
    snippet_or_contract: "Expose per-root results, total locations, total matches, and truncation metadata aligned with the shared search-platform semantics."
    source: "task contract"
    placement_or_consumer: "Schema and handler"
  - surface_name: "Registration contract"
    kind: "signature"
    snippet_or_contract: "Register the endpoint in `registerInspectionToolCatalog()` only; do not move registration into a new catalog file."
    source: "existing-project-state"
    placement_or_consumer: "`src/application/server/register-inspection-tool-catalog.ts`"
authoritative_references:
  - path: ".plan/2-search-platform/2.1-ugrep-adapter-and-classification-surfaces.md"
    kind: "plan-file"
    usage: "Provides the native adapter and classification contract."
    required_before_execution: true
  - path: ".plan/2-search-platform/2.2-refactor-regex-endpoint-to-native-search.md"
    kind: "plan-file"
    usage: "Provides the preserved search-family semantics the new endpoint should align with."
    required_before_execution: true
  - path: "src/application/server/register-inspection-tool-catalog.ts"
    kind: "business-file"
    usage: "Current inspection registration surface."
    required_before_execution: true
  - path: "src/domain/inspection/search-file-contents-by-regex/schema.ts"
    kind: "schema-file"
    usage: "Reference for the shared search-family contract shape."
    required_before_execution: true
  - path: "src/domain/shared/guardrails/tool-guardrail-limits.ts"
    kind: "schema-file"
    usage: "Shared caps and limit registry."
    required_before_execution: true
  - path: "src/domain/shared/guardrails/text-response-budget.ts"
    kind: "schema-file"
    usage: "Shared response-budget utilities."
    required_before_execution: true
upstream_plan_references:
  - task_id: "2.1"
    path: ".plan/2-search-platform/2.1-ugrep-adapter-and-classification-surfaces.md"
    must_read_before_execution: true
    reason: "The new endpoint must be implemented on the shared `ugrep` adapter and classifier surfaces."
  - task_id: "2.2"
    path: ".plan/2-search-platform/2.2-refactor-regex-endpoint-to-native-search.md"
    must_read_before_execution: true
    reason: "The new endpoint should align with the finalized search-family semantics established by the regex refactor."
runtime_discovery:
  required: false
  scope: []
  allowed_topics: []
checkpoint_strategy: "ARTIFACT_MILESTONES_REQUIRED"
resume_validation_contract:
  lowest_level_truth_surface: "artifact-evidence"
  task_lifecycle_truth_surface: ".plan/2-search-platform/orchestration.md"
  global_frontier_truth_surface: "PLAN.md"
  artifact_evidence_sources:
    - "src/domain/inspection/search-file-contents-by-fixed-string/schema.ts"
    - "src/domain/inspection/search-file-contents-by-fixed-string/handler.ts"
    - "src/application/server/register-inspection-tool-catalog.ts"
  conflict_requires_user_confirmation: true
  partial_progress_requires_user_decision: true
artifact_progress_inference_rules:
  - "Treat the task as partially complete if any subset of the two new endpoint files and the registration surface exist or are changed."
  - "Completion requires both new endpoint files and the matching registration update."
user_resume_decision_gate:
  required_on_partial_progress: true
  required_on_state_conflict: true
  default_user_choices:
    - "ACCEPT_AND_CONTINUE"
    - "REVERIFY_EXISTING_OUTPUTS"
halt_conditions:
  plan_reality_mismatch: true
  missing_required_plan_information: true
  autonomous_decision_required: true
user_notification_required: true
---

# Task 2.3: Add the fixed-string search endpoint and register it
[INTENT: ANWEISUNG]

## Goal
[INTENT: KONTEXT]

Add the new public fixed-string search endpoint as the literal-search counterpart to the preserved regex endpoint.

The endpoint must use the same search backend and runtime-policy vocabulary but expose a literal-search contract that is clearer and safer for LLM agents than a shell-specific `grep` name.

## Expected Result
[INTENT: KONTEXT]

- A new public endpoint named `search_file_contents_by_fixed_string` exists.
- The endpoint uses the shared `ugrep` adapter and the shared runtime policy registry.
- The endpoint is registered in the inspection tool catalog and described as the literal/fixed-string search surface.

## Preconditions
[INTENT: CONSTRAINT]

- Task `2.2` must be completed.

## Execution Metadata Register
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Task ID | `2.3` | Unique task identifier |
| Classification | `SEQUENTIAL` | The new endpoint follows the finalized search-family semantics from task `2.2` |
| Information Completeness Mode | `FULLY_SPECIFIED` | All determinable surfaces are bound |
| Implementation Scenarios | `FUNCTION_CREATION`, `SCHEMA_CHANGE` | New endpoint files and one registration update |
| Execution Surface Band | `GREEN` | Three file surfaces inside one endpoint concern |
| Primary Split Axis | `none` | No further split is needed |
| Parent Orchestration | `.plan/2-search-platform/orchestration.md` | Lifecycle authority |
| Blocked By | `2.2` | Sequential dependency |
| Requires Upstream Plan Re-Anchoring | `yes` | Tasks `2.1` and `2.2` must be re-read |
| Runtime Discovery Allowed | `no` | Fully specified |
| Checkpoint Strategy | `ARTIFACT_MILESTONES_REQUIRED` | Three output artifacts |
| User Notification Required On Gap/Mismatch | `true` | Stop on any contract mismatch |

## Task Contract Closure
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Decision Surfaces Closed | `true` | All determinable surfaces are bound |
| Unresolved Determinable Decisions | `0` | No deterministic gap remains |
| Expected Execute-Agent Questions | `0` | The task is execution-ready without clarification |

## Decision Surface Register
[INTENT: REFERENZ]

| Surface | Status | Binding Source | Notes |
|---------|--------|----------------|-------|
| `naming` | `BOUND_LITERAL` | Architecture decision | Use `search_file_contents_by_fixed_string` exactly. |
| `path_and_placement` | `BOUND_LITERAL` | Plan output | New endpoint family goes under `src/domain/inspection/search-file-contents-by-fixed-string`. |
| `literal_values_and_budgets` | `BOUND_REFERENCE` | Shared guardrail + runtime-policy SSOT | Use the shared family caps and the agreed fixed-string presets. |
| `defaults_and_fallbacks` | `BOUND_LITERAL` | Architecture decision | Preview-first and task escalation remain available for large valid workloads. |
| `types_and_shapes` | `BOUND_LITERAL` | Task contract | Keep the search-family contract style consistent while making the search input literal-only. |
| `function_signatures_and_returns` | `BOUND_REFERENCE` | Authoritative code surfaces | Create explicit text and structured result entrypoints. |
| `object_schema_and_mapper_contracts` | `BOUND_LITERAL` | Task contract | Result semantics align with the search-family model. |
| `dependencies_and_import_surfaces` | `BOUND_REFERENCE` | Upstream task contracts | Reuse the shared adapter and runtime policy registry. |
| `validation_and_boundary_rules` | `BOUND_LITERAL` | Architecture decision | Preserve path validation, traversal policy, text/binary classification, and response budgeting. |
| `consumer_and_callsite_bindings` | `BOUND_LITERAL` | Existing project state | Registration stays in the inspection catalog. |

## Authoritative Literal Bindings
[INTENT: REFERENZ]

| Binding Domain | Exact Binding | Source | Placement / Consumer | No Autonomous Choice Rule |
|---------------|---------------|--------|----------------------|---------------------------|
| Public endpoint name | `search_file_contents_by_fixed_string` | Agreed architecture | Schema, handler, registration | Do not replace with `grep`-style public naming. |
| Input semantics | Exact literal search only | Agreed architecture | Request schema and handler | Do not accept regex syntax in this endpoint. |
| Search backend | Shared `ugrep` adapter | Agreed architecture | Handler runtime path | Do not introduce a second backend. |
| Search-family alignment | Preview-first, task escalation, and aggregate budgets stay aligned with the shared search model | Agreed architecture | Handler and registration description | Do not invent endpoint-local semantics. |

## Authoritative Code / Contract Surfaces
[INTENT: REFERENZ]

| Surface Name | Kind | Snippet / Contract | Source | Placement / Consumer |
|-------------|------|--------------------|--------|----------------------|
| Request contract | `shape` | Use file/directory roots plus fixed-string input, include/exclude globs, includeExcludedGlobs, respectGitIgnore, maxResults, and caseSensitive when still semantically valid. | Task contract | Fixed-string schema |
| Result contract | `shape` | Expose per-root locations, total matches, and truncation metadata consistent with the search-family result surface. | Task contract | Fixed-string schema and handler |
| Registration surface | `signature` | Register in `registerInspectionToolCatalog()` with a clear fixed-string description and structured output. | Existing project state | Inspection tool catalog |

## Execute-Agent Zero-Question Gate
[INTENT: CONSTRAINT]

- **Gate Result:** `PASS`
- **Checked Domains:** endpoint naming, literal input semantics, backend choice, result shape, and registration placement
- **Rule:** A fresh execute agent must not need to ask whether this is a grep endpoint, whether regex syntax is allowed, or where the endpoint is registered.

## Authoritative References
[INTENT: REFERENZ]

| Path | Kind | Must Read Before Execution | Purpose | Referenced By |
|------|------|---------------------------|---------|---------------|
| `.plan/2-search-platform/2.1-ugrep-adapter-and-classification-surfaces.md` | `plan-file` | `true` | Shared adapter and classifier contract | Context, actions 1-3 |
| `.plan/2-search-platform/2.2-refactor-regex-endpoint-to-native-search.md` | `plan-file` | `true` | Finalized search-family semantics | Context, actions 1-3 |
| `src/application/server/register-inspection-tool-catalog.ts` | `business-file` | `true` | Inspection registration surface | Action 3 |
| `src/domain/inspection/search-file-contents-by-regex/schema.ts` | `schema-file` | `true` | Reference search-family contract shape | Action 2 |
| `src/domain/shared/guardrails/tool-guardrail-limits.ts` | `schema-file` | `true` | Shared caps and limit registry | Action 2 |
| `src/domain/shared/guardrails/text-response-budget.ts` | `schema-file` | `true` | Shared response-budget utilities | Action 2 |

## Resume Validation Contract
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Lowest Level Truth Surface | `artifact-evidence` | The two new endpoint files and the registration update are the task-local truth |
| Task Lifecycle Truth Surface | `.plan/2-search-platform/orchestration.md` | Parent orchestration controls lifecycle |
| Global Frontier Truth Surface | `PLAN.md` | Root frontier authority |
| Conflict Requires User Confirmation | `true` | Partial endpoint creation requires confirmation |
| Partial Progress Requires User Decision | `true` | Partial registration or file creation requires confirmation |

## Artifact Progress Inference Rules
[INTENT: REFERENZ]

- Treat the task as partially complete if one or two of the three output surfaces exist.
- Treat the task as complete only when both endpoint files and the registration update exist and are aligned.

## User Resume Decision Gate
[INTENT: CONSTRAINT]

- **Required on Partial Progress:** `true`
- **Required on State Conflict:** `true`
- **Allowed User Choices:** `ACCEPT_AND_CONTINUE` / `REVERIFY_EXISTING_OUTPUTS`

## Context
[INTENT: KONTEXT]

- **Domain:** Fixed-string / grep-style content search
- **Related Files:** New fixed-string endpoint files plus the inspection registration file
- **Technical Context:** This endpoint shares the native search backend and runtime policy registry with the regex endpoint
- **Business Context:** Agents need a literal-search endpoint that is semantically clearer and safer than overloading the regex endpoint or using shell terminology as the public contract
- **Architectural Context:** The endpoint adds one new public tool while preserving the existing regex endpoint separately
- **Plan Context Dependencies:** Read tasks `2.1` and `2.2` before execution
- **Runtime Discovery Context:** None

## Execution Surface Assessment
[INTENT: REFERENZ]

- **Execution Surface Band:** `GREEN`
- **Primary Split Axis:** `none`
- **Hard-Gap Result:** `no RED or BLACK trigger active`
- **Single-Agent Viability Rationale:** The task is bounded to one new endpoint family and one registration file.

## Information Units
[INTENT: SPEZIFIKATION]

### IU-001: New fixed-string endpoint name and purpose
**Type:** `constraint`

**Description:**
The public endpoint must be named `search_file_contents_by_fixed_string` and must represent literal/fixed-string search, not free regex search.

**Ist-Zustand:**
No fixed-string endpoint exists.

**Soll-Zustand:**
The new fixed-string endpoint exists and is registered.

**Architectural Rationale:**
The agent-facing contract needs a semantically clearer literal-search surface than `grep`-style naming.

### IU-002: Shared backend and shared policy
**Type:** `function`

**Description:**
The new endpoint must use the shared `ugrep` adapter and shared runtime policy registry, not a new bespoke backend.

**Ist-Zustand:**
The shared adapter exists only after earlier tasks.

**Soll-Zustand:**
The new endpoint consumes that shared adapter and shared policy registry.

**Architectural Rationale:**
Search-domain SSOT requires one primary backend across public search endpoints.

### IU-003: Contract-aligned result surface
**Type:** `schema`

**Description:**
The new endpoint must return per-root results and truncation metadata consistent with the search-family result model.

**Ist-Zustand:**
Only the regex family defines that shape today.

**Soll-Zustand:**
The fixed-string endpoint mirrors the same result semantics where they are still meaningful.

**Architectural Rationale:**
Shared result semantics reduce caller confusion and simplify LLM orchestration.

## Actions
[INTENT: ANWEISUNG]

1. **Re-anchor the upstream search-platform tasks and registration surface**
   - **What:** Re-read the upstream task contracts and the current inspection registration file.
   - **Where:** Declared upstream plan references and authoritative references.
   - **How:** Use only the declared references.
   - **Why:** The new endpoint must align with the native search SSOT and the finalized search-family semantics.
   - **Inputs / References:** All authoritative references in this task file.
   - **Output Contract:** Full contract context is active.
   - **Checkpoint Impact:** Starts checkpoint `cp-1-context-and-registration-read`.
   - **Verification:** All required references are read successfully.
   - **Stop if:** Any required reference is missing.
2. **Create the fixed-string schema and handler**
   - **What:** Implement the new endpoint files.
   - **Where:** `src/domain/inspection/search-file-contents-by-fixed-string/schema.ts` and `handler.ts`
   - **How:** Use the shared adapter, shared runtime policy, and the literal-search contract surface bound above.
   - **Why:** The platform needs a dedicated literal-search endpoint.
   - **Inputs / References:** Upstream tasks, shared guardrail registry, and the search-family contract shape.
   - **Output Contract:** Both new endpoint files exist with enterprise-grade TSDoc.
   - **Checkpoint Impact:** Completes checkpoint `cp-2-new-endpoint-files`.
   - **Verification:** The endpoint is literal-only and uses the shared backend and policy surfaces.
   - **Stop if:** Implementing the endpoint would require regex semantics or an alternate public name.
3. **Register the endpoint in the inspection tool catalog**
   - **What:** Add the new tool registration.
   - **Where:** `src/application/server/register-inspection-tool-catalog.ts`
   - **How:** Register the new fixed-string endpoint with a clear tool description, schema, output schema, and handler wiring.
   - **Why:** The endpoint must become available on the public inspection catalog.
   - **Inputs / References:** Current registration file and the new endpoint surfaces.
   - **Output Contract:** The public tool catalog exposes the new endpoint.
   - **Checkpoint Impact:** Completes checkpoint `cp-3-registration`.
   - **Verification:** The new endpoint appears alongside the other inspection tools without disturbing their existing registrations.
   - **Stop if:** Registration would require moving or renaming existing inspection tools.

## Checkpoint Definitions
[INTENT: REFERENZ]

| Checkpoint ID | Kind | Completion Signal | Produces Artifacts | Resume Meaning |
|---------------|------|-------------------|--------------------|----------------|
| `cp-1-context-and-registration-read` | `sequential_step` | Upstream tasks and registration surface were re-read | none | Implementation may begin |
| `cp-2-new-endpoint-files` | `artifact_milestone` | Both fixed-string endpoint files exist | `schema.ts`, `handler.ts` | Endpoint implementation is complete |
| `cp-3-registration` | `artifact_milestone` | Inspection tool catalog registers the new endpoint | `register-inspection-tool-catalog.ts` | Public exposure is complete |

## Execution Progress Ledger
[INTENT: REFERENZ]

| Checkpoint ID | Status | Evidence | Notes |
|---------------|--------|----------|-------|
| `cp-1-context-and-registration-read` | `PENDING` | authoritative references read | Start here |
| `cp-2-new-endpoint-files` | `PENDING` | `src/domain/inspection/search-file-contents-by-fixed-string/schema.ts`, `handler.ts` | Depends on `cp-1-context-and-registration-read` |
| `cp-3-registration` | `PENDING` | `src/application/server/register-inspection-tool-catalog.ts` | Depends on `cp-2-new-endpoint-files` |

## Resume Conflict Resolution Rules
[INTENT: CONSTRAINT]

- If registration is changed before both endpoint files exist, stop and escalate.
- If the endpoint is created under a different public name, stop and escalate.
- If the new endpoint exists but still uses regex semantics, stop and escalate.

## Checkpoint Update Protocol
[INTENT: ANWEISUNG]

1. Mark `cp-2-new-endpoint-files` as `DONE` only after both new endpoint files exist with complete TSDoc.
2. Mark `cp-3-registration` as `DONE` only after the inspection tool catalog exposes the new endpoint under the exact bound name.

## Acceptance Criteria
[INTENT: REFERENZ]

- [ ] A new public endpoint named `search_file_contents_by_fixed_string` exists.
- [ ] The endpoint uses the shared `ugrep` adapter and shared runtime policy registry.
- [ ] The endpoint request surface enforces literal/fixed-string semantics rather than free regex semantics.
- [ ] The endpoint is registered in `registerInspectionToolCatalog()`.
- [ ] The new TypeScript surfaces contain detailed enterprise-grade TSDoc explaining the architecture and rationale for developers and LLM agents.

## Stop Conditions & User Escalation
[INTENT: CONSTRAINT]

- **Plan-vs-Reality Mismatch:** Stop if the search-platform surfaces already expose a conflicting fixed-string endpoint.
- **Missing Required Plan Information:** Stop if any bound naming, input, or registration surface is missing.
- **Autonomous Decision Required:** Stop if implementing this endpoint would require inventing a new public name, new search semantics, or a different backend.
- **Allowed Runtime Discovery:** None.
- **User Message Contract:** `Plan says X / current reality is Y / missing or conflicting information is Z / continuing would require an autonomous decision.`

## Post-Completion Instructions
[INTENT: ANWEISUNG]

1. Mark this task `[x]` in [`.plan/2-search-platform/orchestration.md`](.plan/2-search-platform/orchestration.md).
2. Re-anchor [`PLAN.md`](PLAN.md) after completion.

## File Path Index
[INTENT: REFERENZ]

| # | Path | Kind | Why Relevant | Must Read Before Execution |
|---|------|------|--------------|----------------------------|
| 1 | `.plan/2-search-platform/2.1-ugrep-adapter-and-classification-surfaces.md` | `plan-file` | Shared adapter and classifier contract | `true` |
| 2 | `.plan/2-search-platform/2.2-refactor-regex-endpoint-to-native-search.md` | `plan-file` | Finalized search-family behavior | `true` |
| 3 | `src/application/server/register-inspection-tool-catalog.ts` | `business-file` | Registration surface | `true` |
| 4 | `src/domain/inspection/search-file-contents-by-regex/schema.ts` | `schema-file` | Search-family contract reference | `true` |
| 5 | `src/domain/shared/guardrails/tool-guardrail-limits.ts` | `schema-file` | Shared caps and limits | `true` |
| 6 | `src/domain/shared/guardrails/text-response-budget.ts` | `schema-file` | Shared response-budget utilities | `true` |

## Notes
[INTENT: KONTEXT]

- The endpoint name must stay LLM-clear and public-contract-stable.
- TSDocs in this task must explain why a dedicated fixed-string endpoint exists instead of forcing agents to overload the regex endpoint or use shell-centric naming.

