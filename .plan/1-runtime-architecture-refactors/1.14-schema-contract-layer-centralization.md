---
file_type: "task"
task_id: "1.14"
task_title: "Schema contract layer centralization — shared Zod schemas, superRefine helper, and error base schema"
unit: "1"
parent_orchestration: ".plan/1-runtime-architecture-refactors/orchestration.md"
classification: "ISOLATED"
status: "pending"
estimated_complexity: "MEDIUM"
files_modified:
  - "src/domain/shared/resume/inspection-resume-contract.ts"
  - "src/domain/inspection/find-paths-by-name/schema.ts"
  - "src/domain/inspection/find-files-by-glob/schema.ts"
  - "src/domain/inspection/list-directory-entries/schema.ts"
  - "src/domain/inspection/search-file-contents-by-regex/schema.ts"
  - "src/domain/inspection/search-file-contents-by-fixed-string/schema.ts"
  - "src/domain/inspection/count-lines/schema.ts"
  - "src/domain/inspection/shared/filesystem-entry-metadata-contract.ts"
  - "src/domain/inspection/get-file-checksums/schema.ts"
  - "src/domain/inspection/get-path-metadata/schema.ts"
  - "src/domain/inspection/verify-file-checksums/schema.ts"
files_read:
  - "src/domain/shared/resume/inspection-resume-contract.ts"
  - "src/domain/inspection/shared/filesystem-entry-metadata-contract.ts"
  - "src/domain/inspection/find-paths-by-name/schema.ts"
  - "src/domain/inspection/find-files-by-glob/schema.ts"
  - "src/domain/inspection/list-directory-entries/schema.ts"
  - "src/domain/inspection/search-file-contents-by-regex/schema.ts"
  - "src/domain/inspection/search-file-contents-by-fixed-string/schema.ts"
  - "src/domain/inspection/count-lines/schema.ts"
  - "src/domain/inspection/get-file-checksums/schema.ts"
  - "src/domain/inspection/verify-file-checksums/schema.ts"
  - "src/domain/inspection/get-path-metadata/schema.ts"
dependencies: []
blocked_by: "none"
information_completeness_mode: "FULLY_SPECIFIED"
implementation_scenarios:
  - "FUNCTION_CREATION"
  - "OBJECT_CREATION"
  - "SCHEMA_CHANGE"
  - "PROPERTY_ADDITION"
execution_surface_band: "YELLOW"
primary_split_axis: "none"
single_agent_viability_note: "11 files are modified but all changes are confined to the schema layer of the inspection domain. Every modification is a mechanical structural extraction: add shared Zod exports to the contract module, then replace five locally defined duplicate schema blocks with import references, update count-lines to use the restricted variant, and add the error base schema and extend it in three metadata endpoint schemas. No handler behavior, no runtime logic, no guardrail policies, and no business rules are changed. Per-file cognitive load is low and anchored to one stable shared contract surface."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
decision_surface_register:
  - surface: "naming"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "InspectionResumeAdmissionSchema, InspectionResumeMetadataSchema, InspectionCompletionOnlyAdmissionSchema, InspectionCompletionOnlyResumeMetadataSchema, applyCommonResumeSchemaRefinement, BatchOperationErrorBaseSchema, BatchOperationError — all names bound below"
  - surface: "path_and_placement"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "All placements listed in files_modified above"
  - surface: "literal_values_and_budgets"
    status: "BOUND_LITERAL"
    binding_source: "project-state"
    notes: "Schema values derived from existing INSPECTION_RESUME_ADMISSION_OUTCOMES, INSPECTION_RESUME_MODES, INSPECTION_RESUME_STATUSES — no new literal budget values introduced"
  - surface: "defaults_and_fallbacks"
    status: "NOT_APPLICABLE"
    binding_source: "plan-output"
    notes: "No defaults changed; schema shapes are purely additive exports"
  - surface: "types_and_shapes"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "Full Zod schema shapes specified in Authoritative Code / Contract Surfaces below"
  - surface: "function_signatures_and_returns"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "applyCommonResumeSchemaRefinement signature specified below"
  - surface: "object_schema_and_mapper_contracts"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "Full schema object shapes in Authoritative Code / Contract Surfaces"
  - surface: "dependencies_and_import_surfaces"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "New zod import in inspection-resume-contract.ts; consumer schemas import InspectionResumeAdmissionSchema, InspectionResumeMetadataSchema, applyCommonResumeSchemaRefinement from @domain/shared/resume/inspection-resume-contract; BatchOperationErrorBaseSchema from @domain/inspection/shared/filesystem-entry-metadata-contract"
  - surface: "validation_and_boundary_rules"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "superRefine logic for common resume discrimination is fully specified in the shared helper; endpoint-specific primary-field checks remain inline per schema"
  - surface: "consumer_and_callsite_bindings"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "All 5 preview-family schemas, count-lines schema, and 3 checksum/metadata schemas listed explicitly in files_modified"
authoritative_literal_bindings:
  - binding_domain: "naming"
    exact_binding: "InspectionResumeAdmissionSchema"
    source: "plan-output (new shared Zod schema export)"
    placement_or_consumer: "src/domain/shared/resume/inspection-resume-contract.ts — exported after existing type declarations"
    no_autonomous_choice_rule: "Name mirrors the existing TypeScript interface InspectionResumeAdmission to ensure discoverability"
  - binding_domain: "naming"
    exact_binding: "InspectionResumeMetadataSchema"
    source: "plan-output (new shared Zod schema export)"
    placement_or_consumer: "src/domain/shared/resume/inspection-resume-contract.ts"
    no_autonomous_choice_rule: "Mirrors InspectionResumeMetadata interface name"
  - binding_domain: "naming"
    exact_binding: "InspectionCompletionOnlyAdmissionSchema"
    source: "plan-output (restricted variant for count_lines)"
    placement_or_consumer: "src/domain/shared/resume/inspection-resume-contract.ts"
    no_autonomous_choice_rule: "Mirrors INSPECTION_COMPLETION_ONLY_RESUME_MODES constant family pattern"
  - binding_domain: "naming"
    exact_binding: "InspectionCompletionOnlyResumeMetadataSchema"
    source: "plan-output (restricted variant for count_lines)"
    placement_or_consumer: "src/domain/shared/resume/inspection-resume-contract.ts"
    no_autonomous_choice_rule: "Mirrors INSPECTION_COMPLETION_ONLY_RESUME_MODES pattern"
  - binding_domain: "naming"
    exact_binding: "applyCommonResumeSchemaRefinement"
    source: "plan-output (shared superRefine helper function)"
    placement_or_consumer: "src/domain/shared/resume/inspection-resume-contract.ts"
    no_autonomous_choice_rule: "Name must be a verb phrase reflecting the refinement role, not confused with the existing validateResumeOnlyRequest function"
  - binding_domain: "naming"
    exact_binding: "BatchOperationErrorBaseSchema"
    source: "plan-output (new shared base schema)"
    placement_or_consumer: "src/domain/inspection/shared/filesystem-entry-metadata-contract.ts"
    no_autonomous_choice_rule: "Follows existing schema naming convention (PascalCase + Schema suffix) in that file"
  - binding_domain: "naming"
    exact_binding: "BatchOperationError"
    source: "plan-output (inferred type export)"
    placement_or_consumer: "src/domain/inspection/shared/filesystem-entry-metadata-contract.ts"
    no_autonomous_choice_rule: "Type alias is z.infer<typeof BatchOperationErrorBaseSchema>"
  - binding_domain: "dependencies_and_import_surfaces"
    exact_binding: "import { z } from \"zod\";"
    source: "plan-output"
    placement_or_consumer: "src/domain/shared/resume/inspection-resume-contract.ts — add as first import line"
    no_autonomous_choice_rule: "File currently has no Zod import; needed because new schema exports use z.object, z.enum, z.string, z.boolean, z.array"
  - binding_domain: "dependencies_and_import_surfaces"
    exact_binding: "import { InspectionResumeAdmissionSchema, InspectionResumeMetadataSchema, applyCommonResumeSchemaRefinement } from \"@domain/shared/resume/inspection-resume-contract\";"
    source: "plan-output"
    placement_or_consumer: "find-paths-by-name/schema.ts, find-files-by-glob/schema.ts, list-directory-entries/schema.ts, search-file-contents-by-regex/schema.ts, search-file-contents-by-fixed-string/schema.ts — add to existing import from that module"
    no_autonomous_choice_rule: "Must be added to the existing @domain/shared/resume/inspection-resume-contract import statement, not as a separate duplicate import"
  - binding_domain: "dependencies_and_import_surfaces"
    exact_binding: "import { InspectionCompletionOnlyAdmissionSchema, InspectionCompletionOnlyResumeMetadataSchema, applyCommonResumeSchemaRefinement } from \"@domain/shared/resume/inspection-resume-contract\";"
    source: "plan-output"
    placement_or_consumer: "src/domain/inspection/count-lines/schema.ts — add to existing import"
    no_autonomous_choice_rule: "count-lines uses restricted variants only (no PREVIEW_FIRST, only COMPLETE_RESULT)"
  - binding_domain: "dependencies_and_import_surfaces"
    exact_binding: "import { BatchOperationErrorBaseSchema } from \"@domain/inspection/shared/filesystem-entry-metadata-contract\";"
    source: "plan-output"
    placement_or_consumer: "get-file-checksums/schema.ts, get-path-metadata/schema.ts, verify-file-checksums/schema.ts"
    no_autonomous_choice_rule: "Must use the canonical alias @domain/inspection/shared/ consistent with existing imports in those files"
requires_authoritative_code_surfaces: true
authoritative_code_surfaces:
  - surface_name: "InspectionResumeAdmissionSchema"
    kind: "schema-fragment"
    snippet_or_contract: |
      export const InspectionResumeAdmissionSchema = z.object({
        outcome: z.enum([
          INSPECTION_RESUME_ADMISSION_OUTCOMES.INLINE,
          INSPECTION_RESUME_ADMISSION_OUTCOMES.PREVIEW_FIRST,
          INSPECTION_RESUME_ADMISSION_OUTCOMES.COMPLETION_BACKED_REQUIRED,
          INSPECTION_RESUME_ADMISSION_OUTCOMES.NARROWING_REQUIRED,
        ]),
        guidanceText: z.string().nullable(),
        scopeReductionGuidanceText: z.string().nullable(),
      });
    source: "plan-output — matches exact field set used in all 5 preview-family local schema copies"
    placement_or_consumer: "src/domain/shared/resume/inspection-resume-contract.ts — placed after existing type declarations, before function exports"
  - surface_name: "InspectionResumeMetadataSchema"
    kind: "schema-fragment"
    snippet_or_contract: |
      export const InspectionResumeMetadataSchema = z.object({
        resumeToken: z.string().nullable(),
        supportedResumeModes: z.array(
          z.enum([INSPECTION_RESUME_MODES.NEXT_CHUNK, INSPECTION_RESUME_MODES.COMPLETE_RESULT]),
        ),
        recommendedResumeMode: z
          .enum([INSPECTION_RESUME_MODES.NEXT_CHUNK, INSPECTION_RESUME_MODES.COMPLETE_RESULT])
          .nullable(),
        status: z.enum([
          INSPECTION_RESUME_STATUSES.ACTIVE,
          INSPECTION_RESUME_STATUSES.CANCELLED,
          INSPECTION_RESUME_STATUSES.COMPLETED,
          INSPECTION_RESUME_STATUSES.EXPIRED,
        ]).nullable(),
        resumable: z.boolean(),
        expiresAt: z.string().nullable(),
      });
    source: "plan-output — matches exact field set from all 5 preview-family local copies"
    placement_or_consumer: "src/domain/shared/resume/inspection-resume-contract.ts"
  - surface_name: "InspectionCompletionOnlyAdmissionSchema"
    kind: "schema-fragment"
    snippet_or_contract: |
      export const InspectionCompletionOnlyAdmissionSchema = z.object({
        outcome: z.enum([
          INSPECTION_RESUME_ADMISSION_OUTCOMES.INLINE,
          INSPECTION_RESUME_ADMISSION_OUTCOMES.COMPLETION_BACKED_REQUIRED,
          INSPECTION_RESUME_ADMISSION_OUTCOMES.NARROWING_REQUIRED,
        ]),
        guidanceText: z.string().nullable(),
        scopeReductionGuidanceText: z.string().nullable(),
      });
    source: "plan-output — count-lines schema does not include PREVIEW_FIRST in its admission outcome set"
    placement_or_consumer: "src/domain/shared/resume/inspection-resume-contract.ts"
  - surface_name: "InspectionCompletionOnlyResumeMetadataSchema"
    kind: "schema-fragment"
    snippet_or_contract: |
      export const InspectionCompletionOnlyResumeMetadataSchema = z.object({
        resumeToken: z.string().nullable(),
        supportedResumeModes: z.array(z.enum([INSPECTION_RESUME_MODES.COMPLETE_RESULT])),
        recommendedResumeMode: z.enum([INSPECTION_RESUME_MODES.COMPLETE_RESULT]).nullable(),
        status: z.enum([
          INSPECTION_RESUME_STATUSES.ACTIVE,
          INSPECTION_RESUME_STATUSES.CANCELLED,
          INSPECTION_RESUME_STATUSES.COMPLETED,
          INSPECTION_RESUME_STATUSES.EXPIRED,
        ]).nullable(),
        resumable: z.boolean(),
        expiresAt: z.string().nullable(),
      });
    source: "plan-output — count-lines supports only complete-result resumeMode"
    placement_or_consumer: "src/domain/shared/resume/inspection-resume-contract.ts"
  - surface_name: "applyCommonResumeSchemaRefinement"
    kind: "signature"
    snippet_or_contract: |
      export function applyCommonResumeSchemaRefinement(
        args: {
          readonly resumeToken?: string | undefined;
          readonly resumeMode?: string | undefined;
        },
        ctx: z.RefinementCtx,
        hasQueryDefiningFields: boolean,
      ): void {
        const isResumeRequest = args.resumeToken !== undefined;

        if (!isResumeRequest && args.resumeMode !== undefined) {
          ctx.addIssue({
            code: z.ZodIssueCode.custom,
            message: "Base requests must not provide a resume mode without a resume token.",
            path: [INSPECTION_RESUME_MODE_FIELD],
          });
        }

        if (isResumeRequest && args.resumeMode === undefined) {
          ctx.addIssue({
            code: z.ZodIssueCode.custom,
            message: "Resume-only requests must provide a resumeMode.",
            path: [INSPECTION_RESUME_MODE_FIELD],
          });
        }

        if (isResumeRequest && hasQueryDefiningFields) {
          ctx.addIssue({
            code: z.ZodIssueCode.custom,
            message:
              "Resume-only requests must omit new query-defining fields and rely on the persisted request context.",
            path: [INSPECTION_RESUME_TOKEN_FIELD],
          });
        }
      }
    source: "plan-output — extracts the three common superRefine checks shared across all 5 preview-family schemas and count-lines"
    placement_or_consumer: "src/domain/shared/resume/inspection-resume-contract.ts — placed after schema exports"
  - surface_name: "BatchOperationErrorBaseSchema"
    kind: "schema-fragment"
    snippet_or_contract: |
      export const BatchOperationErrorBaseSchema = z.object({
        path: z.string(),
        error: z.string(),
      });

      export type BatchOperationError = z.infer<typeof BatchOperationErrorBaseSchema>;
    source: "plan-output — extracts common { path, error } base from get-file-checksums and get-path-metadata error arrays"
    placement_or_consumer: "src/domain/inspection/shared/filesystem-entry-metadata-contract.ts — placed after existing FileSystemEntryMetadataSchema export"
  - surface_name: "verify-file-checksums error schema extension"
    kind: "schema-fragment"
    snippet_or_contract: |
      errors: z.array(
        BatchOperationErrorBaseSchema.extend({
          expectedHash: z.string(),
        })
      ),
    source: "plan-output — verify-file-checksums needs { path, expectedHash, error }; extends the base schema"
    placement_or_consumer: "src/domain/inspection/verify-file-checksums/schema.ts — replace inline z.object({ path, expectedHash, error }) in VerifyFileChecksumsResultSchema"
  - surface_name: "consumer superRefine simplified shape (example: search-regex schema)"
    kind: "schema-fragment"
    snippet_or_contract: |
      .superRefine((args, ctx) => {
        const resumeRequest = args.resumeToken !== undefined;
        const hasQueryDefiningFields =
          args.roots.length > 0
          || args.regex !== ""
          || args.includeGlobs.length > 0
          || args.excludeGlobs.length > 0
          || args.respectGitIgnore
          || args.includeExcludedGlobs.length > 0
          || args.maxResults !== 100
          || args.caseSensitive;

        if (!resumeRequest && args.roots.length === 0) {
          ctx.addIssue({
            code: z.ZodIssueCode.custom,
            message: "Base requests must provide at least one regex search root.",
            path: ["roots"],
          });
        }

        if (!resumeRequest && args.regex === "") {
          ctx.addIssue({
            code: z.ZodIssueCode.custom,
            message: "Base requests must provide a regex pattern.",
            path: ["regex"],
          });
        }

        applyCommonResumeSchemaRefinement(args, ctx, hasQueryDefiningFields);
      })
    source: "plan-output — each endpoint keeps its primary-field check; common mode/token checks are delegated to the helper"
    placement_or_consumer: "All 5 preview-family schemas — replace the three common superRefine checks with the helper call"
authoritative_references:
  - path: "src/domain/shared/resume/inspection-resume-contract.ts"
    kind: "business-file"
    usage: "Current module that receives the new Zod schema exports; read to understand exact existing exports and placement"
    required_before_execution: true
  - path: "src/domain/inspection/shared/filesystem-entry-metadata-contract.ts"
    kind: "business-file"
    usage: "Receives BatchOperationErrorBaseSchema; already imports z from zod and exports FileSystemEntryMetadataSchema"
    required_before_execution: true
  - path: "src/domain/inspection/find-paths-by-name/schema.ts"
    kind: "business-file"
    usage: "Preview-family consumer schema with local duplicate InspectionResumeAdmissionSchema + InspectionResumeMetadataSchema blocks to remove"
    required_before_execution: true
  - path: "src/domain/inspection/find-files-by-glob/schema.ts"
    kind: "business-file"
    usage: "Same pattern as find-paths-by-name"
    required_before_execution: true
  - path: "src/domain/inspection/list-directory-entries/schema.ts"
    kind: "business-file"
    usage: "Same pattern"
    required_before_execution: true
  - path: "src/domain/inspection/search-file-contents-by-regex/schema.ts"
    kind: "business-file"
    usage: "Same pattern; note this was modified in the 1.13 session — re-anchor before editing"
    required_before_execution: true
  - path: "src/domain/inspection/search-file-contents-by-fixed-string/schema.ts"
    kind: "business-file"
    usage: "Same pattern; note this was modified in the 1.13 session"
    required_before_execution: true
  - path: "src/domain/inspection/count-lines/schema.ts"
    kind: "business-file"
    usage: "Uses restricted subset of admission/resume schemas; uses InspectionCompletionOnlyAdmissionSchema and InspectionCompletionOnlyResumeMetadataSchema after this task"
    required_before_execution: true
  - path: "src/domain/inspection/get-file-checksums/schema.ts"
    kind: "business-file"
    usage: "Contains inline z.object({ path, error }) error array; replaced with BatchOperationErrorBaseSchema"
    required_before_execution: true
  - path: "src/domain/inspection/get-path-metadata/schema.ts"
    kind: "business-file"
    usage: "Same pattern as get-file-checksums"
    required_before_execution: true
  - path: "src/domain/inspection/verify-file-checksums/schema.ts"
    kind: "business-file"
    usage: "Contains inline z.object({ path, expectedHash, error }) error array; replaced with BatchOperationErrorBaseSchema.extend({ expectedHash: z.string() })"
    required_before_execution: true
  - path: ".plan/1-runtime-architecture-refactors/1.13-resume-session-remodel-handoff-and-remaining-work.md"
    kind: "plan-file"
    usage: "Documents which schema files were already modified in the 1.13 session (search-regex and search-fixed-string schemas); re-anchor before touching those two files"
    required_before_execution: true
upstream_plan_references: []
runtime_discovery:
  required: false
  scope: []
  allowed_topics: []
checkpoint_strategy: "SEQUENTIAL_CHECKPOINTS_REQUIRED"
checkpoint_eligibility_contract:
  durable_skip_authority_only: true
  ephemeral_freshness_steps:
    - "Initial full-file re-anchor reads of all 11 target files before editing begins"
  durable_resume_checkpoints:
    - "CHK-1.14-A"
    - "CHK-1.14-B"
    - "CHK-1.14-C"
    - "CHK-1.14-D"
    - "CHK-1.14-E"
resume_validation_contract:
  lowest_level_truth_surface: "checkpoint-ledger"
  task_lifecycle_truth_surface: ".plan/1-runtime-architecture-refactors/orchestration.md"
  global_frontier_truth_surface: "PLAN.md"
  artifact_evidence_sources:
    - "src/domain/shared/resume/inspection-resume-contract.ts — presence of InspectionResumeAdmissionSchema export"
    - "src/domain/inspection/shared/filesystem-entry-metadata-contract.ts — presence of BatchOperationErrorBaseSchema export"
    - "5 preview-family schema files — absence of local InspectionResumeAdmissionSchema/InspectionResumeMetadataSchema const declarations"
    - "count-lines/schema.ts — presence of InspectionCompletionOnlyAdmissionSchema import"
    - "get-file-checksums and get-path-metadata schemas — absence of inline z.object({ path, error }) in errors array"
  conflict_requires_user_confirmation: true
  partial_progress_requires_user_decision: true
  durable_skip_authority_only: true
  ephemeral_checkpoint_conflict_requires_reexecution: false
artifact_progress_inference_rules:
  - "If InspectionResumeAdmissionSchema is already exported from inspection-resume-contract.ts, CHK-1.14-A is DONE and must not be re-applied"
  - "If BatchOperationErrorBaseSchema is already exported from filesystem-entry-metadata-contract.ts, CHK-1.14-B is DONE"
  - "If a preview-family schema file no longer contains a local const InspectionResumeAdmissionSchema declaration, that file's portion of CHK-1.14-C is DONE for that specific file"
  - "If count-lines/schema.ts imports InspectionCompletionOnlyAdmissionSchema, CHK-1.14-D is DONE"
  - "If get-file-checksums/schema.ts no longer contains an inline z.object({ path, error }) in the errors array, its portion of CHK-1.14-E is DONE"
  - "EPHEMERAL freshness reads (re-anchoring) never create durable skip authority and must be re-executed at session start regardless of checkpoint state"
user_resume_decision_gate:
  required_on_partial_progress: true
  required_on_state_conflict: true
  default_user_choices:
    - "ACCEPT_AND_CONTINUE"
    - "REVERIFY_EXISTING_OUTPUTS"
task_completion_order_contract:
  ordered_phase_sequence:
    - "task_file_local_completion_closure"
    - "parent_orchestration_status_update"
    - "dependency_resolution_and_unblock"
    - "unit_and_plan_completion_cascade"
  parent_status_write_blocked_until_task_file_done: true
  plan_update_blocked_until_parent_sync: true
current_target_binding_contract:
  binding_surface: "active_task_file"
  proof_scope: "task_file_local"
  invalidate_on_target_or_frontier_change: true
task_local_completion_ledger:
  required_units:
    - "actions_executed"
    - "acceptance_criteria_pass"
    - "required_artifacts_materialized_or_verified"
    - "checkpoint_or_artifact_progress_synced"
    - "persona_write_permission_green_go"
task_local_pre_write_gate_report_contract:
  gate_inputs:
    - "current_target_binding"
    - "ordered_phase_contract"
    - "task_local_completion_ledger"
    - "no_heuristic_exit_governance"
  write_allowed_only_if_all_pass: true
no_heuristic_exit_governance:
  task_file_done_requires_visible_local_proof: true
  broader_parent_or_plan_pass_not_sufficient: true
persona_generation_required: true
persona_generation_mode: "runtime_regenerate"
persona_source_prompt_path: "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
persona_primary_domain_kernel: "TypeScript Node.js MCP Server Domain Architecture — Schema Contract Centralization"
persona_secondary_specializations:
  - "Zod schema design and composition"
  - "DDD single source of truth enforcement at the schema layer"
  - "Enterprise structural refactoring without breaking changes"
persona_current_task_specialization: "Inspection endpoint Zod schema deduplication — extracting shared resume schema exports and a base error schema"
persona_generation_lineage: "Derived from task contract: schema-layer structural extraction, TypeScript + Zod, no runtime behavior change, DDD shared-contract centralization"
persona_runtime_review_required: true
persona_source_load_policy:
  source_loading_mode: "DEFERRED_UNTIL_PRE_EXECUTION_STATE_SNAPSHOT_CLOSED"
  early_source_load_forbidden: true
  late_load_trigger: "after pre_execution_state_snapshot_closed and immediately before execute_time_persona_subprocess"
  transitive_source_chain_must_be_closed_after_load: true
persona_state_disclosure_contract:
  pre_creator_state_report_required: true
  post_creator_ledger_state_report_required: true
  required_state_fields:
    - "task_id"
    - "pre_execution_state_snapshot_closed"
    - "persona_source_documents_loaded"
    - "persona_source_chain_closed"
    - "creator_phase_execution_ledger_status"
    - "runtime_persona_outcome"
    - "persona_write_permission"
  final_dx_log_after_proof_only: true
persona_embedded_creator_execution_contract:
  execution_mode: "FULL_CONTRACT_EQUIVALENT_EXECUTION"
  creator_phase_source: "required_creator_execution_phases"
  creator_phase_execution_ledger_required: true
  creator_phase_order_non_swappable: true
  local_creator_logic_redefinition_forbidden: true
persona_write_gate_contract:
  persona_write_permission_default: "BLOCKED"
  persona_write_permission_release_requirements:
    - "pre_execution_state_snapshot_closed = true"
    - "persona_source_documents_loaded = true"
    - "persona_source_chain_closed = true"
    - "creator_phase_execution_ledger_complete = true"
    - "creator_completion_proof_pass = true"
    - "runtime_persona_outcome = GREEN_GO"
  final_green_go_required_for_write: true
persona_dx_log_contract:
  pre_proof_surface: "persona_state_report_only"
  final_dx_log_after_proof_only: true
  required_final_fields:
    - "runtime_persona_title"
    - "runtime_persona_summary"
    - "runtime_persona_alignment_percent"
    - "runtime_persona_outcome"
    - "creator_completion_proof_summary"
persona_creator_execution_proof_contract:
  transitive_persona_source_chain:
    - "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
    - "src/personas/it/__prompts__/interactive/best-options/enterprise/creation/prompt.md"
  canonical_creator_source_path: "src/personas/it/__prompts__/interactive/best-options/enterprise/creation/prompt.md"
  creator_equivalence_rule: "Runtime persona must match the canonical creator decision for the same final pre_execution_state_snapshot"
  required_creator_execution_phases:
    - "Context extraction and persona axis mapping"
    - "Multi-option generation across domain/language/operation axes"
    - "Weighted scoring and top-option selection"
    - "Quality gate and persona finalization"
  required_creator_proof_fields:
    - "creator_transitive_chain_verified"
    - "creator_required_phases_completed"
    - "creator_equivalence_confirmed"
    - "snapshot_substitution_detected = false"
    - "creator_completion_proof_pass = true"
  non_substitutable_persona_surfaces:
    - "persona_derivation_manifest"
    - "persona_dx_snapshot"
    - "persona_title"
    - "persona_summary"
  proof_gate_blocks_green_go: true
  proof_failure_outcome: "PLAN_GAP"
persona_review_binding_contract:
  contract_source_ingestion_is_not_review: true
  pre_execution_state_required_surfaces:
    - "task_contract"
    - "upstream_plan_references"
    - "authoritative_references"
    - "resume_state_mode"
    - "artifact_evidence"
    - "files_read"
    - "files_modified"
  pre_execution_state_required_reads:
    - "authoritative_references"
    - "files_read"
    - "files_modified"
  persona_review_finalization_gate: "GREEN_GO only after pre_execution_state_snapshot_closed = true"
  persona_review_invalidation_triggers:
    - "new_required_reference"
    - "new_or_changed_files_read_or_files_modified_reality"
    - "new_artifact_evidence"
    - "resume_candidate_change"
    - "shared_file_reality_change_before_first_action"
    - "new_boundary_or_consumer_evidence"
  persona_review_reentry_protocol: "invalidate_persona_outcome_and_rerun_review_before_first_action"
persona_dx_snapshot:
  title: "TypeScript Schema Contract Centralization Architect — MCP Inspection Domain"
  summary: "An enterprise TypeScript architect specializing in DDD schema-layer SSOT enforcement, Zod schema design, and structural extraction refactoring. Applies strict no-breaking-change discipline while centralizing duplicated schema surfaces into shared contract modules."
  weighting_note: "Primary domain kernel (schema contract architecture, DDD SSoT) dominates over language specialization (TypeScript/Zod) and operation family (structural extraction)."
checkpoint_definitions:
  - checkpoint_id: "CHK-1.14-A"
    kind: "sequential_step"
    completion_signal: "InspectionResumeAdmissionSchema, InspectionResumeMetadataSchema, InspectionCompletionOnlyAdmissionSchema, InspectionCompletionOnlyResumeMetadataSchema, and applyCommonResumeSchemaRefinement are all exported from src/domain/shared/resume/inspection-resume-contract.ts and TypeScript compiles without errors for that file"
    produces_artifacts:
      - "src/domain/shared/resume/inspection-resume-contract.ts"
  - checkpoint_id: "CHK-1.14-B"
    kind: "sequential_step"
    completion_signal: "BatchOperationErrorBaseSchema and BatchOperationError are exported from src/domain/inspection/shared/filesystem-entry-metadata-contract.ts"
    produces_artifacts:
      - "src/domain/inspection/shared/filesystem-entry-metadata-contract.ts"
  - checkpoint_id: "CHK-1.14-C"
    kind: "artifact_milestone"
    completion_signal: "All 5 preview-family schema files (find-paths-by-name, find-files-by-glob, list-directory-entries, search-file-contents-by-regex, search-file-contents-by-fixed-string) have had their local InspectionResumeAdmissionSchema and InspectionResumeMetadataSchema const declarations removed and replaced with imports from the shared contract. The superRefine in each now calls applyCommonResumeSchemaRefinement for the three common checks."
    produces_artifacts:
      - "src/domain/inspection/find-paths-by-name/schema.ts"
      - "src/domain/inspection/find-files-by-glob/schema.ts"
      - "src/domain/inspection/list-directory-entries/schema.ts"
      - "src/domain/inspection/search-file-contents-by-regex/schema.ts"
      - "src/domain/inspection/search-file-contents-by-fixed-string/schema.ts"
  - checkpoint_id: "CHK-1.14-D"
    kind: "sequential_step"
    completion_signal: "count-lines/schema.ts imports InspectionCompletionOnlyAdmissionSchema and InspectionCompletionOnlyResumeMetadataSchema from shared; local duplicate const declarations removed; superRefine uses applyCommonResumeSchemaRefinement for common checks"
    produces_artifacts:
      - "src/domain/inspection/count-lines/schema.ts"
  - checkpoint_id: "CHK-1.14-E"
    kind: "artifact_milestone"
    completion_signal: "get-file-checksums/schema.ts and get-path-metadata/schema.ts use z.array(BatchOperationErrorBaseSchema) for their errors fields; verify-file-checksums/schema.ts uses z.array(BatchOperationErrorBaseSchema.extend({ expectedHash: z.string(), ... })) with all original additional fields preserved via extend"
    produces_artifacts:
      - "src/domain/inspection/get-file-checksums/schema.ts"
      - "src/domain/inspection/get-path-metadata/schema.ts"
      - "src/domain/inspection/verify-file-checksums/schema.ts"
execution_progress_ledger:
  ledger_mode: "CHECKPOINT_LEDGER"
  checkpoints:
    - checkpoint_id: "CHK-1.14-A"
      status: "PENDING"
    - checkpoint_id: "CHK-1.14-B"
      status: "PENDING"
    - checkpoint_id: "CHK-1.14-C"
      status: "PENDING"
    - checkpoint_id: "CHK-1.14-D"
      status: "PENDING"
    - checkpoint_id: "CHK-1.14-E"
      status: "PENDING"
resume_conflict_resolution_rules:
  - "If checkpoint ledger says CHK-1.14-A is DONE but InspectionResumeAdmissionSchema is absent from inspection-resume-contract.ts, STOP and request user confirmation — ledger and artifact evidence diverge"
  - "If checkpoint ledger says CHK-1.14-C is DONE but a local const InspectionResumeAdmissionSchema still exists in any of the 5 consumer schemas, STOP and request user decision before continuing"
  - "Already completed checkpoints must not be re-executed; already-removed local const blocks must not be re-added"
checkpoint_update_protocol:
  - "Mark checkpoint IN_PROGRESS when the execute agent begins editing the first file for that checkpoint"
  - "Mark checkpoint DONE immediately after all files for that checkpoint are modified and verified"
  - "Never advance to CHK-1.14-C before CHK-1.14-A is DONE — consumers cannot import before the shared exports exist"
  - "Never advance to CHK-1.14-E before CHK-1.14-B is DONE — error schema consumers cannot extend before base is exported"
halt_conditions:
  plan_reality_mismatch: true
  missing_required_plan_information: true
  autonomous_decision_required: true
user_notification_required: true
---

# Task 1.14: Schema Contract Layer Centralization
[INTENT: ANWEISUNG]

## Goal
[INTENT: KONTEXT]

This task centralizes two categories of duplicated schema-layer constructs that currently exist as independent local declarations in multiple endpoint files. First, the Zod output schemas `InspectionResumeAdmissionSchema` and `InspectionResumeMetadataSchema` — and the three common superRefine checks that accompany them — are defined identically in five preview-family inspection endpoint schemas and should be exported from the canonical shared resume contract instead. Second, a base error schema `BatchOperationErrorBaseSchema` capturing the common `{ path, error }` structure should be added to the shared inspection metadata contract so the three metadata-family endpoint schemas can extend it instead of repeating inline Zod object declarations.

No handler behavior, runtime logic, guardrail policy, or business rule is changed by this task. All changes are pure schema-layer structural extractions with no breaking changes to the public request or response surfaces.

## Expected Result
[INTENT: KONTEXT]

- `InspectionResumeAdmissionSchema`, `InspectionResumeMetadataSchema`, `InspectionCompletionOnlyAdmissionSchema`, `InspectionCompletionOnlyResumeMetadataSchema`, and `applyCommonResumeSchemaRefinement` are exported from `src/domain/shared/resume/inspection-resume-contract.ts`.
- Five preview-family endpoint schemas import these exports from the shared contract and have their local const duplicates removed.
- `count-lines/schema.ts` uses the restricted completion-only variants.
- `BatchOperationErrorBaseSchema` is exported from `src/domain/inspection/shared/filesystem-entry-metadata-contract.ts`.
- Three metadata-family schemas use `BatchOperationErrorBaseSchema` (two directly, one via `.extend()`).

## Preconditions
[INTENT: CONSTRAINT]

- No preconditions beyond task 1.13 being in progress (this task is ISOLATED and does not share files with 1.13's remaining handler work).
- The shared resume contract and all target schema files must be readable at task start.

## Execution Metadata Register
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Task ID | `1.14` | Eindeutige Aufgabenkennung |
| Classification | `ISOLATED` | No file conflicts with other tasks in this plan |
| Information Completeness Mode | `FULLY_SPECIFIED` | All schema shapes, names, placements, and import paths are bound below |
| Implementation Scenarios | `FUNCTION_CREATION, OBJECT_CREATION, SCHEMA_CHANGE, PROPERTY_ADDITION` | Adding shared schema exports and updating consumer imports |
| Execution Surface Band | `YELLOW` | 11 files, all schema-layer, one stable shared contract anchor |
| Primary Split Axis | `none` | Not split; all schema layer |
| Single-Agent Viability Note | All 11 files are schema-only; per-file cognitive load is low and bounded |
| Parent Orchestration | `.plan/1-runtime-architecture-refactors/orchestration.md` |  |
| Blocked By | `none` |  |
| Requires Upstream Plan Re-Anchoring | `yes` | Re-read 1.13 handoff to confirm which schema files were already modified there |
| Runtime Discovery Allowed | `no` |  |
| Checkpoint Strategy | `SEQUENTIAL_CHECKPOINTS_REQUIRED` | Shared exports must be added before consumers can import |
| Durable Skip Authority Only | `true` | Only file edits producing verifiable artifacts create skip authority |
| Ephemeral Freshness Steps Present | `yes` | Initial full-file re-anchor reads |
| User Notification Required On Gap/Mismatch | `true` |  |
| Persona Generation Required | `true` |  |
| Persona Generation Mode | `runtime_regenerate` |  |
| Persona Primary Domain Kernel | `TypeScript Node.js MCP Server Domain Architecture — Schema Contract Centralization` |  |
| Persona Current Task Specialization | `Inspection endpoint Zod schema deduplication` |  |
| Persona Runtime Review Required | `true` |  |
| Persona Source Load Policy | `DEFERRED_UNTIL_PRE_EXECUTION_STATE_SNAPSHOT_CLOSED` |  |
| Persona State Disclosure Contract Present | `true` |  |
| Persona Embedded Creator Execution Contract Present | `true` |  |
| Persona Write Permission Default | `BLOCKED` |  |
| Persona DX Log Contract Present | `true` |  |
| Persona Review Binding Contract Present | `true` |  |
| Persona Creator Proof Contract Present | `true` |  |

## Task Contract Closure
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Decision Surfaces Closed | `true` | All schema shapes, function signatures, import paths, and placements are fully bound |
| Unresolved Determinable Decisions | `0` | No open decisions remain |
| Expected Execute-Agent Questions | `0` | Agent needs no follow-up on determinable surfaces |

## Decision Surface Register
[INTENT: REFERENZ]

| Surface | Status | Binding Source | Notes |
|---------|--------|----------------|-------|
| `naming` | `BOUND_LITERAL` | `plan-output` | All new export names listed in Authoritative Literal Bindings |
| `path_and_placement` | `BOUND_LITERAL` | `plan-output` | All target files in files_modified |
| `literal_values_and_budgets` | `BOUND_LITERAL` | `project-state` | Schema enum values derived from existing exported constants |
| `defaults_and_fallbacks` | `NOT_APPLICABLE` | — | No defaults added or changed |
| `types_and_shapes` | `BOUND_LITERAL` | `plan-output` | Full Zod schemas in Authoritative Code / Contract Surfaces |
| `function_signatures_and_returns` | `BOUND_LITERAL` | `plan-output` | applyCommonResumeSchemaRefinement signature in Authoritative Code surfaces |
| `object_schema_and_mapper_contracts` | `BOUND_LITERAL` | `plan-output` | All schema object shapes specified |
| `dependencies_and_import_surfaces` | `BOUND_LITERAL` | `plan-output` | Import paths and statement forms in Authoritative Literal Bindings |
| `validation_and_boundary_rules` | `BOUND_LITERAL` | `plan-output` | superRefine logic split: common checks → helper; primary-field checks remain inline |
| `consumer_and_callsite_bindings` | `BOUND_LITERAL` | `plan-output` | All 11 consumer files listed in files_modified |

## Authoritative Literal Bindings
[INTENT: REFERENZ]

*(See YAML frontmatter `authoritative_literal_bindings` section for the complete list)*

Key bindings summary:
- **New exports in `inspection-resume-contract.ts`**: `InspectionResumeAdmissionSchema`, `InspectionResumeMetadataSchema`, `InspectionCompletionOnlyAdmissionSchema`, `InspectionCompletionOnlyResumeMetadataSchema`, `applyCommonResumeSchemaRefinement` — all exact shapes in Code/Contract Surfaces below
- **New zod import**: `import { z } from "zod";` added to `inspection-resume-contract.ts`
- **New base schema in `filesystem-entry-metadata-contract.ts`**: `BatchOperationErrorBaseSchema`, `BatchOperationError`
- **Consumer imports**: preview-family schemas add `InspectionResumeAdmissionSchema, InspectionResumeMetadataSchema, applyCommonResumeSchemaRefinement` to existing `@domain/shared/resume/inspection-resume-contract` import
- **count-lines import**: adds `InspectionCompletionOnlyAdmissionSchema, InspectionCompletionOnlyResumeMetadataSchema, applyCommonResumeSchemaRefinement`
- **Error base consumers**: add `BatchOperationErrorBaseSchema` from `@domain/inspection/shared/filesystem-entry-metadata-contract`

## Authoritative Code / Contract Surfaces
[INTENT: REFERENZ]

*(See YAML frontmatter `authoritative_code_surfaces` section for complete schema bodies)*

All complete schema shapes and the helper function signature are specified in the YAML frontmatter. The execute agent MUST use these exact shapes without deviation.

## Execute-Agent Zero-Question Gate
[INTENT: CONSTRAINT]

- **Gate Result:** `PASS`
- **Checked Domains:** naming, path_and_placement, types_and_shapes, function_signatures_and_returns, dependencies_and_import_surfaces, validation_and_boundary_rules, consumer_and_callsite_bindings
- **Rule:** A fresh Execute-Agent reading only this task file plus the listed authoritative references needs no follow-up questions for any determinable surface.

## Authoritative References
[INTENT: REFERENZ]

| Path | Kind | Must Read Before Execution | Purpose | Referenced By |
|------|------|---------------------------|---------|---------------|
| `src/domain/shared/resume/inspection-resume-contract.ts` | business-file | true | Current content before new exports are added | CHK-1.14-A |
| `src/domain/inspection/shared/filesystem-entry-metadata-contract.ts` | business-file | true | Current content before BatchOperationErrorBaseSchema is added | CHK-1.14-B |
| `src/domain/inspection/find-paths-by-name/schema.ts` | business-file | true | Local schema copies to remove | CHK-1.14-C |
| `src/domain/inspection/find-files-by-glob/schema.ts` | business-file | true | Same | CHK-1.14-C |
| `src/domain/inspection/list-directory-entries/schema.ts` | business-file | true | Same | CHK-1.14-C |
| `src/domain/inspection/search-file-contents-by-regex/schema.ts` | business-file | true | Modified in 1.13 session; re-anchor before editing | CHK-1.14-C |
| `src/domain/inspection/search-file-contents-by-fixed-string/schema.ts` | business-file | true | Same as regex schema note | CHK-1.14-C |
| `src/domain/inspection/count-lines/schema.ts` | business-file | true | Uses restricted variant | CHK-1.14-D |
| `src/domain/inspection/get-file-checksums/schema.ts` | business-file | true | Inline error schema to replace | CHK-1.14-E |
| `src/domain/inspection/get-path-metadata/schema.ts` | business-file | true | Same | CHK-1.14-E |
| `src/domain/inspection/verify-file-checksums/schema.ts` | business-file | true | Extended error schema to update | CHK-1.14-E |
| `.plan/1-runtime-architecture-refactors/1.13-resume-session-remodel-handoff-and-remaining-work.md` | plan-file | true | Documents which schema files were already modified in 1.13 session |  |

## Resume Validation Contract
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Lowest Level Truth Surface | `checkpoint-ledger` | CHK-1.14-A through CHK-1.14-E |
| Task Lifecycle Truth Surface | `.plan/1-runtime-architecture-refactors/orchestration.md` |  |
| Global Frontier Truth Surface | `PLAN.md` |  |
| Conflict Requires User Confirmation | `true` |  |
| Partial Progress Requires User Decision | `true` |  |
| Durable Skip Authority Only | `true` |  |
| Ephemeral Freshness Steps Must Re-Execute | `true` |  |

## Artifact Progress Inference Rules
[INTENT: REFERENZ]

- If `InspectionResumeAdmissionSchema` is exported from `inspection-resume-contract.ts`, CHK-1.14-A is DONE and must not be re-applied.
- If `BatchOperationErrorBaseSchema` is exported from `filesystem-entry-metadata-contract.ts`, CHK-1.14-B is DONE.
- If a preview-family schema no longer contains a local `const InspectionResumeAdmissionSchema = z.object(...)` declaration, that file's portion of CHK-1.14-C is complete.
- If `count-lines/schema.ts` imports `InspectionCompletionOnlyAdmissionSchema`, CHK-1.14-D is DONE.
- If `get-file-checksums/schema.ts` errors array uses `z.array(BatchOperationErrorBaseSchema)`, its portion of CHK-1.14-E is DONE.
- Re-anchor reads (freshness steps) never constitute durable skip authority — always re-execute at session start.

## User Resume Decision Gate
[INTENT: CONSTRAINT]

- **Required on Partial Progress:** `true`
- **Required on State Conflict:** `true`
- **Allowed User Choices:** `ACCEPT_AND_CONTINUE` / `REVERIFY_EXISTING_OUTPUTS`

## Task Completion Order Contract
[INTENT: REFERENZ]

- **Ordered Phase Sequence:** `task_file_local_completion_closure` → `parent_orchestration_status_update` → `dependency_resolution_and_unblock` → `unit_and_plan_completion_cascade`
- **Parent Status Write Blocked Until Task File Done:** `true`
- **Plan Update Blocked Until Parent Sync:** `true`

## Current Target Binding Contract
[INTENT: REFERENZ]

- **Binding Surface:** `active_task_file`
- **Proof Scope:** `task_file_local`
- **Invalidate On Target Or Frontier Change:** `true`

## Task Local Completion Ledger
[INTENT: REFERENZ]

| Completion Unit | Status Requirement | Meaning |
|-----------------|-------------------|---------|
| `actions_executed` | `PASS` | All 5 checkpoints completed and verified |
| `acceptance_criteria_pass` | `PASS` | All acceptance criteria below satisfied |
| `required_artifacts_materialized_or_verified` | `PASS` | All 11 files modified and verified |
| `checkpoint_or_artifact_progress_synced` | `PASS` | Ledger reflects actual file state |
| `persona_write_permission_green_go` | `PASS` | Persona gate released |

## Task Local Pre-Write Gate Report Contract
[INTENT: REFERENZ]

| Gate Input | Required Status | Meaning |
|------------|-----------------|---------|
| `current_target_binding` | `PASS` | Proof surfaces belong to this task file |
| `ordered_phase_contract` | `PASS` | Checkpoints executed in required order |
| `task_local_completion_ledger` | `PASS` | All units closed |
| `no_heuristic_exit_governance` | `PASS` | No broader PASS substituted |
| `final_write_allowed` | `PASS` | Task may be marked DONE |

## No-Heuristic Exit Governance
[INTENT: CONSTRAINT]

- Task MUST NOT be marked DONE from plausibility alone.
- A broader PASS in parent orchestration or PLAN.md MUST NOT substitute for local completion proof.
- If any checkpoint is not verified against actual file artifacts, the agent MUST stop before advancing orchestration.

## Persona Derivation Manifest
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Persona Generation Required | `true` | Task requires task-specific persona |
| Persona Generation Mode | `runtime_regenerate` | Persona generated fresh at execute-time |
| Persona Source Prompt Path | `src/personas/it/__prompts__/silent/enterprise/creation/prompt.md` | Authoritative source |
| Persona Primary Domain Kernel | `TypeScript Node.js MCP Server Domain Architecture — Schema Contract Centralization` |  |
| Persona Secondary Specializations | `Zod schema composition, DDD SSOT enforcement, structural extraction refactoring` |  |
| Persona Current Task Specialization | `Inspection endpoint schema deduplication — shared Zod exports and base error schema` |  |
| Persona Generation Lineage | `Derived from task contract: schema-layer only, TypeScript + Zod, no runtime change, DDD` |  |
| Persona Runtime Review Required | `true` |  |

## Persona Creator Execution Proof Contract
[INTENT: REFERENZ]

*(See YAML frontmatter `persona_creator_execution_proof_contract` for complete specification)*

## Persona Source Load Policy
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Source Loading Mode | `DEFERRED_UNTIL_PRE_EXECUTION_STATE_SNAPSHOT_CLOSED` | Load only after snapshot closed |
| Early Source Load Forbidden | `true` | No early load |
| Late Load Trigger | `after pre_execution_state_snapshot_closed and immediately before execute_time_persona_subprocess` |  |
| Transitive Source Chain Must Be Closed After Load | `true` |  |

## Persona State Disclosure Contract, Embedded Creator Execution Contract, Write Gate Contract, DX Log Contract, Review Binding Contract
[INTENT: REFERENZ]

*(See YAML frontmatter for complete specifications of all persona contract surfaces)*

## Persona DX Snapshot
[INTENT: KONTEXT]

- **Persona Title:** `TypeScript Schema Contract Centralization Architect — MCP Inspection Domain`
- **Persona Summary:** An enterprise TypeScript architect specializing in DDD schema-layer SSOT enforcement, Zod schema composition, and structural extraction refactoring with strict no-breaking-change discipline.
- **Persona Weighting Note:** Primary domain kernel (MCP inspection domain schema architecture) dominates over language specialization (TypeScript/Zod) and operation type (centralization refactoring).

## Context
[INTENT: KONTEXT]

- **Domain:** `src/domain/shared/resume/`, `src/domain/inspection/shared/`, `src/domain/inspection/*/schema.ts` files
- **Technical Context:** TypeScript + Zod, MCP server inspection endpoint schemas. `inspection-resume-contract.ts` currently exports only TypeScript interfaces and constants — no Zod schemas. Five endpoint schemas each define identical local Zod blocks: `const InspectionResumeAdmissionSchema` and `const InspectionResumeMetadataSchema`. Additionally, `count-lines/schema.ts` defines a restricted subset. Three metadata-family schemas define inline `z.object({ path, error })` error arrays.
- **Business Context:** Structural deduplication. No public API shape changes. No runtime behavior changes.
- **Architectural Context:** DDD single source of truth. The resume contract module already owns the canonical type authority; adding Zod schemas closes the remaining gap where five endpoint schemas maintain independent copies.
- **Plan Context Dependencies:** Task 1.13 documents which schema files were already modified in an interrupted session (search-regex and search-fixed-string schemas). Re-anchor those files before editing.
- **Runtime Discovery Context:** None required.

## Execution Surface Assessment
[INTENT: REFERENZ]

- **Execution Surface Band:** `YELLOW`
- **Primary Split Axis:** `none`
- **Hard-Gap Result:** 11 files — borderline; all confined to schema layer only
- **Single-Agent Viability Rationale:** All modifications are in schema files exclusively. The shared contract is the single anchor point. Per-file changes are mechanical: add export or replace local const block with import. No multi-layer reasoning required.

## Information Units
[INTENT: SPEZIFIKATION]

### IU-001: Zod schema exports for preview-family admission and resume metadata

**Type:** `object`

**Description:** Add `InspectionResumeAdmissionSchema`, `InspectionResumeMetadataSchema`, `InspectionCompletionOnlyAdmissionSchema`, `InspectionCompletionOnlyResumeMetadataSchema`, and `applyCommonResumeSchemaRefinement` to `inspection-resume-contract.ts` so consumers can import from one canonical location.

**Ist-Zustand:** `inspection-resume-contract.ts` exports only TypeScript interfaces, types, constants, and runtime functions. No Zod schemas. Five endpoint schemas each locally define two identical `z.object()` blocks.

**Soll-Zustand:** `inspection-resume-contract.ts` exports the four Zod schemas and the helper function. Consumer schemas import and remove local copies.

**Architectural Rationale:** The runtime contract is already the single source of truth for types and constants. Zod schemas should be co-located there to prevent drift when the contract surface changes.

**Exact Target:** `src/domain/shared/resume/inspection-resume-contract.ts` — add `import { z } from "zod";` at top; add four schema exports and one function export after the existing type declarations.

**Implementation Contract:** Exact schema bodies are in YAML `authoritative_code_surfaces`. The function body is fully specified. No deviations allowed.

**Checkpoint Relevance:**
- **Checkpoint Strategy:** `SEQUENTIAL_CHECKPOINTS_REQUIRED`
- **Checkpoint or Artifact Impact:** CHK-1.14-A — must be completed before CHK-1.14-C can begin

**Scenario-Specific Specification:**

| Field | Value |
|------|-------|
| Target Artifact | `InspectionResumeAdmissionSchema, InspectionResumeMetadataSchema, InspectionCompletionOnlyAdmissionSchema, InspectionCompletionOnlyResumeMetadataSchema, applyCommonResumeSchemaRefinement` |
| Full Target Shape / Value | See YAML `authoritative_code_surfaces` |
| Authoritative Source | Plan-output |
| Determination Rule | Derived from matching the exact field structure already present in all 5 local schema copies |
| Exact Placement | `src/domain/shared/resume/inspection-resume-contract.ts` after existing type/interface exports |
| Consumers | All 5 preview-family schemas, count-lines schema |
| No-Breaking Constraint | No existing export may be removed; only additions allowed |

---

### IU-002: Consumer schema de-duplication (5 preview-family schemas)

**Type:** `schema`

**Description:** In each of the five preview-family endpoint schemas, remove the local `const InspectionResumeAdmissionSchema` and `const InspectionResumeMetadataSchema` declarations and replace them with imports from the shared contract. Simplify the `superRefine` call by delegating the three common checks to `applyCommonResumeSchemaRefinement`.

**Ist-Zustand:** Each schema has a local const block of ~30 lines defining the two schemas independently. The `superRefine` function contains 5 checks: 2 endpoint-specific (roots required, primary field required) and 3 common (mode-without-token, missing-mode, query-fields-on-resume).

**Soll-Zustand:** Local const blocks removed. Import statement extended. `superRefine` retains 2 endpoint-specific checks and calls `applyCommonResumeSchemaRefinement(args, ctx, hasQueryDefiningFields)` for the 3 common ones.

**Architectural Rationale:** Eliminates five identical copies of the same Zod object structure.

**Exact Target:** Files listed in CHK-1.14-C artifact list.

**Implementation Contract:** See `authoritative_code_surfaces` for the exact simplified superRefine shape (example shown for search-regex). Each endpoint keeps its own primary-field check but delegates the three common checks to the helper.

**Checkpoint Relevance:**
- **Checkpoint Strategy:** `SEQUENTIAL_CHECKPOINTS_REQUIRED`
- **Checkpoint or Artifact Impact:** CHK-1.14-C — must not begin before CHK-1.14-A is DONE

**Scenario-Specific Specification:**

| Field | Value |
|------|-------|
| Target Artifact | 5 endpoint schema files |
| Full Target Shape / Value | Remove local const blocks; import from shared; simplify superRefine |
| Authoritative Source | Plan-output |
| Determination Rule | Mechanical extraction — same change applied to each file |
| Exact Placement | Each schema file as per the files_modified list |
| Consumers | Not changed externally — same public schema shape |
| No-Breaking Constraint | Result schema validates identically to the previous local schema |

---

### IU-003: count-lines schema restricted variant usage

**Type:** `schema`

**Description:** Update `count-lines/schema.ts` to import `InspectionCompletionOnlyAdmissionSchema` and `InspectionCompletionOnlyResumeMetadataSchema` from the shared contract instead of the full preview-capable variants.

**Ist-Zustand:** `count-lines/schema.ts` has a local `InspectionResumeAdmissionSchema` with `[INLINE, COMPLETION_BACKED_REQUIRED, NARROWING_REQUIRED]` (no PREVIEW_FIRST) and a local `InspectionResumeMetadataSchema` with `supportedResumeModes: [COMPLETE_RESULT]` only.

**Soll-Zustand:** Imports `InspectionCompletionOnlyAdmissionSchema` and `InspectionCompletionOnlyResumeMetadataSchema` from shared. Local const blocks removed.

**Architectural Rationale:** count-lines is completion-backed-only per the architecture. The restricted variants encode this distinction at the shared contract level.

**Checkpoint Relevance:**
- **Checkpoint or Artifact Impact:** CHK-1.14-D

**Scenario-Specific Specification:**

| Field | Value |
|------|-------|
| Target Artifact | `src/domain/inspection/count-lines/schema.ts` |
| Full Target Shape / Value | Import restricted variants; remove local const blocks; delegate common superRefine checks |
| No-Breaking Constraint | Schema must still reject PREVIEW_FIRST in outcome; must still reject anything other than complete-result in supportedResumeModes |

---

### IU-004: BatchOperationErrorBaseSchema for metadata-family endpoint schemas

**Type:** `schema`

**Description:** Add `BatchOperationErrorBaseSchema = z.object({ path: z.string(), error: z.string() })` and its inferred type `BatchOperationError` to `filesystem-entry-metadata-contract.ts`. Update `get-file-checksums/schema.ts` and `get-path-metadata/schema.ts` to use it directly for their errors arrays. Update `verify-file-checksums/schema.ts` to use it with `.extend({ expectedHash: z.string() })`.

**Ist-Zustand:** Three schema files each inline `z.object({ path: z.string(), error: z.string() })` (or with `expectedHash` added) independently in their respective `errors` array definitions.

**Soll-Zustand:** One canonical base schema in the shared metadata contract. Two consumers use it directly. One extends it.

**Architectural Rationale:** DDD canonical property surface for the batch error reporting shape across the metadata inspection family.

**Checkpoint Relevance:**
- **Checkpoint or Artifact Impact:** CHK-1.14-B (add base), CHK-1.14-E (update consumers)

**Scenario-Specific Specification:**

| Field | Value |
|------|-------|
| Target Artifact | `BatchOperationErrorBaseSchema, BatchOperationError` in filesystem-entry-metadata-contract.ts |
| Full Target Shape / Value | `z.object({ path: z.string(), error: z.string() })` |
| Authoritative Source | Plan-output |
| Determination Rule | Derived from identical inline schemas in 2 of 3 consumer files |
| Exact Placement | After `FileSystemEntryMetadataSchema` export in filesystem-entry-metadata-contract.ts |
| Consumers | get-file-checksums/schema.ts, get-path-metadata/schema.ts (direct); verify-file-checksums/schema.ts (extended) |
| No-Breaking Constraint | verify-file-checksums must preserve all current error fields: { path, expectedHash, error } |

## Actions
[INTENT: ANWEISUNG]

1. **Re-anchor all target files before editing begins**
   - **What:** Read all 11 files in files_modified plus `1.13-resume-session-remodel-handoff-and-remaining-work.md`
   - **Where:** As listed in Authoritative References
   - **How:** Full read of each file before any edit. This is an EPHEMERAL_FRESHNESS_STEP — does not advance any checkpoint.
   - **Why:** Prevents editing stale content; confirms which schema files the 1.13 session already modified
   - **Inputs / References:** All authoritative references listed
   - **Output Contract:** Internal re-anchored understanding of all file states
   - **Checkpoint Impact:** None (ephemeral)
   - **Verification:** Confirm InspectionResumeAdmissionSchema does NOT yet exist in inspection-resume-contract.ts; confirm local schema copies still exist in consumer schemas
   - **Stop if:** Any file is unreadable or its actual content materially contradicts the plan; inform user

2. **Add Zod import and schema exports to inspection-resume-contract.ts (CHK-1.14-A)**
   - **What:** Add `import { z } from "zod";` and export all four schema objects plus the helper function
   - **Where:** `src/domain/shared/resume/inspection-resume-contract.ts`
   - **How:** Add zod import at the top of the file. Add the four schema exports and the helper function after existing type/interface/function declarations. Use the exact shapes from `authoritative_code_surfaces`.
   - **Why:** Establishes the canonical Zod schema location before consumers can import from it
   - **Inputs / References:** Current file content from re-anchor step
   - **Output Contract:** File exports InspectionResumeAdmissionSchema, InspectionResumeMetadataSchema, InspectionCompletionOnlyAdmissionSchema, InspectionCompletionOnlyResumeMetadataSchema, and applyCommonResumeSchemaRefinement without removing any existing export
   - **Checkpoint Impact:** COMPLETES CHK-1.14-A
   - **Verification:** File can be imported by TypeScript consumers without error; new exports are present; no existing export removed
   - **Stop if:** Cannot add zod import without conflict; inform user

3. **Add BatchOperationErrorBaseSchema to filesystem-entry-metadata-contract.ts (CHK-1.14-B)**
   - **What:** Export `BatchOperationErrorBaseSchema` and `BatchOperationError` type
   - **Where:** `src/domain/inspection/shared/filesystem-entry-metadata-contract.ts`
   - **How:** Add after `FileSystemEntryMetadataSchema` export. File already imports `z from "zod"` — no new import needed.
   - **Why:** Establishes the base error schema before metadata-family consumers can use it
   - **Inputs / References:** Current file content; exact schema shape from `authoritative_code_surfaces`
   - **Output Contract:** File exports `BatchOperationErrorBaseSchema` and `BatchOperationError`
   - **Checkpoint Impact:** COMPLETES CHK-1.14-B
   - **Verification:** New exports visible; existing exports untouched
   - **Stop if:** File structure differs materially from what re-anchor shows; inform user

4. **Update 5 preview-family consumer schemas (CHK-1.14-C)**
   - **What:** Remove local schema const blocks and simplify superRefine in each of the five schemas
   - **Where:** find-paths-by-name, find-files-by-glob, list-directory-entries, search-file-contents-by-regex, search-file-contents-by-fixed-string schemas
   - **How:** For each file: (a) remove the local `const InspectionResumeAdmissionSchema` and `const InspectionResumeMetadataSchema` declarations (~30 lines each); (b) add `InspectionResumeAdmissionSchema, InspectionResumeMetadataSchema, applyCommonResumeSchemaRefinement` to the existing `@domain/shared/resume/inspection-resume-contract` import; (c) in the superRefine, keep endpoint-specific checks (roots required, primary-field required), and replace the three common checks (mode-without-token, missing-mode, query-fields-on-resume) with a call to `applyCommonResumeSchemaRefinement(args, ctx, hasQueryDefiningFields)`. Note: search-regex and search-fixed-string schemas were already modified in the 1.13 session — re-anchor them carefully to understand current state before editing.
   - **Why:** Eliminates five identical duplicate schema definitions
   - **Inputs / References:** Re-anchored file contents; `authoritative_code_surfaces` simplified superRefine shape
   - **Output Contract:** Each file imports from shared; no local schema const blocks for these two schemas remain; superRefine validates identically
   - **Checkpoint Impact:** COMPLETES CHK-1.14-C for each file as it is updated
   - **Verification:** Public schema validation behavior unchanged. Each schema still rejects: base request without roots, base request without primary field, resume request without mode, base request with orphaned mode, resume request with query fields. The validation messages must match exactly what was there before.
   - **Stop if:** Current file state in any of the five schemas differs materially from what the plan describes (e.g., local schema copies already removed); confirm with user before continuing

5. **Update count-lines/schema.ts (CHK-1.14-D)**
   - **What:** Replace local schema const blocks with imports of restricted variants
   - **Where:** `src/domain/inspection/count-lines/schema.ts`
   - **How:** Remove local InspectionResumeAdmissionSchema const (which currently omits PREVIEW_FIRST). Remove local InspectionResumeMetadataSchema const (which currently uses only COMPLETE_RESULT). Add `InspectionCompletionOnlyAdmissionSchema, InspectionCompletionOnlyResumeMetadataSchema, applyCommonResumeSchemaRefinement` to the existing `@domain/shared/resume/inspection-resume-contract` import. Keep the endpoint-specific superRefine checks; delegate the three common checks to the helper.
   - **Why:** count-lines uses restricted variants that must not accidentally include PREVIEW_FIRST
   - **Output Contract:** Schema still validates count-lines-specific constraints; outcome enum remains restricted; supportedResumeModes remains COMPLETE_RESULT only
   - **Checkpoint Impact:** COMPLETES CHK-1.14-D
   - **Verification:** count-lines schema still rejects resumeMode=next-chunk; still rejects preview-first as an admission outcome
   - **Stop if:** Current file state differs from plan; inform user

6. **Update metadata-family error arrays (CHK-1.14-E)**
   - **What:** Replace inline z.object({ path, error }) declarations with BatchOperationErrorBaseSchema references
   - **Where:** get-file-checksums/schema.ts, get-path-metadata/schema.ts, verify-file-checksums/schema.ts
   - **How:** Add `BatchOperationErrorBaseSchema` to the import from `@domain/inspection/shared/filesystem-entry-metadata-contract` in each file. In get-file-checksums and get-path-metadata: replace `z.array(z.object({ path: z.string(), error: z.string() }))` with `z.array(BatchOperationErrorBaseSchema)`. In verify-file-checksums: replace the inline z.object({ path, expectedHash, error }) with `z.array(BatchOperationErrorBaseSchema.extend({ expectedHash: z.string(), ... }))` preserving all original fields in the extension.
   - **Why:** Eliminates duplicate { path, error } inline definitions across metadata family
   - **Output Contract:** All three schemas validate identically to before; verify-file-checksums still validates { path, expectedHash, error }; no existing field removed
   - **Checkpoint Impact:** COMPLETES CHK-1.14-E as each file is updated
   - **Verification:** Error array shape unchanged; all existing fields preserved (especially expectedHash in verify-file-checksums)
   - **Stop if:** verify-file-checksums has additional error fields beyond { path, expectedHash, error } that were not accounted for in the plan; inform user before proceeding

## Checkpoint Definitions
[INTENT: REFERENZ]

| Checkpoint ID | Kind | Completion Signal | Produces Artifacts | Resume Meaning |
|---------------|------|-------------------|--------------------|----------------|
| `CHK-1.14-A` | `sequential_step` | 4 new schema exports + helper function present in inspection-resume-contract.ts | `src/domain/shared/resume/inspection-resume-contract.ts` | Skip adding the shared schemas; proceed directly to CHK-1.14-B or CHK-1.14-C |
| `CHK-1.14-B` | `sequential_step` | BatchOperationErrorBaseSchema and BatchOperationError exported from filesystem-entry-metadata-contract.ts | `src/domain/inspection/shared/filesystem-entry-metadata-contract.ts` | Skip adding base schema; proceed to CHK-1.14-E |
| `CHK-1.14-C` | `artifact_milestone` | All 5 preview-family schema files have local const blocks removed and import from shared | 5 schema files | Skip any files that have already had local blocks removed; continue with remaining |
| `CHK-1.14-D` | `sequential_step` | count-lines schema imports restricted variants and no longer has local const blocks | `src/domain/inspection/count-lines/schema.ts` | Skip count-lines schema change |
| `CHK-1.14-E` | `artifact_milestone` | All 3 metadata-family error arrays use BatchOperationErrorBaseSchema | 3 schema files | Skip any files already updated; continue with remaining |

## Execution Progress Ledger
[INTENT: REFERENZ]

| Checkpoint ID | Status | Evidence | Notes |
|---------------|--------|----------|-------|
| `CHK-1.14-A` | `PENDING` | — | — |
| `CHK-1.14-B` | `PENDING` | — | — |
| `CHK-1.14-C` | `PENDING` | — | — |
| `CHK-1.14-D` | `PENDING` | — | — |
| `CHK-1.14-E` | `PENDING` | — | — |

## Resume Conflict Resolution Rules
[INTENT: CONSTRAINT]

- If ledger says CHK-1.14-A DONE but the shared exports are absent from the file, STOP and request user confirmation.
- If CHK-1.14-C shows partial completion (some files updated, others not), continue only with the files not yet updated; do not re-apply changes to files already missing local const blocks.
- Already materialized changes must not be blindly re-executed.

## Checkpoint Update Protocol
[INTENT: ANWEISUNG]

1. Mark a checkpoint IN_PROGRESS when the execute agent starts editing the first file for that checkpoint group.
2. Mark a checkpoint DONE immediately after all files for that checkpoint are edited and verified.
3. Never begin CHK-1.14-C before CHK-1.14-A is DONE.
4. Never begin CHK-1.14-E before CHK-1.14-B is DONE.
5. Never use the checkpoint ledger as a substitute for the task lifecycle status in the parent orchestration.

## Acceptance Criteria
[INTENT: REFERENZ]

- [ ] `InspectionResumeAdmissionSchema` is exported from `src/domain/shared/resume/inspection-resume-contract.ts` with the exact shape specified in authoritative_code_surfaces
- [ ] `InspectionResumeMetadataSchema` is exported from the same file with the exact shape
- [ ] `InspectionCompletionOnlyAdmissionSchema` is exported (INLINE + COMPLETION_BACKED_REQUIRED + NARROWING_REQUIRED only)
- [ ] `InspectionCompletionOnlyResumeMetadataSchema` is exported (COMPLETE_RESULT mode only)
- [ ] `applyCommonResumeSchemaRefinement` is exported with the exact function signature specified
- [ ] No existing export in `inspection-resume-contract.ts` has been removed or changed
- [ ] `import { z } from "zod";` is present in `inspection-resume-contract.ts`
- [ ] `BatchOperationErrorBaseSchema` and `BatchOperationError` are exported from `filesystem-entry-metadata-contract.ts`
- [ ] No existing export in `filesystem-entry-metadata-contract.ts` has been removed or changed
- [ ] All 5 preview-family schemas have no local `const InspectionResumeAdmissionSchema` or `const InspectionResumeMetadataSchema` declarations
- [ ] All 5 preview-family schemas import from `@domain/shared/resume/inspection-resume-contract`
- [ ] All 5 preview-family schemas delegate the three common superRefine checks to `applyCommonResumeSchemaRefinement`
- [ ] `count-lines/schema.ts` uses `InspectionCompletionOnlyAdmissionSchema` and `InspectionCompletionOnlyResumeMetadataSchema`
- [ ] `get-file-checksums/schema.ts` errors array uses `z.array(BatchOperationErrorBaseSchema)`
- [ ] `get-path-metadata/schema.ts` errors array uses `z.array(BatchOperationErrorBaseSchema)`
- [ ] `verify-file-checksums/schema.ts` errors array uses `z.array(BatchOperationErrorBaseSchema.extend({...}))` and preserves `expectedHash`
- [ ] All validation behavior (error messages, rejection conditions) is identical before and after
- [ ] No public request or response surface shape has changed

## Stop Conditions & User Escalation
[INTENT: CONSTRAINT]

- **Plan-vs-Reality Mismatch:** If a target schema file already has its local const blocks removed, or already imports from shared, stop and confirm with user whether to skip or verify.
- **Missing Required Plan Information:** If the verify-file-checksums errors array contains fields beyond `{ path, expectedHash, error }` that this plan did not account for, stop and inform user before proceeding.
- **Autonomous Decision Required:** If the superRefine endpoint-specific primary-field name for any endpoint does not match what the plan assumes, stop and inform user.
- **Allowed Runtime Discovery:** None.
- **User Message Contract:** `Plan says [X state] / current reality is [Y state] / [what info is missing or contradictory] / continuing would require an autonomous decision`.

## Post-Completion Instructions
[INTENT: ANWEISUNG]

1. Mark this task file locally `DONE` and close the task-local completion ledger plus pre-write gate report.
2. Mark task 1.14 [x] in parent orchestration: `.plan/1-runtime-architecture-refactors/orchestration.md`
3. No dependents are directly blocked by this task (ISOLATED). Verify tasks 1.15-1.17 remain unblocked.
4. If unit 1 has all tasks DONE after 1.14: mark unit DONE in PLAN.md only after all 4 new tasks (1.14-1.17) are complete.

## File Path Index
[INTENT: REFERENZ]

| # | Path | Kind | Why Relevant | Must Read Before Execution |
|---|------|------|--------------|----------------------------|
| 1 | `src/domain/shared/resume/inspection-resume-contract.ts` | business-file | Primary target — receives new Zod exports | true |
| 2 | `src/domain/inspection/shared/filesystem-entry-metadata-contract.ts` | business-file | Receives BatchOperationErrorBaseSchema | true |
| 3 | `src/domain/inspection/find-paths-by-name/schema.ts` | business-file | Preview-family consumer — local blocks to remove | true |
| 4 | `src/domain/inspection/find-files-by-glob/schema.ts` | business-file | Same | true |
| 5 | `src/domain/inspection/list-directory-entries/schema.ts` | business-file | Same | true |
| 6 | `src/domain/inspection/search-file-contents-by-regex/schema.ts` | business-file | Modified in 1.13 session; re-anchor first | true |
| 7 | `src/domain/inspection/search-file-contents-by-fixed-string/schema.ts` | business-file | Modified in 1.13 session; re-anchor first | true |
| 8 | `src/domain/inspection/count-lines/schema.ts` | business-file | Uses restricted schema variants | true |
| 9 | `src/domain/inspection/get-file-checksums/schema.ts` | business-file | Inline error schema to replace | true |
| 10 | `src/domain/inspection/get-path-metadata/schema.ts` | business-file | Same | true |
| 11 | `src/domain/inspection/verify-file-checksums/schema.ts` | business-file | Extended error schema to update | true |
| 12 | `.plan/1-runtime-architecture-refactors/1.13-resume-session-remodel-handoff-and-remaining-work.md` | plan-file | Documents 1.13 session edits to schema files | true |
| 13 | `.plan/1-runtime-architecture-refactors/orchestration.md` | plan-file | Parent orchestration — mark task DONE here post-completion | false |
| 14 | `PLAN.md` | plan-file | Global frontier — advance after all 1.14-1.17 tasks done | false |

## Notes
[INTENT: KONTEXT]

- The search-file-contents-by-regex and search-file-contents-by-fixed-string schema files were already modified in the interrupted 1.13 session. Re-anchor them carefully: the 1.13 session hardened the superRefine validation logic but did NOT remove the local schema const blocks. Task 1.14 removes those blocks.
- Do not remove the `SearchFileContentsByRegexContinuationArgsSchema` or `SearchFileContentsByFixedStringContinuationArgsSchema` local schemas — those are endpoint-specific continuation schemas, not the two InspectionResume schemas targeted here.
- The `applyCommonResumeSchemaRefinement` helper function should use `readonly` parameters to signal it does not mutate args.
