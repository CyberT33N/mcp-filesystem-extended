---
file_type: "task"
task_id: "2.2"
task_title: "`find_paths_by_name` doc set"
unit: "2"
parent_orchestration: ".plan/2-inspection-discovery-docs/orchestration.md"
classification: "WAITING"
status: "pending"
estimated_complexity: "MEDIUM"
files_modified:
  - "src/domain/inspection/find-paths-by-name/CONVENTIONS.md"
  - "src/domain/inspection/find-paths-by-name/DESCRIPTION.md"
  - "src/domain/inspection/find-paths-by-name/README.md"
files_read:
  - "src/domain/inspection/find-paths-by-name/handler.ts"
  - "src/domain/inspection/find-paths-by-name/helpers.ts"
  - "src/domain/inspection/find-paths-by-name/schema.ts"
  - "src/application/server/register-inspection-tool-catalog.ts"
  - "src/domain/shared/guardrails/traversal-scope-policy.ts"
  - "src/domain/shared/guardrails/traversal-runtime-budget.ts"
  - "README.md"
  - "DESCRIPTION.md"
dependencies:
  - task_id: "1.5"
    type: "WAITING"
    status: "UNRESOLVED"
blocked_by: "1.5"
information_completeness_mode: "FULLY_SPECIFIED"
implementation_scenarios:
  - "OBJECT_CREATION"
  - "OTHER"
execution_surface_band: "GREEN"
primary_split_axis: "none"
single_agent_viability_note: "The task is bounded to one endpoint-local documentation triplet and one already existing name-discovery endpoint surface."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
decision_surface_register:
  - surface: "naming"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "The endpoint-local doc triplet must be named `CONVENTIONS.md`, `DESCRIPTION.md`, and `README.md`."
  - surface: "path_and_placement"
    status: "BOUND_LITERAL"
    binding_source: "existing project state"
    notes: "The three files live directly in `src/domain/inspection/find-paths-by-name/`."
  - surface: "literal_values_and_budgets"
    status: "BOUND_REFERENCE"
    binding_source: "current code state"
    notes: "Runtime and traversal details must be derived from current code and finalized task `1.5`, not invented."
  - surface: "defaults_and_fallbacks"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "Endpoint-local docs own endpoint-specific detail; root TOC integration is later."
  - surface: "types_and_shapes"
    status: "BOUND_REFERENCE"
    binding_source: "handler, helper, and schema surfaces"
    notes: "The docs must explain the root-local result model and helper-owned traversal behavior."
  - surface: "function_signatures_and_returns"
    status: "BOUND_REFERENCE"
    binding_source: "authoritative code surfaces"
    notes: "The public tool contract and helper-driven traversal behavior remain authoritative."
  - surface: "object_schema_and_mapper_contracts"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "The three endpoint-local docs have non-overlapping ownership roles."
  - surface: "dependencies_and_import_surfaces"
    status: "BOUND_REFERENCE"
    binding_source: "runtime architecture task"
    notes: "Final traversal wording depends on task `1.5`."
  - surface: "validation_and_boundary_rules"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "The docs must explain case-insensitive name matching, truncation semantics, and traversal restrictions accurately."
  - surface: "consumer_and_callsite_bindings"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "Root docs will later link to this endpoint-local triplet rather than duplicate it."
authoritative_literal_bindings:
  - binding_domain: "endpoint-local SSOT split"
    exact_binding: "`CONVENTIONS.md` owns endpoint-local conventions, `DESCRIPTION.md` owns endpoint-local architecture description, and `README.md` owns endpoint-local DX summary."
    source: "explicit user requirement"
    placement_or_consumer: "endpoint-local doc triplet"
    no_autonomous_choice_rule: "The execute agent must not collapse these roles into fewer files."
  - binding_domain: "runtime-governance dependency"
    exact_binding: "The traversal and truncation wording must reflect the finalized runtime model from task `1.5`."
    source: "plan dependency"
    placement_or_consumer: "local conventions and description"
    no_autonomous_choice_rule: "The execute agent must not finalize traversal wording before task `1.5` resolves."
requires_authoritative_code_surfaces: true
authoritative_code_surfaces:
  - surface_name: "Public registration contract"
    kind: "shape"
    snippet_or_contract: "The tool is registered as `find_paths_by_name` and exposes a name-substring discovery surface with structured roots, totals, and truncation semantics."
    source: "current registration surface"
    placement_or_consumer: "`src/application/server/register-inspection-tool-catalog.ts`"
  - surface_name: "Handler-plus-helper contract"
    kind: "shape"
    snippet_or_contract: "The endpoint delegates recursive traversal to helper logic and returns root-local match arrays with truncation."
    source: "current handler/helper/schema surfaces"
    placement_or_consumer: "`src/domain/inspection/find-paths-by-name/handler.ts`, `helpers.ts`, `schema.ts`"
authoritative_references:
  - path: "src/domain/inspection/find-paths-by-name/handler.ts"
    kind: "business-file"
    usage: "current endpoint behavior"
    required_before_execution: true
  - path: "src/domain/inspection/find-paths-by-name/helpers.ts"
    kind: "business-file"
    usage: "helper-owned traversal behavior and truncation semantics"
    required_before_execution: true
  - path: "src/domain/inspection/find-paths-by-name/schema.ts"
    kind: "schema-file"
    usage: "current request/result contract surfaces"
    required_before_execution: true
  - path: "src/application/server/register-inspection-tool-catalog.ts"
    kind: "business-file"
    usage: "current public registration wording"
    required_before_execution: true
  - path: "src/domain/shared/guardrails/traversal-scope-policy.ts"
    kind: "schema-file"
    usage: "shared traversal exclusion and re-include policy"
    required_before_execution: true
  - path: "src/domain/shared/guardrails/traversal-runtime-budget.ts"
    kind: "schema-file"
    usage: "runtime safeguard semantics after task `1.5`"
    required_before_execution: true
  - path: "README.md"
    kind: "markdown-doc"
    usage: "later root DX TOC reference target"
    required_before_execution: true
  - path: "DESCRIPTION.md"
    kind: "markdown-doc"
    usage: "later root architecture TOC reference target"
    required_before_execution: true
upstream_plan_references:
  - task_id: "1.5"
    path: ".plan/1-runtime-architecture-refactors/1.5-traversal-preflight-and-runtime-budget-refactor.md"
    must_read_before_execution: true
    reason: "This endpoint-local doc set depends on the finalized traversal-governance model."
runtime_discovery:
  required: false
  scope: []
  allowed_topics: []
checkpoint_strategy: "ARTIFACT_MILESTONES_REQUIRED"
checkpoint_eligibility_contract:
  durable_skip_authority_only: true
  ephemeral_freshness_steps:
    - "Re-read endpoint code and root docs"
  durable_resume_checkpoints:
    - "cp-1-context-reanchor"
    - "cp-2-endpoint-conventions"
    - "cp-3-endpoint-description"
    - "cp-4-endpoint-readme"
resume_validation_contract:
  lowest_level_truth_surface: "artifact-evidence"
  task_lifecycle_truth_surface: ".plan/2-inspection-discovery-docs/orchestration.md"
  global_frontier_truth_surface: "PLAN.md"
  artifact_evidence_sources:
    - "src/domain/inspection/find-paths-by-name/CONVENTIONS.md"
    - "src/domain/inspection/find-paths-by-name/DESCRIPTION.md"
    - "src/domain/inspection/find-paths-by-name/README.md"
  conflict_requires_user_confirmation: true
  partial_progress_requires_user_decision: true
  durable_skip_authority_only: true
  ephemeral_checkpoint_conflict_requires_reexecution: true
artifact_progress_inference_rules:
  - "Treat the task as partially complete only when one or more of the endpoint-local doc files exist."
  - "Do not treat root-doc edits as completion evidence for the endpoint-local SSOT triplet."
  - "Ephemeral re-reads never create durable skip authority."
user_resume_decision_gate:
  required_on_partial_progress: true
  required_on_state_conflict: true
  default_user_choices:
    - "ACCEPT_AND_CONTINUE"
    - "REVERIFY_EXISTING_OUTPUTS"
persona_generation_required: true
persona_generation_mode: "runtime_regenerate"
persona_source_prompt_path: "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
persona_primary_domain_kernel: "Enterprise endpoint documentation architecture"
persona_secondary_specializations:
  - "DX-oriented documentation design"
  - "inspection-domain contract description"
persona_current_task_specialization: "Name-based path discovery endpoint documentation"
persona_generation_lineage: "Derived from the endpoint-local SSOT documentation rule and finalized traversal-governance dependency."
persona_runtime_review_required: true
persona_dx_snapshot:
  title: "Enterprise Endpoint Documentation Architect"
  summary: "Creates endpoint-local SSOT documentation triplets that are precise for agents and concise for developers."
  weighting_note: "The endpoint-local SSOT rule dominates over local wording preferences."
checkpoint_definitions:
  - checkpoint_id: "cp-1-context-reanchor"
    kind: "sequential_step"
    completion_signal: "Task `1.5` and all authoritative references are re-read."
    produces_artifacts: []
  - checkpoint_id: "cp-2-endpoint-conventions"
    kind: "artifact_milestone"
    completion_signal: "`CONVENTIONS.md` exists with endpoint-local policy ownership."
    produces_artifacts:
      - "src/domain/inspection/find-paths-by-name/CONVENTIONS.md"
  - checkpoint_id: "cp-3-endpoint-description"
    kind: "artifact_milestone"
    completion_signal: "`DESCRIPTION.md` exists with endpoint-local architecture description."
    produces_artifacts:
      - "src/domain/inspection/find-paths-by-name/DESCRIPTION.md"
  - checkpoint_id: "cp-4-endpoint-readme"
    kind: "artifact_milestone"
    completion_signal: "`README.md` exists with concise DX summary and links."
    produces_artifacts:
      - "src/domain/inspection/find-paths-by-name/README.md"
execution_progress_ledger:
  ledger_mode: "CHECKPOINT_LEDGER"
  checkpoints:
    - checkpoint_id: "cp-1-context-reanchor"
      status: "PENDING"
    - checkpoint_id: "cp-2-endpoint-conventions"
      status: "PENDING"
    - checkpoint_id: "cp-3-endpoint-description"
      status: "PENDING"
    - checkpoint_id: "cp-4-endpoint-readme"
      status: "PENDING"
resume_conflict_resolution_rules:
  - "If one or two endpoint-local doc files exist but the triplet is incomplete, stop and ask whether the partial state should be accepted or reverified."
  - "If endpoint-local docs duplicate root-level TOC content instead of owning endpoint-local detail, stop and escalate."
  - "If local docs are drafted before task `1.5` is complete, stop and escalate."
checkpoint_update_protocol:
  - "Mark `cp-1-context-reanchor` DONE only after all authoritative references are re-read and task `1.5` is complete."
  - "Mark `cp-2-endpoint-conventions` DONE only after endpoint-local conventions own the local policies and rationale."
  - "Mark `cp-3-endpoint-description` DONE only after the endpoint-local architecture description is complete and LLM-optimized."
  - "Mark `cp-4-endpoint-readme` DONE only after the concise DX summary exists and links the local docs."
halt_conditions:
  plan_reality_mismatch: true
  missing_required_plan_information: true
  autonomous_decision_required: true
user_notification_required: true
---

# Task 2.2: `find_paths_by_name` doc set
[INTENT: ANWEISUNG]

## Goal
[INTENT: KONTEXT]

Create the endpoint-local documentation triplet for `find_paths_by_name` so endpoint-specific conventions, helper-driven traversal behavior, and DX guidance are owned locally instead of being implied only by code and root docs.

The docs must explain the endpoint’s case-insensitive substring semantics, root-local truncation behavior, and the traversal policy that still governs broad-root requests.

## Expected Result
[INTENT: KONTEXT]

- `src/domain/inspection/find-paths-by-name/CONVENTIONS.md` exists and owns endpoint-local guardrails and conventions.
- `src/domain/inspection/find-paths-by-name/DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents.
- `src/domain/inspection/find-paths-by-name/README.md` exists and provides a concise DX-oriented summary plus links.

## Preconditions
[INTENT: CONSTRAINT]

- Task `1.5` must be completed because traversal-governance wording depends on the finalized runtime model.

## Execution Metadata Register
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Task ID | `2.2` | Unique task identifier |
| Classification | `WAITING` | The task is blocked by finalized traversal-governance work in another unit |
| Information Completeness Mode | `FULLY_SPECIFIED` | All determinable doc-architecture surfaces are bound |
| Implementation Scenarios | `OBJECT_CREATION`, `OTHER` | Documentation triplet creation |
| Execution Surface Band | `GREEN` | One endpoint-local documentation concern |
| Primary Split Axis | `none` | No further split is required |
| Single-Agent Viability Note | The task is limited to one endpoint-local documentation triplet and one already stable endpoint contract. |
| Parent Orchestration | `.plan/2-inspection-discovery-docs/orchestration.md` | Lifecycle status is tracked there |
| Blocked By | `1.5` | Cross-unit traversal-governance blocker |
| Requires Upstream Plan Re-Anchoring | `yes` | Task `1.5` and all authoritative references must be re-read |
| Runtime Discovery Allowed | `no` | This task is fully specified |
| Checkpoint Strategy | `ARTIFACT_MILESTONES_REQUIRED` | The three documentation files are durable outputs |
| Durable Skip Authority Only | `true` | Only durable documentation artifacts create skip authority |
| Ephemeral Freshness Steps Present | `yes` | Re-reading code and root docs remains non-durable |
| User Notification Required On Gap/Mismatch | `true` | Mismatches must stop execution |
| Persona Generation Required | `true` | Runtime persona regeneration remains mandatory |
| Persona Generation Mode | `runtime_regenerate` | Persona is not fully persisted |
| Persona Primary Domain Kernel | `Enterprise endpoint documentation architecture` | Highest-weight persona kernel |
| Persona Current Task Specialization | `Name-based path discovery endpoint docs` | Current task specialization |
| Persona Runtime Review Required | `true` | Runtime review is required |

## Task Contract Closure
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Decision Surfaces Closed | `true` | All determinable surfaces are bound |
| Unresolved Determinable Decisions | `0` | No deterministic gap remains |
| Expected Execute-Agent Questions | `0` | A fresh execute agent should not need follow-up questions |

## Decision Surface Register
[INTENT: REFERENZ]

| Surface | Status | Binding Source | Notes |
|---------|--------|----------------|-------|
| `naming` | `BOUND_LITERAL` | user requirement | The endpoint-local doc triplet must be named `CONVENTIONS.md`, `DESCRIPTION.md`, and `README.md`. |
| `path_and_placement` | `BOUND_LITERAL` | existing project state | The three files live directly in `src/domain/inspection/find-paths-by-name/`. |
| `literal_values_and_budgets` | `BOUND_REFERENCE` | current code state | Runtime and truncation details must be derived from current code and finalized task `1.5`, not invented. |
| `defaults_and_fallbacks` | `BOUND_LITERAL` | accepted architecture | Endpoint-local docs own endpoint-specific detail; root TOC integration is later. |
| `types_and_shapes` | `BOUND_REFERENCE` | handler, helper, and schema surfaces | The docs must explain the root-local match array and helper-owned traversal behavior. |
| `function_signatures_and_returns` | `BOUND_REFERENCE` | authoritative code surfaces | The public tool contract and helper-driven traversal behavior remain authoritative. |
| `object_schema_and_mapper_contracts` | `BOUND_LITERAL` | user requirement | The three endpoint-local docs have non-overlapping ownership roles. |
| `dependencies_and_import_surfaces` | `BOUND_REFERENCE` | runtime architecture task | Final traversal wording depends on task `1.5`. |
| `validation_and_boundary_rules` | `BOUND_LITERAL` | accepted architecture | The docs must explain case-insensitive substring matching, truncation behavior, and traversal restrictions accurately. |
| `consumer_and_callsite_bindings` | `BOUND_LITERAL` | user requirement | Root docs will later link to this endpoint-local triplet rather than duplicate it. |

## Authoritative Literal Bindings
[INTENT: REFERENZ]

| Binding Domain | Exact Binding | Source | Placement / Consumer | No Autonomous Choice Rule |
|---------------|---------------|--------|----------------------|---------------------------|
| Endpoint-local SSOT split | `CONVENTIONS.md` owns endpoint-local conventions, `DESCRIPTION.md` owns endpoint-local architecture description, and `README.md` owns endpoint-local DX summary. | explicit user requirement | endpoint-local doc triplet | The execute agent must not collapse these roles into fewer files. |
| Runtime-governance dependency | The traversal and truncation wording must reflect the finalized runtime model from task `1.5`. | plan dependency | local conventions and description | The execute agent must not finalize traversal wording before task `1.5` resolves. |

## Authoritative Code / Contract Surfaces
[INTENT: REFERENZ]

| Surface Name | Kind | Snippet / Contract | Source | Placement / Consumer |
|-------------|------|--------------------|--------|----------------------|
| Public registration contract | `shape` | The tool is registered as `find_paths_by_name` and exposes a name-substring discovery surface with structured roots, totals, and truncation semantics. | current registration surface | `src/application/server/register-inspection-tool-catalog.ts` |
| Handler-plus-helper contract | `shape` | The endpoint delegates recursive traversal to helper logic and returns root-local match arrays with truncation. | current handler/helper/schema surfaces | `src/domain/inspection/find-paths-by-name/handler.ts`, `helpers.ts`, `schema.ts` |

## Execute-Agent Zero-Question Gate
[INTENT: CONSTRAINT]

- **Gate Result:** `PASS`
- **Checked Domains:** doc triplet naming, placement, runtime-governance dependency, helper-driven traversal description, root TOC relationship
- **Rule:** A fresh execute agent must not need to ask where the files go, what each doc owns, or whether traversal wording may be finalized before task `1.5` resolves.

## Authoritative References
[INTENT: REFERENZ]

| Path | Kind | Must Read Before Execution | Purpose | Referenced By |
|------|------|---------------------------|---------|---------------|
| `src/domain/inspection/find-paths-by-name/handler.ts` | `business-file` | `true` | current endpoint behavior | actions 1-4 |
| `src/domain/inspection/find-paths-by-name/helpers.ts` | `business-file` | `true` | helper-owned traversal behavior and truncation semantics | actions 1-4 |
| `src/domain/inspection/find-paths-by-name/schema.ts` | `schema-file` | `true` | current request/result contract surfaces | actions 1-4 |
| `src/application/server/register-inspection-tool-catalog.ts` | `business-file` | `true` | current public registration wording | actions 1-4 |
| `src/domain/shared/guardrails/traversal-scope-policy.ts` | `schema-file` | `true` | shared traversal exclusion and re-include policy | actions 1-4 |
| `src/domain/shared/guardrails/traversal-runtime-budget.ts` | `schema-file` | `true` | runtime safeguard semantics after task `1.5` | actions 1-4 |
| `README.md` | `markdown-doc` | `true` | later root DX TOC reference target | context |
| `DESCRIPTION.md` | `markdown-doc` | `true` | later root architecture TOC reference target | context |

## Resume Validation Contract
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Lowest Level Truth Surface | `artifact-evidence` | The three endpoint-local documentation files are the primary resume truth |
| Task Lifecycle Truth Surface | `.plan/2-inspection-discovery-docs/orchestration.md` | Parent orchestration owns lifecycle state |
| Global Frontier Truth Surface | `PLAN.md` | Global frontier SSOT |
| Conflict Requires User Confirmation | `true` | Conflicting partial documentation state requires escalation |
| Partial Progress Requires User Decision | `true` | Partial artifact state requires a user choice |
| Durable Skip Authority Only | `true` | Only durable documentation artifacts may authorize skip |
| Ephemeral Freshness Steps Must Re-Execute | `true` | Pure re-anchor work remains re-executable preparation |

## Artifact Progress Inference Rules
[INTENT: REFERENZ]

- Treat the task as partially complete only when one or more of the endpoint-local doc files exist.
- Do not treat root-doc edits as completion evidence for the endpoint-local SSOT triplet.
- `EPHEMERAL_FRESHNESS_STEP` re-reads never create durable skip authority.

## User Resume Decision Gate
[INTENT: CONSTRAINT]

- **Required on Partial Progress:** `true`
- **Required on State Conflict:** `true`
- **Allowed User Choices:** `ACCEPT_AND_CONTINUE` / `REVERIFY_EXISTING_OUTPUTS`

## Persona Derivation Manifest
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Persona Generation Required | `true` | task-specific persona derivation required |
| Persona Generation Mode | `runtime_regenerate` | execute regenerates the persona at runtime |
| Persona Source Prompt Path | `src/personas/it/__prompts__/silent/enterprise/creation/prompt.md` | authoritative persona source |
| Persona Primary Domain Kernel | `Enterprise endpoint documentation architecture` | highest-weight persona layer |
| Persona Secondary Specializations | `DX-oriented documentation design`, `inspection-domain contract description` | secondary persona layers |
| Persona Current Task Specialization | `name-based path discovery endpoint documentation` | current specialization |
| Persona Generation Lineage | `Derived from the endpoint-local SSOT documentation rule and finalized traversal-governance dependency.` | derivation lineage |
| Persona Runtime Review Required | `true` | runtime review is required |

## Persona DX Snapshot
[INTENT: KONTEXT]

- **Persona Title:** `Enterprise Endpoint Documentation Architect`
- **Persona Summary:** Creates endpoint-local SSOT documentation triplets that are precise for agents and concise for developers.
- **Persona Weighting Note:** The endpoint-local SSOT rule dominates over local wording preferences."

## Context
[INTENT: KONTEXT]

- **Domain:** endpoint-local discovery documentation
- **Related Files:** endpoint handler, helper, schema, current registration, traversal guardrails, root docs
- **Technical Context:** this endpoint exposes helper-driven recursive name discovery with truncation semantics
- **Business Context:** agents and developers need endpoint-local guidance that explains how name-based discovery differs from glob and content-search tools
- **Architectural Context:** endpoint-local SSOT comes first; root TOC integration happens later
- **Plan Context Dependencies:** task `1.5` must be complete first
- **Runtime Discovery Context:** none

## Execution Surface Assessment
[INTENT: REFERENZ]

- **Execution Surface Band:** `GREEN`
- **Primary Split Axis:** `none`
- **Hard-Gap Result:** `no RED or BLACK trigger active`
- **Single-Agent Viability Rationale:** The task is limited to one endpoint-local documentation triplet for one already bounded discovery endpoint.

## Information Units
[INTENT: SPEZIFIKATION]

### IU-001: Endpoint-local `CONVENTIONS.md`
**Type:** `object`

**Description:**
Create the conventions file that becomes the local SSOT for endpoint-specific guardrails, helper-driven traversal rules, and architectural rationale.

**Ist-Zustand:**
The endpoint has code and registration surfaces but no endpoint-local conventions file.

**Soll-Zustand:**
`CONVENTIONS.md` explains helper traversal, truncation, path validation, and the reason the endpoint is separate from glob and content-search tools.

**Architectural Rationale:**
Endpoint-specific conventions must not live only in root docs.

### IU-002: Endpoint-local `DESCRIPTION.md`
**Type:** `object`

**Description:**
Create the endpoint-local architectural description optimized for LLM-agent comprehension.

**Ist-Zustand:**
Only code and registration surfaces describe the endpoint today.

**Soll-Zustand:**
`DESCRIPTION.md` explains the endpoint contract, helper-driven traversal model, truncation semantics, and relationship to adjacent discovery tools.

**Architectural Rationale:**
The endpoint needs a dedicated, LLM-optimized description surface.

### IU-003: Endpoint-local `README.md`
**Type:** `object`

**Description:**
Create the concise DX summary for the endpoint.

**Ist-Zustand:**
There is no endpoint-local DX summary file.

**Soll-Zustand:**
`README.md` summarizes what the endpoint does, links the local conventions and description, and stays concise.

**Architectural Rationale:**
Developers need a short, high-signal entry surface instead of being forced directly into long conventions text.

## Actions
[INTENT: ANWEISUNG]

1. **Re-anchor the finalized traversal-governance task and current endpoint surfaces**
   - **What:** re-read task `1.5` plus the endpoint code, helper, schema, registration, and current root docs
   - **Where:** authoritative references listed above
   - **How:** read them completely before drafting any endpoint-local doc content
   - **Why:** the endpoint-local docs must reflect the final runtime behavior and current code truth
   - **Inputs / References:** all authoritative references in this task file
   - **Output Contract:** current endpoint and dependency context are active
   - **Checkpoint Impact:** completes `cp-1-context-reanchor`
   - **Verification:** all required references are re-read successfully
   - **Stop if:** task `1.5` remains unresolved or any required reference is unreadable
2. **Create endpoint-local `CONVENTIONS.md`**
   - **What:** create the conventions file for this endpoint
   - **Where:** `src/domain/inspection/find-paths-by-name/CONVENTIONS.md`
   - **How:** document endpoint-local guardrails, helper-driven traversal policy, truncation rules, and why the endpoint is distinct
   - **Why:** endpoint-specific policy detail must have a local SSOT
   - **Inputs / References:** current endpoint code plus finalized traversal-governance surfaces
   - **Output Contract:** the endpoint-local conventions SSOT exists
   - **Checkpoint Impact:** completes `cp-2-endpoint-conventions`
   - **Verification:** the file explains endpoint-local policies and does not duplicate root TOC content as its primary structure
   - **Stop if:** conventions would need to invent runtime behavior not implemented in code
3. **Create endpoint-local `DESCRIPTION.md`**
   - **What:** create the endpoint-local description file
   - **Where:** `src/domain/inspection/find-paths-by-name/DESCRIPTION.md`
   - **How:** describe the endpoint contract, helper-driven behavior, truncation semantics, and architectural role in LLM-oriented detail
   - **Why:** agents need a dedicated endpoint description surface beyond raw code and registration text
   - **Inputs / References:** current endpoint code, helper, schema, registration, and conventions
   - **Output Contract:** the endpoint-local architectural description exists
   - **Checkpoint Impact:** completes `cp-3-endpoint-description`
   - **Verification:** the file is detailed, LLM-oriented, and aligned to the current code surface
   - **Stop if:** the description would claim behavior not present in the handler/helper/schema surfaces
4. **Create endpoint-local `README.md`**
   - **What:** create the concise DX summary file
   - **Where:** `src/domain/inspection/find-paths-by-name/README.md`
   - **How:** summarize the endpoint purpose, key conventions, and links to the local `CONVENTIONS.md` and `DESCRIPTION.md`
   - **Why:** developers need a concise entry surface with quick orientation
   - **Inputs / References:** current endpoint code plus the newly created local docs
   - **Output Contract:** the endpoint-local DX summary exists
   - **Checkpoint Impact:** completes `cp-4-endpoint-readme`
   - **Verification:** the README stays concise and points to the local SSOT surfaces
   - **Stop if:** the README would need to duplicate the full conventions or description content

## Checkpoint Definitions
[INTENT: REFERENZ]

| Checkpoint ID | Kind | Completion Signal | Produces Artifacts | Resume Meaning |
|---------------|------|-------------------|--------------------|----------------|
| `cp-1-context-reanchor` | `sequential_step` | Task `1.5` and all authoritative references are re-read | none | drafting may begin |
| `cp-2-endpoint-conventions` | `artifact_milestone` | `CONVENTIONS.md` exists with endpoint-local policy ownership | `src/domain/inspection/find-paths-by-name/CONVENTIONS.md` | conventions SSOT is complete |
| `cp-3-endpoint-description` | `artifact_milestone` | `DESCRIPTION.md` exists with endpoint-local architecture description | `src/domain/inspection/find-paths-by-name/DESCRIPTION.md` | architectural description is complete |
| `cp-4-endpoint-readme` | `artifact_milestone` | `README.md` exists with concise DX summary and links | `src/domain/inspection/find-paths-by-name/README.md` | endpoint doc triplet is complete |

## Execution Progress Ledger
[INTENT: REFERENZ]

| Checkpoint ID | Status | Evidence | Notes |
|---------------|--------|----------|-------|
| `cp-1-context-reanchor` | `PENDING` | authoritative references re-read | start here |
| `cp-2-endpoint-conventions` | `PENDING` | endpoint-local `CONVENTIONS.md` | depends on `cp-1-context-reanchor` |
| `cp-3-endpoint-description` | `PENDING` | endpoint-local `DESCRIPTION.md` | depends on `cp-2-endpoint-conventions` |
| `cp-4-endpoint-readme` | `PENDING` | endpoint-local `README.md` | depends on `cp-3-endpoint-description` |

## Resume Conflict Resolution Rules
[INTENT: CONSTRAINT]

- If one or two endpoint-local doc files exist but the triplet is incomplete, stop and ask whether the partial state should be accepted or reverified.
- If endpoint-local docs duplicate root-level TOC content instead of owning endpoint-local detail, stop and escalate.
- If local docs are drafted before task `1.5` is complete, stop and escalate.

## Checkpoint Update Protocol
[INTENT: ANWEISUNG]

1. Mark `cp-1-context-reanchor` DONE only after all authoritative references are re-read and task `1.5` is complete.
2. Mark `cp-2-endpoint-conventions` DONE only after endpoint-local conventions own the local policies and rationale.
3. Mark `cp-3-endpoint-description` DONE only after the endpoint-local architecture description is complete and LLM-optimized.
4. Mark `cp-4-endpoint-readme` DONE only after the concise DX summary exists and links the local docs.

## Acceptance Criteria
[INTENT: REFERENZ]

- [ ] `src/domain/inspection/find-paths-by-name/CONVENTIONS.md` exists and is the endpoint-local SSOT for conventions and guardrails.
- [ ] `src/domain/inspection/find-paths-by-name/DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents in detail.
- [ ] `src/domain/inspection/find-paths-by-name/README.md` exists and provides a concise DX summary with links to the local docs.
- [ ] The documentation reflects finalized traversal-governance behavior and does not invent runtime behavior.
- [ ] The endpoint-local docs are ready to be referenced later from root-level TOC surfaces.

## Stop Conditions & User Escalation
[INTENT: CONSTRAINT]

- **Plan-vs-Reality Mismatch:** Stop if the current endpoint behavior differs materially from the assumptions documented here.
- **Missing Required Plan Information:** Stop if the finalized runtime-governance dependency or endpoint-local doc-role split cannot be bound exactly.
- **Autonomous Decision Required:** Stop if execution would require inventing endpoint behavior, policy semantics, or root-TOC behavior not already agreed.
- **Decision Surface Not Closed:** If any determinable documentation-ownership or runtime-wording surface remains open, stop and inform the user.
- **Allowed Runtime Discovery:** None.
- **User Message Contract:** `Plan says X / current reality is Y / missing or conflicting information is Z / continuing would require an autonomous decision.`

## Post-Completion Instructions
[INTENT: ANWEISUNG]

1. Mark this task `[x]` in [`.plan/2-inspection-discovery-docs/orchestration.md`](./orchestration.md).
2. Keep the resulting doc triplet ready for later root-level TOC linking.
3. Re-anchor [`PLAN.md`](../../PLAN.md).

## File Path Index
[INTENT: REFERENZ]

| # | Path | Kind | Why Relevant | Must Read Before Execution |
|---|------|------|--------------|----------------------------|
| 1 | `src/domain/inspection/find-paths-by-name/handler.ts` | `business-file` | current endpoint behavior | `true` |
| 2 | `src/domain/inspection/find-paths-by-name/helpers.ts` | `business-file` | helper-owned traversal behavior and truncation semantics | `true` |
| 3 | `src/domain/inspection/find-paths-by-name/schema.ts` | `schema-file` | current request/result contract surfaces | `true` |
| 4 | `src/application/server/register-inspection-tool-catalog.ts` | `business-file` | current public registration wording | `true` |
| 5 | `src/domain/shared/guardrails/traversal-scope-policy.ts` | `schema-file` | shared traversal exclusion and re-include policy | `true` |
| 6 | `src/domain/shared/guardrails/traversal-runtime-budget.ts` | `schema-file` | runtime safeguard semantics after task `1.5` | `true` |
| 7 | `README.md` | `markdown-doc` | later root DX TOC reference target | `true` |
| 8 | `DESCRIPTION.md` | `markdown-doc` | later root architecture TOC reference target | `true` |

## Notes
[INTENT: KONTEXT]

- This task creates endpoint-local documentation only; root TOC integration happens in unit 10.
- The docs must not freeze transient traversal semantics before task `1.5` resolves.

