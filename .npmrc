# -----------------------------
# NPM/PNPM Configuration (Consumer)
# -----------------------------

# Use repository pnpmfile hook to rewrite OS-specific absolute `link:` dependencies.
pnpmfile=pnpmfile.cjs


