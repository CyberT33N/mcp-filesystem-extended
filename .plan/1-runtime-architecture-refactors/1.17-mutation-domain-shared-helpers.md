---
file_type: "task"
task_id: "1.17"
task_title: "Mutation domain shared helpers — formatBatchMutationSummary, assertPathMutationBatchBudget, assertContentMutationInputBudget"
unit: "1"
parent_orchestration: ".plan/1-runtime-architecture-refactors/orchestration.md"
classification: "SEQUENTIAL"
status: "done"
estimated_complexity: "MEDIUM"
files_modified:
  - "src/domain/mutation/shared/mutation-guardrails.ts"
  - "src/infrastructure/formatting/batch-result-formatter.ts"
  - "src/domain/mutation/delete-paths/handler.ts"
  - "src/domain/mutation/copy-paths/handler.ts"
  - "src/domain/mutation/move-paths/handler.ts"
  - "src/domain/mutation/create-directories/handler.ts"
  - "src/domain/mutation/append-files/handler.ts"
  - "src/domain/mutation/create-files/handler.ts"
files_read:
  - "src/domain/mutation/delete-paths/handler.ts"
  - "src/domain/mutation/copy-paths/handler.ts"
  - "src/domain/mutation/move-paths/handler.ts"
  - "src/domain/mutation/create-directories/handler.ts"
  - "src/domain/mutation/append-files/handler.ts"
  - "src/domain/mutation/create-files/handler.ts"
  - "src/infrastructure/formatting/batch-result-formatter.ts"
  - "src/domain/shared/guardrails/tool-guardrail-error-contract.ts"
  - "src/domain/shared/guardrails/tool-guardrail-limits.ts"
dependencies:
  - task_id: "1.16"
    type: "SEQUENTIAL"
    status: "UNRESOLVED"
blocked_by: "1.16"
information_completeness_mode: "FULLY_SPECIFIED"
implementation_scenarios:
  - "FUNCTION_CREATION"
  - "FUNCTION_UPDATE"
  - "OBJECT_CREATION"
execution_surface_band: "YELLOW"
primary_split_axis: "none"
single_agent_viability_note: "8 files — two new helper files (trivial) + 6 mutation handler updates. All changes are structural extractions of patterns that are already fully specified here. No cross-boundary reasoning. Per-file cognitive load is low and uniform."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
decision_surface_register:
  - surface: "naming"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "assertPathMutationBatchBudget, assertContentMutationInputBudget, formatBatchMutationSummary — all bound below"
  - surface: "path_and_placement"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "New file: src/domain/mutation/shared/mutation-guardrails.ts; new function in batch-result-formatter.ts"
  - surface: "literal_values_and_budgets"
    status: "BOUND_LITERAL"
    binding_source: "project-state"
    notes: "MAX_OPERATIONS_PER_PATH_MUTATION_REQUEST, CONTENT_MUTATION_TOTAL_INPUT_CHARS — already exported from tool-guardrail-limits.ts; used in helper functions"
  - surface: "defaults_and_fallbacks"
    status: "NOT_APPLICABLE"
    binding_source: "—"
    notes: "—"
  - surface: "types_and_shapes"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "All function signatures specified below"
  - surface: "function_signatures_and_returns"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "Three function signatures fully specified in Code Surfaces"
  - surface: "object_schema_and_mapper_contracts"
    status: "NOT_APPLICABLE"
    binding_source: "—"
    notes: "—"
  - surface: "dependencies_and_import_surfaces"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "All import paths specified below"
  - surface: "validation_and_boundary_rules"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "Guard behavior is identical to the 4 inline implementations being replaced"
  - surface: "consumer_and_callsite_bindings"
    status: "BOUND_LITERAL"
    binding_source: "plan-output"
    notes: "Exact consumer mapping below: 4 path-mutation handlers + 2 content-mutation handlers"
authoritative_literal_bindings:
  - binding_domain: "naming"
    exact_binding: "assertPathMutationBatchBudget"
    source: "plan-output"
    placement_or_consumer: "src/domain/mutation/shared/mutation-guardrails.ts"
    no_autonomous_choice_rule: "Name mirrors the existing assertActualTextBudget / assertProjectedTextBudget naming pattern in text-response-budget.ts"
  - binding_domain: "naming"
    exact_binding: "assertContentMutationInputBudget"
    source: "plan-output"
    placement_or_consumer: "src/domain/mutation/shared/mutation-guardrails.ts"
    no_autonomous_choice_rule: "Name distinguishes content-bearing mutation from path-only mutation; consistent with CONTENT_MUTATION_TOTAL_INPUT_CHARS constant naming"
  - binding_domain: "naming"
    exact_binding: "formatBatchMutationSummary"
    source: "plan-output"
    placement_or_consumer: "src/infrastructure/formatting/batch-result-formatter.ts"
    no_autonomous_choice_rule: "Name mirrors existing formatBatchTextOperationResults and uses 'mutation' to distinguish from the existing batch result formatter"
  - binding_domain: "path_and_placement"
    exact_binding: "src/domain/mutation/shared/mutation-guardrails.ts"
    source: "plan-output"
    placement_or_consumer: "New file — in mutation domain shared subdirectory parallel to inspection/shared/"
    no_autonomous_choice_rule: "Must be under src/domain/mutation/shared/ to stay within the mutation bounded context; the /shared/ subdirectory may not exist yet and must be created"
  - binding_domain: "dependencies_and_import_surfaces"
    exact_binding: "import { assertPathMutationBatchBudget, assertContentMutationInputBudget } from \"../shared/mutation-guardrails\";"
    source: "plan-output"
    placement_or_consumer: "Applicable mutation handler files — use relative path since files are within the same domain/mutation/ tree"
    no_autonomous_choice_rule: "Use relative path since handler files and shared directory are siblings within domain/mutation/"
  - binding_domain: "dependencies_and_import_surfaces"
    exact_binding: "import { formatBatchMutationSummary } from \"@infrastructure/formatting/batch-result-formatter\";"
    source: "plan-output"
    placement_or_consumer: "Applicable mutation handler files that currently build the summary manually"
    no_autonomous_choice_rule: "Must use the @infrastructure alias consistent with existing imports of formatBatchTextOperationResults in other handlers"
  - binding_domain: "dependencies_and_import_surfaces"
    exact_binding: "import { MAX_OPERATIONS_PER_PATH_MUTATION_REQUEST } from \"@domain/shared/guardrails/tool-guardrail-limits\";"
    source: "project-state"
    placement_or_consumer: "src/domain/mutation/shared/mutation-guardrails.ts"
    no_autonomous_choice_rule: "Already imported by each handler individually; must now be centralized in the new guardrail file"
  - binding_domain: "dependencies_and_import_surfaces"
    exact_binding: "import { CONTENT_MUTATION_TOTAL_INPUT_CHARS } from \"@domain/shared/guardrails/tool-guardrail-limits\";"
    source: "project-state"
    placement_or_consumer: "src/domain/mutation/shared/mutation-guardrails.ts"
    no_autonomous_choice_rule: "Already imported by append-files and create-files individually"
requires_authoritative_code_surfaces: true
authoritative_code_surfaces:
  - surface_name: "assertPathMutationBatchBudget"
    kind: "signature"
    snippet_or_contract: |
      import {
        MAX_OPERATIONS_PER_PATH_MUTATION_REQUEST,
      } from "@domain/shared/guardrails/tool-guardrail-limits";
      import {
        createRuntimeBudgetExceededFailure,
        formatToolGuardrailFailureAsText,
      } from "@domain/shared/guardrails/tool-guardrail-error-contract";

      /**
       * Enforces the shared path-mutation batch-size ceiling before any filesystem
       * mutation begins.
       *
       * @remarks
       * Kept in the mutation domain shared layer so path-mutation handlers can import
       * a single assert function rather than each duplicating the inline guardrail check.
       *
       * @param toolName - Exact MCP tool name that owns the mutation request.
       * @param operationCount - Number of operations in the incoming batch.
       * @returns Nothing when the operation count is within the ceiling.
       */
      export function assertPathMutationBatchBudget(
        toolName: string,
        operationCount: number,
      ): void {
        if (operationCount <= MAX_OPERATIONS_PER_PATH_MUTATION_REQUEST) {
          return;
        }

        throw new Error(
          formatToolGuardrailFailureAsText(
            createRuntimeBudgetExceededFailure({
              toolName,
              budgetSurface: `${toolName}.operations`,
              measuredValue: operationCount,
              limitValue: MAX_OPERATIONS_PER_PATH_MUTATION_REQUEST,
            }),
          ),
        );
      }
    source: "Extracted from identical inline guard blocks in delete-paths, copy-paths, move-paths, create-directories handlers"
    placement_or_consumer: "src/domain/mutation/shared/mutation-guardrails.ts"
  - surface_name: "assertContentMutationInputBudget"
    kind: "signature"
    snippet_or_contract: |
      import {
        CONTENT_MUTATION_TOTAL_INPUT_CHARS,
      } from "@domain/shared/guardrails/tool-guardrail-limits";
      import {
        createRuntimeBudgetExceededFailure,
        formatToolGuardrailFailureAsText,
      } from "@domain/shared/guardrails/tool-guardrail-error-contract";

      /**
       * Enforces the shared content-bearing mutation cumulative input-character ceiling
       * before any filesystem write begins.
       *
       * @param toolName - Exact MCP tool name that owns the mutation request.
       * @param files - Content-bearing file entries whose character counts sum to the total.
       * @returns Nothing when the cumulative input is within the ceiling.
       */
      export function assertContentMutationInputBudget(
        toolName: string,
        files: ReadonlyArray<{ readonly content: string }>,
      ): void {
        const totalInputChars = files.reduce(
          (totalChars, file) => totalChars + file.content.length,
          0,
        );

        if (totalInputChars <= CONTENT_MUTATION_TOTAL_INPUT_CHARS) {
          return;
        }

        throw new Error(
          formatToolGuardrailFailureAsText(
            createRuntimeBudgetExceededFailure({
              toolName,
              budgetSurface: "Cumulative content-bearing mutation input characters",
              measuredValue: totalInputChars,
              limitValue: CONTENT_MUTATION_TOTAL_INPUT_CHARS,
            }),
          ),
        );
      }
    source: "Extracted from identical inline guard blocks in append-files and create-files handlers"
    placement_or_consumer: "src/domain/mutation/shared/mutation-guardrails.ts"
  - surface_name: "formatBatchMutationSummary"
    kind: "signature"
    snippet_or_contract: |
      /**
       * Formats a concise batch mutation summary line suitable for path-mutation and
       * content-mutation handler responses.
       *
       * @remarks
       * This helper keeps mutation output concise and consistent across all handlers
       * that currently hand-build the same `Processed N items: ...` string pattern.
       * Mutation endpoints must never echo large content bodies back to the caller.
       *
       * @param operationNoun - Plural English noun describing the mutation target (e.g. "files", "paths", "directories").
       * @param successCount - Number of successfully completed operations.
       * @param errors - Error messages for each failed operation.
       * @returns Concise formatted summary string.
       */
      export function formatBatchMutationSummary(
        operationNoun: string,
        successCount: number,
        errors: readonly string[],
      ): string {
        const errorCount = errors.length;
        let output = `Processed ${successCount + errorCount} ${operationNoun}:\n`;
        output += `- ${successCount} ${operationNoun} processed successfully\n`;

        if (errorCount > 0) {
          output += `- ${errorCount} ${operationNoun} failed\n\n`;
          output += "Errors:\n" + errors.join("\n");
        }

        return output;
      }
    source: "Extracted from identical inline summary-building patterns in 5 mutation handlers"
    placement_or_consumer: "src/infrastructure/formatting/batch-result-formatter.ts — added after existing formatBatchTextOperationResults export"
  - surface_name: "path-mutation handler replacement pattern"
    kind: "other"
    snippet_or_contract: |
      // Before (in delete-paths, copy-paths, move-paths, create-directories):
      if (operations.length > MAX_OPERATIONS_PER_PATH_MUTATION_REQUEST) {
        return formatToolGuardrailFailureAsText(
          createRuntimeBudgetExceededFailure({
            toolName: "...",
            budgetSurface: "....operations",
            measuredValue: operations.length,
            limitValue: MAX_OPERATIONS_PER_PATH_MUTATION_REQUEST,
          })
        );
      }
      // ... logic ...
      let output = `Processed ${successCount + errorCount} [noun]:\n`;
      output += `- ${successCount} [noun] [verb] successfully\n`;
      if (errorCount > 0) { output += ...; }

      // After:
      assertPathMutationBatchBudget(TOOL_NAME, operations.length);
      // ... logic ...
      return formatBatchMutationSummary("[noun]", successCount, errors);
    source: "plan-output"
    placement_or_consumer: "delete-paths, copy-paths, move-paths, create-directories handlers"
  - surface_name: "content-mutation handler replacement pattern"
    kind: "other"
    snippet_or_contract: |
      // Before (in append-files, create-files):
      const totalInputChars = files.reduce((t, f) => t + f.content.length, 0);
      if (totalInputChars > CONTENT_MUTATION_TOTAL_INPUT_CHARS) {
        throw new Error(formatToolGuardrailFailureAsText(...));
      }
      // ... logic ...
      let output = `Processed ${successCount + errorCount} files:\n`;
      // ...

      // After:
      assertContentMutationInputBudget(TOOL_NAME, files);
      // ... logic ...
      return formatBatchMutationSummary("files", successCount, errors);
    source: "plan-output"
    placement_or_consumer: "append-files, create-files handlers"
authoritative_references:
  - path: "src/domain/mutation/delete-paths/handler.ts"
    kind: "business-file"
    usage: "Has inline path-budget guard + manual summary; modified by 1.16 (error message) — re-anchor"
    required_before_execution: true
  - path: "src/domain/mutation/copy-paths/handler.ts"
    kind: "business-file"
    usage: "Same pattern; modified by 1.16 — re-anchor"
    required_before_execution: true
  - path: "src/domain/mutation/move-paths/handler.ts"
    kind: "business-file"
    usage: "Same; modified by 1.16 — re-anchor"
    required_before_execution: true
  - path: "src/domain/mutation/create-directories/handler.ts"
    kind: "business-file"
    usage: "Has inline path-budget guard + manual summary"
    required_before_execution: true
  - path: "src/domain/mutation/append-files/handler.ts"
    kind: "business-file"
    usage: "Has inline content-budget guard + manual summary; modified by 1.16 — re-anchor"
    required_before_execution: true
  - path: "src/domain/mutation/create-files/handler.ts"
    kind: "business-file"
    usage: "Same; modified by 1.16 — re-anchor"
    required_before_execution: true
  - path: "src/infrastructure/formatting/batch-result-formatter.ts"
    kind: "business-file"
    usage: "Target for new formatBatchMutationSummary export; read to understand existing exports"
    required_before_execution: true
  - path: "src/domain/shared/guardrails/tool-guardrail-error-contract.ts"
    kind: "business-file"
    usage: "Provides createRuntimeBudgetExceededFailure and formatToolGuardrailFailureAsText for the new guardrail helper"
    required_before_execution: true
  - path: "src/domain/shared/guardrails/tool-guardrail-limits.ts"
    kind: "business-file"
    usage: "Provides MAX_OPERATIONS_PER_PATH_MUTATION_REQUEST and CONTENT_MUTATION_TOTAL_INPUT_CHARS"
    required_before_execution: true
  - path: ".plan/1-runtime-architecture-refactors/1.16-extract-error-message-utility.md"
    kind: "plan-file"
    usage: "Documents 1.16's modifications to all 6 mutation handlers; re-anchor those files after 1.16"
    required_before_execution: true
upstream_plan_references:
  - task_id: "1.16"
    path: ".plan/1-runtime-architecture-refactors/1.16-extract-error-message-utility.md"
    must_read_before_execution: true
    reason: "Task 1.16 modified all 5 mutation handlers (append, create, copy, move, replace); re-anchor all of them after 1.16 lands before this task begins"
runtime_discovery:
  required: true
  scope:
    - "src/domain/mutation/copy-paths/handler.ts"
    - "src/domain/mutation/delete-paths/handler.ts"
    - "src/domain/mutation/move-paths/handler.ts"
  allowed_topics:
    - "Whether the path-mutation handlers return the guardrail failure as a string value (not throw) — delete, copy, move do this; verify for each"
    - "Exact return type of each path-mutation handler to confirm string return is appropriate for the refactored assertPathMutationBatchBudget throw semantics"
    - "Whether create-directories also uses return vs throw for its batch-budget guard"
checkpoint_strategy: "SEQUENTIAL_CHECKPOINTS_REQUIRED"
checkpoint_eligibility_contract:
  durable_skip_authority_only: true
  ephemeral_freshness_steps:
    - "Initial full-file re-anchor reads of all 8 target files after 1.16 completes"
  durable_resume_checkpoints:
    - "CHK-1.17-A"
    - "CHK-1.17-B"
    - "CHK-1.17-C"
    - "CHK-1.17-D"
resume_validation_contract:
  lowest_level_truth_surface: "checkpoint-ledger"
  task_lifecycle_truth_surface: ".plan/1-runtime-architecture-refactors/orchestration.md"
  global_frontier_truth_surface: "PLAN.md"
  artifact_evidence_sources:
    - "src/domain/mutation/shared/mutation-guardrails.ts — existence and presence of exports"
    - "src/infrastructure/formatting/batch-result-formatter.ts — presence of formatBatchMutationSummary export"
    - "Any mutation handler — absence of inline MAX_OPERATIONS_PER_PATH_MUTATION_REQUEST guard block"
    - "Any mutation handler — absence of inline summary string building (Processed N items)"
  conflict_requires_user_confirmation: true
  partial_progress_requires_user_decision: true
  durable_skip_authority_only: true
  ephemeral_checkpoint_conflict_requires_reexecution: false
artifact_progress_inference_rules:
  - "If mutation-guardrails.ts exists and exports assertPathMutationBatchBudget, CHK-1.17-A is DONE"
  - "If batch-result-formatter.ts exports formatBatchMutationSummary, CHK-1.17-B is DONE"
  - "If a path-mutation handler no longer contains the inline MAX_OPERATIONS check, its portion of CHK-1.17-C is DONE for that file"
  - "If a content-mutation handler no longer contains the inline CONTENT_MUTATION_TOTAL_INPUT_CHARS check, its portion of CHK-1.17-D is DONE for that file"
  - "Freshness re-anchor reads never create durable skip authority"
user_resume_decision_gate:
  required_on_partial_progress: true
  required_on_state_conflict: true
  default_user_choices:
    - "ACCEPT_AND_CONTINUE"
    - "REVERIFY_EXISTING_OUTPUTS"
task_completion_order_contract:
  ordered_phase_sequence:
    - "task_file_local_completion_closure"
    - "parent_orchestration_status_update"
    - "dependency_resolution_and_unblock"
    - "unit_and_plan_completion_cascade"
  parent_status_write_blocked_until_task_file_done: true
  plan_update_blocked_until_parent_sync: true
current_target_binding_contract:
  binding_surface: "active_task_file"
  proof_scope: "task_file_local"
  invalidate_on_target_or_frontier_change: true
task_local_completion_ledger:
  required_units:
    - "actions_executed"
    - "acceptance_criteria_pass"
    - "required_artifacts_materialized_or_verified"
    - "checkpoint_or_artifact_progress_synced"
    - "persona_write_permission_green_go"
task_local_pre_write_gate_report_contract:
  gate_inputs:
    - "current_target_binding"
    - "ordered_phase_contract"
    - "task_local_completion_ledger"
    - "no_heuristic_exit_governance"
  write_allowed_only_if_all_pass: true
no_heuristic_exit_governance:
  task_file_done_requires_visible_local_proof: true
  broader_parent_or_plan_pass_not_sufficient: true
persona_generation_required: true
persona_generation_mode: "runtime_regenerate"
persona_source_prompt_path: "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
persona_primary_domain_kernel: "TypeScript Node.js MCP Server Mutation Domain Architecture — Guardrail and Formatting Helper Extraction"
persona_secondary_specializations:
  - "Domain layer guardrail enforcement patterns"
  - "Infrastructure formatting module design"
  - "DDD bounded context internal shared helper placement"
persona_current_task_specialization: "Mutation domain guardrail helper and batch summary formatter — eliminating inline guard + summary patterns across 6 handlers"
persona_generation_lineage: "Derived from task contract: two new helper files + 6 handler updates, TypeScript, DDD mutation bounded context, no runtime behavior change"
persona_runtime_review_required: true
persona_source_load_policy:
  source_loading_mode: "DEFERRED_UNTIL_PRE_EXECUTION_STATE_SNAPSHOT_CLOSED"
  early_source_load_forbidden: true
  late_load_trigger: "after pre_execution_state_snapshot_closed and immediately before execute_time_persona_subprocess"
  transitive_source_chain_must_be_closed_after_load: true
persona_state_disclosure_contract:
  pre_creator_state_report_required: true
  post_creator_ledger_state_report_required: true
  required_state_fields:
    - "task_id"
    - "pre_execution_state_snapshot_closed"
    - "persona_source_documents_loaded"
    - "persona_source_chain_closed"
    - "creator_phase_execution_ledger_status"
    - "runtime_persona_outcome"
    - "persona_write_permission"
  final_dx_log_after_proof_only: true
persona_embedded_creator_execution_contract:
  execution_mode: "FULL_CONTRACT_EQUIVALENT_EXECUTION"
  creator_phase_source: "required_creator_execution_phases"
  creator_phase_execution_ledger_required: true
  creator_phase_order_non_swappable: true
  local_creator_logic_redefinition_forbidden: true
persona_write_gate_contract:
  persona_write_permission_default: "BLOCKED"
  persona_write_permission_release_requirements:
    - "pre_execution_state_snapshot_closed = true"
    - "persona_source_documents_loaded = true"
    - "persona_source_chain_closed = true"
    - "creator_phase_execution_ledger_complete = true"
    - "creator_completion_proof_pass = true"
    - "runtime_persona_outcome = GREEN_GO"
  final_green_go_required_for_write: true
persona_dx_log_contract:
  pre_proof_surface: "persona_state_report_only"
  final_dx_log_after_proof_only: true
  required_final_fields:
    - "runtime_persona_title"
    - "runtime_persona_summary"
    - "runtime_persona_alignment_percent"
    - "runtime_persona_outcome"
    - "creator_completion_proof_summary"
persona_creator_execution_proof_contract:
  transitive_persona_source_chain:
    - "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
    - "src/personas/it/__prompts__/interactive/best-options/enterprise/creation/prompt.md"
  canonical_creator_source_path: "src/personas/it/__prompts__/interactive/best-options/enterprise/creation/prompt.md"
  creator_equivalence_rule: "Runtime persona must match the canonical creator decision for the same final pre_execution_state_snapshot"
  required_creator_execution_phases:
    - "Context extraction and persona axis mapping"
    - "Multi-option generation across domain/language/operation axes"
    - "Weighted scoring and top-option selection"
    - "Quality gate and persona finalization"
  required_creator_proof_fields:
    - "creator_transitive_chain_verified"
    - "creator_required_phases_completed"
    - "creator_equivalence_confirmed"
    - "snapshot_substitution_detected = false"
    - "creator_completion_proof_pass = true"
  non_substitutable_persona_surfaces:
    - "persona_derivation_manifest"
    - "persona_dx_snapshot"
    - "persona_title"
    - "persona_summary"
  proof_gate_blocks_green_go: true
  proof_failure_outcome: "PLAN_GAP"
persona_review_binding_contract:
  contract_source_ingestion_is_not_review: true
  pre_execution_state_required_surfaces:
    - "task_contract"
    - "upstream_plan_references"
    - "authoritative_references"
    - "resume_state_mode"
    - "artifact_evidence"
    - "files_read"
    - "files_modified"
  pre_execution_state_required_reads:
    - "authoritative_references"
    - "upstream_plan_references"
    - "files_read"
    - "files_modified"
  persona_review_finalization_gate: "GREEN_GO only after pre_execution_state_snapshot_closed = true"
  persona_review_invalidation_triggers:
    - "new_required_reference"
    - "new_or_changed_files_read_or_files_modified_reality"
    - "new_artifact_evidence"
    - "resume_candidate_change"
    - "shared_file_reality_change_before_first_action"
    - "new_boundary_or_consumer_evidence"
  persona_review_reentry_protocol: "invalidate_persona_outcome_and_rerun_review_before_first_action"
persona_dx_snapshot:
  title: "TypeScript Mutation Domain Architecture Specialist — Guardrail and Formatter Extraction"
  summary: "An enterprise TypeScript architect specializing in mutation domain bounded context design, guardrail helper placement, and infrastructure formatting patterns. Applies strict DDD principles to extract inline guardrail logic into bounded-context shared helpers."
  weighting_note: "Mutation domain bounded context architecture dominates over language specialization and narrow operation type."
checkpoint_definitions:
  - checkpoint_id: "CHK-1.17-A"
    kind: "sequential_step"
    completion_signal: "src/domain/mutation/shared/mutation-guardrails.ts exists and exports assertPathMutationBatchBudget and assertContentMutationInputBudget"
    produces_artifacts:
      - "src/domain/mutation/shared/mutation-guardrails.ts"
  - checkpoint_id: "CHK-1.17-B"
    kind: "sequential_step"
    completion_signal: "src/infrastructure/formatting/batch-result-formatter.ts exports formatBatchMutationSummary"
    produces_artifacts:
      - "src/infrastructure/formatting/batch-result-formatter.ts"
  - checkpoint_id: "CHK-1.17-C"
    kind: "artifact_milestone"
    completion_signal: "All 4 path-mutation handlers (delete, copy, move, create-directories) use assertPathMutationBatchBudget and formatBatchMutationSummary; no inline MAX_OPERATIONS guard remains"
    produces_artifacts:
      - "src/domain/mutation/delete-paths/handler.ts"
      - "src/domain/mutation/copy-paths/handler.ts"
      - "src/domain/mutation/move-paths/handler.ts"
      - "src/domain/mutation/create-directories/handler.ts"
  - checkpoint_id: "CHK-1.17-D"
    kind: "artifact_milestone"
    completion_signal: "Both content-mutation handlers (append-files, create-files) use assertContentMutationInputBudget and formatBatchMutationSummary; no inline CONTENT_MUTATION guard remains"
    produces_artifacts:
      - "src/domain/mutation/append-files/handler.ts"
      - "src/domain/mutation/create-files/handler.ts"
execution_progress_ledger:
  ledger_mode: "CHECKPOINT_LEDGER"
  checkpoints:
    - checkpoint_id: "CHK-1.17-A"
      status: "DONE"
    - checkpoint_id: "CHK-1.17-B"
      status: "DONE"
    - checkpoint_id: "CHK-1.17-C"
      status: "DONE"
    - checkpoint_id: "CHK-1.17-D"
      status: "DONE"
task_local_completion_ledger:
  actions_executed: true
  acceptance_criteria_pass: true
  required_artifacts_materialized_or_verified: true
  checkpoint_or_artifact_progress_synced: true
  persona_write_permission_green_go: true
task_local_pre_write_gate_report:
  current_target_binding: "PASS"
  ordered_phase_contract: "PASS"
  task_local_completion_ledger: "PASS"
  no_heuristic_exit_governance: "PASS"
  final_write_allowed: "PASS"
resume_conflict_resolution_rules:
  - "If CHK-1.17-A DONE but mutation-guardrails.ts is absent, STOP and request user confirmation"
  - "If CHK-1.17-C shows partial completion, continue only with uncompleted handlers"
  - "Never begin CHK-1.17-C or CHK-1.17-D before CHK-1.17-A and CHK-1.17-B are both DONE"
checkpoint_update_protocol:
  - "Mark checkpoint IN_PROGRESS when editing begins on first file of that group"
  - "Mark checkpoint DONE when all files in the group are verified"
  - "CHK-1.17-A and CHK-1.17-B may be done in parallel since they target different files"
  - "CHK-1.17-C and CHK-1.17-D may be done in parallel since they target different files"
  - "Never substitute checkpoint ledger for task lifecycle status in parent orchestration"
halt_conditions:
  plan_reality_mismatch: true
  missing_required_plan_information: true
  autonomous_decision_required: true
user_notification_required: true
---

# Task 1.17: Mutation Domain Shared Helpers
[INTENT: ANWEISUNG]

## Goal
[INTENT: KONTEXT]

This task extracts three categories of inline mutation-layer patterns into canonical shared helper locations. First, the identical 5-line inline path-mutation batch-size guardrail check (using `MAX_OPERATIONS_PER_PATH_MUTATION_REQUEST`) is duplicated in four handlers and should move to a new `assertPathMutationBatchBudget` function in a new `src/domain/mutation/shared/mutation-guardrails.ts` module. Second, the identical inline content-mutation input budget check (using `CONTENT_MUTATION_TOTAL_INPUT_CHARS`) is duplicated in two handlers and should move to `assertContentMutationInputBudget` in the same module. Third, the identical batch-summary output pattern (`Processed N items:... / N succeeded / M failed\n...Errors:...`) appears in five handlers and should be extracted into `formatBatchMutationSummary` in the existing `batch-result-formatter.ts` infrastructure module (which already exports `formatBatchTextOperationResults` used by other handlers).

All changes are structural extractions. No guardrail logic, no response content, and no error behavior is changed.

## Expected Result
[INTENT: KONTEXT]

- `src/domain/mutation/shared/mutation-guardrails.ts` exports `assertPathMutationBatchBudget` and `assertContentMutationInputBudget`.
- `batch-result-formatter.ts` exports `formatBatchMutationSummary`.
- All 6 mutation handlers use the helpers and have no inline duplicates of the extracted patterns.

## Preconditions
[INTENT: CONSTRAINT]

- Task 1.16 must be DONE — all 5 mutation handlers were modified in 1.16 (error message extraction); those must be re-anchored before this task edits them.

## Execution Metadata Register
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Task ID | `1.17` | Eindeutige Aufgabenkennung |
| Classification | `SEQUENTIAL` | After 1.16; shared-file conflict on 5 mutation handlers |
| Information Completeness Mode | `FULLY_SPECIFIED` | All function signatures, placement, and import paths bound |
| Implementation Scenarios | `FUNCTION_CREATION, FUNCTION_UPDATE, OBJECT_CREATION` | Two new helper exports + handler updates |
| Execution Surface Band | `YELLOW` | 8 files, all mutation layer, two anchor points |
| Primary Split Axis | `none` | Not further split |
| Single-Agent Viability Note | Uniform patterns; two stable anchor files; no cross-boundary reasoning |
| Parent Orchestration | `.plan/1-runtime-architecture-refactors/orchestration.md` |  |
| Blocked By | `1.16` |  |
| Requires Upstream Plan Re-Anchoring | `yes` | Re-read 1.16 task; re-anchor all mutation handler files after 1.16 |
| Runtime Discovery Allowed | `yes` | Verify return vs throw semantics in path-mutation handlers |
| Checkpoint Strategy | `SEQUENTIAL_CHECKPOINTS_REQUIRED` | New helpers must exist before handlers can import |
| User Notification Required On Gap/Mismatch | `true` |  |
| Persona Primary Domain Kernel | `TypeScript Node.js MCP Server Mutation Domain Architecture — Guardrail and Formatting Helper Extraction` |  |

## Task Contract Closure
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Decision Surfaces Closed | `true` | All binding surfaces fully specified |
| Unresolved Determinable Decisions | `0` | None |
| Expected Execute-Agent Questions | `0` | None; limited runtime discovery for return/throw semantic confirmation |

## Decision Surface Register / Authoritative Literal Bindings / Authoritative Code / Contract Surfaces
[INTENT: REFERENZ]

*(See YAML frontmatter for complete specifications)*

## Execute-Agent Zero-Question Gate
[INTENT: CONSTRAINT]

- **Gate Result:** `PASS`
- **Checked Domains:** naming, path_and_placement, function_signatures_and_returns, dependencies_and_import_surfaces, consumer_and_callsite_bindings, validation_and_boundary_rules
- **Rule:** Limited runtime discovery allowed only for return/throw semantic confirmation in path-mutation handlers.

## Authoritative References
[INTENT: REFERENZ]

| Path | Kind | Must Read Before Execution | Purpose |
|------|------|---------------------------|---------|
| `src/domain/mutation/delete-paths/handler.ts` | business-file | true | Path-budget guard pattern; modified by 1.16 |
| `src/domain/mutation/copy-paths/handler.ts` | business-file | true | Same |
| `src/domain/mutation/move-paths/handler.ts` | business-file | true | Same |
| `src/domain/mutation/create-directories/handler.ts` | business-file | true | Same |
| `src/domain/mutation/append-files/handler.ts` | business-file | true | Content-budget guard pattern; modified by 1.16 |
| `src/domain/mutation/create-files/handler.ts` | business-file | true | Same |
| `src/infrastructure/formatting/batch-result-formatter.ts` | business-file | true | Target for new summary formatter export |
| `src/domain/shared/guardrails/tool-guardrail-error-contract.ts` | business-file | true | Provides createRuntimeBudgetExceededFailure |
| `src/domain/shared/guardrails/tool-guardrail-limits.ts` | business-file | true | Provides the budget constants |
| `.plan/1-runtime-architecture-refactors/1.16-extract-error-message-utility.md` | plan-file | true | Documents 1.16 modifications to mutation handlers |

## Context
[INTENT: KONTEXT]

- **Domain:** `src/domain/mutation/` bounded context
- **Technical Context:** TypeScript. Path-mutation handlers currently return (not throw) the guardrail failure text because their return type is `Promise<string>`. The new `assertPathMutationBatchBudget` **throws** instead of returning — this is the architecturally correct behavior consistent with all other guardrail assert functions in the codebase. The runtime discovery scope allows confirming whether any handler catches that throw or propagates it.
- **Business Context:** SSOT extraction. No guardrail behavior changes.
- **Architectural Context:** `src/domain/mutation/shared/` is the correct placement for mutation-bounded-context-internal shared helpers, parallel to `src/domain/inspection/shared/`. The batch summary formatter belongs in `@infrastructure/formatting/batch-result-formatter` because it is a presentation concern, not a domain business rule.
- **Runtime Discovery Context:** Verify return vs throw semantic for each path-mutation handler's batch guard; if any handler catches and swallows the error from the new throw form, it needs a try/catch wrapper around `assertPathMutationBatchBudget`.

## Critical Note on Return vs Throw Semantics
[INTENT: CONSTRAINT]

The current handlers return the guardrail failure text as a string (e.g., `return formatToolGuardrailFailureAsText(...)`). The new `assertPathMutationBatchBudget` **throws** instead. This is a semantic change that is architecturally correct (consistent with all other assert helpers), but the execute agent MUST verify that the callers handle this correctly:

- If the handler catches exceptions and converts them to error strings, `assertPathMutationBatchBudget` will work correctly — the exception propagates up to the MCP server shell which converts it to an error response.
- If the handler does NOT catch exceptions (the MCP shell handles them), same result.
- If the handler currently uses `return` because it catches and formats guardrail failures differently than exceptions, a `try/catch` wrapper around `assertPathMutationBatchBudget` may be needed.

**Action required:** During runtime discovery, re-read each handler to confirm the call pattern. If a handler's guardrail return behavior differs from `throw`, inform the user before applying the change.

## Information Units
[INTENT: SPEZIFIKATION]

### IU-001: Create src/domain/mutation/shared/mutation-guardrails.ts

**Type:** `object`

**Description:** New module in the mutation bounded context's shared subdirectory, exporting `assertPathMutationBatchBudget` and `assertContentMutationInputBudget`. The file contains both functions and their shared imports.

**Ist-Zustand:** File does not exist; `src/domain/mutation/shared/` may not exist.

**Soll-Zustand:** File exists with both function exports as specified in `authoritative_code_surfaces`.

**Checkpoint Relevance:**
- **Checkpoint or Artifact Impact:** CHK-1.17-A (must be done before handlers can import)

---

### IU-002: Add formatBatchMutationSummary to batch-result-formatter.ts

**Type:** `function`

**Description:** New function export in the existing infrastructure formatting module.

**Ist-Zustand:** `batch-result-formatter.ts` exports `formatBatchTextOperationResults` only (already used by copy-paths, diff handlers, and get-path-metadata). No mutation summary formatter exists.

**Soll-Zustand:** Also exports `formatBatchMutationSummary` with the exact signature from `authoritative_code_surfaces`.

**Checkpoint Relevance:**
- **Checkpoint or Artifact Impact:** CHK-1.17-B (must be done before handlers can import)

---

### IU-003: Update 4 path-mutation handlers

**Type:** `other`

**Description:** Replace the inline batch-size guard block and the manual summary construction in delete-paths, copy-paths, move-paths, and create-directories.

**Implementation Contract:**
- Add imports: `assertPathMutationBatchBudget` from `../shared/mutation-guardrails` and `formatBatchMutationSummary` from `@infrastructure/formatting/batch-result-formatter`
- Replace inline guard block with `assertPathMutationBatchBudget(toolName, operations.length)`
- Replace manual summary construction with `return formatBatchMutationSummary(operationNoun, successCount, errors)`
- Remove no-longer-needed imports of `MAX_OPERATIONS_PER_PATH_MUTATION_REQUEST`, `createRuntimeBudgetExceededFailure`, `formatToolGuardrailFailureAsText` IF those were only used by the extracted guard block (confirm no other usage)

**Checkpoint Relevance:**
- **Checkpoint or Artifact Impact:** CHK-1.17-C

---

### IU-004: Update 2 content-mutation handlers

**Type:** `other`

**Description:** Replace the inline content-budget guard block and the manual summary construction in append-files and create-files.

**Implementation Contract:**
- Add imports: `assertContentMutationInputBudget` from `../shared/mutation-guardrails` and `formatBatchMutationSummary` from `@infrastructure/formatting/batch-result-formatter`
- Replace inline content-budget guard with `assertContentMutationInputBudget(toolName, files)`
- Replace manual summary construction with `return formatBatchMutationSummary("files", successCount, errors)`
- Remove no-longer-needed imports IF exclusively used by the extracted patterns

**Checkpoint Relevance:**
- **Checkpoint or Artifact Impact:** CHK-1.17-D

## Actions
[INTENT: ANWEISUNG]

1. **Verify task 1.16 completion and re-anchor all mutation handler files (ephemeral)**
   - **What:** Read 1.16 task file; re-anchor all 6 mutation handler files to understand current state after 1.16's error-message extraction
   - **Checkpoint Impact:** None (ephemeral)
   - **Stop if:** 1.16 is not DONE; inform user

2. **Perform bounded runtime discovery (ephemeral)**
   - **What:** Re-read each path-mutation handler to confirm return vs throw semantics of the current inline guard block
   - **Allowed Topics:** Whether handler uses `return formatToolGuardrailFailureAsText(...)` or `throw new Error(...)`; whether any handler catches exceptions
   - **Output:** Internal decision on whether `assertPathMutationBatchBudget` (which throws) is a direct drop-in or needs a try/catch wrapper in any handler
   - **Checkpoint Impact:** None (ephemeral)
   - **Stop if:** A handler's semantics are materially different from what this plan describes; inform user before proceeding

3. **Create mutation-guardrails.ts (CHK-1.17-A)**
   - **What:** Create new file `src/domain/mutation/shared/mutation-guardrails.ts` with both guard functions
   - **How:** Use exact function bodies from `authoritative_code_surfaces`. Create `/shared/` subdirectory if needed.
   - **Output Contract:** File exports `assertPathMutationBatchBudget` and `assertContentMutationInputBudget`
   - **Checkpoint Impact:** COMPLETES CHK-1.17-A
   - **Stop if:** Cannot create file or directory; inform user

4. **Add formatBatchMutationSummary to batch-result-formatter.ts (CHK-1.17-B)**
   - **What:** Add new function export after existing `formatBatchTextOperationResults`
   - **How:** Use exact function body from `authoritative_code_surfaces`
   - **Output Contract:** File exports `formatBatchMutationSummary`; no existing export removed
   - **Checkpoint Impact:** COMPLETES CHK-1.17-B
   - **Stop if:** File structure differs materially; inform user

5. **Update 4 path-mutation handlers (CHK-1.17-C)**
   - **What:** Replace inline guard + manual summary in delete-paths, copy-paths, move-paths, create-directories
   - **How:** (a) Add imports; (b) replace guard block with `assertPathMutationBatchBudget(toolName, operations.length)`; (c) replace manual summary block with `return formatBatchMutationSummary(noun, successCount, errors)`. If runtime discovery shows the handler uses `return` for the guard failure (rather than `throw`), wrap the assert call: `try { assertPathMutationBatchBudget(toolName, count); } catch (guardError) { return extractErrorMessage(guardError); }` — inform user of this adjustment.
   - **Note on operationNoun:** Use the plural noun that matches the current handler's summary text. For delete-paths: `"paths"`. For copy-paths: consult the re-anchored file. For move-paths: same. For create-directories: `"directories"`.
   - **Output Contract:** No inline MAX_OPERATIONS guard or manual summary block remains in any of the 4 files
   - **Checkpoint Impact:** COMPLETES CHK-1.17-C file by file
   - **Stop if:** The inline guard block in a specific file is not found; check if it was already removed; confirm with user

6. **Update 2 content-mutation handlers (CHK-1.17-D)**
   - **What:** Replace inline budget guard + manual summary in append-files and create-files
   - **How:** (a) Add imports; (b) replace inline budget check with `assertContentMutationInputBudget(toolName, files)` (this throws, consistent with the current throw pattern in these two handlers); (c) replace manual summary block with `return formatBatchMutationSummary("files", successCount, errors)`
   - **Output Contract:** No inline CONTENT_MUTATION_TOTAL_INPUT_CHARS guard or manual summary block remains
   - **Checkpoint Impact:** COMPLETES CHK-1.17-D file by file
   - **Stop if:** Pattern not found; confirm with user

## Acceptance Criteria
[INTENT: REFERENZ]

- [ ] `src/domain/mutation/shared/mutation-guardrails.ts` exists and exports `assertPathMutationBatchBudget(toolName: string, operationCount: number): void`
- [ ] Same file exports `assertContentMutationInputBudget(toolName: string, files: ReadonlyArray<{ readonly content: string }>): void`
- [ ] `batch-result-formatter.ts` exports `formatBatchMutationSummary(operationNoun: string, successCount: number, errors: readonly string[]): string`
- [ ] No inline `if (operations.length > MAX_OPERATIONS_PER_PATH_MUTATION_REQUEST)` block remains in any of the 4 path-mutation handlers
- [ ] No inline `if (totalInputChars > CONTENT_MUTATION_TOTAL_INPUT_CHARS)` block remains in append-files or create-files
- [ ] No inline `Processed ${N} [...]:` summary construction remains in any of the 6 mutation handlers
- [ ] All 6 handlers import and use the extracted helpers
- [ ] All mutation handler outputs are functionally identical before and after

## Stop Conditions & User Escalation
[INTENT: CONSTRAINT]

- **Plan-vs-Reality Mismatch:** If a path-mutation handler uses `return` (not `throw`) for the guard failure AND does NOT have a try/catch wrapper, stop and inform user of the semantic difference before applying `assertPathMutationBatchBudget`.
- **Missing Required Plan Information:** If the operationNoun for a specific handler differs from what the plan assumes, stop and confirm with user.
- **Autonomous Decision Required:** If removing a handler's no-longer-needed imports would also remove an import still needed for another purpose, stop and inform user before removing.
- **User Message Contract:** `Plan says [X] / current reality is [Y] / [what specifically differs] / continuing would require an autonomous decision`.

## Post-Completion Instructions
[INTENT: ANWEISUNG]

1. Mark this task file locally `DONE` and close the task-local completion ledger.
2. Mark task 1.17 [x] in parent orchestration: `.plan/1-runtime-architecture-refactors/orchestration.md`
3. All four new tasks (1.14, 1.15, 1.16, 1.17) are now complete — update orchestration.md to reflect the correct task count and advance the frontier.
4. Update PLAN.md frontier: unit 1 is now fully DONE (all 17 tasks complete including the 4 new ones).
5. PLAN.md frontier should now advance to unit 2 (resume_frontier_unit: "2", resume_frontier_task: "2.1").

## File Path Index
[INTENT: REFERENZ]

| # | Path | Kind | Why Relevant | Must Read Before Execution |
|---|------|------|--------------|----------------------------|
| 1 | `src/domain/mutation/shared/mutation-guardrails.ts` | business-file | New file — CHK-1.17-A | false (new) |
| 2 | `src/infrastructure/formatting/batch-result-formatter.ts` | business-file | Receives new helper — CHK-1.17-B | true |
| 3 | `src/domain/mutation/delete-paths/handler.ts` | business-file | Inline patterns to replace; modified by 1.16 | true |
| 4 | `src/domain/mutation/copy-paths/handler.ts` | business-file | Same | true |
| 5 | `src/domain/mutation/move-paths/handler.ts` | business-file | Same | true |
| 6 | `src/domain/mutation/create-directories/handler.ts` | business-file | Same | true |
| 7 | `src/domain/mutation/append-files/handler.ts` | business-file | Content guard + summary to replace; modified by 1.16 | true |
| 8 | `src/domain/mutation/create-files/handler.ts` | business-file | Same | true |
| 9 | `src/domain/shared/guardrails/tool-guardrail-error-contract.ts` | business-file | Source of createRuntimeBudgetExceededFailure | true |
| 10 | `src/domain/shared/guardrails/tool-guardrail-limits.ts` | business-file | Source of budget constants | true |
| 11 | `.plan/1-runtime-architecture-refactors/1.16-extract-error-message-utility.md` | plan-file | Documents 1.16 modifications | true |
| 12 | `.plan/1-runtime-architecture-refactors/orchestration.md` | plan-file | Mark task DONE + advance frontier | false |
| 13 | `PLAN.md` | plan-file | Advance to unit 2 frontier after all 1.14-1.17 done | false |

## Notes
[INTENT: KONTEXT]

- `copy-paths/handler.ts` already uses `formatBatchTextOperationResults` from batch-result-formatter — it does NOT use the manual summary pattern. Re-anchor the file to confirm whether it needs `formatBatchMutationSummary` or can keep its existing formatting call. If `copy-paths` already has a different output format than the manual pattern, it may not need this task's summary formatter. Stop and confirm with user if this is the case.
- `replace-file-line-ranges/handler.ts` is intentionally NOT in this task's `files_modified` — its output format includes diff blocks and is semantically different from the simple `Processed N items:` pattern; it should not be forced into `formatBatchMutationSummary`.
- The `create-directories/handler.ts` does not currently use `assertActualTextBudget` for its output — unlike the 5 content handlers that call it. This is intentional and should not be changed here.
- After task 1.17 completes, the orchestration.md needs to be updated to show all 17 tasks (1.1–1.17), with 1.14–1.17 as new tasks beyond the original 12, and the unit status updated to DONE once all 17 are complete.
