---
file_type: "task"
task_id: "9.1"
task_title: "`list_allowed_directories` doc set"
unit: "9"
parent_orchestration: ".plan/9-server-scope-docs/orchestration.md"
classification: "ISOLATED"
status: "pending"
estimated_complexity: "MEDIUM"
files_modified:
  - "src/application/server/list-allowed-directories/CONVENTIONS.md"
  - "src/application/server/list-allowed-directories/DESCRIPTION.md"
  - "src/application/server/list-allowed-directories/README.md"
files_read:
  - "src/application/server/register-server-scope-tools.ts"
  - "src/application/server/filesystem-server.ts"
  - "src/application/server/register-tool-catalog.ts"
  - "README.md"
  - "DESCRIPTION.md"
dependencies: []
blocked_by: "none"
information_completeness_mode: "FULLY_SPECIFIED"
implementation_scenarios:
  - "OBJECT_CREATION"
  - "OTHER"
execution_surface_band: "GREEN"
primary_split_axis: "none"
single_agent_viability_note: "The task is bounded to one server-scope endpoint and a docs-only local documentation folder."
task_contract_closure:
  decision_surfaces_closed: true
  unresolved_determinable_decisions: 0
  expected_execute_agent_questions: 0
decision_surface_register:
  - surface: "naming"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "The docs-only endpoint triplet must be named `CONVENTIONS.md`, `DESCRIPTION.md`, and `README.md`."
  - surface: "path_and_placement"
    status: "BOUND_LITERAL"
    binding_source: "orchestration contract"
    notes: "Because the tool has no dedicated domain folder, create a docs-only folder at `src/application/server/list-allowed-directories/`."
  - surface: "literal_values_and_budgets"
    status: "BOUND_REFERENCE"
    binding_source: "current code state"
    notes: "The tool reports allowed-directory scope directly from the server-owned `allowedDirectories` array and has no separate request arguments or endpoint-local budget model."
  - surface: "defaults_and_fallbacks"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "This doc set must explain that the tool is application-owned because it describes server scope rather than domain behavior."
  - surface: "types_and_shapes"
    status: "BOUND_REFERENCE"
    binding_source: "registration and server-shell surfaces"
    notes: "The docs must explain the text-only server-scope output and the absence of a domain handler/schema pair accurately."
  - surface: "function_signatures_and_returns"
    status: "BOUND_REFERENCE"
    binding_source: "authoritative code surfaces"
    notes: "The current registration function and server-owned `allowedDirectories` source remain authoritative."
  - surface: "object_schema_and_mapper_contracts"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "The three local markdown files have non-overlapping SSOT roles even though the endpoint folder is documentation-only."
  - surface: "dependencies_and_import_surfaces"
    status: "BOUND_REFERENCE"
    binding_source: "current server-scope registration surfaces"
    notes: "The docs must reflect that this endpoint is registered from the application layer and composed through the tool catalog."
  - surface: "validation_and_boundary_rules"
    status: "BOUND_LITERAL"
    binding_source: "accepted architecture"
    notes: "The docs must explain that the tool exposes effective filesystem scope only and does not mutate or introspect file contents."
  - surface: "consumer_and_callsite_bindings"
    status: "BOUND_LITERAL"
    binding_source: "user requirement"
    notes: "Root docs will later re-reference this local triplet rather than duplicate it."
authoritative_literal_bindings:
  - binding_domain: "docs-only folder placement"
    exact_binding: "Create `CONVENTIONS.md`, `DESCRIPTION.md`, and `README.md` inside `src/application/server/list-allowed-directories/` because the tool has no dedicated code directory."
    source: "unit orchestration note"
    placement_or_consumer: "docs-only endpoint folder"
    no_autonomous_choice_rule: "The execute agent must not place the docs at root level or inside an unrelated domain directory."
  - binding_domain: "server-scope ownership"
    exact_binding: "The tool is application-owned because it describes server scope rather than domain behavior."
    source: "accepted architecture"
    placement_or_consumer: "local conventions and description"
    no_autonomous_choice_rule: "The execute agent must not document the tool as a domain-owned filesystem behavior endpoint."
requires_authoritative_code_surfaces: true
authoritative_code_surfaces:
  - surface_name: "Server-scope registration contract"
    kind: "shape"
    snippet_or_contract: "The tool is registered as `list_allowed_directories` with a text-only result built directly from the `allowedDirectories` array."
    source: "current registration surface"
    placement_or_consumer: "`src/application/server/register-server-scope-tools.ts`"
  - surface_name: "Server-shell ownership contract"
    kind: "shape"
    snippet_or_contract: "The authoritative source of the tool’s output is the server-owned allowed-directory scope held by `FilesystemServer` and passed into the registration context."
    source: "current server-shell surfaces"
    placement_or_consumer: "`src/application/server/filesystem-server.ts`, `src/application/server/register-tool-catalog.ts`"
authoritative_references:
  - path: "src/application/server/register-server-scope-tools.ts"
    kind: "business-file"
    usage: "Current server-scope tool registration and output shape"
    required_before_execution: true
  - path: "src/application/server/filesystem-server.ts"
    kind: "business-file"
    usage: "Current application-owned server shell that holds the `allowedDirectories` scope"
    required_before_execution: true
  - path: "src/application/server/register-tool-catalog.ts"
    kind: "business-file"
    usage: "Current composition-root context for the server-scope tool"
    required_before_execution: true
  - path: "README.md"
    kind: "markdown-doc"
    usage: "Current root DX surface that will later reference the endpoint-local README"
    required_before_execution: true
  - path: "DESCRIPTION.md"
    kind: "markdown-doc"
    usage: "Current root architecture surface that will later reference the endpoint-local description"
    required_before_execution: true
upstream_plan_references: []
runtime_discovery:
  required: false
  scope: []
  allowed_topics: []
checkpoint_strategy: "ARTIFACT_MILESTONES_REQUIRED"
checkpoint_eligibility_contract:
  durable_skip_authority_only: true
  ephemeral_freshness_steps:
    - "Re-read the current server-scope registration, server-shell code, composition root, and root documentation surfaces"
  durable_resume_checkpoints:
    - "cp-1-context-reanchor"
    - "cp-2-endpoint-conventions"
    - "cp-3-endpoint-description"
    - "cp-4-endpoint-readme"
resume_validation_contract:
  lowest_level_truth_surface: "artifact-evidence"
  task_lifecycle_truth_surface: ".plan/9-server-scope-docs/orchestration.md"
  global_frontier_truth_surface: "PLAN.md"
  artifact_evidence_sources:
    - "src/application/server/list-allowed-directories/CONVENTIONS.md"
    - "src/application/server/list-allowed-directories/DESCRIPTION.md"
    - "src/application/server/list-allowed-directories/README.md"
  conflict_requires_user_confirmation: true
  partial_progress_requires_user_decision: true
  durable_skip_authority_only: true
  ephemeral_checkpoint_conflict_requires_reexecution: true
artifact_progress_inference_rules:
  - "Treat the task as partially complete only when one or more of the endpoint-local documentation files already exist."
  - "Do not treat edits to application registration files alone as completion evidence for the endpoint-local SSOT triplet."
  - "Ephemeral re-reads never create durable skip authority."
user_resume_decision_gate:
  required_on_partial_progress: true
  required_on_state_conflict: true
  default_user_choices:
    - "ACCEPT_AND_CONTINUE"
    - "REVERIFY_EXISTING_OUTPUTS"
persona_generation_required: true
persona_generation_mode: "runtime_regenerate"
persona_source_prompt_path: "src/personas/it/__prompts__/silent/enterprise/creation/prompt.md"
persona_primary_domain_kernel: "Enterprise endpoint documentation architecture"
persona_secondary_specializations:
  - "DX-oriented documentation design"
  - "server-scope contract description"
persona_current_task_specialization: "Application-owned server-scope endpoint documentation"
persona_generation_lineage: "Derived from the endpoint-local SSOT documentation rule and the current server-scope registration contract."
persona_runtime_review_required: true
persona_dx_snapshot:
  title: "Enterprise Endpoint Documentation Architect"
  summary: "Creates endpoint-local SSOT documentation triplets that are precise for agents and concise for developers."
  weighting_note: "The endpoint-local SSOT rule dominates over local wording preferences."
checkpoint_definitions:
  - checkpoint_id: "cp-1-context-reanchor"
    kind: "sequential_step"
    completion_signal: "All authoritative references are re-read."
    produces_artifacts: []
  - checkpoint_id: "cp-2-endpoint-conventions"
    kind: "artifact_milestone"
    completion_signal: "`CONVENTIONS.md` exists and owns endpoint-local server-scope policies and guardrails."
    produces_artifacts:
      - "src/application/server/list-allowed-directories/CONVENTIONS.md"
  - checkpoint_id: "cp-3-endpoint-description"
    kind: "artifact_milestone"
    completion_signal: "`DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents."
    produces_artifacts:
      - "src/application/server/list-allowed-directories/DESCRIPTION.md"
  - checkpoint_id: "cp-4-endpoint-readme"
    kind: "artifact_milestone"
    completion_signal: "`README.md` exists and provides a concise DX summary with links to the local docs."
    produces_artifacts:
      - "src/application/server/list-allowed-directories/README.md"
execution_progress_ledger:
  ledger_mode: "CHECKPOINT_LEDGER"
  checkpoints:
    - checkpoint_id: "cp-1-context-reanchor"
      status: "DONE"
    - checkpoint_id: "cp-2-endpoint-conventions"
      status: "DONE"
    - checkpoint_id: "cp-3-endpoint-description"
      status: "DONE"
    - checkpoint_id: "cp-4-endpoint-readme"
      status: "DONE"
resume_conflict_resolution_rules:
  - "If one or two endpoint-local doc files exist but the triplet is incomplete, stop and ask whether the partial state should be accepted or reverified."
  - "If the docs are placed anywhere other than the docs-only server-scope folder, stop and escalate."
  - "If endpoint-local docs misclassify the tool as domain-owned rather than application-owned, stop and escalate."
checkpoint_update_protocol:
  - "Mark `cp-1-context-reanchor` DONE only after all authoritative references are re-read."
  - "Mark `cp-2-endpoint-conventions` DONE only after endpoint-local server-scope guardrails and policy rationale are fully documented."
  - "Mark `cp-3-endpoint-description` DONE only after the endpoint-local architectural description is complete and LLM-optimized."
  - "Mark `cp-4-endpoint-readme` DONE only after the DX summary exists and links the local docs."
halt_conditions:
  plan_reality_mismatch: true
  missing_required_plan_information: true
  autonomous_decision_required: true
user_notification_required: true
---

# Task 9.1: `list_allowed_directories` doc set
[INTENT: ANWEISUNG]

## Goal
[INTENT: KONTEXT]

Create the endpoint-local documentation triplet for `list_allowed_directories` so endpoint-specific server-scope conventions, application-ownership rationale, and DX guidance are owned locally instead of being implied only by registration code and root docs.

## Expected Result
[INTENT: KONTEXT]

- `src/application/server/list-allowed-directories/CONVENTIONS.md` exists and owns endpoint-local guardrails and conventions.
- `src/application/server/list-allowed-directories/DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents.
- `src/application/server/list-allowed-directories/README.md` exists and provides a concise DX-oriented summary plus links.

## Preconditions
[INTENT: CONSTRAINT]

- No preconditions (ISOLATED task).

## Execution Metadata Register
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Task ID | `9.1` | Unique task identifier |
| Classification | `ISOLATED` | No blocker exists |
| Information Completeness Mode | `FULLY_SPECIFIED` | All determinable doc surfaces are bound |
| Implementation Scenarios | `OBJECT_CREATION`, `OTHER` | Documentation triplet creation |
| Execution Surface Band | `GREEN` | One endpoint-local documentation concern |
| Primary Split Axis | `none` | No further split is required |
| Parent Orchestration | `.plan/9-server-scope-docs/orchestration.md` | Lifecycle status is tracked there |
| Blocked By | `none` | No blocker |
| Requires Upstream Plan Re-Anchoring | `no` | No upstream plan task must complete first |
| Runtime Discovery Allowed | `no` | This task is fully specified |
| Checkpoint Strategy | `ARTIFACT_MILESTONES_REQUIRED` | The three documentation files are durable outputs |
| Durable Skip Authority Only | `true` | Only durable documentation artifacts create skip authority |
| Ephemeral Freshness Steps Present | `yes` | Re-reading code and root docs remains non-durable |
| User Notification Required On Gap/Mismatch | `true` | Mismatches must stop execution |
| Persona Generation Required | `true` | Runtime persona regeneration remains mandatory |

## Task Contract Closure
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Decision Surfaces Closed | `true` | All determinable surfaces are bound |
| Unresolved Determinable Decisions | `0` | No deterministic gap remains |
| Expected Execute-Agent Questions | `0` | A fresh execute agent should not need follow-up questions |

## Decision Surface Register
[INTENT: REFERENZ]

| Surface | Status | Binding Source | Notes |
|---------|--------|----------------|-------|
| `naming` | `BOUND_LITERAL` | user requirement | The docs-only endpoint triplet must be named `CONVENTIONS.md`, `DESCRIPTION.md`, and `README.md`. |
| `path_and_placement` | `BOUND_LITERAL` | orchestration contract | Because the tool has no dedicated code directory, the docs must live in `src/application/server/list-allowed-directories/`. |
| `literal_values_and_budgets` | `BOUND_REFERENCE` | current code state | The tool reports the effective allowed-directory scope directly from the server-owned `allowedDirectories` array and has no separate request budget model. |
| `defaults_and_fallbacks` | `BOUND_LITERAL` | accepted architecture | The docs must explain that the tool is application-owned because it describes server scope rather than domain behavior. |
| `types_and_shapes` | `BOUND_REFERENCE` | registration and server-shell surfaces | The docs must explain the text-only server-scope output and the absence of a domain handler/schema pair accurately. |
| `function_signatures_and_returns` | `BOUND_REFERENCE` | authoritative code surfaces | The current registration function and server-owned `allowedDirectories` source remain authoritative. |
| `object_schema_and_mapper_contracts` | `BOUND_LITERAL` | user requirement | The three local markdown files have non-overlapping SSOT roles even though the endpoint folder is documentation-only. |
| `dependencies_and_import_surfaces` | `BOUND_REFERENCE` | current server-scope registration surfaces | The docs must reflect that this endpoint is registered from the application layer and composed through the tool catalog. |
| `validation_and_boundary_rules` | `BOUND_LITERAL` | accepted architecture | The docs must explain that the tool exposes effective filesystem scope only and does not mutate or introspect file contents. |
| `consumer_and_callsite_bindings` | `BOUND_LITERAL` | user requirement | Root docs will later re-reference this local triplet rather than duplicate it. |

## Authoritative Literal Bindings
[INTENT: REFERENZ]

| Binding Domain | Exact Binding | Source | Placement / Consumer | No Autonomous Choice Rule |
|---------------|---------------|--------|----------------------|---------------------------|
| Docs-only folder placement | Create `CONVENTIONS.md`, `DESCRIPTION.md`, and `README.md` inside `src/application/server/list-allowed-directories/` because the tool has no dedicated code directory. | unit orchestration note | docs-only endpoint folder | The execute agent must not place the docs at root level or inside an unrelated domain directory. |
| Server-scope ownership | The tool is application-owned because it describes server scope rather than domain behavior. | accepted architecture | local conventions and description | The execute agent must not document the tool as a domain-owned filesystem behavior endpoint. |

## Authoritative Code / Contract Surfaces
[INTENT: REFERENZ]

| Surface Name | Kind | Snippet / Contract | Source | Placement / Consumer |
|-------------|------|--------------------|--------|----------------------|
| Server-scope registration contract | `shape` | The tool is registered as `list_allowed_directories` with a text-only result built directly from the `allowedDirectories` array. | current registration surface | `src/application/server/register-server-scope-tools.ts` |
| Server-shell ownership contract | `shape` | The authoritative source of the tool’s output is the server-owned allowed-directory scope held by `FilesystemServer` and passed into the registration context. | current server-shell surfaces | `src/application/server/filesystem-server.ts`, `src/application/server/register-tool-catalog.ts` |

## Execute-Agent Zero-Question Gate
[INTENT: CONSTRAINT]

- **Gate Result:** `PASS`
- **Checked Domains:** docs-only folder placement, application ownership, text-only result shape, and root TOC relationship.
- **Rule:** A fresh execute agent must not need to ask where the files go, what each doc owns, or whether this tool belongs to the application shell or a domain folder.

## Authoritative References
[INTENT: REFERENZ]

| Path | Kind | Must Read Before Execution | Purpose | Referenced By |
|------|------|---------------------------|---------|---------------|
| `src/application/server/register-server-scope-tools.ts` | `business-file` | `true` | current server-scope tool registration and output shape | actions 1-4 |
| `src/application/server/filesystem-server.ts` | `business-file` | `true` | current application-owned server shell that holds the `allowedDirectories` scope | actions 1-4 |
| `src/application/server/register-tool-catalog.ts` | `business-file` | `true` | current composition-root context for the server-scope tool | actions 1-4 |
| `README.md` | `markdown-doc` | `true` | current root DX surface that will later reference the endpoint-local README | context |
| `DESCRIPTION.md` | `markdown-doc` | `true` | current root architecture surface that will later reference the endpoint-local description | context |

## Resume Validation Contract
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Lowest Level Truth Surface | `artifact-evidence` | The three endpoint-local documentation files are the primary resume truth |
| Task Lifecycle Truth Surface | `.plan/9-server-scope-docs/orchestration.md` | Parent orchestration owns lifecycle state |
| Global Frontier Truth Surface | `PLAN.md` | Global frontier SSOT |
| Conflict Requires User Confirmation | `true` | Conflicting partial documentation state requires escalation |
| Partial Progress Requires User Decision | `true` | Partial artifact state requires a user choice |
| Durable Skip Authority Only | `true` | Only durable documentation artifacts may authorize skip |
| Ephemeral Freshness Steps Must Re-Execute | `true` | Pure re-anchor work remains re-executable preparation |

## Artifact Progress Inference Rules
[INTENT: REFERENZ]

- Treat the task as partially complete only when one or more of the endpoint-local documentation files already exist.
- Do not treat edits to application registration files alone as completion evidence for the endpoint-local SSOT triplet.
- `EPHEMERAL_FRESHNESS_STEP` re-reads never create durable skip authority.

## User Resume Decision Gate
[INTENT: CONSTRAINT]

- **Required on Partial Progress:** `true`
- **Required on State Conflict:** `true`
- **Allowed User Choices:** `ACCEPT_AND_CONTINUE` / `REVERIFY_EXISTING_OUTPUTS`

## Persona Derivation Manifest
[INTENT: REFERENZ]

| Field | Value | Meaning |
|------|-------|---------|
| Persona Generation Required | `true` | task-specific persona derivation required |
| Persona Generation Mode | `runtime_regenerate` | execute regenerates the persona at runtime |
| Persona Source Prompt Path | `src/personas/it/__prompts__/silent/enterprise/creation/prompt.md` | authoritative persona source |
| Persona Primary Domain Kernel | `Enterprise endpoint documentation architecture` | highest-weight persona layer |
| Persona Secondary Specializations | `DX-oriented documentation design`, `server-scope contract description` | secondary persona layers |
| Persona Current Task Specialization | `application-owned server-scope endpoint documentation` | current specialization |
| Persona Generation Lineage | `Derived from the endpoint-local SSOT documentation rule and the current server-scope registration contract.` | derivation lineage |
| Persona Runtime Review Required | `true` | runtime review is required |

## Persona DX Snapshot
[INTENT: KONTEXT]

- **Persona Title:** `Enterprise Endpoint Documentation Architect`
- **Persona Summary:** Creates endpoint-local SSOT documentation triplets that are precise for agents and concise for developers.
- **Persona Weighting Note:** The endpoint-local SSOT rule dominates over local wording preferences.

## Context
[INTENT: KONTEXT]

- **Domain:** application-owned server-scope documentation
- **Related Files:** server-scope registration, application server shell, composition root, and root docs
- **Technical Context:** this tool reports the effective allowed-directory scope directly from the application-owned server shell and has no dedicated domain folder
- **Business Context:** agents and developers need endpoint-local guidance that explains why this tool belongs to server scope rather than to the inspection domain
- **Architectural Context:** endpoint-local SSOT comes first; root TOC integration happens later
- **Plan Context Dependencies:** none
- **Runtime Discovery Context:** none

## Execution Surface Assessment
[INTENT: REFERENZ]

- **Execution Surface Band:** `GREEN`
- **Primary Split Axis:** `none`
- **Hard-Gap Result:** `no RED or BLACK trigger active`
- **Single-Agent Viability Rationale:** The task is limited to one endpoint-local documentation triplet for one application-owned server-scope tool.

## Information Units
[INTENT: SPEZIFIKATION]

### IU-001: Endpoint-local `CONVENTIONS.md`
**Type:** `object`

**Description:**
Create the conventions file that becomes the local SSOT for server-scope guardrails, application-ownership rationale, and architectural boundaries.

**Ist-Zustand:**
The tool is registered from the application layer but has no endpoint-local conventions file.

**Soll-Zustand:**
`CONVENTIONS.md` explains server-scope ownership, docs-only folder placement, text-only scope disclosure, and why the endpoint is distinct from domain-owned inspection tools.

### IU-002: Endpoint-local `DESCRIPTION.md`
**Type:** `object`

**Description:**
Create the endpoint-local architectural description optimized for LLM-agent comprehension.

**Ist-Zustand:**
Only registration and server-shell code describe the endpoint today.

**Soll-Zustand:**
`DESCRIPTION.md` explains the endpoint contract, output source, application-ownership rationale, and server-scope role in agent-oriented detail.

### IU-003: Endpoint-local `README.md`
**Type:** `object`

**Description:**
Create the concise DX summary for the endpoint.

**Ist-Zustand:**
There is no endpoint-local DX summary file.

**Soll-Zustand:**
`README.md` summarizes what the endpoint does, links the local conventions and description, and stays concise.

## Actions
[INTENT: ANWEISUNG]

1. **Re-anchor the current server-scope registration, server-shell, composition-root, and root surfaces**
   - **What:** re-read the current server-scope registration, server-shell code, composition root, and root docs
   - **Where:** authoritative references listed above
   - **How:** read them completely before drafting any endpoint-local doc content
   - **Why:** the docs must reflect the current server-scope code truth and application-ownership model
   - **Inputs / References:** all authoritative references in this task file
   - **Output Contract:** current endpoint context is active
   - **Checkpoint Impact:** completes `cp-1-context-reanchor`
   - **Verification:** all required references are re-read successfully
   - **Stop if:** any required reference is unreadable
2. **Create endpoint-local `CONVENTIONS.md`**
   - **What:** create the conventions file for this endpoint
   - **Where:** `src/application/server/list-allowed-directories/CONVENTIONS.md`
   - **How:** document server-scope ownership, docs-only folder placement, text-only output semantics, and separation from domain-owned inspection tools
   - **Why:** endpoint-specific policy detail must have a local SSOT
   - **Inputs / References:** current registration and server-shell code
   - **Output Contract:** the endpoint-local conventions SSOT exists
   - **Checkpoint Impact:** completes `cp-2-endpoint-conventions`
   - **Verification:** the file explains endpoint-local policies and does not duplicate root TOC content as its primary structure
   - **Stop if:** conventions would need to invent runtime behavior not implemented in code
3. **Create endpoint-local `DESCRIPTION.md`**
   - **What:** create the endpoint-local description file
   - **Where:** `src/application/server/list-allowed-directories/DESCRIPTION.md`
   - **How:** describe the endpoint contract, output source, and application-owned server-scope role in LLM-oriented detail
   - **Why:** agents need a dedicated endpoint description surface beyond raw registration and server-shell text
   - **Inputs / References:** current server-scope registration, server-shell, composition root, and conventions
   - **Output Contract:** the endpoint-local architectural description exists
   - **Checkpoint Impact:** completes `cp-3-endpoint-description`
   - **Verification:** the file is detailed, LLM-oriented, and aligned to the current code surface
   - **Stop if:** the description would claim behavior not present in the registration/server-shell surfaces
4. **Create endpoint-local `README.md`**
   - **What:** create the concise DX summary file
   - **Where:** `src/application/server/list-allowed-directories/README.md`
   - **How:** summarize the endpoint purpose, key conventions, and links to the local `CONVENTIONS.md` and `DESCRIPTION.md`
   - **Why:** developers need a concise entry surface with quick orientation
   - **Inputs / References:** current endpoint code plus the newly created local docs
   - **Output Contract:** the endpoint-local DX summary exists
   - **Checkpoint Impact:** completes `cp-4-endpoint-readme`
   - **Verification:** the README stays concise and points to the local SSOT surfaces
   - **Stop if:** the README would need to duplicate the full conventions or description content

## Checkpoint Definitions
[INTENT: REFERENZ]

| Checkpoint ID | Kind | Completion Signal | Produces Artifacts | Resume Meaning |
|---------------|------|-------------------|--------------------|----------------|
| `cp-1-context-reanchor` | `sequential_step` | All authoritative references are re-read | none | drafting may begin |
| `cp-2-endpoint-conventions` | `artifact_milestone` | `CONVENTIONS.md` exists and owns endpoint-local server-scope policies and guardrails | `src/application/server/list-allowed-directories/CONVENTIONS.md` | conventions SSOT is complete |
| `cp-3-endpoint-description` | `artifact_milestone` | `DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents | `src/application/server/list-allowed-directories/DESCRIPTION.md` | architectural description is complete |
| `cp-4-endpoint-readme` | `artifact_milestone` | `README.md` exists and provides a concise DX summary with links | `src/application/server/list-allowed-directories/README.md` | endpoint doc triplet is complete |

## Execution Progress Ledger
[INTENT: REFERENZ]

| Checkpoint ID | Status | Evidence | Notes |
|---------------|--------|----------|-------|
| `cp-1-context-reanchor` | `PENDING` | authoritative references re-read | start here |
| `cp-2-endpoint-conventions` | `PENDING` | endpoint-local `CONVENTIONS.md` | depends on `cp-1-context-reanchor` |
| `cp-3-endpoint-description` | `PENDING` | endpoint-local `DESCRIPTION.md` | depends on `cp-2-endpoint-conventions` |
| `cp-4-endpoint-readme` | `PENDING` | endpoint-local `README.md` | depends on `cp-3-endpoint-description` |

## Resume Conflict Resolution Rules
[INTENT: CONSTRAINT]

- If one or two endpoint-local doc files exist but the triplet is incomplete, stop and ask whether the partial state should be accepted or reverified.
- If the docs are placed anywhere other than the docs-only server-scope folder, stop and escalate.
- If endpoint-local docs misclassify the tool as domain-owned rather than application-owned, stop and escalate.

## Checkpoint Update Protocol
[INTENT: ANWEISUNG]

1. Mark `cp-1-context-reanchor` DONE only after all authoritative references are re-read.
2. Mark `cp-2-endpoint-conventions` DONE only after endpoint-local server-scope guardrails and policy rationale are fully documented.
3. Mark `cp-3-endpoint-description` DONE only after the endpoint-local architectural description is complete and LLM-optimized.
4. Mark `cp-4-endpoint-readme` DONE only after the DX summary exists and links the local docs.

## Acceptance Criteria
[INTENT: REFERENZ]

- [ ] `src/application/server/list-allowed-directories/CONVENTIONS.md` exists and is the endpoint-local SSOT for conventions and guardrails.
- [ ] `src/application/server/list-allowed-directories/DESCRIPTION.md` exists and describes the endpoint architecture for LLM agents in detail.
- [ ] `src/application/server/list-allowed-directories/README.md` exists and provides a concise DX summary with links to the local docs.
- [ ] The documentation reflects application ownership and text-only server-scope disclosure accurately.
- [ ] The endpoint-local docs are ready to be referenced later from root-level TOC surfaces.

## Stop Conditions & User Escalation
[INTENT: CONSTRAINT]

- **Plan-vs-Reality Mismatch:** Stop if the current endpoint behavior differs materially from the assumptions documented here.
- **Missing Required Plan Information:** Stop if docs-only folder placement, server-scope ownership, or doc-role split surfaces cannot be bound exactly.
- **Autonomous Decision Required:** Stop if execution would require inventing endpoint behavior, ownership semantics, or root-TOC behavior not already agreed.
- **Decision Surface Not Closed:** If any determinable documentation-ownership or runtime-wording surface remains open, stop and inform the user.
- **Allowed Runtime Discovery:** None.
- **User Message Contract:** `Plan says X / current reality is Y / missing or conflicting information is Z / continuing would require an autonomous decision.`

## Post-Completion Instructions
[INTENT: ANWEISUNG]

1. Mark this task `[x]` in [`.plan/9-server-scope-docs/orchestration.md`](./orchestration.md).
2. Keep the resulting doc triplet ready for later root-level TOC linking.
3. Re-anchor [`PLAN.md`](../../PLAN.md).

## File Path Index
[INTENT: REFERENZ]

| # | Path | Kind | Why Relevant | Must Read Before Execution |
|---|------|------|--------------|----------------------------|
| 1 | `src/application/server/register-server-scope-tools.ts` | `business-file` | current server-scope tool registration and output shape | `true` |
| 2 | `src/application/server/filesystem-server.ts` | `business-file` | current application-owned server shell that holds the `allowedDirectories` scope | `true` |
| 3 | `src/application/server/register-tool-catalog.ts` | `business-file` | current composition-root context for the server-scope tool | `true` |
| 4 | `README.md` | `markdown-doc` | current root DX surface that will later reference the endpoint-local README | `true` |
| 5 | `DESCRIPTION.md` | `markdown-doc` | current root architecture surface that will later reference the endpoint-local description | `true` |

## Notes
[INTENT: KONTEXT]

- This task creates endpoint-local documentation only; root TOC integration happens later.
- The local docs must preserve the application-owned server-scope classification of this tool and must not invent a domain-owned endpoint folder.
